# slot-tools｜機能構想メモ

> 本書は「実装項目の台帳」と「未着手アイデアの置き場」。
> 実行順（優先順位）は持たない。順位は docs/ARCHITECTURE.md §5 が唯一の正本。
> 最終更新日は本書に書かない（git log が正）。
> 記号: 完了 [x] / 進行中 [~] / 未着手 [ ] / 取りやめ [-]

このファイルは、今すぐ着手しない案も含めて、サイトの機能案・改善案・将来構想を忘れないために保管する場所。

## 運用ルール

- 思いついた案は、完成度を気にせず該当カテゴリへ追加する
- 着手する案は「次にやる候補」へ移す
- 実装中は `[~]`、完了は `[x]`、未着手は `[ ]` として扱う
- 実装後も削除せず、完了済みに残す
- AIへ作業依頼するときは、最初にこのファイルを確認させる

## UI設計原則（全ツール共通）

- カウント・入力系の画面は「スクロールなしの固定全画面」とする。ユーザーの操作負担を最小にすることを最優先する（2026-08-09 確立）
- 数値入力はOSキーボードではなくページ内の電卓パッドで行う。開いた時点で入力欄は空にし、初期値0を消す手間を発生させない
- タップカウンターは長押しで数値の直接入力（まとめて入力）に対応する
- 不用意に不安を煽る表示を避ける。解釈を含む数値は常時表示せず、ユーザーが見に行った時に文脈（信頼度・G数）と共に示す
- タップ可能な要素は高さ44px以上を下限、48px以上を推奨とする（WCAG 2.5.5 / Apple HIG 44pt / Google Material Design 48dp準拠）。小型画面では44pxまで圧縮を許容し、スクロールなしを優先する
- 最頻入力項目は片手持ちの親指ホームポジション（下段左）に配置する
- 破壊的操作（クリア等）は「元に戻す」で復元可能にする
- 使う人によって必要性が分かれる記録項目は、既定で表示しつつ利用者側で非表示にできるようにする
- 固定全画面の原則はカウント・入力系の画面に適用する。選択モーダルなど項目数が増える一覧は、高さ上限を設けて内部スクロールを許容する

---

# 現在の主軸

- [x] 設定判別ツール（Phase 1）… エンジン・機種データ・UI・ガイドページまで公開済み。実戦記録ツールへ発展
- [x] 実戦記録・設定推測ツール（Phase 1.5）… ホール用のカウント+投資記録+リアルタイム推測。6号機ジャグラー全9機種対応、ガイドページ公開済み
- 対応機種: SマイジャグラーV、SアイムジャグラーEX（6号機）、Sゴーゴージャグラー3（6号機）、Sファンキージャグラー2（6号機）、SハッピージャグラーVⅢ（6号機）、SネオアイムジャグラーEX（6号機）、Sウルトラミラクルジャグラー（6号機）、SジャグラーガールズSS（6号機）、Sミスタージャグラー（6号機）。6号機ジャグラーシリーズ全9機種に対応完了。以降の機種追加はJSON1本+機種リスト1行で完結する構造
- [x] マイログ（Phase 1.75）… 打った台の記録を蓄積し振り返る自分専用の場所。実戦記録ツール側の保存機能と、一覧・集計ページ（mylog.html）を公開済み
- [~] マイホレポ（Phase 2）… 店の台データを蓄積して傾向を比べるツール。入力・保存・比較まで実装済み
- [x] 遊タイム期待値計算ツール（yutime-calc.html）… 記録機能なしの1画面計算ツール。記事・外部サイトへのiframe埋め込み用。期待値エンジンは yutime-v3.html からインラインで複製し、`tests/yutime-calc.test.js` の文字列完全一致テストで二重管理を防ぐ。カウンター基準の回転数はエンジンのプリセットが持つ `spec.counterOffset` で内部低確回転数に換算する（アグネスPE＝11、大海5SP＝0。B97でv3と共通化）。localStorage不使用。公開範囲（トップ導線・sitemap）は未判断
- [x] 期待値パラメータの出典一本化（B98）… yutime-v3.html の `MACHINE_PRESETS` はUI情報（roundTypes・evSupported等）だけを持ち、`spec` / `defaults` は期待値エンジンのプリセットから解決する。天井・当り確率・時短テーブル・残保留・counterOffset・純払い出しをページ側のプリセットに書かないことを `tests/yutime-v3.test.js` のアサートで固定する
- [x] 履歴の実収支・期待値の並列表示と日別サマリー（B96）… yutime-v3.html の履歴カード上部を「実収支」「期待値」の2枠にし、どちらにも必ずラベルを付ける（金額だけを単独で大きく出さない）。日付区切りの直下に「台数／実収支／期待値／実働／時給」の日別サマリーを出す。実働は各セッションの `endTime − startTime` の合計で、時刻欠損セッションは実働から除外（実収支・期待値の合算には算入）、`endTime < startTime` は0扱い。表示のみで計算式・保存データ・schemaは不変
- [x] コーナー基準値を選択中のコーナーの機種で解決（G5）… yutime-v3.html の「このコーナーの基準値」チップが `umi-sp5` 決め打ちだった不具合の修正。選択中のコーナー（島）の左右プリセットと、そこに並ぶ台の機種から遊タイム機種を解決し、機種ごとに1つチップを出す。表示・「変更」フォームの初期値・保存先プリセットIDが同じ解決結果を使う。期待値計算は従来どおり台ごとの `normalizeMachinePresetId(machine)` で機種を決めるため計算式・保存データ・schemaは不変
- [x] 通常時区間モデルの導入とデータ移行（S1）… yutime-v3.html のセッションに `segments[]`（kind: normal / yutime、startSpin・endSpin・startTrackedBalls・endRemainBalls・endTrackedBalls・holdSpins）を持たせ、通常回転数と通常消費玉を通常区間の合算から求める。`investments[]` / `hits[]` は `segmentId` で所属区間に紐づく。旧フラットフィールド（hitSpin / hitRemainBalls / yutimeEnterBalls 等）は削除せず並走させる。schema 30→31、移行前データは `ytv3:backup:latest` へ退避してから変換する。`holdSpins` はS1では常に0（時短抜け区間はS2）。この移行で、遊タイム経由の当選で遊タイム中の回転を通常回転数に数えつつ消費玉は突入時で止めていた不整合が直り、当該セッションの通常回転数が `hitSpin − startSpin` から `yutimeEnterSpin − startSpin` に変わる（表示値が変わるのはこのパターンのみ。合成データ10パターン×2店の全表示値比較で確認）
- [x] 区間はフラットフィールドの写像として保存時に組み直す（S1）… 記録の修正で当選回転数や遊タイム突入回転数を直したとき、保存済みの区間が古いまま残って表示値が追従しない事故を防ぐため、`persist()` で `resyncSessionSegments()` を通す。区間IDは種別ごとに引き継ぎ、参照先が消えた `segmentId` は付け直す。S2で区間を直接編集できるようにする際は、ここで上書きしてよい範囲を見直すこと
- [x] 時短抜け入力と区間の連鎖（S2）… yutime-v3.html の「現在回転数をリセット」モーダルを時短抜けの選択に変え、選ぶと新しい通常時区間を追加する。選択肢はプリセットの `spec.jitanTable`（ST機）または `spec.jitanNormal` / `jitanChain`（確変ループ機）と `spec.stSpins` から `時短回数 + ST回転数` で生成し、値をページ側に書かない（アグネスPE＝25/50/100、大海5SP＝100/200）。任意の値を入れる直接入力も置く。区間には `source`（migrated / user）と `endSource`（hit / yutime / end）を持たせ、当選・遊タイム突入・ヤメでそれぞれ区間の終端を確定する
- [x] 区間の打ち出し回転数から先頭の無料保留を引く（S2/G1）… `打ち出し回転数 = endSpin − startSpin − holdSpins`。時短抜けで作った区間はプリセット既定（アグネスPE＝5）、打ち始め区間と遊タイム区間は0。引いた結果が負になる場合は0で止める（終端が開始より小さい入力ミスは従来どおり負のまま警告に回す）。記録の修正画面から区間ごとに0〜9で変更できる。既存データは全区間 holdSpins:0 なので表示値は変わらない
- [x] resync はユーザーが追加した区間を上書きしない（S2）… user 区間が無いセッション（S1と同じ形）だけを従来どおり丸ごと組み直し、user 区間があるセッションはフラットフィールドと1対1で対応する端点（先頭区間の開始／遊タイム区間の境界／endSource が hit・end の区間の終端）だけを追従させる。これで「時短抜けで追加した区間が記録の修正操作で消える」事故を防ぐ。区間ごとの holdSpins は組み直し経路でも保つ
- [x] 通常区間が複数あるセッションの消費玉と投資集計（S2）… 通常区間が2つ以上あるとセッション単位の集計をそのまま区間の消費玉に使うと二重計上になるため、B95 と同じ式（区間のタップ合計 −（台の持ち玉表示 − ツール追跡値））を区間単位に適用する。投資は打っていた区間の `segmentId` に紐づける。2周目以降の投資が「当選前」フィルタから漏れる問題も、通常区間が複数のときだけ遊タイム以外を全て通常時として集計して直す。単一区間セッションは従来の経路のままで表示値は不変
- [x] 転記用の貯玉引出に再プレイを算入（S2/§5）… `貯玉引出 = 開始持ち玉 + 再プレイ投資の合計`。従来はパーソナル店で開始持ち玉のみ、非パーソナル店で持ち玉投資＋再プレイを見ていて再プレイ分が落ちていた。実収支・差玉の式は変更しない
- [ ] 判別ツールと店舗分析ツールの連携（Phase 3）

---

# 1. 設定判別ツール（Phase 1）

## コア設計

- [x] ベイズ推定＋二項分布による判別エンジン
  - 各要素（BIG確率、REG確率、小役確率など）ごとに尤度を計算して掛け合わせる
- [x] 解析値はコードに埋め込まず、出典URL・取得日付きのデータファイルで管理する
  - 数値の更新とロジックを分離し、検証・差し替えを容易にする
- [x] 実在の根拠が確認できないフラグ（例: ハナハナのBIG中ハズレ）は入力欄を設けない

## 事前分布（他ツールとの差別化ポイント）

- [x] 事前分布をプラグイン可能なインターフェースとして設計する
- [x] 選択肢1: 均等分布（各設定1/6）
- [x] 選択肢2: 手動プリセット（自分で設定配分を入力）
- [ ] 選択肢3: 過去イベントデータからの推定配分（Phase 3で接続）
- [x] チェック式で「事前分布を参考にする / しない」を切り替え可能にする
- [x] 事前分布ON時は、均等時との比較など「事前分布が結果に与えた影響」を可視化する

## 信頼性・検証

- [x] シミュレーションによる自己検証
  - 既知の設定の確率値で疑似データを生成し、ツールが正しい設定を最上位に出すかを全設定×多数回で確認する
- [x] サンプル数（総G数）に応じた信頼度表示
- [x] Phase 1の時点で、事前分布のデータ形式（設定1〜6の重み＋出典ラベル）を確定させておく
  - Phase 3の連携時に手戻りを発生させないため

---

---

# 1.5 実戦記録・設定推測ツール（juggler-record.html）

ホールで打ちながら使う統合ツール。設定判別ツール（settei-estimator.html）は事後の一括計算用として併存させる。

## 実装済み

- [x] 2タブ構成（カウント / 設定推測）+ 現在地サマリー常時表示（消化G・BIG/REG・投資）
- [x] データカウンタ（打ち始め時点）入力。現在値はデータGのみ入力し、ボーナスは自分のカウントから算出
- [x] タップカウント6ボタン: ぶどう・単独BIG・チェリー重複BIG / チェリー・単独REG・チェリー重複REG
- [x] 不明BIG/REGボタン。不明分がある種別は内訳ではなく合算で計算に切り替え（統計的整合のため）
- [x] チェリーは記録・確率表示のみ（設定2〜5の数値が未公開のため判別には不使用）
- [x] 元に戻す（カウント・投資共通のundoログ）
- [x] 投資記録（+500円/+1000円、貸レート選択、推定持ち枚数から推定差枚・等価換算を表示）
- [x] 区間分割ベイズ推定（打ち始め前データ + 自分の実戦区間）
- [x] セッションの端末内保存（打ち始めスナップショット含む。Phase 2の台単位データ形式の母体）
- [x] カウントタブの固定全画面化（スクロールなし）。数値入力は電卓パッド（初期値0なし・即入力可）
- [x] カウンター長押しで数値の直接入力（誤リセット時の復旧用）
- [x] 入力エラーは実際の値と修正すべき欄を明示する文言に変更
- [x] 「計算に使うカウント」トグル（ぶどう／ボーナス内訳）。オフ時は記録のみ・推測には不使用。部分カウントによる推定の歪みを防ぐ
- [x] カウントボタンの高さ圧縮と投資エリアの引き上げ、最頻項目ぶどうを親指ホームポジション（下段左）へ配置。片手持ちの親指可動域を基準にボタンを並べる
- [x] 投資をカード残高方式へ変更（yutime-v3と同仕様）。「入金」でカード残を追加、「−1000円」でカード残−1000・投資+1000。入金は投資に含めない。カード未使用なら投資のみ記録
- [x] カウントタブに「クリア」ボタン。カウントのみクリア（台データ・投資は保持）と、すべてクリア（新しい台）の2択モーダル
- [x] カウントのみクリアを「元に戻す」で復元可能に（クリア前スナップショットをundoログに保持）
- [x] すべてクリアも「元に戻す」で復元可能に（保存削除ではなく全状態スナップショット方式へ変更）
- [x] 上部サマリーから「最有力」を削除（点推定の常時表示は不安を煽るため。設定推測タブで信頼度と併せて表示）
- [x] 打ち始め入力の強調（「最初に入力」バッジ＋未入力時のパネル注意枠）
- [x] 全タップ要素を48px以上に統一（タブ・不明BIG/REG・元に戻す・クリア・投資系・数値フィールド・電卓決定）。6ボタン領域は23dvhに再配分し固定全画面を維持。720px以下の小型画面は44px下限で圧縮表示
- [x] タブ切替の視認性改善（選択中はランプ赤の下線、非選択側は背景・文字を沈める、アイコン付与 ✚/📊）
- [x] 投資額・カード残高をタップで電卓修正可能に（押し忘れの事後修正用。持ち枚数と同じ操作性）
- [x] カウントボタンのJSON駆動化（機種データの ui.count_buttons から並び・ラベル・色・記録専用フラグを読んで生成。機種追加はJSON1本で完結）
- [x] 機種選択UI（ヘッダの機種名タップで切替モーダル。記録は機種ごとに分離保存）
- [x] 2機種目: SアイムジャグラーEX（6号機）追加。REGが設定5・6同値のためぶどう(設定6のみ1/5.80)が6判別の主役になる構造をJSONで表現
- [x] 打ち始めパネルの折りたたみ（現在G入力後は1行サマリーに縮小。修正は投資エリアの「初期登録」ボタンから）
- [x] 現在のデータGを主役化（中央寄せ・大型数字・アクセント枠）

- [x] 3機種目: Sゴーゴージャグラー3（6号機）追加。REG合計が最大の判別要素（設定1と6で約1.51倍）だが設定5・6が近接。ぶどうは全設定に段階的な設定差があり補助要素として有効。単独ボーナスは前作比率からの推定値のため信頼度区分を derived_estimate として明示

- [x] 4機種目: Sファンキージャグラー2（6号機）追加。REG合計が設定1と6で約1.68倍と全設定単調で判別しやすい。内訳はアプリ算出値をそのまま使うと公表合算値と最大約12%乖離するため、公表値をアンカーに「公表合算 − チェリー重複」で単独ボーナスを算出（derived_estimate）。第三者の独立推定値と概ね一致することを確認済み

- [x] 実戦値パネルのJSON駆動化。BIG・REG・合算の固定3項目に加え、ui.count_buttons の全項目を自動生成して確率表示する。記録専用項目は「（参考）」を付記。機種ごとにカウント項目を増やしても推測タブに自動で反映される

- [x] ベル・ピエロを全機種にカウントボタンとして追加（record_only）。判別要素としては引き続き不採用だが、稼働ノートとしての記録価値を優先（2026-08-23 方針変更）。確率値は出典未確認のため機種JSONに記載せず、実戦値のみ表示する
- [x] 8ボタン3行に対応した高さ再配分（通常30dvh・最大280px、小型画面26dvh・最大200px）。3行時も1ボタン48px以上を確保し固定全画面を維持

- [x] 打ち始めパネルを常時折りたたみに変更。未入力時は「打ち始めデータ未入力／タップして入力」の1行を警告色で表示し、行自体をタップで展開できる。未入力時に投資エリアが下へ押し出される問題を解消
- [x] カウントボタンの表示カスタム機能。記録専用ボタン（チェリー・ベル・ピエロ）を推測タブのトグルで表示/非表示にできる。非表示にしてもカウント値は保持され、判別計算には影響しない。判別に使う項目は常に表示

- [x] カウントボタンのラベルを2行化（BIG/単独、BIG/🍒重複 のように種別と内訳を分離）。文字を拡大し視認性を改善。表示名は機種JSONの label_top / label_sub で定義
- [x] 設定推測タブの並び替え。判別結果を最上部へ移動し、タブを開いてすぐ結果が見えるようにした
- [x] 収支計算の修正。開始時の持ちメダルを差枚から控除するようにし（未控除のバグを修正）、交換率（等価20円/18.5/17.9/16.6）を選択可能にした。差枚には計算式の内訳を併記して検算できるようにした

- [x] 5機種目: SハッピージャグラーVⅢ（6号機）追加。REG合計が設定1と6で約1.55倍と全設定単調で判別しやすく、BR比率（設定6で約1:1）も材料になる。内訳は公表値アンカー方式で算出し、単独REGが第三者の独立推定値とほぼ完全一致（設定差1.49倍）することを確認。中段チェリー（1/6553.6・BIG確定）は実戦で角チェリー重複と区別できないため cherry_big に合成済み。ベル・ピエロは設定ごとの数値が判明しているが単調性がないため記録専用

- [x] カウントタブのヒント行（タップで+1／長押しでまとめて入力・直前の操作表示）を、3列グリッドに余りセルができる場合はその空きに配置するよう変更。ボタンが8個（3行）のときに行が押し出されて見えなくなる問題を解消し、余白も有効活用する

- [x] 6機種目: SネオアイムジャグラーEX（6号機）追加。アイムEXと同一スペックの後継機で、REG設定差1.72倍・単独REG1.75倍とシリーズ最大級。ただしBIGはほぼ横ばい（1.07倍）で設定5・6はREGが同値のため、設定6の判別にはぶどう（設定1〜5共通1/6.02、設定6のみ1/5.78）が事実上必須。内訳は前作比率からの推定値だが、合成値が公表合算値と誤差0.03%未満で一致するためアンカー調整なしでそのまま採用

- [x] 7機種目: Sウルトラミラクルジャグラー（6号機）追加。前作の変則スペックが解消され、BIG（1.24倍）・REG（1.53倍）とも全設定単調に軽くなる素直な構成。チェリー重複BIGの設定差1.45倍はシリーズの中では例外的に大きい。ぶどうは設定1〜4が同値で設定5・6のみ優遇。非重複チェリーも全設定の数値が判明しており設定差があるが、入力経路が未対応のため記録専用

- [x] 機種選択モーダルのリストに高さ上限（画面の60%）と内部スクロールを設定。機種が増えてもモーダル全体が画面からはみ出さないようにした。10機種以上でも収まることを試算で確認

- [x] 8機種目: SジャグラーガールズSS（6号機）追加。BIG（1.21倍）・REG（1.51倍）とも全設定単調だが、REGの設定差幅はシリーズの中では狭い部類で判別には試行が必要。ぶどうは設定1〜4が同値で設定5・6のみ優遇。BIG/REG個別値は juggler7 と けんのスロットシミュレーション の2ソースで一致を確認（juggler7の合算欄は個別値と食い違うため不採用）。チェリー重複は公表合算と単独の差から算出

- [x] 9機種目: Sミスタージャグラー（6号機）追加。これで6号機ジャグラーシリーズ全機種に対応完了。シリーズで唯一チェリーに設定差がなく（全設定1/37.24）、代わりにぶどうが全設定単調（1/6.29〜1/5.96）という特殊な構成。設定6はBIGとREGが同確率（1/237.4）でBR比率1:1が看破材料
- [x] 内訳（単独・チェリー重複）の確率値を持たない機種への対応。ミスタージャグラーは対応する5号機が存在せず推定値も算出できないため、内訳ボタンを記録専用にしたうえで、カウントした値はBIG・REGの合算として判別に反映する仕組みを buildObservation に実装（hasElement による分岐と undefined 対策）

- [x] マイジャグラーVとアイムジャグラーEXの内訳を公表値アンカー方式へ統一。この2機種はファンキー2以降で確立した方式より前に作成しており、アプリ算出の単独ボーナス値をそのまま採用していたため内訳合計が公表合算値と乖離していた（マイジャグのBIGで最大-14.4%、アイムのREGで-5.9%）。「公表合算 − チェリー重複」で単独を再算出し、全機種で誤差0.03%未満の整合を達成。マイジャグの単独REG設定差は1.88倍→2.07倍に向上し、特殊役によるBIG当選分も単独BIGに含まれる実戦の数え方と一致するようになった

- [x] ガイドページ（juggler-guide.html）作成。ツールの目的、基本の流れ、カウントの取り方、投資と収支の記録、設定推測の読み方（事前分布・信頼度の目安）、数値の出典区分、FAQを掲載。特に「数えるなら最後まで、数えないなら0のまま」という最重要の注意点と、確率であって断定ではないという限界を明記。既存の yoshimune-guide.html と同一の体裁

- [x] 投資エリアの整理（実戦フィードバック）。差枚表示が画面端で途切れる問題に対応し、(1)「開始持ちメダル」を打ち始めパネル内へ移設（初期登録の一部として自然な位置に）、(2)「初期登録」チップを廃止し打ち始めパネル側に「閉じる」ボタンを設置（展開はサマリー行タップ）、(3)「入金」ボタンにカード残高を表示して統合（タップで入金、モーダル内の「残高を直接修正」で修正）。投資行2が2チップになり、差枚と内訳が全幅で折り返せるようになった
- [x] 貸メダル枚数と交換枚数を選択式から数値入力（電卓）に変更。3種類・4種類の固定選択肢では対応できない店が多いため、任意の値を入れられるようにした。交換率は「円/枚」ではなく「1,000円分にするのに必要な枚数」で保持し、ホールの表示（〇枚交換）と直接対応する。等価は50枚。旧形式（円/枚）で保存された端末のデータは読み込み時に自動で枚数へ変換する

- [x] 差枚表示の途切れを解消。投資行の .sum に後勝ちのCSS（flex:1 / white-space:nowrap / text-overflow:ellipsis）が残っており、全幅化の指定を上書きしていた。定義を1本化し全幅・折り返し可に変更。360px端末でも差枚と計算式が省略されずに表示される

- [x] ピエロ重複ボーナスの記録対応（ミスタージャグラー）。ピエロボタンの長押しで「ピエロのみ／ピエロ＋BIG／ピエロ＋REG／まとめて入力」を選べる選択シートを追加し、ボタンを増やさずに記録できるようにした。ピエロ重複を記録してもピエロのみの回数には加算しない（チェリー重複と同じ扱い）。設定推測には使わないが、将来解析値が公開された際にそのまま判別へ組み込める形でデータを残す
- [x] ボーナス総数の集計を一般化。従来は「単独＋チェリー重複＋不明」の決め打ちだったが、機種JSONの count_buttons と sub_counts の group 指定から BIG系／REG系を集約する方式に変更。ピエロ重複が自動的にBIG・REGの総数へ算入される。確率値を持たない重複役が記録されている場合は内訳ではなく合算で計算する安全側の分岐も追加。既存9機種の挙動が変わらないことを検証済み

- [x] 結果カード画像の生成。設定推測タブから、その時点の判別結果を1080×1080のPNGとして作成できる。載せるのは機種名・日付・総回転数・BIG/REG・合算・ぶどう・全6設定の事後確率・信頼度・注意書きのみで、収支と店名は含めない（夢爽の判断 2026-08-28）。端末内のCanvasで生成し外部送信はしない。点推定が独り歩きしないよう、G数・信頼度・「断定するものではない」という注記を必ず画像に焼き込む
- [x] myjuggler5-checker.html（共通エンジン版カウンター）を正式にリタイア。判別性能は juggler-record が全面的に上位で、唯一の独自機能だった共有カードも juggler-record に実装したため役目を終えた。導線からは削除済み、ファイルは404回避のため残置

- [x] 結果カードを設定判別ツール（checker系）と同じ意匠に統一。背景・ピンクの発光枠・M PLUS 1pフォント・見出し「SETTING CHECK RESULT」・機種名・日付+G数・右上の丸アイコン・4ブロックの実戦データ・フッタ（slot-tools.jp＋ハッシュタグ＋解析出典）まで揃えた。判別結果は6設定すべてを横バーで表示
- [x] カードに画像を入れられるようにした。「画像を選ぶ」から端末の画像を右上の丸枠に配置でき、256pxへ縮小して機種ごとに端末内保存する。「画像を外す」で解除
- [x] カードのテキスト出力。「テキスト」ボタンで機種名・日付・G数・BIG/REG・合算・ぶどう・全6設定の確率・信頼度・ハッシュタグをクリップボードへコピーする（約200字でXの上限内）。収支帳への貼り付けにも使える
- [x] カードの「長押しで保存できます」という常時表示の案内を削除。「保存する」で保存できるため不要で、保存に失敗した場合のみ案内を出す方式に変更
- [x] 内訳の解析値を持たない機種（ミスタージャグラー）で「ボーナス内訳」トグルが実質無効な件に対応。トグル自体は残したまま、切り替えても結果が変わらない理由を注記として表示する

- [x] マイログ（実戦記録ツール側）。設定推測タブに「マイログに保存して次へ」を追加し、店名・台番号・イベント名・台全体の差枚（概算）・メモを入れて1セッションを記録できる。店名とイベント名は過去の入力から選択できる。保存後はカウントをクリアし、直後なら「元に戻す」で復元できる（貸出枚数と交換枚数は次の台へ引き継ぐ）
- [x] マイログの記録内容。日付・店名・台番号・イベント／打ち始め（総G・BIG・REG・開始持ち）／打ち終わりの台全体（総G・BIG・REG・差枚は「約」表記の任意入力）／自分の区間（消化G・カウント全項目）／判別結果（6設定の確率・信頼度）／収支（投資・持ち枚数・差枚・貸出・交換枚数・換算額）／メモ。台全体のBIG・REGは判別結果の有無に関わらずカウントから算出する
- [x] マイログの一覧表示（ツール内モーダル）。「この機種のみ」「この店のみ」で絞り込め、打ちながら過去の記録を参照できる。記録は個別に削除可能
- [x] マイログの書き出し・読み込み（JSON）。localStorageは端末のデータ削除やブラウザの制限で失われうるため、最初から書き出しを備える。読み込みは追記型で、同じidの記録は重複させない
- [x] 名称を「マイログ」に決定（2026-08-28 夢爽の判断）。サイト名「スロット稼働ノート」と混同せず、解析値を見る場所ではなく自分が打ってきた結果が蓄積される自分専用の場所であることを表す。将来AT機などへ広げてもそのまま使える

- [x] 書き出しファイル名の日付をローカル時刻基準に統一。マイログのJSONと結果カードのPNGのファイル名がUTC基準だったため、日本時間の0〜9時に書き出すと前日の日付になっていた（深夜まで打ってそのまま書き出す使い方で1日ずれる）。localDateStamp() を新設して記録内の日付と共通化

- [x] マイログの一覧・集計ページ（mylog.html）。機種・店舗・期間（月単位）で絞り込み、全体集計（記録数・総G数・差枚合計・収支合計・勝率・BIG/REG合算）と、機種別・店舗別・月別の集計表を表示する。記録一覧からの個別削除、書き出し・読み込みにも対応。実戦記録ツールと同じ localStorage（juggler-mylog:v1）を読むため二重管理にならない
- [x] 集計の設計判断。差枚・収支・勝率は持ち枚数を入力した記録だけを母数にする（未入力の記録を0として扱うと収支が実態より悪く見えるため）。BIG/REGは打ち終わりから打ち始めを引いた自分の区間の回数のみを集計する
- [x] mylog.html は noindex とし、sitemap には登録しない。自分専用の記録置き場であり検索から入る性質のページではないため

- [x] マイログの日付を保存時に指定できるようにした。既定は当日だが変更できるため、過去の稼働をあとから記録することもできる
- [x] マイログの記録を編集できるようにした（mylog.html）。日付・店名・台番号・イベント・打ち始めと打ち終わりの台データ・台全体の差枚・投資・持ち枚数・貸出・交換枚数・メモを修正でき、差枚と収支は入力から再計算する。小役のカウントと判別結果は記録時の値のまま残す（再計算の根拠がないため）
- [x] 設定推測タブのセクション見出しを見やすくした。文字を大きくして本文色にし、左に赤いラインを引いて区切りを明確化。各見出しに役割の一文を添え、目的の項目を探しやすくした
- [x] 結果カードの既定アイコンをサイト共通のキャラクター画像（assets/toaru2-default-icon.jpg）に設定。画像を選んでいない状態でも枠が埋まる。「画像を外す」を押すと既定へ戻る

- [x] 事前分布のトグルのタップ領域を48px（小型画面44px）に拡大。UI設計原則の下限44pxを下回る24pxのままだったのを是正。チェックボックス自体も22pxに拡大

- [x] タップ領域の全面点検と是正。設定推測タブの操作ボタン（.use-btn 13個）を42px→48px（小型画面44px）、「新しい台（すべてリセット）」を35px→同様に、マイログ一覧の「編集」「削除」を32px→44pxで横並びに、mylog.htmlのヘッダリンクを30px→44pxに拡大。あわせて .prior-toggle の旧ルール（18px指定）を削除
- [x] 実戦記録ツールのヘッダリンク（TOP・使い方）は17pxのまま据え置く判断。カウント中の誤タップでページ遷移すると記録中のデータから離れてしまうため、押しやすさより誤タップ防止を優先する。固定全画面の縦の余白を確保する意味もある。WCAG 2.5.5 のインライン例外にも該当する

- [x] 打ち始めデータの連続入力。総回転数→BIG→REG→開始持ちメダルの4項目を、電卓の「次へ」で続けて入力できるようにした。項目ごとに電卓を開き直す手間がなくなる。電卓には「打ち始めの入力 1／4」の進捗を表示する
- [x] 打ち始めの総回転数を入力すると「現在のデータG」にも自動で反映するようにした（現在Gが打ち始めより小さい場合のみ）。座った直後に消化0Gの状態で台データによる判別がすぐ見られるようになり、現在Gを別途入力する手間がなくなる

- [x] マイホレポ（myhorepo.html）を新規作成。ホールの台データ（日付・イベント・機種・台番号・G数・BIG・REG・差枚）を記録し、店全体の傾向を比べられる。名称は夢爽の判断（2026-08-30）。マイログ＝自分が打った台の記録／マイホレポ＝自分がチェックしているホールの記録、という対比
- [x] マイホレポの保存先はIndexedDB。localStorageは1件121文字でも3店舗×50台/日で約5ヶ月で上限に達するため、最初からIndexedDBを採用した。マイログ（localStorage）とは別管理
- [x] マイホレポの入力方式（サイトセブン方式）。日付・店名・イベント・機種を一度指定し、以降は台番号・G数・BIG・REG・差枚を連続入力。追加するたび台番号が自動で+1され、その日の登録済み一覧がその場で確認できる。差枚はサイトセブンで取得できないため任意入力
- [x] マイホレポの分析。各台を判別エンジンにかけて事後分布を出し、店全体の「平均設定」と「設定4以上の割合」を算出する。この日／いつも／平日／同じ曜日／前回の同じイベント、を同じ条件で並べて比較する。日別一覧と台番号別の傾向も表示
- [x] 集計の設計判断。データが少ない台は判別が定まらず平均を基準値3.5へ引き寄せるため、(1)1000G未満を除外する、(2)回転数で重み付けする、の2つを既定でオンにし、切り替えられるようにした。基準線（平均設定3.5／設定4以上50%）を常に併記し、絶対値ではなく「いつもと比べてどうか」で読む指標であることを明示
- [x] マイログからの取り込み。自分が打った台は両方に該当するため、マイログの記録（店名・台番号・台データが揃っているもの）をマイホレポへ流し込める。二重入力が発生しない
- [-] 機械割シミュレーターは取りやめ（2026-08-30）。公開されている機械割は「メーカー公表の出玉率から逆算したぶどう確率」を前提にしており、判別に使うアプリ実測値とは別物だった。同じ機種で2種類のぶどう確率を使い分けることになり説明が破綻するうえ、他者の推定値を中核データにすることになるため

## 検討中・将来


- [ ] セッション履歴の保存・一覧（1日複数台の記録） → Phase 2の入力口を兼ねる
- [~] 残数スナップショット方式による収支管理（SF6ツールで確立したモデルの移植）。財布（現金・持ちメダル・再プレイ）と台（クレジット）を分離し、「今いくらあるか」の残数で差枚を算出する。投入記録は不要になり記録し忘れによる事故が構造的に起きない。第一段階として開始持ちメダルの控除と交換率選択を実装済み（2026-08-24）。クレジット・再プレイ・回収の分離は今後
- [ ] セッション収支のカード保存と分析（機種別・店別・日別の勝敗集計）→ Phase 2の店舗データと統合
- [ ] 非重複チェリーを判別要素として使えるようにする。ハッピージャグラーVⅢは角チェリーに明確な設定差（高設定ほど重い逆方向）があるが、現行の入力経路が対応していないため記録専用にとどめている
- [ ] 実戦結果・判別結果のX（SNS）共有機能 — 別途構想中（アイデア★パチスロ開発側の議論が固まってから着手。共有フォーマットの手戻り防止のため保留）
- [ ] 打ち始めパネルの自動折りたたみ。現在は電卓で確定しても畳まれず「初期登録」のタップが必要。3項目を続けて入力する運用では自動で畳まない方が自然という判断だが、実戦での使用感を見て再検討する

---

# 1.6 汎用 実戦記録ツール（tools/rec/）

## tools/rec 実戦記録ツール：お金の記録形式と移行規則

### 現行形式（rec7以降）
- `money.deposits` … 入金（サンドに入れた現金）
- `money.spends` … メダル貸出（＝投資）
- 投資 = sum(spends) ／ 残高 = sum(deposits) − sum(spends)
- 残高が1,000円未満で「−1000」を押した場合、同額を deposits にも自動追加する
  （サンドに入れず1,000円ずつ使う打ち方に対応するため）

### 旧形式（rec4以前）からの移行
旧形式は `money.cash` のみを持ち、入金と使用を区別していない。
移行は `fixMoney()` が起動時に一度だけ行い、結果を保存する。

規則：
1. `Math.abs(sum(cash))` を求める
2. その額を deposits と spends の両方に入れる（残高0）

符号を無視する理由：rec4 は入金を正、「−1000」を負で記録していたため、
符号に意味がない。−1000 のみで打った台は合計が負になるが、
使った額は変わらないため絶対値で拾う。

限界：入金と使用が混在した台は正確な復元ができない。
rec4 の画面表示と同じ値になるよう、単純合算の絶対値を採用している。
（例：+3,000 と −1,000 の台 → 投資2,000円。実態は投資1,000円の可能性あり）

---

## tools/rec 実戦記録ツール：ログ表記と kado-note 連携

### ログのゲーム数表記（rec13以降）

ログ1行の左に出すゲーム数は、実Gと液晶Gの関係で表記を変える。

| 実G | 液晶G | 表記 |
|---|---|---|
| 80 | 80 | `80` |
| 57 | 86 | `57/86` |
| 80 | なし | `80` |
| なし | 86 | `86` |

液晶Gを持たない機種（沖ドキ等）でも実Gが表示される。
`gsText()`（テキスト用）と `gsHtml()`（画面用・液晶G側に色）の2つを用意し、
画面・テキスト書き出し・kado-note用出力のすべてで同じ表記に揃える。

### kado-note 連携（rec13以降）

rec は schemaVer 2 のエンベロープを出力できる。変換は `toEnvelope()`。

| エンベロープ | rec から入れる値 |
|---|---|
| sessionId | rec の セッションid（毎回同じ値。重複検出のため） |
| machineId | 機種名（日本語は toKebab() で落ちる。下記の制約参照） |
| payloadVer | `"rec-1"` |
| invest | 投資額（円）＝ `invested(money)` |
| payout | 終了時の 持ちメダル＋再プレイ（枚） |
| lendRate / exchangeRate | レート（等価前提で同じ値を入れる） |
| memo | 機種名 ／ 想定期待値 ／ 差枚 |
| payload.logs | `{t: epoch_ms, type:"memo", text:"57/86G　強🍒 (1回目)"}` |

payload には機種名・開始終了のG数・持ちメダル・想定期待値・レア役回数も残す。

出力は2種類。どちらも kado-note で取り込める（kado-note 側で配列対応済み）。
- 各台の「この台をノート用に保存」… 単一オブジェクト
- 「同・保存」… 全台の配列。1回の取り込みで複数台が入る

### kado-note 側の対応（55feb5d）

rec 連携のために kado-note.html を3点改修した。

1. `normalizeSchemaV2()` / `isLegacySession()` に `machineLabel` を追加し、
   元の機種名文字列を保持する
2. `machineName()` は、機種名にASCII以外の文字が含まれる場合、
   `machineLabel`（なければ `payload.machine`）を表示する。
   日本語機種名が `unknown-machine` と表示される問題はこれで解消
3. `importFiles()` が配列に対応。1ファイルに複数セッションが入っていても読む。
   途中の要素が壊れていても他は取り込まれ、エラーには `ファイル名[n]` で位置が出る

SF6 の描画は改修前後でDOMがバイト一致することを確認済み。

残る制約：
- 旧版で取り込み済みの日本語機種名セッションは、保存時点で機種名が失われて
  いるため名前が戻らない。該当データは取り込み直しが必要
- `toKebab()` はASCII以外を落とすため、機種名が短い語に潰れることがある
  （例：「沖ドキDUOアンコール」→ `duo`）。表示は machineLabel で正しく出るが、
  **machineId は機種の同一性を判定する値としては使えない**。
  機種別の集計を行う場合は machineId の生成方法から見直すこと

---

# 2. 店舗データ蓄積・分析ツール（Phase 2）

## データ設計

- [ ] 入力単位は「店の合算」ではなく「台単位」とする
  - 記録項目: 日付・イベント名・機種・総G数・BIG回数・REG回数
  - 理由: 店平均だけでは「全台設定4」と「設定1と6の混在」を区別できない
- [ ] 店平均は台単位データからの集計値として自動算出する（別途入力しない）

## 出力（3階層）

- [ ] 階層1: 店平均（合算のBIG/REG確率）
- [ ] 階層2: 設定配分の推定
  - ベタ置き型（全台中間設定）か、メリハリ型（低設定と高設定を分ける）かの傾向
  - 高設定の投入率の推定 → Phase 3で事前分布の元データになる
- [ ] 階層3: 当日速報比較
  - 今日の途中経過 vs 過去の同イベント平均で「前回より強い / 弱い」を提示
  - 総G数が閾値未満の場合は「参考値」と明示する
  - 将来案: 時刻付き記録にして「過去の同時刻帯の途中経過」と比較する（まずは終日データ比較から）

## 検討事項

- [ ] 当日データの収集手段（手入力か、転記か）と運用負荷の整理
- [ ] 開催回数が少ないイベントの配分推定は信頼できないため、件数に応じて均等分布と混ぜる（縮小推定）を検討

---

# 3. ツール間連携（Phase 3）

- [ ] 店舗分析ツールの推定配分を、判別ツールの事前分布プリセットとして選択可能にする
- [ ] 判別ツール＝計算機（状態を持たない）、店舗分析＝データ蓄積（状態を持つ）として分離を維持し、連携はデータ形式のみで行う

---

# 完了済み

（なし）
