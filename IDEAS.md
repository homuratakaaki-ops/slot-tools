# slot-tools｜機能構想メモ

> 本書は「実装項目の台帳」と「未着手アイデアの置き場」。
> 実行順（優先順位）は持たない。順位は docs/ARCHITECTURE.md §5 が唯一の正本。
> 最終更新日は本書に書かない（git log が正）。
> 記号: 完了 [x] / 進行中 [~] / 未着手 [ ] / 取りやめ [-]

このファイルは、今すぐ着手しない案も含めて、サイトの機能案・改善案・将来構想を忘れないために保管する場所。

## 運用ルール

- 思いついた案は、完成度を気にせず該当カテゴリへ追加する
- 着手する案は「次にやる候補」へ移す
- 実装中は `[~]`、完了は `[x]`、未着手は `[ ]` として扱う
- 実装後も削除せず、完了済みに残す
- AIへ作業依頼するときは、最初にこのファイルを確認させる

## UI設計原則（全ツール共通）

- カウント・入力系の画面は「スクロールなしの固定全画面」とする。ユーザーの操作負担を最小にすることを最優先する（2026-08-09 確立）
- 数値入力はOSキーボードではなくページ内の電卓パッドで行う。開いた時点で入力欄は空にし、初期値0を消す手間を発生させない
- タップカウンターは長押しで数値の直接入力（まとめて入力）に対応する
- 不用意に不安を煽る表示を避ける。解釈を含む数値は常時表示せず、ユーザーが見に行った時に文脈（信頼度・G数）と共に示す
- タップ可能な要素は高さ44px以上を下限、48px以上を推奨とする（WCAG 2.5.5 / Apple HIG 44pt / Google Material Design 48dp準拠）。小型画面では44pxまで圧縮を許容し、スクロールなしを優先する
- 最頻入力項目は片手持ちの親指ホームポジション（下段左）に配置する
- 破壊的操作（クリア等）は「元に戻す」で復元可能にする
- 使う人によって必要性が分かれる記録項目は、既定で表示しつつ利用者側で非表示にできるようにする
- 固定全画面の原則はカウント・入力系の画面に適用する。選択モーダルなど項目数が増える一覧は、高さ上限を設けて内部スクロールを許容する

---

# 現在の主軸

- [x] 設定判別ツール（Phase 1）… エンジン・機種データ・UI・ガイドページまで公開済み。実戦記録ツールへ発展
- [x] 9/7導入の新台6機種（リコリコ／青ブタ／かのかり／見える子ちゃん／タコスロ／モグモグ風林火山）を2026/9/6に正式公開。guide作成・checkers.html導線・sitemap登録まで実施し、公開機種数は21。分母条件が未確認のため6機種とも1/x表示は保留し、回数のみ記録する
- [x] トップページ（index.html）を再構成（2026-09-08）。セクション順を 設定判別／パチンコ計算（遊タイム実戦→遊タイム期待値→回転単価）／実戦ログ／L南国育ち SPECIAL／北斗の拳 転生の章2 に並び替え、サイト説明本文を全セクションの下へ移してリード文だけを上部に残した。更新履歴ブロックは廃止し、代わりに「NEW」欄を新設（1行＝公開日｜ツール名、最大5件・最新が上・6件目の追加で最古を削除、履歴は残さない。運用は AGENTS.md §9-92）。title を「スロット稼働ノート｜パチスロ・パチンコ無料実戦ツール」に変更し、meta description と og:title / og:description の「パチスロ実戦ツール」を「パチスロ・パチンコ」へ広げた
- [x] 公開ページの文言整備（2026-09-08）。公開ガイドに残っていた内部開発用語（公開前QA・フェーズ1/1.5・内部フラグ・B群仕様）を除去し、実装と食い違っていた説明を現行機能へ合わせた（godeater＝ベイズ推定・自動推定を持たないことを確認、monkey5＝5枚役の1/x表示とベイズ推定は実装済みのため実態に合わせて記述）。9/7新台6機種のガイドは「1/x表示は現在準備中です」を「1/x表示は現在行っていません」に統一し、分母条件が未確認である理由文はそのまま残した（§9-70）。運営者情報・提携ページはツール一覧を現状の分類（設定判別チェッカー／パチスロ実戦記録／パチンコ計算／機種専用／実戦ログ・記事）へ更新し、計算ロジックとツール設計は独自実装・機種の仕様や解析値は各ページ明記の出典による旨と、利用者の入力データを当サイトが取得・提供しない旨を明記した。アグネスPE記事の導入前時制も言い換えた
- [x] 実戦記録・設定推測ツール（Phase 1.5）… ホール用のカウント+投資記録+リアルタイム推測。6号機ジャグラー全9機種対応、ガイドページ公開済み
- 対応機種: SマイジャグラーV、SアイムジャグラーEX（6号機）、Sゴーゴージャグラー3（6号機）、Sファンキージャグラー2（6号機）、SハッピージャグラーVⅢ（6号機）、SネオアイムジャグラーEX（6号機）、Sウルトラミラクルジャグラー（6号機）、SジャグラーガールズSS（6号機）、Sミスタージャグラー（6号機）。6号機ジャグラーシリーズ全9機種に対応完了。以降の機種追加はJSON1本+機種リスト1行で完結する構造
- [x] マイログ（Phase 1.75）… 打った台の記録を蓄積し振り返る自分専用の場所。実戦記録ツール側の保存機能と、一覧・集計ページ（mylog.html）を公開済み
- [~] マイホレポ（Phase 2）… 店の台データを蓄積して傾向を比べるツール。入力・保存・比較まで実装済み
- [x] 遊タイム期待値計算ツール（yutime-calc.html）… 記録機能なしの1画面計算ツール。記事・外部サイトへのiframe埋め込み用。期待値エンジンは yutime-v3.html からインラインで複製し、`tests/yutime-calc.test.js` の文字列完全一致テストで二重管理を防ぐ。カウンター基準の回転数はエンジンのプリセットが持つ `spec.counterOffset` で内部低確回転数に換算する（アグネスPE＝11、大海5SP＝0。B97でv3と共通化）。localStorage不使用。公開範囲（トップ導線・sitemap）は未判断
- [x] リコリコ 前兆MAPポイントチェッカー（tools/lycoris-map）— ネネ試作をもとに公開。初期10マスの追加位置は仮配置（実機未確認）。発展→復帰の期待度差は公表値なしのため未反映
- [x] 期待値パラメータの出典一本化（B98）… yutime-v3.html の `MACHINE_PRESETS` はUI情報（roundTypes・evSupported等）だけを持ち、`spec` / `defaults` は期待値エンジンのプリセットから解決する。天井・当り確率・時短テーブル・残保留・counterOffset・純払い出しをページ側のプリセットに書かないことを `tests/yutime-v3.test.js` のアサートで固定する
- [x] 履歴の実収支・期待値の並列表示と日別サマリー（B96）… yutime-v3.html の履歴カード上部を「実収支」「期待値」の2枠にし、どちらにも必ずラベルを付ける（金額だけを単独で大きく出さない）。日付区切りの直下に「台数／実収支／期待値／実働／時給」の日別サマリーを出す。実働は各セッションの `endTime − startTime` の合計で、時刻欠損セッションは実働から除外（実収支・期待値の合算には算入）、`endTime < startTime` は0扱い。表示のみで計算式・保存データ・schemaは不変
- [x] コーナー基準値を選択中のコーナーの機種で解決（G5）… yutime-v3.html の「このコーナーの基準値」チップが `umi-sp5` 決め打ちだった不具合の修正。選択中のコーナー（島）の左右プリセットと、そこに並ぶ台の機種から遊タイム機種を解決し、機種ごとに1つチップを出す。表示・「変更」フォームの初期値・保存先プリセットIDが同じ解決結果を使う。期待値計算は従来どおり台ごとの `normalizeMachinePresetId(machine)` で機種を決めるため計算式・保存データ・schemaは不変
- [x] 通常時区間モデルの導入とデータ移行（S1）… yutime-v3.html のセッションに `segments[]`（kind: normal / yutime、startSpin・endSpin・startTrackedBalls・endRemainBalls・endTrackedBalls・holdSpins）を持たせ、通常回転数と通常消費玉を通常区間の合算から求める。`investments[]` / `hits[]` は `segmentId` で所属区間に紐づく。旧フラットフィールド（hitSpin / hitRemainBalls / yutimeEnterBalls 等）は削除せず並走させる。schema 30→31、移行前データは `ytv3:backup:latest` へ退避してから変換する。`holdSpins` はS1では常に0（時短抜け区間はS2）。この移行で、遊タイム経由の当選で遊タイム中の回転を通常回転数に数えつつ消費玉は突入時で止めていた不整合が直り、当該セッションの通常回転数が `hitSpin − startSpin` から `yutimeEnterSpin − startSpin` に変わる（表示値が変わるのはこのパターンのみ。合成データ10パターン×2店の全表示値比較で確認）
- [x] 区間はフラットフィールドの写像として保存時に組み直す（S1）… 記録の修正で当選回転数や遊タイム突入回転数を直したとき、保存済みの区間が古いまま残って表示値が追従しない事故を防ぐため、`persist()` で `resyncSessionSegments()` を通す。区間IDは種別ごとに引き継ぎ、参照先が消えた `segmentId` は付け直す。S2で区間を直接編集できるようにする際は、ここで上書きしてよい範囲を見直すこと
- [x] 時短抜け入力と区間の連鎖（S2）… yutime-v3.html の「現在回転数をリセット」モーダルを時短抜けの選択に変え、選ぶと新しい通常時区間を追加する。選択肢はプリセットの `spec.jitanTable`（ST機）または `spec.jitanNormal` / `jitanChain`（確変ループ機）と `spec.stSpins` から `時短回数 + ST回転数` で生成し、値をページ側に書かない（アグネスPE＝25/50/100、大海5SP＝100/200）。任意の値を入れる直接入力も置く。区間には `source`（migrated / user）と `endSource`（hit / yutime / end）を持たせ、当選・遊タイム突入・ヤメでそれぞれ区間の終端を確定する
- [x] 区間の打ち出し回転数から先頭の無料保留を引く（S2/G1）… `打ち出し回転数 = endSpin − startSpin − holdSpins`。時短抜けで作った区間はプリセット既定（アグネスPE＝5）、打ち始め区間と遊タイム区間は0。引いた結果が負になる場合は0で止める（終端が開始より小さい入力ミスは従来どおり負のまま警告に回す）。記録の修正画面から区間ごとに0〜9で変更できる。既存データは全区間 holdSpins:0 なので表示値は変わらない
- [x] resync はユーザーが追加した区間を上書きしない（S2）… user 区間が無いセッション（S1と同じ形）だけを従来どおり丸ごと組み直し、user 区間があるセッションはフラットフィールドと1対1で対応する端点（先頭区間の開始／遊タイム区間の境界／endSource が hit・end の区間の終端）だけを追従させる。これで「時短抜けで追加した区間が記録の修正操作で消える」事故を防ぐ。区間ごとの holdSpins は組み直し経路でも保つ
- [x] 通常区間が複数あるセッションの消費玉と投資集計（S2）… 通常区間が2つ以上あるとセッション単位の集計をそのまま区間の消費玉に使うと二重計上になるため、B95 と同じ式（区間のタップ合計 −（台の持ち玉表示 − ツール追跡値））を区間単位に適用する。投資は打っていた区間の `segmentId` に紐づける。2周目以降の投資が「当選前」フィルタから漏れる問題も、通常区間が複数のときだけ遊タイム以外を全て通常時として集計して直す。単一区間セッションは従来の経路のままで表示値は不変
- [x] 転記用の貯玉引出に再プレイを算入（S2/§5）… `貯玉引出 = 開始持ち玉 + 再プレイ投資の合計`。従来はパーソナル店で開始持ち玉のみ、非パーソナル店で持ち玉投資＋再プレイを見ていて再プレイ分が落ちていた。実収支・差玉の式は変更しない
- [x] 実戦リザルト画面（S3b）… yutime-v3.html の履歴カードをタップすると、1台ぶんの振り返り用モーダルを開く。構成は ①今回の結果（実収支・実測回転率・大当たり・1R平均・実働・時給）②打つ前の判断の記録（開始カウンター・遊タイムまでの残り・想定回転率と出所・開始期待値）③想定と実測のズレ（回転率と期待値を想定／実測／差の表で出す）④区間ごとの内訳 ⑤これまでの積み上げ ⑥転記用 ⑦フッタ（実戦一覧に戻る／記録の修正／記録の削除）。戦果報告からも「リザルトを見る」で遷移できる。計算は既存の算出をそのまま使い、式は一切変えない
- [x] 単発の結果に良し悪しを付けない（S3b の設計原則）… 1回の稼働の「期待値と実収支の差」は事実として中立に表示し、判定バッジを置かない。勝った日は良い判断・負けた日は悪い判断という結果論を作らないため。ズレの表の直下には「1回のブレです。判断の良し悪しは下の通算で見ます。」を必ず出す。回転率の差は実測−想定で符号を取る（大きいほど良いので、想定18.0・実測18.5 は +0.5）。「上振れ」「下振れ」「ほぼ想定どおり」「サンプル不足」「良化」「悪化」等の語がリザルト画面のコードに無いことをテストで固定する
- [x] 積み上げも数字だけを出す（S3b）… ブロック名は「これまでの積み上げ」。同一機種（プリセット、解決できない台は機種名）の確定済みセッションと、同一店×同一機種の2軸で、実働・期待値合計・実収支合計・その差・期待値時給・実収支時給を出す（店軸は平均回転率も）。時給は合計÷実働。実働はB96の日別サマリーと同じ算出（時刻欠損は除外、日またぎは0扱い）。台数による分岐や判定は作らない
- [x] 履歴カードは展開せずリザルトを開く（S3b）… 履歴一覧は「日付見出し／日別サマリー／機種・台番・時刻／実収支・期待値の2枠／確定バッジ」だけのスキャン用にし、従来カード内に展開していた転記用・記録の修正・期待値の内訳・差玉行はリザルト画面へ集約した。同じ情報を2箇所に持たないため
- [x] 2周目以降の回転率が更新されない不具合の修正（S4/A-1）… 稼働中パネルの回転率・通常時合計を区間モデル（`deriveSession`）から求めるようにした。閉じていない通常区間の終端は、当選の有無に関わらず現在回転数で閉じる（従来は当選が1回でもあると `segmentEndSpinForRate` が null を返し、`runningNormalSpinCount` も `hitSpin − startSpin` で止まっていたため、時短抜け後に回した分が回転率に反映されなかった）。遊タイム突入回転数が未記録の旧データのフォールバックと、完了済みセッションの算出は従来のまま。検算＝区間①20回転/200玉＝25.0・時短抜け50・現在70回転/消費375玉 → 区間②10.0・セッション合計15.2
- [x] 投資タップの取り消し（S4/A-2）… 投資ボタンの隣に「取り消し」を置き、直前の1件（残高調整を除く）を削除する。投資が無いときは無効表示。トーストの「元に戻す」と同じ `deleteInvestment` を共有し、削除処理を二重に持たない
- [x] 大当たり履歴の一覧・修正・削除（S4/B-1）… 大当たり登録画面とリザルト画面から開く一覧。区間ごとに見出しを付け、新しい区間・新しい当選が上。R種別・当選カウンター・累計獲得出玉を修正でき、削除は二段確認。累計獲得出玉の修正はB88の差分方式（入力した累計 − それより前の当選の獲得出玉合計）で今回分を出し直す。当選には記録時に `segmentId` を持たせ、持たない旧データは当選カウンターで区間へ寄せる
- [x] 大当たり登録画面の整理（S4/C）… 画面名を「時短抜け」から「大当たり登録」に変更（C-1）。当選カウンターは当選ウィザードの記録値を読み取り専用で表示し、二重入力をやめる（C-2）。ラベルの無い数値を残さない（C-3）。R数は「今回の連チャン（直前の通常時区間に紐づく当たり群）」と「このセッション」を分けて出し、引いていないR種別は表示しない（C-4）。理論値の「R数ベース出玉」を削除し、実測の獲得出玉合計 ÷ 合計R で「1R当たり◯◯玉」を出す（実測が無ければ「—」で、理論値では代用しない）（C-5）。持ち玉は確定ボタンを廃止し入力した時点で反映する（C-6）。他人が当てた回数を含む「累計大当たり」を出さず「今回◯回」だけにする（C-7）
- [x] 時短抜けで投資元を持ち玉へ自動切替（S4/C-8）… 実戦の流れは「再プレイ→当たる→出玉で打つ」。再プレイのままだと気づかずに再プレイを減らすため、時短抜けを選んだ時点で投資元を持ち玉に切り替える。持ち玉が0のときは切り替えない。切り替えたことはトーストの1行で伝え、手動で戻すことは従来どおりできる
- [x] 状態バッジは「通常時」「遊タイム中」の2つだけにする（S4/D-1）… S2で時短抜けを入力できるようになり、当選後に宙ぶらりんになる期間がほぼ無くなったため「当り後（時短消化中）」と補助表示「リセット前」を削除した
- [x] 「総投入の内訳」を「消費玉数の内訳」に改称（S4/D-2）… 「総投入」は一般的な用語ではなく伝わらない。ラベルのみの変更で値の定義は変えない
- [x] 入力の即反映化と未保存警告（S5）… 原則は「入力した値はその場で反映される。保存ボタンを押させない。明示保存が妥当な画面では、未保存のまま閉じようとしたら警告する。黙って捨てない」。台情報は「台情報保存」ボタンを廃止し、各欄の `change`（フォーカスが外れた時）で保存する（釘評価チップと同じ挙動。トーストは400msのデバウンスで連続保存を1回にまとめる）（§1）。朝イチチェックはチェック・チップ選択の時点で保存し、「保存して閉じる」を「閉じる」に変えた（§2）。閉店チェックは連続入力の途中の桁を確定値として保存してしまうため即保存にせず、入力が残ったまま閉じようとしたら［保存して閉じる／破棄して閉じる／キャンセル］を出す（§2代替案）。フロアマップ編集は明示保存を維持し（編集途中が即反映されると台データとの対応が壊れるため）、未保存の変更があるまま閉じようとしたら同じ確認を出す（§3）。戦果報告の理論値「R数ベース出玉」を削除し、S4/C-5と同じ算出（実測の獲得出玉合計 ÷ 合計R、実測が無ければ「—」）の「1R当たり◯◯玉」に置き換えた（§4）。共通の仕組みとして `modalGuard`（false を返すとモーダルを閉じない関門）を追加した
- [x] 獲得出玉の累計を連チャン単位にする（S8）… 実機のデータカウンターが出す獲得出玉は「その連チャンの累計」で、時短が終わると0に戻る。B88はこれをセッション累計として扱っていたため、2回目の初当りで入力値が前回までの合計を下回り、警告が出て今回分が0で保存されていた（実測出玉が過少になり1R平均が小さく出る）。累計の範囲を連チャン＝同じ区間に紐づく当たり群（`hits[].segmentId`、S4/C-4と同じ切り出し）に変え、今回分＝入力値 − 同じ連チャン内のそれ以前の合計とする。下回り警告も同じ連チャン内の比較にする。セッションの実測出玉合計（`sessionActualBallsTotal()`）は各当選の今回分の単純合計に一本化し、B90でヤメ入力に置いた「累計獲得出玉」欄は実機が表示しない数字だったので削除する（`sessionActualBalls` フィールドは旧データのフォールバックとして残す）。大当たり履歴の累計表示と修正フォームの初期値も連チャン単位に揃えた。検算＝連チャン①（4R 1,380／6R 2,800）→時短抜け→連チャン②（4R 1,380／4R 2,600）で今回分 1,380/1,420/1,380/1,220・合計5,400玉・18R → 1R当たり300玉
- [x] 1R実質出玉の可視化・実測平均化（S9）… 期待値エンジンが使う `netBallsPerWin`（1R実質出玉）が判定画面のどこにも出ておらず、回転率にはある「手入力欄／実測平均／参考値」の3点セットが無かった。台詳細の期待値判定パネルに回転率の手入力と並べて「1R実質出玉（手入力）」を追加し（空欄なら自動決定・プレースホルダに自動値と出典を出す）、採用順位を パネルの手入力 → プリセットの手入力 → 実測平均 → ラウンド集計 → 理論値 にする。実測平均はS8の「今回分」を出典にし（当選ごとの記録が無い旧データだけヤメ入力の累計を使う）、実測出玉合計と出典を1つにそろえた。台詳細のメトリクスと稼働中パネルの再判定行に「参考1R出玉（出典・n付き）」を出し、根拠行を `使用回転率 17.0（手入力） / 使用1R実質出玉 990玉（実測平均・n=2）` の形にした。理論値の桁は落とさない（アグネスPE＝587.5玉）。期待値の式・保存データ・schemaは不変
- [x] 1R実質出玉の実測平均を合計R数ベースに統一（S10）… S9の実測平均は当選件数で割っており、当り1回あたりの構成R数（4R/6R/10R）を無視していた。`netBallsPerWinInfo()` の実測平均を `Σ今回分の獲得出玉 ÷ Σ消化R数` に変え、戦果報告の「1R当たり」（`sessionBallsPerRound`、S5で統一済み）と同じ考え方にそろえた。消化R数の出所は `roundCountFromRoundType` 1本（`totalRoundsForPreset` と同じ）。獲得出玉が未入力の当選は分子・分母とも除外する。`count` は合計R数の意味に変わったので表示も `n=◯` から `◯R分` にした。手入力・ラウンド集計・理論値の経路は不変。検算＝6R540玉→90玉／10R1,300玉＋4R420玉→123玉／10R未入力＋4R420玉→105玉／当選1回10R1,300玉→130玉
- [x] 1R実質出玉の単位を期待値エンジンとそろえる（S11）… S10で実測平均だけが「玉/R」になり、理論値・ラウンド集計・手入力は当選あたりのままだったため、同じ `netBallsPerWin` に2つの単位が混ざっていた。方針B（全部を玉/Rへ揃える）を採用し、エンジンのプリセット `spec` に `averageRoundsPerWin`（大海5SP=10R、アグネスPE=587.5÷108≒5.44R）を足して `winBalls = expectedWins × (netBallsPerWin × averageRoundsPerWin) + 電サポ増減` に変えた。平均R数は既存の `standardPayout ÷ standardRounds` の関係から出し、新しい推定は足していない。`defaults.netBallsPerWin` は 587.5→108 / 1400→140、定数も `DEFAULT_NET_BALLS_PER_ROUND = 140` に改名。ラウンド集計も玉/Rへそろえ（R種別の公称出玉 ÷ R数）、`yutime-calc.html` のページ側変換はアグネスPEが素通し・大海5SPが平均R数で割る形になった。schema 32 で 31以前の手入力値（当選あたり）を平均R数で割って移行する。受け入れ検算＝カウンター150・回転率17・1R実質100玉・等価・現金 → +1,569円、カウンター0・105玉 → +310円、記事v5の期待値表90セル全一致、いずれも変換前と同値
- [x] 貯玉引出の店種別判定への修正とリザルト表示の整理（S12）… 転記用の `貯玉引出` が `transferSummaryForSession()` で `store` を見ておらず、非パーソナル店でも常にパーソナル式（開始持ち玉＋再プレ）を返していたバグの修正。B84で確定した条件がS4の修正で落ちた同型の事故の2回目のため、分岐を `usesPersonalBalanceFormula(store)` に、投資玉数を `playerInvestedBalls()` に1本化し、`profitYenForSession()` と `withdrawBalls` が同じ関数を通るようにした。条件式もパーソナル式そのものもファイル全体で1箇所だけであることをテストで固定している。**分岐の主軸は店の種別だけにする**（タップ投資モードを条件に混ぜると、パーソナル店に実在する investments 空の旧記録が非パーソナル式＝0玉へ落ちるため）。検算＝非パーソナル125玉×19回→2,375個／パーソナル2,500＋再プレ500→3,000個（不変）／パーソナル・investments空・開始持ち玉26,637個・11,462個→そのまま26,637個・11,462個。差玉の式は不変。パーソナル店でtapModeでない記録は実収支も `開始持ち玉＋再プレ` を引く形に変わる（従来は投資玉0扱いだった）。あわせて表示を2件、リザルトの「期待値との差」を「開始期待値との差」に改称（数値は不変）、「区間ごとの内訳」を 区間／起点／終点／回転数／消費玉／回転率 の表にした（保留控除の注記は表の下に1行でまとめる。320pxで表幅264px・はみ出しなしを実測）
- [x] 開始持ち玉と稼働中の持ち玉の分離（S15）… `session.startMochidama` が稼働中の持ち玉修正で上書きされ続け、「開始時点の持ち玉」ではなく「最後に修正した時点の持ち玉」になっていたバグの修正。9/7の実戦データ37セッション中17件で判定パネルの入力と保存値が食い違っていた。`startMochidama`（打ち始めの持ち玉。以後不変）と `currentMochidama`（稼働中の持ち玉の起点）を分け、稼働中の残高修正はすべて後者に載せる（書き込み先の分岐は `balanceCurrentKey()` の1箇所）。`deriveBalances()` は `mochidamaBaseValue()` を通すので画面上の現在持ち玉は不変。実収支・貯玉引出・投入玉の参照先は `startMochidama` のままで変えない。**区間の `segments[0].startTrackedBalls` も同じ値で汚染されていた**（`persist()` が毎回 `resyncSessionSegments()` で `startMochidama` から作り直すため）ので復元根拠に使えず、移行は ①引き継ぎ元の `終了合計玉＋残保留` ②`startEv.mochidamaInput` ③現行値維持 の順で復元する。schema 32→33、移行前データは `ytv3:backup:s15` へ1度だけ退避する（`backup:latest` は persist のたびに上書きされるため）。修復前の値は `currentMochidama` として残す
- [x] 遊タイム状態の判定を区間モデル基準にする（S7/A-1・A-2）… `isYutimeInvestmentPhase()` が `yutimeEnterTime || yutimeEnterBalls !== null` というフラグだけを見ていたため、遊タイム経由で当選したあとも「遊タイム中」から戻らず（9/7実戦③）、以後の投資・当選が遊タイム区間に紐づき続けて「今回の連チャン」が前回のまま止まっていた（同①）。判定を「末尾の区間が kind: yutime かつ endSpin が空」に変え、`yutimeEnterTime` / `yutimeEnterBalls` は記録としてだけ残す。遊タイム区間を持たない旧データ（区間が無い／突入回転数が未記録で遊タイム区間を組めない）は従来の判定へフォールバックするので表示は変わらない。あわせて遊タイム突入の玉数入力が `yutimeEnterBalls` にしか入らず稼働中パネルの持ち玉に反映されなかった不具合（同②）を、`updateMochidamaBalanceWithUndo()` を通して直した
- [x] 消費玉を起点・終点の実測差で出す（S7）… `消費玉 = 起点の持ち玉 + 区間内に外から足した玉 − 終点の持ち玉`。起点は打ち始め入力／時短抜け入力／遊タイム突入入力、終点は当選ウィザードの残り持ち玉かヤメ入力の終了玉で、いずれも実測値。外から足した玉は再プレイ投資と現金投資（玉換算）だけで、持ち玉タップは起点に含まれるため加算しない。区間に `startBallsSource`（measured / tracked）を持たせ、起点が実測の区間だけ新式を使う。大当たり登録の時短抜けに「そのときの台の持ち玉」欄を追加し（初期値＝現在の持ち玉＋この連チャンの獲得出玉合計）、入力値を新しい通常区間の `startTrackedBalls` と `currentMochidama` の両方へ入れる。データカウンターの獲得出玉は1R平均専用で回転率には使わない。**適用範囲はセッションの `consumedModel: "endpoints"` で切り、S7以降に打ち始めたセッションだけが新式**。旧セッション（フラグ無し）はB95/B85式のままで表示値は1つも変わらない（HEAD版と新版の同一データ比較で旧セッション1,388件の全表示値・全区間が一致することを実測）。新式の区間ではB89の乖離選択UIを出さない。schema 33→34。検算＝起点0/再プレイ1,000/終点50→950、起点1,620/追加なし/終点120→1,500、起点1,620/現金1,000円(250玉)/終点370→1,500、起点1,620/持ち玉タップ1,500玉/終点120→1,500（タップは加算しない）、起点1,620/終点1,700→警告・null。回転率＝区間①20回転/200玉＝25.0・区間②15回転/375玉＝10.0・合計35回転/575玉＝15.2（S4/A-1と同値）
- [x] 持ち玉を修正したタイミングで回転率を再計算（S7b/A）… S7で回転率の主役が「起点と終点の実測差」になったので、稼働中に持ち玉を実測値へ直す操作も終点と同じ扱いにする。開いている区間に**途中測定点**（`lastMeasuredBalls` / `lastMeasuredSpin`、最新の1つだけ）を持たせ、`その時点の消費玉 = 起点 + 区間内の外部投入（再プレイ・現金換算） − 修正後の持ち玉` で確定する。書き込み経路は `recordSegmentMeasurement()` の1箇所にまとめ、稼働中パネルの「修正」（`openBalanceEditForm`）と `updateMochidamaBalanceWithUndo()` の両方から通す。トーストの「元に戻す」では測定点も戻す（`segmentMeasurementSnapshot` / `restoreSegmentMeasurement`）。当選・ヤメで終点が入ったら終点が優先（`segmentMeasuredEndBalls`）。測定点が無い区間は従来どおりタップ合計を目安に使う。稼働中パネルの回転率の横に出典を1語添える（`（実測）`＝終点または途中測定点で確定／`（目安）`＝タップ合計。B94「判断の場に前提を置く」）。旧セッション（`consumedModel` 無し）には測定点を記録せず表示も出さない。検算＝起点1,620・追加なし・持ち玉を1,120に修正・現在75/起点50/保留5 → 20回転・500玉・10.0（実測）／同条件で修正せずタップ500玉 → 10.0（目安）／その後に当選（終点120）→ 1,500玉で確定
- [x] 残保留当選の自動判定（S7b/B）… 時短抜け直後の残保留（既定5個）で当たると、実態は通常時0回転なのにツールは時短抜けカウンターから当選カウンターまでの数回転を通常時として数えていた（9/7実戦の⑯⑳、時短抜け25→28で当選）。期待値エンジン（B91）が残保留を連チャンの引き戻しに含めている立場に記録側も揃え、**残保留当選は前の連チャンの続き**として扱う。入力は増やさず、`当選カウンター − 区間の起点カウンター ≦ その区間の holdSpins` で自動判定する（境界は「≦」）。判定対象は時短抜けで始まった区間だけで、区間に `startSource: "jitan"` を持たせて識別する（打ち始め区間・遊タイム区間は対象外。旧データはこの印を持たないので判定が走らず表示が変わらない）。該当区間は通常時なし＝打ち出し回転数0・消費玉0で回転率の集計から外し、投資が残っていれば警告を出す。当たりの `segmentId` は打っていた区間のまま保存し、寄せるのは参照するときだけ（`resolveSegmentChainId`）にして、手動で戻したときに元の区間へ戻るようにする。これで「今回の連チャン」（S4/C-4）・獲得出玉の累計（S8）・1R当たりが紐づけ先の連チャンで集計される。大当たり登録画面とリザルトの区間内訳に「残保留当選（通常時なし）」を明示し、大当たり履歴に「残保留当選の扱い」の節を置いて区間ごとに手動で切り替えられるようにする（`holdCarryHit` が null なら自動判定、true / false は手動指定）。エンジン側は変更しない。検算＝時短抜け25→当選28（保留5）は残保留当選／25→31は通常当選／50→54で保留4に編集済みは残保留当選（≦）／打ち始め区間の0→3は通常当選
- [x] 残保留当選の判定を「打ち出し状態」方式へ変更（S7c）… S7bはカウンターの差（起点からの回転数 ≦ 残保留数）で判定していたが、残保留で当たったかどうかは回転数ではなく**打ち出しがあったか**で決まる。遊タイム狙いは時短抜けで打ち出しを止めて保留が回るのを待つ。打ち切りは時短抜け後も打ち続ける（保留5個以上で短縮変動が効く）ので、数回転後に当たっても新しい当たりになる。判定を `kind: normal` かつ `startSource: "jitan"` かつ `endSource: "hit"` かつ **`shooting === "before"`** に差し替え、回転数の条件は判定から外した。セッションに `playStyle`（"yutime" / "continuous"、既定は遊タイム狙い）、区間に `shooting`（"before" / "started"）を持たせる。`playStyle` は時短抜けで作る区間の初期状態を決めるだけで判定には使わない。`"before"` → `"started"` は「打ち出し開始」ボタンか、その区間への投資タップのどちらか早い方で切り替わり、一度 started になったら戻さない（戻すときは大当たり履歴の手動切り替え）。回転数は警告にだけ使う：`before` で `当選カウンター − 起点 > holdSpins + 3` なら「打ち出し開始の押し忘れ？」、`started` で `≦ holdSpins` かつ投資タップ無しなら「打ち出し前の当選では？」（どちらも判定は変えない）。打ち出していない区間は当選の有無に関わらず通常時なし（回転数0・消費玉0）として回転率の集計から外す（`segmentSkipsNormalPlay`）。S7bの部品（`holdCarryHit` の手動指定・前の連チャンへの寄せ・大当たり履歴の切り替え・区間内訳の「残保留当選」表示）はそのまま使う。schema 34→35、既存区間は **S7bの回転数ベースの判定（`legacyHoldCarryHit`）が true だったものだけ `shooting: "before"`** に補完するので、既存データの判定結果・回転率・集計は変わらない（S7b期のデータ384件＋実データ37件＋合成旧データ1,388件をa11a7c6版と並走比較して確認）。UIは打ち始めウィザードに「打ち方」の2択、時短抜け画面に「この後（打ち出しを止める／続ける）」、稼働中パネルに状態バッジ「保留消化中」と保留消化中だけ出る「打ち出し開始」ボタンを追加した。**稼働中パネルには常時表示の操作を足していない**（320pxではパネルの縦が既に余白ゼロ（530/530）で、44pxの常時表示を1つ足すだけで固定全画面がスクロールに変わるため）。方針のセッション中変更は稼働中パネルの「記録の修正・削除」に置いた。検算＝時短抜け25→当選28は、止めていれば残保留当選／打ち出し開始を押していれば通常当選／タップ1回でも通常当選。打ち切りの初期状態は started だが時短抜け画面で「止める」を選べば残保留当選
- [x] 投資phaseの修復（S17/第1部）… 9/7時点のバグ（`isYutimeInvestmentPhase` が当選後も true のまま。S7 rev2/A-1で修正済み）で、遊タイム経由の当選後に打ったタップが `phase: "yutime"` のまま保存されていた。区間の紐づけ（`segmentId`）は正しく通常区間を指しているのに phase が遊タイムなので、通常時の消費玉から漏れて回転率が壊れる（実例：9/7・台325の「時短抜け50→当選170」区間が消費84玉・回転率342.3）。schema 35→36 の移行で、**`segmentId` が指す区間の種別と phase が矛盾していれば区間の種別に合わせる**。`segmentId` が解決できない投資は触らない。移行前データは `ytv3:backup:s17` へ1度だけ退避し、修復件数をコンソールに出す。検算＝実データ37件で修復10件・1,250玉（すべて台325の1セッション、他は0件）。当該区間は 84玉/342.3 → **1,334玉/21.6**、セッション実測回転率は 24.25 → **21.78**。他36セッションの表示値は1つも動かない
- [x] 獲得期待値（Σ区間期待値）（S17/第2部）… 「開始期待値」は最初の判断しか表さないので、1日打ち切りだと仕事量が見えない。**獲得期待値＝Σ（通常区間ごとの、起点カウンターからの期待値）** を追加する。各通常区間は「その起点から当たるまで打つ」という判断なので、足せば1日の仕事量になる。遊タイム区間・残保留当選の区間・打ち出しをしなかった区間（`segmentSkipsNormalPlay`）は判断としての打ち出しが無いので含めない。入力は区間ごとに揃える：起点は区間の `startSpin`（宵越しは打ち始め区間だけ）、回転率は**セッション全体の実測**（区間ごとの実測はタップ数に引きずられて異常値になる）、1R実質出玉は既存の優先順位（`netBallsPerWinInfo`）、持ち玉・再プレ・現金は `startEv` の入力を流用。計算は既存の `calculateMachineExpectation()`（＝`YUTIME_EXPECTATION_ENGINE.calculate`）を呼ぶだけで、**エンジンは1文字も変えない**（yutime-calc との一致テストで担保）。実測が出せないセッションは開始期待値の想定回転率で代用し出典を「想定」と出す。**実測が 1〜50（試算カードの `clampTrialRate` と同じ帯）の外なら実測として採らない**（2回転でヤメた記録＝持ち玉11,462個・0.04回転率をそのまま渡すと −580万円が出るため）。表示はリザルトの区間内訳に「期待値」列（対象外は「—」）＋合計行「獲得期待値 ◯◯円（回転率◯◯・実測／1R◯玉・出典）」、「今回の結果」に1項目、履歴カード・日別サマリー・積み上げの期待値を獲得期待値ベースへ置き換え、積み上げに注記を1行。**開始期待値は「打つ前の判断」「想定と実測のズレ」「転記用」にそのまま残す**（判断の記録と仕事量は役割が違う）。検算＝9/7・台325は13区間・**+20,586円**（回転率21.78・実測／1R108玉）で開始期待値 −22円と対照的。実データ37件では開始期待値合計 53,032円 → 獲得期待値合計 63,019円
- [x] S17の追補3件（S17b）… ①**遊タイム玉減りを区間の端点から出す**。`突入時玉数 ＋ 遊タイム区間内の投資 − 遊タイム区間の endRemainBalls`。セッションのフラット項目（`hitRemainBalls`＝最後の当選時の残り持ち玉）は、当選が複数あると遊タイム区間の終点と別物になるので使わない。区間の終点が無い旧データだけ従来式へフォールバックする。検算＝9/7・台325が `1,535 − 1,500 = 35玉`（S17時点では −777 で算出不可＋警告になっていた）。②**残保留当選の手動切り替えの対象を広げる**。`holdCarrySegments` の条件を「`startSource === "jitan"`」から「`startSource === "jitan"` または 起点がプリセットの時短抜けカウンター（`jitanExitOptions` の `counterSpin`）と一致し、かつ直前の区間が当選で終了」に変えた（当選で終わった通常区間であることは共通の前提）。S7b以前の記録には `startSource` の印が無く一覧に出せなかったため。**自動判定（`detectHoldCarryHit`）は変えない**ので既存の判定結果は不変。台325では0件→10件になり、区間9（25→28）・区間13（50→51）が切り替えられるようになる。③**獲得期待値の回転率の出典を3段にする**。①セッションの実測（通常回転数 `EARNED_EV_MIN_SPINS`＝100以上、かつ 1〜50）→ ②その台の参考回転率（`machineStats`。同じ条件で判定）→ ③開始期待値の想定回転率。出典表示は「実測／参考／想定」。母数の薄い実測（10回転で当たった記録の10.0など）が期待値を大きく歪めていたための追加。実データ37件では 実測29件 → 実測20/参考6/想定4/算出不可7件、獲得期待値合計 63,019円 → 100,578円。8/26・台360（10回転）は「参考20.8」へ、8/5・台290（2回転・0.04）は引き続き算出不可
- [x] アグネスPEの1R実質出玉の既定値を記事の実戦基準へ（S18）… エンジンの `agnes-pe` の `defaults.netBallsPerWin` が公称払い出し（648÷6＝108）のままで、yutime-calc の `payoutDefault: 100` と記事v5（1R100玉が中心）とずれていた。9/7の実測（4R 370〜430／6R 570〜630／10R 1,000〜1,010）もほぼ100玉/Rなので、**既定を100玉/Rに変更**。`averageRoundsPerWin` は当選あたりの平均R数（R構成の重み）であって出玉ではないので `587.5 / 108` のまま動かさない。エンジンは yutime-calc.html にも同じブロックがあるので同時に直し、一致テストで担保する。既定値の出典ラベルは機種ごとに性格が違うので `MACHINE_PRESETS` に `defaultNetBallsLabel` を持たせ、アグネスPEは「**基準値（記事の1R100玉）**」と出す（公称ではなく実戦基準だと分かる表記）。大海5SPの1,400玉/当選＝140玉/Rは夢爽が実戦基準で置いた値なのでラベルも既定値もそのまま。検算＝カウンター150・回転率17・等価・現金・手入力なし・実測なしで **+1,569円**（記事v5の代表点と一致。変更前は 2,002円で、同条件のcalcの1,569円とずれていた）。calcとv3が同条件で同値になることと、calcの `payoutDefault` とエンジンの既定値がそろっていることをテストで固定した。実データ37件では、実測平均やラウンド集計が出ているセッションは不変で、既定値に落ちる2件（9/3・台200 1,678→905円、9/5・台201 2,342→1,181円）だけが動く。umi-sp5の29件は全て不変
- [x] 1R実質出玉の「ラウンド集計」の段を廃止（S19）… `netBallsPerWinInfo()` の採用順位から「ラウンド集計（R種別の公称出玉 ÷ そのR数）」を外し、**パネル手入力 → プリセット手入力 → 実測平均（合計R数ベース・S10）→ 基準値（プリセット既定）** の4段にした。アグネスPEは 1080/10・648/6・432/4 がどれも108玉/R なので、当選のR種別を記録しただけで常に公称108玉が採用され、実測（S18で100玉に直した基準値）より優先されて甘い判定を出していた。獲得出玉を入れていない台が全部108玉で判定されるのが実害。実測が無いときは公称に寄せず基準値へ落とす。umi-sp5も同じ扱い。出典表示は「手入力」「実測平均・◯R分」「基準値」の3種になり、ラウンド集計専用だった `n=◯` の書式も削除した。検算＝アグネスPEで当たりのR種別だけ記録して獲得出玉が無いセッションは 100玉「基準値（記事の1R100玉）」、獲得出玉ありは実測平均のまま不変。実データ37件では108玉→100玉に変わったのは2セッション（9/7・台325 獲得期待値 20,586→15,567円、9/5・台201 2,342→1,181円、合計 −6,180円）で、実測平均が出ているセッションと umi-sp5 は不変。エンジンは変更なし（この関数はエンジン外）
- [x] 1R実質出玉（減算込み）を持ち玉差から算出（S20）… 記事とエンジンの「1R実質出玉」は減算込み（電サポ中の減りを含む純増）の定義なのに、ツールの実測平均（S8〜S10）はデータカウンターの獲得出玉＝払い出し（グロス）で集計していて定義が食い違っていた。S7で時短抜け時の持ち玉を入力するようになったので、**連チャンごとの純増＝次の通常区間の起点持ち玉 − 当選時の持ち玉 − 時短中に外から足した玉**を直接出せる。1R実質出玉＝Σ純増 ÷ ΣR数。連チャンの起点は「当選で閉じた区間」で、残保留当選の区間は前の連チャンの続き（S7bの寄せ先）なので起点にせず、純増の窓もその区間をまたいで次の通常区間まで延ばす。最後の連チャンで次の区間が無いときはヤメ入力の終了玉を次の起点に使う。採用は**連チャン単位**で判定し、①持ち玉差 → ②カウンターの獲得出玉（S8。従来値）→ ③どちらも無ければ集計から除外。出典表記は「実測平均・持ち玉差・◯R分」「実測平均・カウンター・◯R分」、混在時は「実測平均・持ち玉差◯R＋カウンター◯R」。手入力・基準値の経路は不変。時短中の投資は当選で閉じた区間に紐づいて通常時の投資と区別できないため、`addInvestment` が「開いている区間が無いとき（＝当選から時短抜けまで）」に `duringJitan` の印を付ける（印の無い旧データは推測で差し引かない）。リザルトの「今回の結果」に「1R実質出玉 ◯玉（減算込み）／ 払い出し ◯玉 ／ 時短中の減り ◯玉」を1行足し、区間ごとの内訳に「純増／1R」列（8列目）を追加した（純増と1Rは1列にまとめ、320pxでは表を横スクロールで見る）。エンジンは変更なし（この関数はエンジン外。yutime-calcとの一致テストで担保）。検算＝9/7・台325は連チャン12件の純増 +2,323／+1,354／+588／+595／+1,614／+1,539／+417／+389／+2,528／+978／+1,168／+1,072 で合計 **14,565玉 ÷ 150R ＝ 97.1玉**（カウンター未入力なので払い出しは「—」）。実データ37件では9セッションの実測平均が動き（台325は基準値100玉→実測97.1玉で獲得期待値 15,567→13,747円）、獲得期待値の合計は 93,625 → 76,052円。8/26・台360は 純増0玉／10R になる（ヤメ入力の終了玉に大当り出玉が入っていない記録）ため、実測平均が実態より辛くなる。記録側の確認が要る
- [x] 1R実質出玉の実測平均を機種単位（全店）で集計（S22）… 1R実質出玉はアタッカーのこぼれ具合が主因で、釘（店ごと）より台の個体差・機種の性質が効くので、機種単位の集計を**アクティブ店から全店の同機種セッション**へ広げた。台単位（その台の記録がある台）は現行どおりで不変。採用順位は **パネル手入力 → プリセット手入力 → その台の実測平均 → 全店の同機種の実測平均（＋店補正）→ 基準値** の5段。集計条件（持ち玉差→カウンター→除外の連チャン単位フォールバック・分母は合計R数・`status === "completed"` のみ）は S10・S20 のまま。店ごとの差は店設定の「1R実質出玉の補正（玉）」（既定0・マイナス可・保存ボタンなしの即反映）で吸収し、**全店の実測平均を使うときだけ**足し引きする（台単位の実測・手入力・基準値には掛けない）。出典表記に集計範囲を添える（「実測平均・持ち玉差・150R分（この台）」「（全店・n台）」、補正時は「（全店・4台、この店 −2玉）」）。回転率の集計範囲は店単位のまま（釘は店ごと）。エンジンは変更なし（この関数はエンジン外。yutime-calcとの一致テストで担保）。実データ37件では、台単位の実測がある13台・16セッションは値も獲得期待値も不変（表記に「（この台）」が付くだけ）で、実測が無い台だけが動いた＝アグネスPE 35台が基準値100→96.33玉（全店・4台・188R）、大海5SP 105台が理論値140→87.70玉（全店・9台・240R）。セッションは17件で使用値が変わり、いずれも大海5SPの140→87.70で獲得期待値は約 −5,300〜−5,700円ずつ動く。**注意**：大海5SPの全店平均は 8/31・アムズ台103 の −73.75玉/R（40R・ヤメ入力の終了玉に大当り出玉が入っていない記録）に引きずられており、この1台を除くと120.0玉/R。記録側の確認が要る（S20の8/26・台360と同じ性質の記録）
- [x] テスト店の除外と、純増0以下の連チャンの除外（S22b）… ①店設定に「テスト用の店（集計から除外）」（既定OFF・即保存・店一覧に「テスト」バッジ）を追加し、ONの店の記録を**店をまたぐ集計**（全店の1R実質出玉の実測平均・リザルトの「これまでの積み上げ」の機種軸）から外す。その店を選んでいる間の自分の表示（台の参考値・履歴・リザルト）は従来どおり出す＝除外の判定は `sessionCountsForAggregate(session, keepStoreId)` 1本で、keepStoreId（見ている台の店／そのリザルトの店）だけは通す。店×機種軸の積み上げと店内の累計参考回転率はもともと店単位なので変更なし。schema 37 へ上げ、移行時の初期値だけ店名「アムズABCビッグトマト」をONにする（以後は保存値を尊重し、名前で上書きしない）。②`netBallsActualTotals` で**純増が0以下の連チャンを分子・分母から除外**する（当たって玉が減ることはないので入力ミスとみなす）。除外した連チャンはカウンター経路にも落とさない（同じ当選の二重採用を避ける）。カウンター経路は元から `actualBalls > 0` の当選しか採らないので実質は持ち玉差の段の防御。除外したR数は出典に「除外◯R」で添え（例：`129.6玉（実測平均・持ち玉差・150R分（全店・6台、除外10R））`）、リザルトの区間内訳では該当行の純増セルに⚠を付けて表の下に「⚠ ①は純増が0以下のため集計から除外（入力を確認）」を出す。判定は `chainExcludedFromNetBalls` 1本で集計と表示が共有する。エンジンは変更なし。実データ37件（アムズ＝テスト店）では、大海5SPの全店平均が 87.70→**129.56玉/R**（DSG＋KEIZの6台・150R、除外10R。理論値140との差 −10.4玉/R）、アグネスPEは 96.33→**96.85玉/R**（DSG2台・160R。負の連チャンが無いので②の除外は0R）。8/26・DSG台360の純増0/10Rが外れて台360は 111.38→139.225玉（40R）、8/31・アムズ台103の −73.75玉/R は台単位の実測から外れた。テスト店以外では17セッションの獲得期待値が動き（+2,954〜+4,541円／件、合計 **+70,784円**）、大海5SPの機種軸の積み上げは29台 −45,841円 → 26台 +43,625円（DSG選択時）
- [x] 出来事の時刻を自動記録し、通常時の時速と当たり消化時間を出す（S23）… 当選（当選ウィザード完了）・時短抜け・遊タイム突入・ヤメの瞬間を区間の `startAt`/`endAt` にISOで自動保存する。手入力欄は増やさず、事後の修正だけ大当たり履歴（当選時刻）と記録の修正（時短抜けの開始時刻）にHH:MM:SSの欄を1つずつ足した。導出は 区間の所要時間＝`endAt − startAt`、通常時の時速（回転/分）＝打ち出し回転数÷所要時間、セッション平均＝Σ打ち出し回転数÷Σ通常時所要時間、当たり消化時間＝当選`at`→次の通常区間の`startAt`（連チャン全体で1つ。残保留当選の区間はまたぐ）。表示はリザルトの「今回の結果」に通常時の時速、区間内訳に「所要」列と当選行の下の「当たり消化 ◯分◯秒」、日別サマリーと積み上げに「平均時速」、台詳細の参考回転率の枠に「参考時速」。時速は単独では意味が薄い（回転率が高いほど短縮変動で速くなる）ので、表示は必ず回転率とセットにする。期待値の想定時速は 手入力（プリセット設定）→ 台の実測 → 店の実測 → 既定250/h の4段で、根拠行に出典を出す（実測として採るのは 100回転以上・20分以上・1〜12回転/分の範囲のみ）。schema 37→38。移行では区間の時刻をISOへそろえ、当選で閉じた区間の `endAt` を `hits[].at` から埋める（埋められない時刻はnullのまま）。**TZ対策**：HH:MMの欄は記録した端末のローカル時刻、`hits[].at` はISO（絶対時刻）なので、混ぜて差を取るには記録時のUTCオフセットが要る。`startTime` と `createdAt` は打ち始めの同じ瞬間に書かれるので、その差の最頻値（15分刻み）を記録時のオフセットとし、実行中の端末のTZに依存させない（`deriveRecordedUtcOffset`）。同じ理由で、投資の前後判定 `isoTimeToMinutes`（S17から実行中のTZで `hits[].at` を読んでいた）も記録時のオフセットで読むよう直した。エンジンは変更なし。実データ37件では期待値・回転率・獲得期待値・参考回転率はすべて不変で、想定時速が実測に変わった28件の消化時間・時給だけが動く（判定の変化は0件）。TZ=UTC と Asia/Tokyo で全表示値が完全一致することを実データで確認
- [x] 持ち玉入力の一本化と実戦不具合の修正（S24）… 9/9の実戦（DSG高岡・7台）で見つかった不具合。中心は**大当たり登録の画面に持ち玉の入力欄が2つあった**こと。①同じ瞬間の同じ数字を2回聞く状態（S4/C-6の「持ち玉（消化後の値）」とS7/B-3の「そのときの台の持ち玉」）を解消し、**後者1つだけ**にした。この欄の値は従来どおり区間の起点（`startTrackedBalls`）と `currentMochidama` の両方に入る。稼働中パネルの「持ち玉 修正」は残す。②初期値は **①直前の当選時の残り持ち玉＋その連チャンの獲得出玉（入力済みなら実測）→ ②直前の当選時の残り持ち玉＋その連チャンのR数×1R実質出玉（実測平均→基準値）→ ③追跡値（currentMochidama）** の順で埋め、②のときは「見積もり（12R×100玉）。台の表示に合わせて直してください」を1行添える（注記がそのまま検算できるよう1R実質出玉は丸めてから掛ける）。**0は初期値にしない**（当たった以上、出玉ぶんは必ず増えているため。どれも埋められないときは空欄）。実戦では再プレイ始まりで `currentMochidama` が0のまま、獲得出玉も未入力だったため、初期値0がそのまま確定されて区間の起点が0になっていた。③「そのときの台の持ち玉 ≦ 直前の当選時の残り持ち玉」で確定しようとしたら「当たったのに持ち玉が増えていません。台の表示を確認してください。」と確認を1回挟む（計数機へ全部入れた場合もあるので**確定は止めない**）。④純増0以下で集計から外れた区間（S22b）の注記に「起点の持ち玉が正しくない可能性」と直し方を添え、記録の修正に**時短抜け区間の起点の持ち玉**の欄を足した。既存データは移行で書き換えない（推測で直さない）。⑤R種別の押し間違いを直前1件だけ取り消せるボタンを、投資タップの取り消し（S4/A-2）と同じ形で置いた。取り消せるのはいま登録している連チャンの直前1件だけ。⑥大当たり登録はR種別を押すたびに画面を開き直すため、「この後」の選択と手で直した起点の持ち玉が毎回初期値へ戻っていた。画面内の下書き（連チャン単位）に残して開き直しでも消えないようにした。初期選択はセッションの `playStyle` のままで、画面で選び直してもセッションの方針は変えない（S7c/1-1の設計どおり）。⑦打ち出し回転数が0のときの回転率を「0.0（目安）」から「—」にした。実測が無いだけの区間が回らない台に見え、稼働中の再判定まで回転率0.0で走っていた。⑧当日すでに当選している台（前日ヤメを使わない条件・B85）ではラムクリアのチェックも無効化し、「当日すでに当選しているため、ラムクリアの有無は判定に使いません。」を1行出す。⑨台移動の引き継ぎ（この持ち玉で次の台へ）が打ち始めの残高欄に入っていなかったのを直した。引き継ぎ中は台詳細の持ち玉・再プレイ・カード残高を引き継ぎ値で初期化し（0と未入力は空欄のまま）、カード残高の欄にも入力イベントを繋ぐ。従来は空のまま「この台を打つ」を押すと開始残高が上書きで消え、現金の投資タップが「現金が未入力です。」で弾かれていた（＝実戦で「台移動後に現金欄が反応しない」）。⑩**打ち始めでも打ち方（遊タイム狙い／打ち切り）を選べるようにした**。打ち始めウィザードを通らない導線しか無いため `playStyle` を選ぶ場所が無く、常に既定の「遊タイム狙い」で始まっていた（＝時短抜けのたびに既定へ戻って見える原因）。台詳細の期待値判定パネルに2択を置き、選んだ値は `ytv3:start:playStyle` に覚えて次の打ち始めの初期選択にする（投資元・固定表示と同じ即反映のプリファレンス）。セッションの方針は従来どおり各記録が持ち、途中変更は「記録の修正・削除」から。schema変更なし・エンジン変更なし。検証は実データ9/9版39件で回転率・期待値・獲得期待値・区間内訳・参考回転率が**すべて不変**（TZ=UTC と Asia/Tokyo で一致）。検算＝純増0以下の区間は 台326の区間1（−41玉）・区間2（−54玉）と台317の区間1（−33玉）の3件で、区間内訳に⚠と注記が出て集計から除外される。台326の区間3（前の当選時の残り9玉・連チャン12R）を再現すると初期値は **9＋12×94＝1,137玉**（実測平均93.9玉/R。実戦で手入力した1,175玉と38玉差）。全記録の連チャン47件で初期値が0になるものは0件、実ブラウザ（headless Chrome・実ボタン操作）で大当たり登録〜時短抜け〜リザルト〜記録の修正まで通し、320/390/412pxではみ出し・文字切れなし
- [x] リザルト画面の再構成（S26）… yutime-v3.html のリザルトを「今回の判断に必要な情報」と「後から検証するときだけ必要な情報」に分け、後者は展開したときだけ出す。計算ロジック・schema 38 は不変で、表示の並びと出し方だけを変えた。①ヘッダーの「未入力あり」「入力済み」バッジを廃止し、入力確認の枠へ集約する（概算・台移動の続きのバッジは従来どおり）②今回の結果を「実収支／獲得期待値／実測回転率」の3カード横並びにする（390pxで1行。回転率が出せないときの理由はカードに残す）③打つ前との比較を2行に整理し、想定回転率の出典は想定セルへ入れて再掲行を消す。2行目のラベルを「獲得期待値」に統一し、**差を「獲得期待値 − 開始期待値」に変更**（従来は「実収支 − 開始期待値」。9/9・台326 で -6,784円 → +6,318円。`summary.evDiffYen` の算出は保持）④**入力確認**を新設（該当があるときだけ）。警告条件ごとに「項目／影響する数値／修正画面の遷移先」を持つ対応表 `RESULT_INPUT_WARNINGS` から1件1行を組み立てる（`resultInputWarnings`）。文言を警告箇所ごとにべた書きしない。**未入力でも別経路で数値が出ている項目には、その事実をそのまま書く**（`resultRateWarningImpact`。例：終了総当たり回数が未入力 → 記録済みの大当たり回数を表示しています（累計差では計算していません））。「計算していません」で一律に書くと画面の数値と矛盾するため。実際に数値が欠けている項目を先に並べる。起点の持ち玉は `missingFields` がセッション単位でどの区間か特定できないので番号を出さず「起点の持ち玉が未入力の区間があります」とする⑤実戦の流れを縦タイムラインにする（`resultTimelineHtml`）。1通常区間1行で「① 打ち始め → 24回転で当選（7分04秒・回転率18.0）」の形。遊タイム区間は直前の行に終点だけを接続し、**回転数は生成しない**（区間モデルが遊タイムの `spins` を持たず、既存の区間表でも回転数欄は「-」。起点・終点カウンターの引き算で新しい数値を作らない）。残保留当選は行を作らない。純増0以下で除外された区間は行頭に⚠を付け、区間表の番号と一致させる。本文は「→ 当選」まで折り返さず、長い行はその行の中だけ横スクロールする（ページは横スクロールしない）。既存9列の区間表は［詳しい区間データを見る］で展開（既定は閉）。区間が1つだけの稼働は S3b/G4 どおりブロックごと出さない⑥これまでの積み上げを「この店・この機種 N台｜平均回転率｜期待値合計｜実収支合計」の1行にし、既存表示は［詳しく見る］で展開（既定は閉）。全体（機種軸）は台数が違うときだけ2行目に出す（店×機種は機種軸の部分集合なので、台数が同じなら同じ集合＝同じ値）。項目は区切り（｜）の位置でだけ折り返す⑦転記用データは見出しと［転記用をコピー］だけを出し、中身は［詳細を表示］で展開（既定は閉）。転記内容は不変で、閉じたままでもコピーできる。検証＝9/9・台326 の実データで、③の差1行と削除した重複表示（実測回転率・想定回転率・開始期待値 各1回）以外の数値が完全一致。区間表と転記用の内容はバイト一致。警告0件のデータで枠が出ないこと、3つの展開部が既定で閉じて開閉できること、390pxで3カードが1行に収まること、［修正する］が記録の修正へ遷移することを実ブラウザで確認
- [x] 結果直下のテキスト行を整理（S26b）… S26 の実機確認を受けて、3カード直下の4行を「実働 ／ 時給 ／ 通常時の時速（通常時の時間・回転数）」の1行にした。外したのは 大当たり回数・1R平均・1R実質出玉・払い出し・時短中の減り。1R実質出玉は獲得期待値カードの出典に出ており、未入力の項目は入力確認の枠に出るので、この行では重ねない。未使用になった `netSummary` と S20/§2-2 のコメントを行と一緒に消し、`sessionNetBallsSummary` の関数定義は残す。通常時の時速が出せないときに括弧を出さない条件は従来どおり。3行ぶん詰まったことで、390pxで入力確認の枠まで最初の1画面に収まる。計算・schema 38 は不変で、外した5項目以外の数値・HTMLは完全一致
- [x] 時短抜けの持ち玉を必須化（S27）… 時短抜けを、**実機の台の持ち玉表示を見て入力した値でしか確定できない**ようにした。それまでは①時短ボタンが持ち玉欄より上にあり押すとその場で確定して閉じる、②持ち玉欄に `jitanExitStartBallsPreset` の見積もり値が最初から入っている、の2つが重なって、**見積もりが実測として保存される経路**があった（9/9・台326 と台317 の起点の持ち玉の修正がこれ）。schema 38・計算ロジックは変更なし。①「時短が終わりました」の枠内を 持ち玉欄 → 時短ボタン3つ → この後の2択 → 直接入力＋確定 の順に並び替える②持ち玉欄が空、または `normalizeNumber` が null を返すあいだ、時短ボタン3つと `applyJitanExitBtn` を `disabled` にし、理由の1行「台の持ち玉を入力すると、時短抜けを確定できます」を欄の直下に出す（有効になったら消す）。切り替えは `input` イベントで、`change`・`blur` を待たない③**持ち玉欄の初期値を空にする**。プリセットの `value` を入力欄に入れず、下書きに手入力が残っているときだけその値を出す④見積もりはヒント行に出すだけにする。「参考：前回134玉＋6R×93玉＝約692玉。台の表示を入力してください」「参考：前回◯玉＋累計出玉◯玉＝約◯玉。台の表示を入力してください」の2種で、出せないときはヒントなし。**仕様書は「◯R×◯玉＝約◯玉」だったが、R数分岐の戻り値が `base + rounds × perRound` なので `base` を省くと等式が成立しない**。「注記がそのまま検算できるように」という S24 の設計意図に合わせて「前回◯玉＋」を入れた。「約◯玉」は戻り値そのものを出し、ヒント用に足し直さない（丸めのタイミングがずれて画面内で食い違うため）。ヒントは下書きの状態に関わらず常に出す（値として入らなくなった以上は参考情報で、一度入力すると消える挙動は開き直したときに分かりにくい）⑤**参考値をワンタップで欄に入れるボタンは付けない**（見積もりを入れて確定する経路が1タップで残るため。案A）⑥`jitanExitStartBallsPreset` は `estimate` のメタ情報（`kind` / `base` / `chainBalls` / `rounds` / `perRound`）だけを増やし、`value` の算出（分岐条件・順序・丸め）は変えない。合成7件（累計出玉・R数・小数の丸め・追跡値・null・base=0）で変更前と全一致を確認⑦**`button:disabled`（`opacity: 0.5` と `cursor: not-allowed` だけ）を追加**。それまで `disabled` のボタンが有効なものと同じ見た目で、押しても無反応だった。対象は S27 の4つのほか、マップ編集4件（島を追加・保存・このマップを削除・直前のマップ定義に戻す）・投資の取り消し・閉店チェックと朝イチチェックの端の矢印・R種別の取り消し・ウィザードの戻るの11件で、いずれも押せないのが正しい状態。色・寸法・余白は変えず、44px下限を維持（`disabled` と有効で同じ高さ＝時短ボタン46px・確定ボタン44px）。下書き（S24/§A-2-2）・`confirmJitanExitStartBalls`・「閉じる」・この後の2択・直接入力は従来どおり。**この画面は固定全画面ではない**（`.modal { max-height: min(86vh, 760px); overflow: auto; }` で元から内部スクロールし、変更前で 1,446px / 表示枠 760px）。並び替えと参考ヒントの2行化で入力後は 1,452px（+6px）になったが、余白は詰めていない（44px下限より1画面に収めることを優先しない）。検証＝実DOMで、空欄時の無効化と理由行、`input` 直後の有効化（`abc` で再無効化・`0` は有効）、R種別タップで開き直しても入力値が残ること、確定後の `segments[].startTrackedBalls` が入力値そのもの（見積もりでない）になること、参考ヒントの2種類が戻り値と一致すること、空のまま閉じても現在回転数が変わらないことを確認。390px で横スクロールなし
- [x] 大当たり履歴を連チャン単位に（S28）… 大当たり履歴を「当選1件ごと」から「**1区間＝1連チャン1行**」に変え、見る画面（一覧・連チャン詳細）と直す画面（当選の修正）を分けた。データ構造・schema 38・判定ロジック（`segmentIsHoldCarryHit` / `detectHoldCarryHit` / `holdCarrySegments` / `toggleSegmentHoldCarry`）は不変で、表示と導線だけを変えている。①一覧（`openHitHistory`）は1区間1行・新しい順で「区間③ 2連（6R・6R）当選135 通常当選 ［詳細］」。説明は「今回の稼働の連チャン一覧です。新しいものが上。」（「セッション」を使わない）。サマリー1行「連チャン N件 ／ 当たり合計 M回 ／ R種別なし K件」で、K=0 なら R種別なしの項自体を出さない。R種別は時系列順に「・」区切り、なしは「−」。残保留当選には「→ 区間◯の続き」を添える。行内の修正・削除ボタンは廃止し［詳細］だけにする。区間に紐づかない当選は最下部の「区間不明 N件」1行にまとめる②**連チャン詳細（`openHitChainDetail`・新規）に判定を集約する**。見出し「区間③の連チャン」＋「当選135 ／ 2連 ／ 6R・6R」、判定1行と根拠1行（「打ち出し中に当選（時短抜け25 → 当選135・110回転）」。S7c の判定材料をその区間の記録から出す）。切り替えボタンは `holdCarrySegments()` の区間だけに出し、押すと確認を1回だけ出して、確定後は一覧に戻さず詳細を開き直す。当たり内訳は時系列順で「#5　6R　1回目　当選カウンター135　累計出玉 −　［修正］」③当選の修正（`openHitEditForm`）は見出し直下に「#4 ／ 区間③ ／ 1回目」の文脈行を足し、最下部に「この当たりを削除」（既存の二重確認をそのまま使う）を置く。削除後は詳細へ、その区間に当選が残っていなければ一覧へ。**判定（通常／残保留）はこの画面に置かない**（判定は区間単位なので詳細だけ）④「残保留当選の扱い」セクション（`holdCarrySectionHtml`）は廃止。S7b 時代のヒント文（時短抜けから残保留の数以内で…）も現状と違うので使わない⑤判定を出す区間の条件は `hitChainJudgmentSegment()` に共通化し、一覧と詳細で同じにする。打ち始め区間・遊タイム区間には判定も根拠も切り替えも出さない（判定対象外のため）⑥`hitHistoryGroups()` の表示グループは `storedHitSegmentId()` で分ける（残保留当選の区間を1行として見せるため）。累計出玉を積む `hitHistoryRows()` と集計側の連チャン解決（`resolveSegmentChainId`）は従来どおりで、リザルトの数値には影響しない。検証＝9/9・台326 の実データで一覧4行・当たり合計6回（`hitCount` と一致）、R種別が時系列順、区間①（打ち始め）に判定が出ないこと、区間③の根拠が区間データ（起点25・終点135・打ち出し中）と一致することを確認。全39セッションで `hitHistoryGroups()` の (区間ID, 当選件数) が変更前と全一致。残保留区間の切り替えで `holdCarryHit` が従来と同じ値になり、切り替え後のリザルト（区間内訳・回転率・消費玉・収支）も一致。削除の二重確認と遷移（詳細へ／最後の1件で一覧へ）、大当たり登録・リザルト両方からの「戻る」も確認。390px で `body.scrollWidth` 390・はみ出し0・文字切れ0、一覧の行は「当選◯」まで同一行
- [x] 開いている区間の消費玉に追跡中の持ち玉を仮終点として使う（S29）… 時短抜けのあと、持ち玉タップ（−125）だけで打ち進めても稼働中パネルの「通常時合計」の玉数が増えず、回転数だけが伸びて回転率が上がっていく状態を直した（テスト店・台200 の録画で確認。183回転／2,250玉・20.3 → 233回転／2,250玉・25.9）。schema 38・区間モデルは不変。①原因は**起点と終点に同じ値が入ること**。`applyJitanExit()` が時短抜けの入力値を `updateMochidamaBalanceWithUndo()` 経由で持ち玉へ反映する（S7/B-3）とき、その中の `recordSegmentMeasurement()` が**開いたばかりの新区間に `lastMeasuredBalls` として同じ値を書いて**いた（S7b/A-2）。`segmentEndpointConsumedBalls()` は `起点 ＋ 足した玉 − 終点` なので、起点＝終点になって消費玉が0に固定される。回転数は `runningNormalSpinCount()` の別経路なので、そちらだけ増えていた②**時短抜けの入力値を測定点として記録しない**。`updateMochidamaBalanceWithUndo(session, value, { recordMeasurement })` を足し、`applyJitanExit()` からは `false` で呼ぶ。時短抜けの入力値は区間の**起点**（`startTrackedBalls`）であって区間途中の測定点ではない。残高更新・トースト・Undo は従来経路のまま（S7/B-3「直接代入せず既存経路を通す」を維持）で、Undo と保存失敗時の復元も同じ条件でそろえる③`segmentEndBallsInfo(segment, session)` を新設し、終点の値と「実測か」を同じ経路で返す。開いている区間（`endRemainBalls` も `endSpin` も無い）は `deriveBalances(session).mochidama`（ツールが追跡している現在の持ち玉）を**仮の終点**にし、その追跡値が `lastMeasuredBalls` と一致するあいだだけ実測として扱う（＝測定のあとに持ち玉タップが無い状態）。追跡値が null のときは従来どおり測定点を使い、それも無ければ消費玉を出さない。閉じた区間は従来どおり `endRemainBalls` が最優先で、測定点がある区間の扱いも変えない④`deriveSession()` の `consumedBallsMeasured` を `.measured` と併せて判定し、**仮の終点を使っているあいだは「（実測）」と出さない**（`consumedBallsSourceNote` の「（目安）」）。実測でないものを実測と表示しないための条件で、B94「判断の場に前提を置く」の続き⑤**この症状は S27 で新しく出たものではない**。S7/B-3 と S7b/A-2 が重なった a11a7c6（2026/9/8）の時点からあり、S26b でも持ち玉を入力すれば同じ（タップ3回後も 2,250玉のまま・回転率23.11）。S26b には「持ち玉を入力せずに確定」すると `startBallsSource` が `tracked` になって新式の経路に入らない逃げ道があり、S27 で入力を必須にしたことでその逃げ道が消えて必ず踏むようになっただけ。S27 自体（見積もりが実測として保存される経路を塞いだ変更）は巻き戻さない。検証＝headless Chrome の実操作で、時短抜け（950玉）→ 持ち玉タップ −125×3 のとき通常時合計が 2,250玉 → 2,625玉（＋375）、回転率が 23.11 → 19.81 になり、注記が「（目安）」になることを確認（未捕捉エラー0件）。持ち玉を「修正」で実測値に直すと終点が測定点に置き換わって「（実測）」へ戻り、当選で区間を閉じると `起点＋足した玉−当選時の持ち玉` で確定する。2a53604（S26b）・673fef4（S27）でも同じシナリオを回し、持ち玉を入力した場合はどちらも 2,250玉に固定されること、S26b の未入力時だけ 2,625玉になることを確認。実データ39セッションの `deriveSession()` 全出力は変更前後で差分0件（9/9・台326 は 372回転／3,187玉／29.1810 で一致）。保存済みデータで開いている区間を持つのは旧式（`consumedModel` なし）の2件だけなので、過去データは新経路を通らない
- [x] 版の表示と旧版警告（S30 / 26fb133）… 2026-09-11、ブラウザのキャッシュによって古い版の yutime-v3.html が開かれ、本番の実戦データに旧仕様の操作が記録された（戻し済み）。原因は「いま開いている版が古いことに気づけなかった」ことなので、①気づける状態にする②気づかなくても止まる、の2段で入れた。schema 38 据え置きで、計算ロジックと既存表示は変更していない。①`APP_VERSION_SEQ` / `APP_VERSION_DATE` を定数に持ち、タイトル直下に「S○○ / 日付」を実行時に組み立てて出す（文字列をべた書きしない。12px・補助テキスト色で、リンクでもボタンでもない）②端末に「最後に開いた版」を整数1つで保存する（`ytv3:app:lastVersionSeq`）。**保存値のほうが大きいときだけ旧版とみなす**。そのとき保存値は上書きしないので、警告を閉じても再読み込みすればまた出る。保存値がない・現在より小さいときは現在の版で保存し、警告は出さない③**判定は `loadData()` より前に置く**。旧版と判定した場合は読み込み・schema移行・描画のいずれも行わない。人の操作を止めるだけでは、移行と書き戻しで古い形に直される経路が残るため。具体的には `loadData` / `loadCarryover` / `persist` / `saveIslandFilters` の4経路を止め、`renderAll()` も呼ばない（空のデータを描くと「記録が消えた」ように見えるので描画自体をしない）④**実測の裏付け**：通常起動は描画の副作用で `ytv3:islandFilter` を**人が操作しなくても書き込んでいた**（`renderMachineMap → selectedIslandFilter → saveIslandFilters`。S29以前からの既存挙動）。旧版と判定したときは localStorage への書き込みが0件になることを確認した⑤ブロックの範囲は警告の帯の中以外の button / input / textarea / select。見た目は S27 で入れた `button:disabled` にそのまま乗せ、新しい装飾を足さない。**DOM上の入力値は初期化しない**⑥逃げ道として帯の中に「承知のうえで使う」を置く。押すとブロックのみ解除し、そこで初めて読み込みと移行が走る。帯は残り、解除は保存しないので再読み込みで元に戻る⑦保存キーは実戦データ（`ytv3:data`）と別枠で、**書き出し（`exportJson` は data だけを出す）にも schema移行にも含まれない**。検証＝headless Chrome に実データ39セッションを投入し、保存キー 31 / 30 / 29 / なし の4状態と 390px の表示で全項目PASS。旧版起動では `ytv3:data` が不変で localStorage のキーも増えず、一連の操作の前後で書き出しJSON 432,886バイトが完全一致した
- [x] 旧版警告の文面整理と「閉じる」の廃止（S31 / 5b79a76）… S30 の警告は、ブロック中に「閉じる」を押すと帯ごと消えた。結果、**理由の表示が無いまま全操作が不能**になり、使う人からは故障にしか見えない。「閉じる」は警告が操作を止めない前提で置いたもので、ブロックを入れた時点で不要になっていた。①「閉じる」（`versionWarningCloseBtn`）とそのイベント登録を廃止する。ブロック中、警告の帯は消せない②文面を見出し「新しい版に更新してください」と本文「保存データを守るため、操作を一時停止しています。記録は削除していません。」に固定する。ブロック中は描画を行わないため、画面から記録が消えて見えることへの説明を本文に入れた③**帯に版の番号は入れない**（タイトル直下の版表示が常に出ているため）。文面はマークアップ側に固定で持ち、`showVersionWarning()` は引数と `innerHTML` の組み立てをやめて `hidden` を外すだけにした④「承知のうえで使う」は位置・見た目・挙動とも残す。保存値が現在より大きい状態は放置しても解消しないので、端末側で戻す唯一の手段がこのリンクになる⑤**版番号の運用ルール**：版番号は通し番号とし、**コードを変えるコミットごとに1つ上げ、S番号もそれに合わせる。枝番（S30b 等）は使わない**。ドキュメントのみの変更では上げない（この作業も当初 S30b と呼んでいたものを S31 に改めた）。**確認状況＝表示（`S31 / 2026-09-11`）は実機確認済み。警告のブロック側は実機未確認で、390px・実データ39件の自動検証（57項目PASS）のみ**
- [x] 実戦ヒアリングと現行実装の突き合わせ（2026-09-13）… 9/7〜9/9 の実戦で机上では出ない不具合（S24・S29）が続いたため、夢爽が実際にやっている手順を1日の時系列（台選び／打ち始め／通常時／当選／時短中／時短抜け後／遊タイム突入／台移動・ヤメ／記録の修正／1日の締め）で聞き取り、IDEAS.md の S1〜S31 と突き合わせた。結果：**新しいバグは0件**。台選び（前日ヤメ・現在回転数・ヘソ評価の日次化・ラムクリア）、打ち始め（再プレイ→持ち玉→現金）、通常時（持ち玉タップはまとめて押し回転数も同時に更新。先読み中に先走って計数した分は「持ち玉 修正」で戻す）、当選（回転数→当選ボタン→持ち玉。R種別は当選直後）、時短中（操作なし。時短抜けの持ち玉はパーソナルの表示）、ヤメ判断（稼働中パネルの期待値）、1R出玉を「持ち玉の差÷R数」で見る手順（＝S20の算出）は現行実装と一致。残保留当選の自動判定（S7c）は新ツールで未経験のため実戦未検証のまま。裁定：①待ち行列10（遊タイム中の回転数と時速の画面表示）は**廃止**。遊タイム中に見たい数字はない。秒/回転のプリセット化は11の時速の扱いの中で要否を判断する ②台移動の引き継ぎ「この持ち玉で次の台へ」は存在を忘れられていた。同じ日・同じ店なら**自動で引き継ぐ**方式に変える（下の [ ] 項目） ③転記用（リザルト⑥）は収支帳やXへの貼り付けが目的なのに見づらく未使用（下の [ ] 項目） ④機械割の表示は、遊タイム狙いでは期待値しか見ておらず105%基準が判断を迷わせている一方、打ち切り判断では105%を使っている。消さずに「どの判断に使う数字か」の説明を待ち行列13（用語と説明表示の整理）に含める ⑤時短中の投資の区別（S20の `duringJitan`）は「時短中の投資はあり得ない」とのことだが無害なので触らない ⑥JSON書き出しは現状使い道がなく、稼働記録ノート構想まで保留。ヒアリングの生記録はリポジトリに入れない。実行順は ARCHITECTURE.md §5 に従う（11 → 12 → 13 → 台移動の自動引き継ぎ → 転記用の作り直し）
- [x] 遊タイム突入前の通常区間の終点に突入時玉数を入れる／1R出玉の入力確認を持ち玉差の事実に合わせる（S32 / 06bbde0）… 9/13 台320 の実戦データで、通常時の消費玉が投資合計（2,500玉）のまま出ていた。起点・終点方式（S7）の終点は「当選時の持ち玉」と「ヤメの終了玉」からしか取っておらず、`endSource:"yutime"` の区間は `endRemainBalls` が常に null だったため、遊タイム突入時の持ち玉（74玉）が引かれずタップ合計に落ちていた。突入時玉数（`yutimeEnterBalls`）を直前の通常区間の終点に入れる1行を、突入記録・`syncSegmentEdges`・組み直しの3か所へ、ヤメ終点（S7/B-4）と同じ `usesEndpointConsumedModel` の条件付きで追加。台320 は 2,426玉・**15.8→16.3**、9/9 台331 は 1,850玉・18.4→17.4。遊タイム経由でない記録と旧式セッションは1件も変わらない（9/10エクスポート39件の全件比較）。既存の記録は保存が走ったとき（店選択など）に終点が入る。もう1件、入力確認の「1R出玉が未入力 → 実測出玉による1R平均を計算していません」は、すぐ上に持ち玉差の「1R95玉・実測平均」が出ているのに「計算していません」と書いていて S26④ に反していた。持ち玉差の1R実質出玉（`sessionNetBallsSummary().netPerRound`）があるときは「獲得出玉（データカウンター）が未入力 → 1R平均はデータカウンター基準では出せません。期待値の1R出玉は持ち玉差（1R実質出玉 ◯玉）で計算しています」に切り替え、`missingValue:false` で下に落とす。実機確認済み（AQUOS Sense9・台320 で 16.3 と新文言を確認）。**残った乖離**：リザルトの「実戦の流れ」と区間内訳は、通常区間が2つ以上あると `segmentBreakdownRows` がタップ合計を直接使うため、台320 の①は 15.8 のまま（端点式の 16.3 と食い違う）。S32 以前から 9/9 台326・台316 で同じ乖離あり。端点式へ寄せる案は台326 で異常値が出たが、台326 は起点の持ち玉の手直しが未実施の台なので、データが直るまで式の良し悪しは判断しない（下の [ ] 項目）
- [ ] 区間内訳の消費玉を端点式にそろえる（yutime-v3）… 「実戦の流れ」と区間内訳（`segmentBreakdownRows`）は多区間のときタップ合計、実測回転率は端点式（`segmentEndpointConsumedBalls`）で、同じ画面に違う回転率が並ぶ（台320：16.3 と 15.8）。出典は1つにする（§9-84）。ただし端点式に寄せて試すと台326 の区間②③が 53.8／108.9 の異常値になった。台326・台317 は起点の持ち玉の手直しが未実施なので、**夢爽の手直し後に**もう一度端点式で評価してから仕様を決める。仕様書未作成
- [x] 想定時速の採用ルールと旧境界の記録の印（S33 / 21aa1b8 / schema 39）… 待ち行列11・12。9/10の実戦で「回転率が違う台の実測時速をそのまま持ってくると外れる」と分かっていた問題への対処。前チャットで残っていた案C（打ち出し玉数/分を基準に時速を組み立てる）は**不採用**。理由：回転率が高い台では保留が埋まって打ち出しを止めるため玉/分は一定にならない（台316・回転率22.3で55玉/分、他は64〜77）。案Cの利点だった「回転率が違う台を同じ土俵で平均できる」が成り立たない（夢爽の指摘）。採ったのは案A：`spinsPerHourInfo` の店の実測（`storeSpeedStats`）を「同じ店・同じ機種のうち、その記録の実測回転率が期待値判定の回転率±2.0の範囲」に限る（`SPEED_RATE_BAND`、概算の記録は除外、回転率なしなら店の実測を飛ばして既定へ）。既定は 250 → **290回転/h**（`DEFAULT_SPINS_PER_HOUR`）。夢爽の裁定で基準は回転率16の台（パチマガの等価ボーダー16.6に対し店が利益を出す一般的な水準。手元の実測は台320・回転率16.3で293回転/h、9/10の推定線でも16×15+50＝290）。期待値エンジンのプリセット既定250は yutime-calc とのバイト一致の不変条件があるため触らず、`spinsPerHourInfo` 側で上書き。台の実測・手入力・`usableSpinsPerHour` の条件は変更なし。もう1件、S25（2026-09-11）より前の記録は大当たり時刻がウィザード完了時刻（旧境界）で通常時の所要時間が長めに出るため、schema 39 の移行で `date < 2026-09-11` の記録に `speedBoundary:"legacy"` を付け、`sessionNormalSpeed` を短絡させて台・店の参考時速と日別・積み上げの平均時速から分子・分母とも外す。収支・回転率・期待値・1R実質出玉は一切変えない（41件で不変を確認）。その記録自身のリザルトでは時速を出し、末尾に「（旧境界・参考）」。印の付け外しUIは無し。判定が変わった台は1件：9/9 台326 が「微妙→打てる」（旧境界の実測260.9回転/hが外れて既定290になり時給2,366→2,520円）。12の狙いどおりの動き。実機確認済み（AQUOS Sense9・台320「293回転/h（実測・台）」・台331「実測・店」・台331リザルトの旧境界注記）。**残った課題（待ち行列13へ）**：時速は「休憩などの中断を含む実時間」で、休憩を挟む人の実測は遅く出て時給も低く出る。既定290も「回転率16の台を休まず打った場合」の前提を持つ。前提が違う人には前提を書いて渡す必要があるため、時速・時給の近くと既定使用時の根拠行に前提の文言を出す件を13に含める。あわせて正式公開前の guide に「時速・時給の前提」を1段落入れる。yutime-calc の時速チップ（250/300/350）は未判断のまま
- [x] 「セッション」を「今回の稼働」に／説明表示のスイッチ／機械割と時速・時給の前提を表示（S34 / 2fd27f6）… 待ち行列13。9/3 の S6 仕様書を S33 時点の画面に合わせて書き直したもの。表示のみで、41件の計算値（実収支・回転率・獲得期待値・1R実質出玉・時給・判定・想定時速の出典・参考時速・旧境界の印）は1件も変わらない。①画面の「セッション」12箇所を「今回の稼働」「記録」「件」に置き換え（可視文字列で0件に。識別子・保存キー・schema の `sessions` は不変。稼働中パネルの「打ち始めから」は「今から打つ場合」と対になる起点のラベルで別概念なので残置）。②説明文（`class="hint"` 87箇所）を A説明／B照合先／C警告・状態 に仕分け、A（31箇所）だけを店選択画面の「説明を表示する」スイッチで隠す。初期ON、切り替えた時だけ `ytv3:app:showHints` に保存（実戦データ・書き出しJSON・schema移行の外）。実装は A に `hint-help` を足して `.hints-off .hint-help{display:none}`（B・Cのクラスを付け替えると `<strong class="hint">` 等の見た目が変わるため）。C には見出し・フォームラベル・空状態・動的な値・根拠行・警告・入力確認・「（旧境界・参考）」・モーダル副題（1要素を全モーダルで共有し、破棄の確認文を含む）・時短抜けの確定条件の表示（disabled と連動）を含め、迷ったものは C に寄せた。ヤメ入力のヒントに埋まっていた「（実機：計数機に流す玉数）」はラベル側へ分離。旧版ブロック中は既存の `applyVersionBlock()` で自動的に disabled。③期待値判定パネルの「スロ換算機械割」に「打ち切り判断の目安」（常時）と、打ち切り（時短抜け後も打ち続ける）の判断に使う数字で105%≒時給約2,400円という説明（A）。④「参考時速」に「中断込み」（常時）、リザルトの実働・時給・時速の行の下に「休憩などの中断を含めた実際の時間で出している」説明（A）、根拠行は既定なら「（既定：回転率16の台を休まず打った目安）」、実測なら「（実測・台・中断込み）」。回転率16は `DEFAULT_SPINS_PER_HOUR_BASE_RATE` で持ち、16×15+50＝290（S33の関係）をテストで突き合わせる。実機確認済み（AQUOS Sense9・録画：スイッチOFFで説明が消え、入力確認・「（旧境界・参考）」・注意書きが残る。期待値判定パネルの文言はツムギの実ブラウザ表示で確認）。**残った候補**：他の開発用語が画面に出ている（「プリセット」「JSON書き出し」「localStorage」「サンプル」「調整レコード」等）。変更せず一覧のみ。正式公開前の guide に「時速・時給の前提」を1段落入れる件は未着手
- [x] 台移動の自動引き継ぎ（S35 / 17bdc8b）… 待ち行列14。ヒアリングで手動の「この持ち玉で次の台へ」が使われていない（存在を忘れていた）と分かり、同じ日・同じ店で前の台をヤメ入力で閉じていれば、次の台の打ち始めに終了玉（＋残保留玉）・再プレイ残り・カード残高を**自動で初期値に入れる**方式へ。`autoCarryoverForStore(storeId)` が `latestCompletedSessionForStoreToday` の記録で終了玉があるときだけ `createCarryoverFromSession` を返し、期待値判定パネル・ウィザード・`startSessionBase`（`carriedFromSessionId`）がそれを使う。手動ボタン・マップの引き継ぎバナー・「引き継ぎを破棄」・待機キー `ytv3:carryover` は廃止（起動時に残っていれば1回だけ消す）。ウィザードにあった再プレイ・カード残高だけの本日プリセット（`latestStoreBalances`）は自動引き継ぎに吸収して削除（差：終了玉が無い記録からは残高も引き継がない）。初期値は書き換え可能で、直しても紐づけとリザルトの「⇐ 台◯◯から続き」は付く。パネルの案内「台◯◯（本日 ◯◯:◯◯ ヤメ）の終了玉を初期値にしています。スロットへ寄るなどして違えば直してください。」は常時表示、ウィザード側の同文は説明スイッチで隠れる。同台続行・記録の形・書き出しJSONは不変（41件の計算値に変化なし）。確認：ネネちゃん（ChatGPT）のブラウザ操作で、空データにテスト店と台1〜2を作り、台1を終了玉1,000で閉じてから台2を開くと1,000が初期値に入り案内文が出ることを確認（2026-09-16）
- [x] 転記用の作り直し（S36 / 3887f03）… 待ち行列15。文面の構成は夢爽の指定（ネネちゃん案）。①出す内容を 収支／打つ前の想定／実戦結果／実戦の流れ の4項目からチェックで選ぶ（初期は全部ON、選択は `ytv3:app:transferSections` に保存、書き出しJSONに含めない）。未選択は見出しごと出さない。②文面は「遊タイム実戦メモ｜機種名」「YYYY/M/D」「---」＋選んだ項目＋「by slot-tools.jp」。店名・台番号は出さない。③「X用にコピー」「収支帳用にコピー」の2ボタン。収支帳用は `checker-engine.js` の `plainText()` と同じ規則（絵文字・異体字セレクタ除去、▶↪→「→」、波ダッシュ→「~」、①〜⑳→半角数字）を同内容で持つ。④「X換算 ◯/280」の表示。X の数え方の近似（U+0000〜10FF・U+2000〜200D・U+2010〜201F・U+2032〜2037 は1、それ以外は2、改行1、`slot-tools.jp` はURLとして23）。超過しても削らずコピーできる。⑤**値の出どころをリザルトと同じ `sessionResultSummary` に統一**。旧転記用は「消化回転数＝終了カウンター−開始カウンター」で当選後にカウンターが戻ると負になり「-」、「残り回転数」はヤメ時点（リザルトは打ち始め時点）で食い違っていた。`transferSummaryForSession` から旧フィールド（`startExpectedSpins` / `remainingSpins` / `consumedBalls` / `playedSpins` / `startEvYen`）を削除。「実戦の流れ」は `resultTimelineLines` で主行・副行の配列を作り、HTMLと文字の両方が同じ元から出る。入力確認の startSpin / endSpin の文言は「実戦の流れの起点／終点を表示していません」に。旧グリッド・1行転記文・`copyTransferSummary` は削除。確認：ネネちゃんが twitter-text v3.1.0 の `parseTweet` で検算し、全項目460・2項目274 がツール値と一致（■｜→は各2、-と改行は各1、slot-tools.jp は23）。実機確認済み（AQUOS Sense9・録画：台320の全項目文面、X換算460/280（＋180 超過）、収支帳用をパチマガ収支帳のメモに貼って ■・｜・---・→ が化けずに登録）。**残った候補**：ネネちゃんの全体デバッグ（S36・コード再現テスト108ケース）で挙がった4件は §5 の待ち行列 17〜19 に（残り6件はレポート待ち）
- [x] 残保留増減の二重加算・日付のずれ・複数タブの上書き（S37 / 0ead60a / schema 40）… 待ち行列17＋ネネちゃん（ChatGPT）の全体デバッグ報告1。①ヤメ入力の「残保留増減」は玉数の欄だが、初期値に回転数（現在回転数−当選回転数）が入る作り（`zanhoryuPresetFromHit`）で、夢爽も残保留の回転数を入れていた。9/19エクスポート47件のうち値が0でない29件はすべて4〜8で、時短抜け後の通常区間の回転数と一致。終了玉（計数機の値＝残保留消化後の総持ち玉）に玉として足していたため、実収支と次の台への持ち玉が数玉ずれていた（9/19 台323：引き継ぎ2,702→2,695、実収支+6,969→+6,944円）。ヤメ入力から欄を削除し、schema 40 の移行で旧値を `zanhoryuSpins` に移して `zanhoryuBalls` を0にする（値は捨てない・冪等・計算には使わない）。加算式4か所（`finalMochidamaForCarryover` / `profitYenForSession` / `deriveSession` の `diffBalls` / `legacyCarryoverMochidama`）は変えない。記録の修正の欄は「残保留での増減玉（終了玉に含めていない場合だけ。通常は0）」で残す。引き継ぎで上乗せが開始持ち玉に入っていた2件（9/19 台324＝3,186、8/26 台295＝6,859）は移行で直さず夢爽の判断（打ち始めで実測を入れていれば手直し不要）。②`today()` と `offsetDate()` が `toISOString()`（UTC）で文字列化していたため、日本時間では「前日」が前々日、朝9時前は当日が前日になり、閉店チェックは一度も宵越し判定に使われていなかった。既にあった `localDateString` / `offsetLocalDate` に統一。③保存が別タブの変更を検知せず後勝ちだったので、読み込み時の `meta.updatedAt` を保持し、保存時に端末の値と違えば保存せず S31 と同じ帯（「別のタブで記録が更新されています…」再読み込みのみ、「承知のうえで使う」なし）。`storage` イベントでも帯。検証：ネネちゃんがコードと9/19 JSONで再計算し、台323 2,695玉・+6,944円、移行対象全件の `zanhoryuSpins`＝旧値・`zanhoryuBalls`＝0、実収支の差＝旧値×玉単価（整数円の丸め差のみ）、非対象18件は変化0、冪等、日付（9/20 00:30 JST で today＝9/20・前日＝9/19・閉店チェックが9/19を参照）、2タブでの帯と上書き防止、をすべて一致で確認（2026-09-20）。**残った課題**：帯の「再読み込み」ボタンが36px（S31と同じ、UI規約の44px下限未満）→ 待ち行列21で揃える。ネネちゃん報告の D05（移行前退避の失敗で空データ起動→上書きの経路）・D07（同台続行で資金が開始期待値に渡らない）・D09（逆行区間が合計で相殺され警告なし）・D10（台番の小数・大量生成）は待ち行列21、報告2・3は22、4・5は23、D06＝19、D08＝18
- [x] 大当たり登録画面の作り直し（S38 / fcc6d83）… 待ち行列20。構成は夢爽がネネちゃんと確認して採用した見本（記録済みのラウンドと回数を大きく表示し、データカウンターと見比べて入力漏れ・押し間違いに気づける）。表示と操作だけの変更で、`hits[]` の形・`roundBreakdown` の集計・期待値・収支は不変（既存記録の計算値に変化0（ツムギ＝9/13の41件、ネネ＝9/19の46件版）、R押下→取り消し後の `hits[]` は押す前と同一）。①ラウンド登録ボタンは `roundTypes` から生成し R数の小さい順（4R／6R／10R）に横一列・高さ72px・文字28px（`orderedRoundTypes`。プリセットのデータは降順のまま）。②「この連チャンの記録」を最も目立つ確認エリアに：「3回／合計16R」を30px程度、記録順のカード（最新に「最新」）、内訳「4R：1回／6R：2回／10R：0回」（0回も出す。`roundBreakdown` に表示用の `counts` を追加）。③「◯Rを記録しました」「◯Rを取り消しました」＋「直前を取り消す」。**画面を開き直さず**確認エリアだけを差し替える（`refreshHitConfirm`。従来はR種別のタップごとに `openHitResetPrompt` を呼び直していたため、スクロール位置と入力途中の値が消えていた）。④「データカウンターの当日大当たり回数と見比べる」：開始時 ◯＋今回 ◯＝◯回（今回＝この稼働の合計。連チャンの回数と別に出す）。開始時が未入力なら推測せず「開始時の回数が未入力のため見比べられません」。実機と自動照合したかのような「一致」表示はしない。⑤「出玉を記録する（任意）」「時短が終わったら」は `<details>` の折りたたみ（既定は閉）。中身（累計出玉・時短抜けの持ち玉・時短→カウンター・この後・直接入力・この回転数で開始）と必須条件は不変。旧 `hitRoundSummaryHtml` とそのCSSは未使用のまま残置（待ち行列21で削除）。確認：ツムギの実ブラウザ（390px）で検証11項目、ネネちゃんのブラウザ操作で a〜g とコード確認が一致（2026-09-20）。夢爽の端末での押しやすさは次の実戦で見る
- [x] 小修正の束（S39 / 250dff7）… 待ち行列21。ネネちゃん（ChatGPT）の全体デバッグ D05・D07・D09・D10、S37・S38 の残課題、追補でネネちゃんの稼働中デバッグ3件（9/20）。記録の形・schema・書き出しJSONは不変、既存41件の計算値は変化0。①D05：`loadData` の移行前退避（`PREMIGRATE_KEY`）が個別の try/catch の外にあり、容量不足の例外で「読めなかった」扱い→空データで起動→次の保存で本物を上書き、という経路があった。退避は失敗しても続行（`backupBeforeMigrate`）、JSONが読めない・形が違うときは `loadFailed` を立てて `persist()` を止め、S37 の帯の文面を変えて再読み込みだけ促す（`ytv3:corrupt` に退避）。②D07：「同じ台で続行」が `calculateStartEvSnapshot` を presets なしで呼び、持ち玉・再プレイが開始期待値に渡っていなかった（ネネちゃん再現：アグネス200回転・回転率16・持ち玉3,000＋再プレイ1,000 で +2,230→**+2,490円**）。`mochidamaInput` / `saipureiInput` を渡す。既存の続行記録の `startEv` は書き換えない。③D09：終了回転数＜開始回転数の通常区間が、合計がプラスなら通っていた。記録の修正・ヤメ入力・当選で `blockedByReversedSegments` が弾く（「区間◯（…）の終了回転数 ◯ が開始回転数 ◯ より小さくなっています」）。ヤメ入力の経路は区間がまだ開いているため、複製の側で resync→close の順に閉じてから検査する（close→resync だと自動生成区間で見逃す。ツムギが実測して順序をテストで固定）。既存記録に逆行があれば `rateUnavailableReason` を立てて回転率を表示しない（`rate` の値は残す。集計からの除外は未対応。該当は現状0件）。④D10：台番の範囲を整数のみ・1列200台まで（`ISLAND_SIDE_MAX`）。小数や1〜100001は保存されない。既存4マップは不変。⑤S31・S37 の帯の「再読み込み」を44pxに（`button.warning-reload`。他の small ボタンは36pxのまま）。⑥S38 で未使用になった `hitRoundSummaryHtml` とCSSを削除。⑦追補§8：開始時のカード残高が未入力だと入金しても残高が「未入力」のままで貸玉も拒否された（`deriveBalances` が `startCredit === null` なら入金合計を無視）。入金が1件でもあれば 0＋入金合計−現金投資 で出す（開始残高0は null にしない）。⑧追補§9：「現在回転数を修正」が `spinUndoStacks` を更新せず、「元に戻す」が修正前ではなく古い差分で戻っていた（110→＋5→直接150→元に戻す→145）。直接修正も `setCurrentSpinWithUndo` を通す（→115→110）。⑨追補§10：遊タイム突入で回転数249を保存しても現在回転数が145のままで、その後の投資タップが145回転時として記録されていた。突入回転数が現在より大きければ現在回転数を突入回転数に合わせる。確認：ツムギの静的・vm 37項目と実ブラウザ（390px）13項目、既存41件で不変。コミットメッセージには⑦〜⑨が入っていない（追補分。記録はここで補う）。夢爽の実機確認は「同じ台で続行」を使う場面があれば期待値判定に資金が反映されていること
- [x] 記録に店条件を写して固定／マップ編集の守り（S40 / 7ee9409 / schema 41）… 待ち行列24。出典はネネちゃん（ChatGPT）の「店登録＋マップ デバッグ結果」（9/20）。①実収支・投資玉・1R実質出玉の玉単価・転記用が、記録の `storeId` から**現在の店設定**（パーソナル有無・貸玉レート・交換玉数）を毎回引いていたため、店設定を後から変えると過去の実戦の数字が動いた（再現：開始1,000→終了1,000 がパーソナル→非パーソナルで 0円→+4,000円、28玉→25玉交換で +1,786→+2,000円）。夢爽裁定「必ず修正」。打ち始めの3経路（`startSessionBase`・ウィザード・同じ台で続行）で店条件を `storeTerms` に写し、読み側は `storeTermsForSession(session)` に集約（無ければ現在の店設定＝旧データの安全網）。打ち始め前の期待値判定・店カードは現在の店設定のまま。schema 41 の移行は店条件が無い記録に**現在の店設定**を写す（過去の店条件は残っていないため。移行時点で数字は1つも動かない。店が見つからない記録は既定を写す＝該当0件）。記録の修正に「この記録の店条件」3項目を追加し、明示的にここで直したときだけ過去の記録が変わる。店設定モーダルに「変更は次の実戦から反映されます…」を常時表示。**例外**：獲得期待値（`calculateMachineExpectation` が `storeById(machine.storeId)` を引く）だけは今も店設定に追随（ネネちゃん検証：交換条件変更で32/32件が変動）。これは待ち行列22（評価の固定・S41）で扱う。②マップ編集の守り5件：未保存の草稿を捨てる操作（店の切り替え・機種コーナーの変更・マップ追加・別マップへ）は `confirmDiscardMapDraft` で「保存して続ける／破棄して続ける／キャンセル」の3択／`parseIslandLayout` のエラーがあれば `saveCurrentMap` は保存せずトースト（`map.errors` の保存は廃止）／`applyColumnPresetsToMachines` を `parsed.rows[islandIndex]` から `row.index === islandIndex` に（空島があると後続の島の機種指定がズレた。エラー時保存禁止によりUIからは起きなくなり旧データ耐性の修正）／別マップに登録済みの台番は禁止ではなく**「移す」**（夢爽裁定：ホールの入替でコーナーが変わるため）。確認のうえ元マップから外し、列が空になれば範囲と除外の痕跡も消す（`clearEmptyIslandSide`。残すと後で台番が復活し得た）。同じ台番が2マップに同時に存在する状態は保存で作れない／店の新規登録・店設定で店名空・貸玉/交換玉数の不正値をトーストで止め、黙って既定値に戻さない（貸玉0.5〜4・交換玉数20〜50）。運用上の変化：台番を全部移した島は空になるので、そのマップを次に保存するときは島を埋めるか削除する。確認：ツムギの実ブラウザ検証と41件の不変性、ネネちゃんの再現条件での検収（46件版で全項目一致、2026-09-20）。夢爽の端末では店設定変更で既存記録が動かないことを次に見る
- [x] 終了時の評価を記録に保存して固定／少数サンプルの実測／startEv に1R出玉／獲得期待値の店条件（S41 / 84b1c82 / schema 42）… 待ち行列22。出典はネネちゃん（ChatGPT）の全体デバッグ報告2・3（9/19）。①獲得期待値はリザルトを開くたびに現在の台別平均出玉・参考回転率・店設定で計算し直していたため、記録を変えなくても後日の実戦追加や設定変更で過去の数字が黙って動いた（ネネちゃん再現：翌日データ追加で変化、S40検収で交換条件変更により32件変動）。ヤメ確定の時点で `buildEndEv` が評価と条件（`totalYen`・`rate`・`rateSource`・`rateSpins`・`netBallsPerRound`・`netSource`・`availableBalls`・`storeTerms`・区間内訳・`formula:"S17"`・`version`・`computedAt`）を `endEv` に写し、表示・集計（カード・比較表・区間内訳・転記用・履歴カード・日別・積み上げ）は `earnedForDisplay` の1関数から `endEv` を読む（無ければ都度計算＝旧データの安全網。出典に「保存前の計算」）。書き直しは、その記録の `updatedAt` が `endEv.computedAt` より新しいときに `persist()` 側で作り直す（ツムギの判断：記録を変える保存経路が数十あり、個別に足すと足し忘れで「直したのに評価が古い」が起きるため）。稼働中の記録には付かず、ヤメ確定で付く。schema 42 の移行は読み込み直後の `backfillEndEv` で行う（`normalizeData` の中では `data` がまだ無く `deriveSession` が動かない。置き場所は全 `let` 宣言の後・`renderAll` の直前。前だと `selectedLabelFilters` の初期化前参照で起動が止まる＝TDZ）。localStorage への書き込みは次の保存時。②少数サンプル（夢爽裁定 9/20→案B 9/21）：通常回転数 **30以上かつ通常時の消費玉 500以上** ならその実戦の実測回転率で計算し、100回転未満は出典「実測・参考（◯回転）」。それ未満は 参考→想定 に戻して「実測◯回転は少数のため不採用」と理由を書く（`EARNED_EV_MEASURED_MIN_SPINS`=30・`_BALLS`=500）。当初の裁定「回転数に関係なく実測」で試算すると10回転の記録が −11,649円 になった（獲得期待値は「開始地点から当たるまで打ち続けた場合」の式なので、粒度で壊れた回転率が丸ごと効く）ため下限を置いた。S33の参考回転率の採用条件（100回転・20分）は不変。③「最新条件で再計算すると +◯円（今より±◯円。回転率◯・1R◯玉）」を、保存値と今の条件の差が10円以上のときだけ1行（分類A）。参考回転率の変化（同機種の記録追加）でも出るのが正しい。店設定の変更では出ない（④で店条件も固定されたため）。④`startEv` に `netBallsPerRound` / `netSource`（新規の打ち始めから。旧記録は null）。⑤`calculateMachineExpectation` に `store` を渡せるようにし、獲得期待値は `storeTermsForSession` を使う（S40検収で動いた34件→0件）。移行で金額が動いた記録：41件版で4件（8/05 台360 +2,510→+3,177、9/01 台289 +216→−2,165、9/09 台322 +414→+321、9/11 台200 +821→+922）、46件版でさらに9/19の4件（台323・325×2・328。台328は +434→+1,602 で出典「実測・参考（52回転）」）。100回転以上と30回転・500玉未満の記録は不変。確認：ツムギ（41件・移行/固定12項目・境界11ケース・実ブラウザ）、ネネちゃん（46件・数値/固定/再計算/境界すべて一致、2026-09-21）。**残った課題**：出典の文言が「参考（実測なし：実測29回転は少数のため不採用）」と「実測なし」「実測◯回転」が同じ括弧に並ぶ（金額に無関係。S42で式と一緒に直す）。仕事量の定義（途中ヤメでも当たるまで打つ期待値を丸ごと計上）は S42
- [x] 獲得期待値を「実戦後評価」に改め、実測と参考の合成で条件を更新（S42 / 8895cac / schema 43。yutime-calc も同時更新）… 待ち行列23。出典はネネちゃん（ChatGPT）報告4・5。シオンは当初「仕事量＝打った回転ぶんの1回転期待値の合計＋遊タイム到達の価値」を提案したが、夢爽が取り下げ「**実戦後評価＝実戦で分かったことで条件を更新し、各打ち始め地点の期待値を計算し直した数字。式は開始時の想定と同じ。到達・当選・連チャンの結果は反映しない**」で裁定（9/21）。①回転率：`postRate`＝(実測×通常回転数＋参考×K)÷(回転数＋K)。K は既定100回転、店選択画面で利用者が10〜500に変更可（`ytv3:app:rateWeightK`。変えても保存済みは動かず `endEv.rateWeightK` に残す）。参考は 台の過去実測（100回転以上）→店の同機種→`startEv.usedRate`。実測が使えない（概算・逆行・範囲外・0回転）ときは参考のみ。S41の「30回転・500玉未満は不採用」の枝と `EARNED_EV_MEASURED_MIN_*` は削除。②1R実質出玉：`postNetBalls`＝(実測×今回R＋参考×参考R)÷(今回R＋参考R)。参考は「台の直近（終了時刻順）で50Rまで→不足分を店の同機種で50Rまで→そのうえで基準×30R（`NET_REF_BASE_ROUNDS`）を常に加える」。基準30Rを常に加えるのは、S42の初回実装で台320の参考が「店52R・61.5玉」と出て評価が−314円になり、原因を追うと **9/9 台326 の時短抜け持ち玉「0」2か所（引き継ぎ書§4の未修正データ）** が店の参考に混じっていたため（当初は基準100玉→店に52Rあれば基準を使わない設計だった）。あわせて「直近」が `updatedAt` 順（後日修正した台326が先頭に来る）だったのを終了時刻→当選時刻→日付の順に修正、台と店で同じ記録を二重に数えるバグも修正。③**基準1R実質出玉：アグネスPE 100→95玉/R**（夢爽裁定「出てくる数字は辛めに。100だと上手い人基準」）。エンジンのプリセット（`netBallsPerWin`・`defaultNetBallsLabel`「基準値（実戦基準95玉）」）を yutime-v3 と yutime-calc の両方で同じ内容に変え、バイト一致テスト通過。calc の UI 既定値 `payoutDefault` が直書きで取り残されたためエンジン参照に変更（チップ 105/95/90玉）。記事「アグネスPE 遊タイム期待値」は100玉前提のままなので、記事第2段で基準95に更新する。④各通常区間の期待値はその区間の開始時点の持ち玉・再プレイ残りで計算（`endEv.rows[].availableBalls` / `fundsSource`＝"segment"、復元できない旧記録は "startEv"）。⑤表示：カード「実戦後評価」、出典行「回転率18.2（実測16.3・158回転＋参考21.2・店）／1R93玉（実測94.6・38R＋参考92.0・店86R）」、比較表「開始時の想定→実戦後評価→差」、転記用・積み上げ（実戦後評価の合計）・履歴・日別も改名。「獲得期待値」はコメント3か所を除き消えた。⑥schema 43 で完了済み全件（評価できる記録）の `endEv` を S42 の式で一度計算し直し（`formula:"S42"`）。台320は +154（S41）→ +522円。確認：ツムギ（41件・20項目）、ネネちゃん（46件）。ネネちゃんの検収で「**過去の記録の参考に未来の実戦が混ざる**」を検出（台320の参考に9/19の記録が入り +578円）→ S43。「完了済み全件に endEv」は評価できない記録（startEv無し等）を除くのが正しい。K=20の期待値16.45はシオンの計算違い（正16.83）
- [x] 実戦後評価の参考値をその記録の終了時刻より前の記録に限る（S43 / b4263ea / schema 44）… ネネちゃんのS42検収から。`pastSessionsForRate` / `postNetReference` の候補を「評価する記録の終了時刻（無ければ当選時刻→日付）より前に終わった完了済み記録」に締め切り（同着は含めない）、締切を `endEv.referenceAsOf` に残す。「最新条件で再計算すると」の1行も同じ締切。S42では未来参照が広範囲に起きていて（8/05の記録が9/02の記録を参考にする等）、9/13の41件で29件が動いた。回転率の参考の「なし」（実測のみ）の枝は廃止し、台→店→`startEv.usedRate`→島の想定回転率（`activeMapAssumedRate`）の順で必ず参考を持つ（8/04 台292 が13回転の生の実測で −4,595円になっていたのが −340円に）。台320はネネちゃんの検算値（rateRef 21.1873・ratePost 18.1832・netRef 91.9884/86R・netPost 92.7903・**+522円**）と完全一致、K=20で16.83。schema 44 でもう一度だけ全件計算し直し（評価あり35件・できない5件）。エンジンのバイト一致テストは比較起点が宣言行のインデントを含まないことを assert で固定（yutime-calc は無変更）。未来の記録を足しても既存記録の評価が動かないことを確認
- [x] yutime-calc に打ち切りモード（時間制約つき期待値シミュレーター）（C2）… 遊タイム狙いモードが「打ち始め→1回目の当たり（＋連チャン）」までの期待値なのに対し、打ち切りモードは時短抜けから打ち続ける前提で、残り時間が尽きるまでの合計期待値・時給を出す。閉店取り残し（打ちかけの投資が回収できない損失）を織り込む（B案）。エンジンは解析式で時間上限を扱えないため、ページ内で固定シードのモンテカルロ（既定20,000試行）を回す。**エンジンブロックは変更しない**（yutime-v3との文字列完全一致テストで担保）。1回転ずつ乱数を引くと数秒かかるので、区間ごとに「何回転目で当たるか」を幾何分布の逆関数で1回引き、時間と玉代をまとめて進める（分布は1回転ずつと同じ。別実装の1回転ずつシミュレータと4万試行で突き合わせて確認）。1試行を360分まで回して各チェックポイント時点の累積収支を記録するので、5分刻み72点の探索が追加コストなしで出る。**やめるルール（C2-2）**：時短抜けの時点で残り時間が「その起点から1サイクル打って期待値が0以上になる最短時間 T*」を下回っていたら、そこで打ち止める（実戦で座り直さない判断）。T* は残り時間に依存しないので、試行は最後まで回してサイクルの区切りを記録し、チェックポイントごとに「どこでやめたか」を後から当てる。出力は合計期待値・時給・想定当たり・遊タイム到達・時短抜け内訳・時間切れの損失見込みと、**起点ごと（現在カウンター／15抜け／40抜け／90抜け）× 残り時間（30/45/60/90/120/180分）の1サイクル期待値・時給・判定の表**。**再スタート加重は振り分けの30/66/4ではなく終端分布の約36/62/2**（長い時短ほど引き戻して連チャンが続き、サイクルの終端になりにくい。終端分布は w_J × (1-p)^(J+残保留) に比例する）。ST確定機のモデルなので大海5SPは未対応としてトグルを無効化する（推測値を出さない）。localStorage不使用・埋め込み可。検算＝カウンター150・17回転・1R100玉・等価・現金の1サイクル期待値がエンジンの +1,590円 と +1.6%、平均連 2.49連と +0.3%、24時間の時給が終端加重のサイクル時給と −1.4%。既存の遊タイム狙いモードは記事v5の90セル一致のまま不変。**C2-3で「座れる残り時間」の定義を期待値±0（T*）から時給基準へ変更**：座れる残り時間＝その起点から残り T 分で1サイクル打ったときの**時給が閾値以上になる最小の T**（5分刻み）。届かなければ「この条件では打てる水準（時給2,400円）に届きません」と明示する。表の判定はB93と同じ（時給≧閾値＝打てる／期待値プラスで閾値未満＝微妙／期待値マイナス＝打てない）。閾値の既定はv3と同じ2,400円で、`?hourly=` で変えられる（打ち切りモード限定。遊タイム狙いモードの判定は2,400円固定のまま）。**やめるルールは「時短抜けで、残り時間で打つ1サイクルの期待値が0未満ならヤメ」**（C2-3で一度これを時給閾値基準にしたが誤りだったのでC2-5で戻した）。時給の閾値は**判定ラベルと「座れる残り時間」の表示にだけ**使い、合計期待値・時給・時間切れ損失の集計には効かせない（`?hourly=` を変えても集計は1円も動かないことをテストで固定）。この表の時給は「1サイクルの期待値 ÷ 消化時間」で、パネル上段の時給（合計期待値 ÷ 入力の残り時間）とは分母が違う。根拠行に**定常時給**（時短抜け25/50/100を終端分布36/62/2で加重した1サイクル期待値÷所要時間。エンジンの値）を添える。12時間打ち切りの検算＝22回転・時速250 → 定常時給2,439円/h・合計27,980円、22回転・時速350 → 2,986円/h・34,719円、17回転・時速250 → 414円/h・4,578円（判定は「微妙」で「届きません」表示は残る）。**C2-6で表示と入力を整理**：①ヤメ予定に種類を持たせた（既定「打ちかけは消化してやめる」／「必ずやめる」）。消化ならその時刻以降は新しいサイクルを始めず、通常時なら当たるまで・連チャン中なら時短抜けまで打って終わる（取り残しなし）。閉店は種類を選ばせず常に「必ずやめる」で上限に効き、やめるルールの残り時間も閉店までで評価する。時給の分母は打ち終わる予定の時刻まで。検算＝22回転・時速350・9:00→ヤメ予定21:00・閉店23:45 で 消化36,885円（取り残し0）＞必ずやめる34,719円（取り残し−773円）、閉店21:15なら上限が効いて取り残し−778円。②残り時間別の1サイクル表を廃止し、上段を 合計期待値／時給＋判定ラベル／定常時給／想定当たり／遊タイム到達／時短抜け／時間切れ損失、最下段を「閉店の判断」（今から＋時短抜け15/40/90の座れる残り時間4値。「届きません」もここ）にした。③数値欄は共通の `bindNumberInput` に統一：タップした瞬間に空にし（0を消す手間をなくす）、何も入れずに離れたらタップ前の値へ戻す。空にしただけでは state を触らないので再計算も表示の変化も起きない。時刻欄は `type="time"` のまま端末のピッカーを使う。④交換率のチップから30玉を外し、20〜40玉の自由入力（小数可）を足した。入力があればチップより優先し、範囲外は「—」＋1行案内。`?exchange=` も小数を受ける。検算＝28.01玉 → 3.570円/玉 でエンジン直叩きと一致（v3のDSG高岡と同条件）。①③④は両モード共通、②は打ち切りモードのみ。検算＝17回転・時速250回転/h・c50 は定常の時給が約500円なので全起点で「届きません」、22回転・時速350回転/h・c50 は 今から50分〜／15抜け60分〜／40抜け50分〜／90抜け40分〜。**C2-4で入力を実戦の形に寄せた**：①通常時の時速の単位を回転/分 → **回転/時**（チップ 250＝記事の前提／300／350＝導入日の実測）。②残り時間の手入力を廃止し、**現在時刻・ヤメ予定時刻・閉店時刻（既定23:45）から残り時間を出す**（残り時間＝min(ヤメ予定, 閉店) − 現在時刻）。現在時刻は端末の時計から入れて1分ごとに進め、手で直したら以後は上書きしない。根拠を「残り 63分（22:45ヤメ予定・現在21:42）」の形で添え、終了時刻を過ぎていれば計算せず「終了時刻を過ぎています」と出す。URLは `?minutes=` を廃して `?close=23:45&quit=22:30` に。③持ち玉を「持ち玉」と「再プレイ」の2枠にし（v3と同じ）、合計を `availableBalls` としてエンジンへ渡す（どちらも交換単価の玉）。`?saipurei=` を追加。持ち玉2枠は両モード共通。**打ち切りモードの再計算は入力停止から150msのデバウンス**（20,000試行は実ブラウザで1計算あたり約365msかかるので、1打鍵ごとに走らせると入力が固まる）。待っている間は「計算中…」を出し、未対応機種・入力不足のメッセージは計算せずに即時で出す。保留中にモードを戻したり機種を変えたら計算を捨てる。遊タイム狙いモードは解析式なので即時反映のまま（実測0.2ms）
- [x] 記事md→HTMLの変換規約（太字）… `**…**` は **改行（`<br>`）をまたぐものも `<strong>…</strong>` に変換する**。対象は1段落の中で閉じている `**…**` だけで、段落をまたぐ対応は取らない。変換後のHTMLに `**` が1件でも残っていたら変換漏れとして扱い、`grep -c '\*\*'` が0件であることを公開前に確認する。agnespe-yutime.html で複数行の太字6箇所（`**` 12個）が生テキストのまま表示されていたための規約化
- [ ] 判別ツールと店舗分析ツールの連携（Phase 3）

---

# 1. 設定判別ツール（Phase 1）

## コア設計

- [x] ベイズ推定＋二項分布による判別エンジン
  - 各要素（BIG確率、REG確率、小役確率など）ごとに尤度を計算して掛け合わせる
- [x] 解析値はコードに埋め込まず、出典URL・取得日付きのデータファイルで管理する
  - 数値の更新とロジックを分離し、検証・差し替えを容易にする
- [x] 実在の根拠が確認できないフラグ（例: ハナハナのBIG中ハズレ）は入力欄を設けない

## 事前分布（他ツールとの差別化ポイント）

- [x] 事前分布をプラグイン可能なインターフェースとして設計する
- [x] 選択肢1: 均等分布（各設定1/6）
- [x] 選択肢2: 手動プリセット（自分で設定配分を入力）
- [ ] 選択肢3: 過去イベントデータからの推定配分（Phase 3で接続）
- [x] チェック式で「事前分布を参考にする / しない」を切り替え可能にする
- [x] 事前分布ON時は、均等時との比較など「事前分布が結果に与えた影響」を可視化する

## 信頼性・検証

- [x] シミュレーションによる自己検証
  - 既知の設定の確率値で疑似データを生成し、ツールが正しい設定を最上位に出すかを全設定×多数回で確認する
- [x] サンプル数（総G数）に応じた信頼度表示
- [x] Phase 1の時点で、事前分布のデータ形式（設定1〜6の重み＋出典ラベル）を確定させておく
  - Phase 3の連携時に手戻りを発生させないため

## 実戦中のUI（2026-09-23・uiV2・リコリコのみ適用）

リコリコ向けのUI小改修依頼を `checker-engine.js` に実装し、**機種データの `uiV2:true` がある機種だけに適用する**（2026-09-23 夢爽の裁定「まずリコリコのみ」）。実装は全機種共通で、フラグを外せばそのまま全機種へ広げられる。フラグ無しの22機種は、DOM・寸法・挙動が変更前と完全一致であることを確認済み（§9-71・全23機種で回帰確認）。

- [x] タブごとのスクロール位置を保持。`tabScroll` / `tabJumpReturn` をページを開いている間だけ持ち、タブを離れるときに位置と「↑戻る」の戻り先を控え、戻ったときに復元する。初めて開くタブは控えが無いので先頭から表示する。localStorage には保存しない。カウント時の再描画は従来どおり現在位置を保つ（`pendingScrollTop` が null のとき）
- [x] 長い説明文を `<details>`「説明を見る」に畳む。**畳む単位は「文」**で、先頭から短い文が続く範囲（1文30字以内・合計40字以内）を行内に残し、その後ろを畳む。閾値は全体60字以上・畳む側30字以上。操作手順の短い先頭文（例「各ゾーン到達時に記録。当選したら当選側を押してください。」）が行内に残り、背景・出典・計算の説明だけが畳まれる。`.hint.warn`（警告）とリンク・強調を含む説明は畳まない。機種側で `data-fold="no"` を付ければ個別に除外できる
- [x] 折りたたみの開閉は**描画直前にDOMから読んで**控える（`captureFoldState`）。`toggle` イベントは非同期に飛ぶため、イベントで控えるとカウント→再描画の間に合わず開閉が飛ぶ
- [x] リセットをヘッダーから外し、最初のタブの最下部の折りたたみ「データ操作」へ移した。2度押しの確認（「実行する？」・3秒で解除）は従来の `reset()` のまま。確認中に再描画が起きても表示が戻らないよう、ボタンの文字は `resetArm` から描く。下端の余白76pxは、固定表示の「↑ 上へ」（下から98pxまで）が重ならない高さ
- [x] タップ領域を実測して44px未満だけ底上げ（§9-57）。`min-width` / `min-height` で指定するので、既に44px以上ある要素（`.act`＝45px、`.srcchip`＝48px）は変わらない。対象は `.plus` 34→44、`nav button` 42→44、`.hd-btn` 32→44、`.jump-nav button` 36→44、`.jump-back` 40→44、`#main` 内の button / input 19〜38→44
- [x] 数値入力欄に id と `label[for]` を付け、読み上げ名を `data-field-group` ＋ `.gpair-h` ＋ 欄の文字から組み立てる（例「マイスロ 通常ゲーム数 開始」）。見た目と `data-number-key` は変えない。行の途中にボタンが挟まる形（ミリオンゴッドの「打ち始めと同じにする」）は、欄が1つだけの行に限りその行のラベルを引き当てる
- [x] リコリコのみ、下部タブの表示名を「初当り」→「入力」に変更（画面内の項目名との重複解消）。内部キー・保存データ・カード・テキスト出力の「初当り」は変えない

**裁定（2026-09-23 夢爽）**：ヘッダーのリンク（← 機種選択／使い方）は30pxのまま据え置く（実戦中に触らないため、縦の余白を優先）。リンク・強調を含む説明を畳まない方針は承認。godeaterの `.crow.cycle-row` が360px幅で2.5pxはみ出す件は変更前から同じなので別件として保留。

**他機種へ広げるときの注意**：360px幅では `.plus` が34→44pxになるぶん、とある2（ゾーン）と真打吉宗（周期CZ）のラベルが1行増える。どちらも変更前からほとんどの行が折り返しているが、広げる前に実測で確認すること。

### 全機種への展開（2026-09-24）

- [x] 22機種の `checker-data/*.js` に `uiV2:true` を付け、全23機種で新UIに統一。各HTMLのヘッダーからリセットを削除（リセットは各機種とも最初のタブ最下部の「データリセット」の中）
- [x] 入力タブの `.cycle-row` の文字サイズがブラウザ既定任せだった機種を実測して明示指定に改めた。喰霊-零-Re・ミリオンゴッド・東京喰種は `b` のみ既定（16px）だったので同値を固定（見た目は不変）。ゴッドイーター・モンキーターン5は `b`＝既定16px・`small`＝既定約13.3px＋本文色だったので、`b` 16px・`small` 13px＋`var(--muted)` にそろえた（サブの色だけ変わる）。青ブタは `b` 12px を自前で持っているので触っていない。カバネリ2・真打吉宗の `.cycle-row` は `.lbl > .nm/.mn` 構造でHTML側のCSSが効くため対象外
- 実測（390px・全23機種）：`.plus` 44×44／`nav button` 44／`.hd-btn` 44／`.jump-nav button` 44、44px未満の操作要素は0（ヘッダーのリンク30pxは据え置きの裁定どおり）
- 出力（テンプレ・共有文・収支帳用・カード・詳細カード行）は**23/23機種で旧版とバイト一致**

- [x] **「⚠」で始まる説明は畳まない**（uiV2の規則として `checker-engine.js` に固定。`.hint.warn` と同じ扱い）。これで注意書きが折りたたみの中に隠れなくなる。対象は5件（乙女5・真打吉宗の全文折りたたみ2件＋カバネリ2・からくり2・乙女5の行内ありだった3件）
- [x] 先頭文が長く操作手順ごと畳まれていた9件に、**30字以内の要約1文を既存文の前へ追加**（既存の文言は変えない）。これで行内に操作手順が残る。結果、189件中71件が畳まれ、**操作手順が丸ごと畳まれるものと⚠始まりの折りたたみは0件**
- [x] 360px の2列表示（`.cgrid.two`）は、`.plus` 44pxを保ったまま `@media (max-width:379px)` で行の左右padding・gap・数値列の下限だけを詰めた（とある2・真打吉宗）。実幅360pxの実測で**ラベル列 35→45px**になり、とある2は全ラベルが1行に収まる（main では「100G台」が2行だった）。390px・412pxはメディアクエリの外なので変化なし

**計測方法の訂正**：幅を `html,body{width:360px}` で注入する方法は、headless のビューポート（約500px）が変わらないため**メディアクエリが評価されず、flex の解決結果も実機と異なる**。360px級の判定は実幅の iframe で測ること。前回報告した「360pxで文字切れが9件・6件増える」はこの注入方式の値で、実幅で測ると main でも変更後でも文字切れは0だった。

## リコリコ 解析更新（2026-09-23・ちょんぼりすた様 9/23版）

- [x] AT終了画面の私服を一致／矛盾の2行ずつに分割（計9行）。リコリスラッシュ中のキャラと同じなら一致＝高設定示唆(弱)、違うなら矛盾＝設定4以上濃厚。矛盾の2行に rank 4 を持たせ、確定演出の集計・分布グラフに算入する。上位ST（リコリスラッシュW）終了後の私服はキャラを問わず一致側で記録する（出典の「いずれも高設定期待度UP(弱)」より）
- [x] 旧キー（`takina` / `chisato`）の既存回数は**一致側へ移行**する。矛盾かどうかを後から判別できないため、確定演出に数えない側へ倒すのが安全側。新キーが保存データに無いときだけ移すので再読み込みで二重に足さない。旧キーは消さずに残す（§9-59）ため、終了画面の合計は `sum()` ではなく現行の行だけを足す `atEndTotal()` を使う
- [x] テンプレ出力（通常・収支帳用とも）は なな様の書式どおり「たきな私服▶︎」「千束私服▶︎」の各1行に**一致＋矛盾の合計**を出す（§9-95）。行の対応は `AT_END_TPL` に分離した。詳細カードは一致／矛盾を分けて出す
- [x] エピソード3系統（プロローグ／RUSH中／W中）の EP3＝高設定示唆(弱)・EP4＝高設定示唆(強) を反映。EP1・EP2はデフォルトのまま
- [x] 示唆タブに「筐体上部ランプの色」4行（青・黄＝デフォルト／緑＝弱／赤＝強）を新設。発光のたびに1色なので §9-90 の割合表示の対象。テンプレには なな様書式に無いため足さず、カードのサマリーと詳細カードにのみ出す
- [x] カードのサマリーが右列6行になったため `startY:760→752` / `rowGap:44→36` に詰めた。最終行 752+5×36＝932 で、フッタ（y=976）に掛からない上限936の内側

出典の「黄」は原文の表で `rowspan="2"` により「デフォルト」に含まれる（テキスト抽出では空セルに見えるため、HTMLを直接確認して判断した）。storageKey は v1 のまま（キー追加と旧キー移行のみ）。

### 表記の短縮と折りたたみの見出し（2026-09-23 追補）

- [x] 150G変換高確の行名を「150G 変換高確移行」→「150G変換高確」、サブを「設1〜3:50.0%⇔設6:62.5%（設4:54.7%／設5:58.6%）」→「設1:50.0%⇔設6:62.5%」に短縮し、設定別の内訳は折りたたみ側の説明へ移した
- [x] **この行が折り返していた原因は文字サイズだった。** `.cycle-row .ct b / small` の font-size 指定は示唆タブの `<style>` にしかなく、入力タブでは `b`＝16px・`small`＝ブラウザ既定（約13px）で描かれていた。示唆タブと同じ 12px / 9.5px をこの行だけに当て、あわせて数値列の下限（num 38→30・pct 56→44）と行の左右padding（10→6px）を詰めてラベル列を 90→110px にした。数値列は下限だけなので桁が増えれば内容に合わせて広がる
- 実測（実フォントを読ませて計測。390px）：行の高さ **159px → 62px**、サブ 95px(約8行) → **12px(1行)**。412pxも1行。360pxはラベル列80pxで2行のまま（行の高さ 220→73px）
- [x] uiV2 の折りたたみ見出しを「データ操作」→「データリセット」に変更（中のボタン「全データをリセット」と2度押し確認は据え置き）。CSSクラスと id は `dataops` / `dataOps` のまま

**残っている不揃い**：入力タブの `.cycle-row`（規定ゲーム数5ゾーン・変換からのCZ当選）は従来どおり `b`＝16px のままなので、150G行だけ文字が小さい。揃えるなら入力タブの `.cycle-row` 共通指定に font-size を足すことになるが、最も使う規定ゲーム数の行の見た目が変わるため今回は触っていない。

## かのかり 解析更新（2026-09-25・ちょんぼりすた様 9/22版）

公表済みの設定示唆5種を示唆タブへ追加した（初当りタブの「解析待ちの示唆」セクションは役目を終えたので削除）。
行定義は他機種と同じ `[id, 名前, サブ, rank, カード略号]` に統一し、rank>0 を確定演出として集計・グラフ・詳細カードへ乗せる。

- [x] 終了画面7行（枠なし＝デフォルト／白枠BBQ＝高設定示唆(弱)／白枠浜辺＝高設定示唆(強)／赤枠＝設定2以上／紫枠＝設定4以上／銀枠＝設定5以上／金枠＝設定6）。発生のたびに必ず1種が選ばれるので §9-90 の割合表示の対象
- [x] RB中のキャラ紹介9行。出典の8パターンに加えて「その他のパターン」を1行置き、全15種のうち示唆が未判明の7種をここへ寄せて割合の分母を閉じる。⑥〜⑧の水着は出典が「高設定」とだけ記し**対象となる設定の範囲が非公表**のため、rank を持たせず確定演出に数えない
- [x] 1GレンCHANCE 攻略キャラは初回シナリオ4行（①麻美＝偶数／②瑠夏＝奇数／③墨＝偶数／④千鶴＝奇数）を割合表示の対象にし、「同一シナリオが連続」（設定5以上）は同じセクションの**別行**として割合の分母から外した。4人攻略後の切替は対象外・引き戻し時は初回扱い
- [x] センチメートル3行（44＝設定4以上／55＝設定5以上／66＝設定6）と獲得枚数表示4行（246＝設定2・4・6／456＝設定4以上／666・394＝設定6）。どちらも出現有無型なので割合は出さない
- [x] 「濃厚」は使わず rank>0 は「設定n以上確定演出」「設定6確定演出」と表記する。段位の表記（`2・4・6` / `4以上` / `6確定`）はサブラベルから導出し、値の出典を1つにする（§9-84）
- [x] 246枚OVER は設定1・3・5を否定するので、同じ rank2 の赤枠よりカードのサマリーで先に出す（とある2・からくり2の `246 OVER(2・4・6)` と同じ扱い）
- [x] カードは blocks に「確定演出 計n回」、カウント分布に「確定」の棒を追加。サマリーは右列6行になったので `startY:752` / `rowGap:36`（最終行 752+5×36＝932）。テンプレは確定演出の行に段位を添える
- [x] storageKey は v1 のまま（キー追加のみ。旧データの `games` / `counts` はそのまま読み、新キーは0で初期化される）

設定別の出現率はいずれも未公表のため、示唆は記録・共有に徹して数値推測には使わない。
エンディング中のボイスは 2026-09-26 にメーカー公式が全11種を公表したので行を設けた（下記）。

### 監視ボットの取りこぼし（調査結果）

- slot-tools-watch は**この示唆を一度も「要対応」として上げていない**。終了画面とRB中のキャラ紹介は 2026-09-12 の初回スナップショットに入っていたため差分にならず（`if (!prev)` はIssueを作らない）、攻略キャラ・センチメートル・獲得枚数表示とRB紹介の⑥〜⑧は 2026-09-14 の巡回で差分に出たが Issue #13 で「不要（全項目とも設定別数値なし）」と判定された
- 9/22 の更新（Issue #75）で実際に変わったのは「天井 > 朝一リセット恩恵」「通常時 > モード・規定ゲーム数」の2セクションだけで、示唆のセクションは 9/14 以降ハッシュが変わっていない。9/22 の判定自体は正しい
- 原因は `src/judge.mjs` の判断基準。「対応」の定義が「全設定の数値が揃った項目」を主に置き「終了画面等のパターン確定」を従に置いているため、設定別％を持たない確定演出（設定2以上・設定6など）が「不要＝数値のない定性情報」側に倒れる。加えて当時の `kanokari.js` にあった「パターン・内容とも解析待ちのため未実装」というコメントを、判定が据え置きの根拠として引用していた

## かのかり 解析更新（2026-09-26・エンディング中のボイス／メーカー公式）

出典は SANKYO「開発こぼれ話」の記事「エンディング」（2026-09-26） https://www.secret-story.sankyo-fever.jp/article/szf_56 。
ちょんぼりすた様は同項目を「調査中」としているため、この11行だけ**公式を出典にした初めての行**になる。

- [x] 示唆タブに「エンディング中のボイス」を新設（全11行・`voice.v01`〜`v11`・全行 rank0）。エンディング中のレア役でPUSHすると必ず1種が出るので §9-90 の割合表示の対象
- [x] セリフの表記と並びは公式記事のまま。`水原「今は”恋人”。遠慮しない」` の引用符は記事どおり U+201D（ASCIIの `"` ではない）。ダンプした記事HTMLとコードポイント単位で照合済み
- [x] サブは全行「示唆内容は非公表」。水原「私……どんなカオ…してたかな……」だけ「示唆内容は非公表(公式が『特に注目』と記載)」とし、公式が挙げた注目点だけを添えた
- [x] 記事は11行を3つの段落（素・太字・赤太字）に分けているが、段階を示すとは書かれていないため rank も示唆内容も付けていない。**赤太字＝強い示唆と読んで序列を作らないこと**
- [x] テンプレ出力にも節を追加（`■エンディング中のボイス`、割合つき）。カードのサマリーは右列が6行で上限（最終行932／上限936）なので載せず、`示唆の記録 計n回` にだけ算入する
- [x] storageKey は v1 のまま。`voice` キーを持たない旧データは `normalizeState` が11キーを0で埋める（vm で確認済み）
- [x] **詳細カードは6列目の略記で載せる。** engine の詳細行は幅420px・最小18px・省略記号なしで描くため、全文だと `麻美「もう恋なんてしないって決めてるんだから！」 ×1 (9%)` が513px、`水原「私……どんなカオ…してたかな……」 ×1 (9%)` が441pxになり、22行規模のセッションで使われる右列（x=560）に掛かる。engine を触る案（§9-85の詳細カード版・全機種に波及）は採らず、**`ED_VOICE` の6列目に詳細カード専用の略記を持たせた**（2026-09-26 夢爽裁定）。画面の行とテンプレ出力は2列目の出典どおりの全文のままで、`detailItemsShort()` だけが6列目を読む
- [x] 短縮したのは長い6行（麻美「もう恋なんて…」／瑠夏「私が一番…」／瑠夏「お酒の味…」／墨「私がお饗しする番…」／水原「今は”恋人”…」／水原「どんなカオ…」）。残る5行は全文のまま。これで全機種の詳細行が420px以内に収まり（最長は kabaneri2 の 23.0em）、22行のセッションで左列の最大右端429.7px・最終行 y=908（フッタ y=976 の手前）を実描画で確認した


---

---

# 1.5 実戦記録・設定推測ツール（juggler-record.html）

ホールで打ちながら使う統合ツール。設定判別ツール（settei-estimator.html）は事後の一括計算用として併存させる。

## 実装済み

- [x] 2タブ構成（カウント / 設定推測）+ 現在地サマリー常時表示（消化G・BIG/REG・投資）
- [x] データカウンタ（打ち始め時点）入力。現在値はデータGのみ入力し、ボーナスは自分のカウントから算出
- [x] タップカウント6ボタン: ぶどう・単独BIG・チェリー重複BIG / チェリー・単独REG・チェリー重複REG
- [x] 不明BIG/REGボタン。不明分がある種別は内訳ではなく合算で計算に切り替え（統計的整合のため）
- [x] チェリーは記録・確率表示のみ（設定2〜5の数値が未公開のため判別には不使用）
- [x] 元に戻す（カウント・投資共通のundoログ）
- [x] 投資記録（+500円/+1000円、貸レート選択、推定持ち枚数から推定差枚・等価換算を表示）
- [x] 区間分割ベイズ推定（打ち始め前データ + 自分の実戦区間）
- [x] セッションの端末内保存（打ち始めスナップショット含む。Phase 2の台単位データ形式の母体）
- [x] カウントタブの固定全画面化（スクロールなし）。数値入力は電卓パッド（初期値0なし・即入力可）
- [x] カウンター長押しで数値の直接入力（誤リセット時の復旧用）
- [x] 入力エラーは実際の値と修正すべき欄を明示する文言に変更
- [x] 「計算に使うカウント」トグル（ぶどう／ボーナス内訳）。オフ時は記録のみ・推測には不使用。部分カウントによる推定の歪みを防ぐ
- [x] カウントボタンの高さ圧縮と投資エリアの引き上げ、最頻項目ぶどうを親指ホームポジション（下段左）へ配置。片手持ちの親指可動域を基準にボタンを並べる
- [x] 投資をカード残高方式へ変更（yutime-v3と同仕様）。「入金」でカード残を追加、「−1000円」でカード残−1000・投資+1000。入金は投資に含めない。カード未使用なら投資のみ記録
- [x] カウントタブに「クリア」ボタン。カウントのみクリア（台データ・投資は保持）と、すべてクリア（新しい台）の2択モーダル
- [x] カウントのみクリアを「元に戻す」で復元可能に（クリア前スナップショットをundoログに保持）
- [x] すべてクリアも「元に戻す」で復元可能に（保存削除ではなく全状態スナップショット方式へ変更）
- [x] 上部サマリーから「最有力」を削除（点推定の常時表示は不安を煽るため。設定推測タブで信頼度と併せて表示）
- [x] 打ち始め入力の強調（「最初に入力」バッジ＋未入力時のパネル注意枠）
- [x] 全タップ要素を48px以上に統一（タブ・不明BIG/REG・元に戻す・クリア・投資系・数値フィールド・電卓決定）。6ボタン領域は23dvhに再配分し固定全画面を維持。720px以下の小型画面は44px下限で圧縮表示
- [x] タブ切替の視認性改善（選択中はランプ赤の下線、非選択側は背景・文字を沈める、アイコン付与 ✚/📊）
- [x] 投資額・カード残高をタップで電卓修正可能に（押し忘れの事後修正用。持ち枚数と同じ操作性）
- [x] カウントボタンのJSON駆動化（機種データの ui.count_buttons から並び・ラベル・色・記録専用フラグを読んで生成。機種追加はJSON1本で完結）
- [x] 機種選択UI（ヘッダの機種名タップで切替モーダル。記録は機種ごとに分離保存）
- [x] 2機種目: SアイムジャグラーEX（6号機）追加。REGが設定5・6同値のためぶどう(設定6のみ1/5.80)が6判別の主役になる構造をJSONで表現
- [x] 打ち始めパネルの折りたたみ（現在G入力後は1行サマリーに縮小。修正は投資エリアの「初期登録」ボタンから）
- [x] 現在のデータGを主役化（中央寄せ・大型数字・アクセント枠）

- [x] 3機種目: Sゴーゴージャグラー3（6号機）追加。REG合計が最大の判別要素（設定1と6で約1.51倍）だが設定5・6が近接。ぶどうは全設定に段階的な設定差があり補助要素として有効。単独ボーナスは前作比率からの推定値のため信頼度区分を derived_estimate として明示

- [x] 4機種目: Sファンキージャグラー2（6号機）追加。REG合計が設定1と6で約1.68倍と全設定単調で判別しやすい。内訳はアプリ算出値をそのまま使うと公表合算値と最大約12%乖離するため、公表値をアンカーに「公表合算 − チェリー重複」で単独ボーナスを算出（derived_estimate）。第三者の独立推定値と概ね一致することを確認済み

- [x] 実戦値パネルのJSON駆動化。BIG・REG・合算の固定3項目に加え、ui.count_buttons の全項目を自動生成して確率表示する。記録専用項目は「（参考）」を付記。機種ごとにカウント項目を増やしても推測タブに自動で反映される

- [x] ベル・ピエロを全機種にカウントボタンとして追加（record_only）。判別要素としては引き続き不採用だが、稼働ノートとしての記録価値を優先（2026-08-23 方針変更）。確率値は出典未確認のため機種JSONに記載せず、実戦値のみ表示する
- [x] 8ボタン3行に対応した高さ再配分（通常30dvh・最大280px、小型画面26dvh・最大200px）。3行時も1ボタン48px以上を確保し固定全画面を維持

- [x] 打ち始めパネルを常時折りたたみに変更。未入力時は「打ち始めデータ未入力／タップして入力」の1行を警告色で表示し、行自体をタップで展開できる。未入力時に投資エリアが下へ押し出される問題を解消
- [x] カウントボタンの表示カスタム機能。記録専用ボタン（チェリー・ベル・ピエロ）を推測タブのトグルで表示/非表示にできる。非表示にしてもカウント値は保持され、判別計算には影響しない。判別に使う項目は常に表示

- [x] カウントボタンのラベルを2行化（BIG/単独、BIG/🍒重複 のように種別と内訳を分離）。文字を拡大し視認性を改善。表示名は機種JSONの label_top / label_sub で定義
- [x] 設定推測タブの並び替え。判別結果を最上部へ移動し、タブを開いてすぐ結果が見えるようにした
- [x] 収支計算の修正。開始時の持ちメダルを差枚から控除するようにし（未控除のバグを修正）、交換率（等価20円/18.5/17.9/16.6）を選択可能にした。差枚には計算式の内訳を併記して検算できるようにした

- [x] 5機種目: SハッピージャグラーVⅢ（6号機）追加。REG合計が設定1と6で約1.55倍と全設定単調で判別しやすく、BR比率（設定6で約1:1）も材料になる。内訳は公表値アンカー方式で算出し、単独REGが第三者の独立推定値とほぼ完全一致（設定差1.49倍）することを確認。中段チェリー（1/6553.6・BIG確定）は実戦で角チェリー重複と区別できないため cherry_big に合成済み。ベル・ピエロは設定ごとの数値が判明しているが単調性がないため記録専用

- [x] カウントタブのヒント行（タップで+1／長押しでまとめて入力・直前の操作表示）を、3列グリッドに余りセルができる場合はその空きに配置するよう変更。ボタンが8個（3行）のときに行が押し出されて見えなくなる問題を解消し、余白も有効活用する

- [x] 6機種目: SネオアイムジャグラーEX（6号機）追加。アイムEXと同一スペックの後継機で、REG設定差1.72倍・単独REG1.75倍とシリーズ最大級。ただしBIGはほぼ横ばい（1.07倍）で設定5・6はREGが同値のため、設定6の判別にはぶどう（設定1〜5共通1/6.02、設定6のみ1/5.78）が事実上必須。内訳は前作比率からの推定値だが、合成値が公表合算値と誤差0.03%未満で一致するためアンカー調整なしでそのまま採用

- [x] 7機種目: Sウルトラミラクルジャグラー（6号機）追加。前作の変則スペックが解消され、BIG（1.24倍）・REG（1.53倍）とも全設定単調に軽くなる素直な構成。チェリー重複BIGの設定差1.45倍はシリーズの中では例外的に大きい。ぶどうは設定1〜4が同値で設定5・6のみ優遇。非重複チェリーも全設定の数値が判明しており設定差があるが、入力経路が未対応のため記録専用

- [x] 機種選択モーダルのリストに高さ上限（画面の60%）と内部スクロールを設定。機種が増えてもモーダル全体が画面からはみ出さないようにした。10機種以上でも収まることを試算で確認
- [x] 機種違いの事故対策。ヘッダを2段にして機種名を大きく全文表示し、機種固有部分を実機パネル色で着色（背景と左ラインも同系色の薄い着色）。判別結果の見出しと打ち始めの電卓にも機種名を出し、気づける場所を3箇所にした。色を記憶させるのではなく目の前の実機と照合させる設計で、背景が同色になる組（アイム＝金／ネオ＝緑、ハッピー＝緑／ガールズSS＝ピンク）は文字色で区別する。あわせて記録がある状態での機種切替に確認を追加（2026-09-09 実戦で機種違いに気づかない事例が発生）

- [x] 8機種目: SジャグラーガールズSS（6号機）追加。BIG（1.21倍）・REG（1.51倍）とも全設定単調だが、REGの設定差幅はシリーズの中では狭い部類で判別には試行が必要。ぶどうは設定1〜4が同値で設定5・6のみ優遇。BIG/REG個別値は juggler7 と けんのスロットシミュレーション の2ソースで一致を確認（juggler7の合算欄は個別値と食い違うため不採用）。チェリー重複は公表合算と単独の差から算出

- [x] 9機種目: Sミスタージャグラー（6号機）追加。これで6号機ジャグラーシリーズ全機種に対応完了。シリーズで唯一チェリーに設定差がなく（全設定1/37.24）、代わりにぶどうが全設定単調（1/6.29〜1/5.96）という特殊な構成。設定6はBIGとREGが同確率（1/237.4）でBR比率1:1が看破材料
- [x] 内訳（単独・チェリー重複）の確率値を持たない機種への対応。ミスタージャグラーは対応する5号機が存在せず推定値も算出できないため、内訳ボタンを記録専用にしたうえで、カウントした値はBIG・REGの合算として判別に反映する仕組みを buildObservation に実装（hasElement による分岐と undefined 対策）

- [x] マイジャグラーVとアイムジャグラーEXの内訳を公表値アンカー方式へ統一。この2機種はファンキー2以降で確立した方式より前に作成しており、アプリ算出の単独ボーナス値をそのまま採用していたため内訳合計が公表合算値と乖離していた（マイジャグのBIGで最大-14.4%、アイムのREGで-5.9%）。「公表合算 − チェリー重複」で単独を再算出し、全機種で誤差0.03%未満の整合を達成。マイジャグの単独REG設定差は1.88倍→2.07倍に向上し、特殊役によるBIG当選分も単独BIGに含まれる実戦の数え方と一致するようになった

- [x] ガイドページ（juggler-guide.html）作成。ツールの目的、基本の流れ、カウントの取り方、投資と収支の記録、設定推測の読み方（事前分布・信頼度の目安）、数値の出典区分、FAQを掲載。特に「数えるなら最後まで、数えないなら0のまま」という最重要の注意点と、確率であって断定ではないという限界を明記。既存の yoshimune-guide.html と同一の体裁

- [x] 投資エリアの整理（実戦フィードバック）。差枚表示が画面端で途切れる問題に対応し、(1)「開始持ちメダル」を打ち始めパネル内へ移設（初期登録の一部として自然な位置に）、(2)「初期登録」チップを廃止し打ち始めパネル側に「閉じる」ボタンを設置（展開はサマリー行タップ）、(3)「入金」ボタンにカード残高を表示して統合（タップで入金、モーダル内の「残高を直接修正」で修正）。投資行2が2チップになり、差枚と内訳が全幅で折り返せるようになった
- [x] 貸メダル枚数と交換枚数を選択式から数値入力（電卓）に変更。3種類・4種類の固定選択肢では対応できない店が多いため、任意の値を入れられるようにした。交換率は「円/枚」ではなく「1,000円分にするのに必要な枚数」で保持し、ホールの表示（〇枚交換）と直接対応する。等価は50枚。旧形式（円/枚）で保存された端末のデータは読み込み時に自動で枚数へ変換する

- [x] 差枚表示の途切れを解消。投資行の .sum に後勝ちのCSS（flex:1 / white-space:nowrap / text-overflow:ellipsis）が残っており、全幅化の指定を上書きしていた。定義を1本化し全幅・折り返し可に変更。360px端末でも差枚と計算式が省略されずに表示される

- [x] ピエロ重複ボーナスの記録対応（ミスタージャグラー）。ピエロボタンの長押しで「ピエロのみ／ピエロ＋BIG／ピエロ＋REG／まとめて入力」を選べる選択シートを追加し、ボタンを増やさずに記録できるようにした。ピエロ重複を記録してもピエロのみの回数には加算しない（チェリー重複と同じ扱い）。設定推測には使わないが、将来解析値が公開された際にそのまま判別へ組み込める形でデータを残す
- [x] ボーナス総数の集計を一般化。従来は「単独＋チェリー重複＋不明」の決め打ちだったが、機種JSONの count_buttons と sub_counts の group 指定から BIG系／REG系を集約する方式に変更。ピエロ重複が自動的にBIG・REGの総数へ算入される。確率値を持たない重複役が記録されている場合は内訳ではなく合算で計算する安全側の分岐も追加。既存9機種の挙動が変わらないことを検証済み

- [x] 結果カード画像の生成。設定推測タブから、その時点の判別結果を1080×1080のPNGとして作成できる。載せるのは機種名・日付・総回転数・BIG/REG・合算・ぶどう・全6設定の事後確率・信頼度・注意書きのみで、収支と店名は含めない（夢爽の判断 2026-08-28）。端末内のCanvasで生成し外部送信はしない。点推定が独り歩きしないよう、G数・信頼度・「断定するものではない」という注記を必ず画像に焼き込む
- [x] myjuggler5-checker.html（共通エンジン版カウンター）を正式にリタイア。判別性能は juggler-record が全面的に上位で、唯一の独自機能だった共有カードも juggler-record に実装したため役目を終えた。導線からは削除済み、ファイルは404回避のため残置（2026-09-08 sitemap から除外・noindex・juggler-record への案内を追加）

- [x] 結果カードを設定判別ツール（checker系）と同じ意匠に統一。背景・ピンクの発光枠・M PLUS 1pフォント・見出し「SETTING CHECK RESULT」・機種名・日付+G数・右上の丸アイコン・4ブロックの実戦データ・フッタ（slot-tools.jp＋ハッシュタグ＋解析出典）まで揃えた。判別結果は6設定すべてを横バーで表示
- [x] カードに画像を入れられるようにした。「画像を選ぶ」から端末の画像を右上の丸枠に配置でき、256pxへ縮小して機種ごとに端末内保存する。「画像を外す」で解除
- [x] カードのテキスト出力。「テキスト」ボタンで機種名・日付・G数・BIG/REG・合算・ぶどう・全6設定の確率・信頼度・ハッシュタグをクリップボードへコピーする（約200字でXの上限内）。収支帳への貼り付けにも使える
- [x] カードの「長押しで保存できます」という常時表示の案内を削除。「保存する」で保存できるため不要で、保存に失敗した場合のみ案内を出す方式に変更
- [x] 内訳の解析値を持たない機種（ミスタージャグラー）で「ボーナス内訳」トグルが実質無効な件に対応。トグル自体は残したまま、切り替えても結果が変わらない理由を注記として表示する

- [x] マイログ（実戦記録ツール側）。設定推測タブに「マイログに保存して次へ」を追加し、店名・台番号・イベント名・台全体の差枚（概算）・メモを入れて1セッションを記録できる。店名とイベント名は過去の入力から選択できる。保存後はカウントをクリアし、直後なら「元に戻す」で復元できる（貸出枚数と交換枚数は次の台へ引き継ぐ）
- [x] マイログの記録内容。日付・店名・台番号・イベント／打ち始め（総G・BIG・REG・開始持ち）／打ち終わりの台全体（総G・BIG・REG・差枚は「約」表記の任意入力）／自分の区間（消化G・カウント全項目）／判別結果（6設定の確率・信頼度）／収支（投資・持ち枚数・差枚・貸出・交換枚数・換算額）／メモ。台全体のBIG・REGは判別結果の有無に関わらずカウントから算出する
- [x] マイログの一覧表示（ツール内モーダル）。「この機種のみ」「この店のみ」で絞り込め、打ちながら過去の記録を参照できる。記録は個別に削除可能
- [x] マイログの書き出し・読み込み（JSON）。localStorageは端末のデータ削除やブラウザの制限で失われうるため、最初から書き出しを備える。読み込みは追記型で、同じidの記録は重複させない
- [x] 名称を「マイログ」に決定（2026-08-28 夢爽の判断）。サイト名「スロット稼働ノート」と混同せず、解析値を見る場所ではなく自分が打ってきた結果が蓄積される自分専用の場所であることを表す。将来AT機などへ広げてもそのまま使える

- [x] 書き出しファイル名の日付をローカル時刻基準に統一。マイログのJSONと結果カードのPNGのファイル名がUTC基準だったため、日本時間の0〜9時に書き出すと前日の日付になっていた（深夜まで打ってそのまま書き出す使い方で1日ずれる）。localDateStamp() を新設して記録内の日付と共通化

- [x] マイログの一覧・集計ページ（mylog.html）。機種・店舗・期間（月単位）で絞り込み、全体集計（記録数・総G数・差枚合計・収支合計・勝率・BIG/REG合算）と、機種別・店舗別・月別の集計表を表示する。記録一覧からの個別削除、書き出し・読み込みにも対応。実戦記録ツールと同じ localStorage（juggler-mylog:v1）を読むため二重管理にならない
- [x] 集計の設計判断。差枚・収支・勝率は持ち枚数を入力した記録だけを母数にする（未入力の記録を0として扱うと収支が実態より悪く見えるため）。BIG/REGは打ち終わりから打ち始めを引いた自分の区間の回数のみを集計する
- [x] mylog.html は noindex とし、sitemap には登録しない。自分専用の記録置き場であり検索から入る性質のページではないため

- [x] マイログの日付を保存時に指定できるようにした。既定は当日だが変更できるため、過去の稼働をあとから記録することもできる
- [x] マイログの記録を編集できるようにした（mylog.html）。日付・店名・台番号・イベント・打ち始めと打ち終わりの台データ・台全体の差枚・投資・持ち枚数・貸出・交換枚数・メモを修正でき、差枚と収支は入力から再計算する。小役のカウントと判別結果は記録時の値のまま残す（再計算の根拠がないため）
- [x] マイログの集計に時給・BIG確率・REG確率を追加。時給は1時間を800回転として収支を回した時間で割る。機械割は投入枚数の数え方（リプレイを含めるか）で約2%変わり、確認しきれないリプレイ確率が必要になるため採用せず、収支と回転数だけで出せる時給を選んだ
- [x] 設定推測タブのセクション見出しを見やすくした。文字を大きくして本文色にし、左に赤いラインを引いて区切りを明確化。各見出しに役割の一文を添え、目的の項目を探しやすくした
- [x] 結果カードの既定アイコンをサイト共通のキャラクター画像（assets/toaru2-default-icon.jpg）に設定。画像を選んでいない状態でも枠が埋まる。「画像を外す」を押すと既定へ戻る

- [x] 事前分布のトグルのタップ領域を48px（小型画面44px）に拡大。UI設計原則の下限44pxを下回る24pxのままだったのを是正。チェックボックス自体も22pxに拡大

- [x] タップ領域の全面点検と是正。設定推測タブの操作ボタン（.use-btn 13個）を42px→48px（小型画面44px）、「新しい台（すべてリセット）」を35px→同様に、マイログ一覧の「編集」「削除」を32px→44pxで横並びに、mylog.htmlのヘッダリンクを30px→44pxに拡大。あわせて .prior-toggle の旧ルール（18px指定）を削除
- [x] 実戦記録ツールのヘッダリンク（TOP・使い方）は17pxのまま据え置く判断。カウント中の誤タップでページ遷移すると記録中のデータから離れてしまうため、押しやすさより誤タップ防止を優先する。固定全画面の縦の余白を確保する意味もある。WCAG 2.5.5 のインライン例外にも該当する

- [x] 打ち始めデータの連続入力。総回転数→BIG→REG→開始持ちメダルの4項目を、電卓の「次へ」で続けて入力できるようにした。項目ごとに電卓を開き直す手間がなくなる。電卓には「打ち始めの入力 1／4」の進捗を表示する
- [x] 打ち始めの総回転数を入力すると「現在のデータG」にも自動で反映するようにした（現在Gが打ち始めより小さい場合のみ）。座った直後に消化0Gの状態で台データによる判別がすぐ見られるようになり、現在Gを別途入力する手間がなくなる

- [x] マイホレポ（myhorepo.html）を新規作成。ホールの台データ（日付・イベント・機種・台番号・G数・BIG・REG・差枚）を記録し、店全体の傾向を比べられる。名称は夢爽の判断（2026-08-30）。マイログ＝自分が打った台の記録／マイホレポ＝自分がチェックしているホールの記録、という対比
- [x] マイホレポの保存先はIndexedDB。localStorageは1件121文字でも3店舗×50台/日で約5ヶ月で上限に達するため、最初からIndexedDBを採用した。マイログ（localStorage）とは別管理
- [x] マイホレポの入力方式（サイトセブン方式）。日付・店名・イベント・機種を一度指定し、以降は台番号・G数・BIG・REG・差枚を連続入力。追加するたび台番号が自動で+1され、その日の登録済み一覧がその場で確認できる。差枚はサイトセブンで取得できないため任意入力
- [x] マイホレポの分析。各台を判別エンジンにかけて事後分布を出し、店全体の「平均設定」と「設定4以上の割合」を算出する。この日／いつも／平日／同じ曜日／前回の同じイベント、を同じ条件で並べて比較する。日別一覧と台番号別の傾向も表示
- [x] 集計の設計判断。データが少ない台は判別が定まらず平均を基準値3.5へ引き寄せるため、(1)1000G未満を除外する、(2)回転数で重み付けする、の2つを既定でオンにし、切り替えられるようにした。基準線（平均設定3.5／設定4以上50%）を常に併記し、絶対値ではなく「いつもと比べてどうか」で読む指標であることを明示
- [x] マイログからの取り込み。自分が打った台は両方に該当するため、マイログの記録（店名・台番号・台データが揃っているもの）をマイホレポへ流し込める。二重入力が発生しない
- [-] 機械割シミュレーターは取りやめ（2026-08-30）。公開されている機械割は「メーカー公表の出玉率から逆算したぶどう確率」を前提にしており、判別に使うアプリ実測値とは別物だった。同じ機種で2種類のぶどう確率を使い分けることになり説明が破綻するうえ、他者の推定値を中核データにすることになるため
- [x] マイホレポに故障・非稼働の記録を追加。個別入力のチェックボックスで指定でき、一括入力ではG数0の行を自動で非稼働として扱う。集計からは常に除外され、一覧には「故障・非稼働」と表示される。サイトセブンで0Gと表示される台をそのまま貼り付けられる
- [x] マイホレポの故障・非稼働チェックは、登録のたびに自動で外れる。チェックしたまま次の台を登録して意図せず非稼働になるのを防ぐ
- [x] マイホレポに一括入力欄を追加。1行に1台分を「G数 BIG REG（差枚）」のスペース区切りで打ち、まとめて登録できる。台番号は先頭の番号から自動で連番になり、飛び番があるときは「各行の先頭に台番号を書く」に切り替えられる。区切りは半角・全角スペース・カンマ・タブに対応し、項目数の過不足や数字以外は行番号付きで指摘する
- [x] マイホレポに入力を楽にするコツを掲載。Androidの分割画面の手順（履歴ボタン→アイコン→分割画面）と機種ごとの補足、iPhoneには画面分割がないため一括入力を使う案内
- [x] 画像から自動で読み取る機能（OCR）は採用しない方針を確認（2026-08-30）。データサイトの規約は機械的な取得を禁じていることが多く、ツール側にその機能を持つこと自体が自動取得を助長する仕組みと見なされうるため。転記は人が行い、ツールは打鍵回数を減らす方向で支援する
- [ ] マイホレポ：1台×複数日の一括入力モード（日付 G数 BIG REG ＋差枚は選択式・未入力を保持／当日分と重複を自動除外・除外内訳を表示）
- [ ] マイホレポ：まとめて入力の貼り付け欄の上に店名・機種・台番号（1台×複数日）／日付（1日×複数台）を連動表示し−1／＋1ボタンを置く。1台×複数日は登録後に台番号を自動＋1。結果表示に台番号／日付を入れる
- [ ] マイホレポ：台の履歴（1台の記録を日付順に、日付・G数・BIG・REG・合算・REG確率。差枚があれば差枚も）
- [ ] マイホレポ：店名＋台番号から前回登録した機種を自動で選ぶ（「前回登録：◯◯」表示。手で変えれば上書きしない）
- [ ] マイホレポ：台番号別の傾向に1行の帯（絞り込み条件「◯◯ のみ ›」タップで大元へ＋［設定順］［台番号順］チップ）。件数→日数（同じ日は1日）、脚注は集計の条件を指す、設定4以上の注記。並び順のみ記憶（機種・基準日は記憶しない）
- [ ] マイホレポ：不具合修正（40台の打ち切りを黙って切らない→ほか◯台＋さらに表示、店未選択時の別店合算→店選択まで非表示、台の履歴に集計対象外の印、fmtSum の単位を引数化）
- [ ] マイホレポ：桁区切りカンマ付き数字を正しく読む・列が多い行は黙って捨てず読めなかった行にする。台の履歴に「絞り込み外」印。同じ店・台・日の重複は最後に保存した1件だけ集計し履歴に「重複」印
- [ ] マイホレポ：行の種類（カンマ区切り／スペース区切り）を先に決め、桁区切りは項目の中でだけ結合（カンマ区切り＋差枚の誤登録と不正カンマの穴を解消）。重複除外→絞り込み→集計の順に統一し、採用判定を集計と履歴で共通化
- [ ] マイログ→マイホレポ取り込みは記録日時で新旧を判定し古い方で上書きしない（取り込み時刻は別項目）。マイログ記録に小役の対象G数を保存し数え直し後の小役確率を正しく表示。旧記録の差枚は残して勝敗判定に使い、円は「不明」として0円扱いしない。結果カードのBIG/REGは台全体（前任者込み）で仕様確定
- [ ] 実戦記録ツール：資金欄を組み替え（上段＝−1000円・入金・投資／下段＝持ち枚数・「この台を終了して記録」）、貸出・交換は終了シートへ。終了シートで投資・開始持ち・終了持ち・貸出交換・円収支を確認してから保存（未確定を明示）。記録に sessionId を持たせ、元に戻す→再保存の二重保存を防ぐ。設定推測タブのボタンは同じシートを開く

## 検討中・将来


- [ ] セッション履歴の保存・一覧（1日複数台の記録） → Phase 2の入力口を兼ねる
- [~] 残数スナップショット方式による収支管理（SF6ツールで確立したモデルの移植）。財布（現金・持ちメダル・再プレイ）と台（クレジット）を分離し、「今いくらあるか」の残数で差枚を算出する。投入記録は不要になり記録し忘れによる事故が構造的に起きない。第一段階として開始持ちメダルの控除と交換率選択を実装済み（2026-08-24）。クレジット・再プレイ・回収の分離は今後
- [ ] セッション収支のカード保存と分析（機種別・店別・日別の勝敗集計）→ Phase 2の店舗データと統合
- [ ] 非重複チェリーを判別要素として使えるようにする。ハッピージャグラーVⅢは角チェリーに明確な設定差（高設定ほど重い逆方向）があるが、現行の入力経路が対応していないため記録専用にとどめている
- [ ] 実戦結果・判別結果のX（SNS）共有機能 — 別途構想中（アイデア★パチスロ開発側の議論が固まってから着手。共有フォーマットの手戻り防止のため保留）
- [ ] 打ち始めパネルの自動折りたたみ。現在は電卓で確定しても畳まれず「初期登録」のタップが必要。3項目を続けて入力する運用では自動で畳まない方が自然という判断だが、実戦での使用感を見て再検討する
- [ ] 実戦記録ツール：機種色の視認性強化（外周に機種色の固定枠＋機種名帯のベタ塗り。太さ6px初期・変数2つで調整。カウントボタンは不変。実戦で強さを検証後に調整）
- [ ] 実戦記録ツール：Chromeで資金パネルに潜る行の隠れ解消＋資金パネルの折りたたみ（たたむと−1000円と差枚の1行。状態は端末保存。ベル・ピエロ非表示は見送り）
- [ ] 実戦記録ツール／マイログ：収支計算を js/money.js に共通化（投資ありは収支成立・非等価は持ち×交換単価−投資円・保存値でなく入力値から毎回計算）。カウントクリアは「ここから数え直す」（打ち始めデータを現在値に付け替え・元に戻す対応）
- [ ] 実戦記録ツール／マイログ：数え直し地点を打ち始めと別管理（実戦全体の記録を保持・判別は最大3区間）。勝率は円収支で判定し±0円は引き分け（母数に含める）

---

# 1.6 汎用 実戦記録ツール（tools/rec/）

## tools/rec 実戦記録ツール：お金の記録形式と移行規則

### 現行形式（rec7以降）
- `money.deposits` … 入金（サンドに入れた現金）
- `money.spends` … メダル貸出（＝投資）
- 投資 = sum(spends) ／ 残高 = sum(deposits) − sum(spends)
- 残高が1,000円未満で「−1000」を押した場合、同額を deposits にも自動追加する
  （サンドに入れず1,000円ずつ使う打ち方に対応するため）

### 再プレイの扱い（rec25以降）
再プレイは「貯メダルから途中で入れる投入」として `money.replays[]` に履歴を持つ。
開始時・終了時の再プレイ欄は廃止。開始時は持ちメダルのみ、終了時は
貯メダルに預けた分も含めた手元の総枚数を入れる。

- 差枚 ＝ 終了持ち − 開始持ち − 現金で買った枚数 − 再プレイ枚数
- 収支 ＝（終了持ち − 開始持ち − 再プレイ枚数）× 1000/lend − 現金

上限が低い店で「一気におろして持ち玉扱い」にしても、開始持ちに含めれば計算は同じ。

旧形式（`startReplay` / `endReplay`）は起動時に `fixMoney()` が変換する：
`startReplay` → `replays` に1件、`endReplay` → `endMedal` に加算。計算結果は変わらない。

kado-note に渡す `payout` は「終了持ち − 開始持ち − 再プレイ」の純増枚数。
rec22 までは終了持ちをそのまま渡していたため、開始持ちがある台で収支がずれていた。

### 旧形式（rec4以前）からの移行
旧形式は `money.cash` のみを持ち、入金と使用を区別していない。
移行は `fixMoney()` が起動時に一度だけ行い、結果を保存する。

規則：
1. `Math.abs(sum(cash))` を求める
2. その額を deposits と spends の両方に入れる（残高0）

符号を無視する理由：rec4 は入金を正、「−1000」を負で記録していたため、
符号に意味がない。−1000 のみで打った台は合計が負になるが、
使った額は変わらないため絶対値で拾う。

限界：入金と使用が混在した台は正確な復元ができない。
rec4 の画面表示と同じ値になるよう、単純合算の絶対値を採用している。
（例：+3,000 と −1,000 の台 → 投資2,000円。実態は投資1,000円の可能性あり）

---

## tools/rec 実戦記録ツール：ログ表記と kado-note 連携

### ログのゲーム数表記（rec13以降）

ログ1行の左に出すゲーム数は、実Gと液晶Gの関係で表記を変える。

| 実G | 液晶G | 表記 |
|---|---|---|
| 80 | 80 | `80` |
| 57 | 86 | `57/86` |
| 80 | なし | `80` |
| なし | 86 | `86` |

液晶Gを持たない機種（沖ドキ等）でも実Gが表示される。
`gsText()`（テキスト用）と `gsHtml()`（画面用・液晶G側に色）の2つを用意し、
画面・テキスト書き出し・kado-note用出力のすべてで同じ表記に揃える。

### kado-note 連携（rec13以降）

rec は schemaVer 2 のエンベロープを出力できる。変換は `toEnvelope()`。

| エンベロープ | rec から入れる値 |
|---|---|
| sessionId | rec の セッションid（毎回同じ値。重複検出のため） |
| machineId | 機種名（日本語は toKebab() で落ちる。下記の制約参照） |
| payloadVer | `"rec-2"` |
| invest | 投資額（円）＝ `invested(money)` |
| payout | 純増枚数 ＝ 終了持ち − 開始持ち − 再プレイ（枚。rec25以降。それ以前は終了持ちをそのまま渡していた） |
| lendRate / exchangeRate | レート（等価前提で同じ値を入れる） |
| memo | 機種名 ／ 想定期待値 ／ 差枚 |
| payload.logs | `{t: epoch_ms, type:"memo", text:"57/86G　強🍒 (1回目)"}` |

payload には機種名・開始終了のG数・持ちメダル・想定期待値・レア役回数も残す。

出力は2種類。どちらも kado-note で取り込める（kado-note 側で配列対応済み）。
- 各台の「この台をノート用に保存」… 単一オブジェクト
- 「同・保存」… 全台の配列。1回の取り込みで複数台が入る

### kado-note 側の対応（55feb5d）

rec 連携のために kado-note.html を3点改修した。

1. `normalizeSchemaV2()` / `isLegacySession()` に `machineLabel` を追加し、
   元の機種名文字列を保持する
2. `machineName()` は、機種名にASCII以外の文字が含まれる場合、
   `machineLabel`（なければ `payload.machine`）を表示する。
   日本語機種名が `unknown-machine` と表示される問題はこれで解消
3. `importFiles()` が配列に対応。1ファイルに複数セッションが入っていても読む。
   途中の要素が壊れていても他は取り込まれ、エラーには `ファイル名[n]` で位置が出る

SF6 の描画は改修前後でDOMがバイト一致することを確認済み。

残る制約：
- 旧版で取り込み済みの日本語機種名セッションは、保存時点で機種名が失われて
  いるため名前が戻らない。該当データは取り込み直しが必要
- `toKebab()` はASCII以外を落とすため、機種名が短い語に潰れることがある
  （例：「沖ドキDUOアンコール」→ `duo`）。表示は machineLabel で正しく出るが、
  **machineId は機種の同一性を判定する値としては使えない**。
  機種別の集計を行う場合は machineId の生成方法から見直すこと

## tools/rec 実戦記録ツール：記録の修正

### 修正の入口（rec23以降）
- 進行中：投資・回収・出力 →「打ち始めの情報を直す」「お金の履歴を直す」
- 終了後：ノートの各台 →「打ち始め・終了の情報を直す」「お金の履歴を直す」「記録の行を直す」

`editInfo` / `editMoney` / `editEventIn` は進行中・終了後の両方で同じ関数を使う。
お金の履歴を消しても記録のログ行は残る（ログは事実の記録、履歴は計算用）。

### 実Gと液晶Gの連動（rec89以降）

画面上の実Gをタップして直すとき、液晶Gも一緒に直す（`editRealWithLcd()`）。
液晶Gだけを直したいときは液晶Gのカウンターをタップする（従来どおり、実Gは動かない）。

3択で、確定する前に両方の値を見せる：

- **差を保つ**（既定・選択済みの色）… 上乗せ分の差を残す。`液晶 = 実G + (元の差)`
- **実Gと同じにする** … 両方を同じ値にする。CZ失敗直後など、0から数え直す場面
- **液晶は変えない** … 実Gだけ直す

押すまで値もログも確定しない。押すと `gFix` の行（「G数を直した：実◯/液晶◯ → 実◯/液晶◯」）を残し、
シートを閉じて `checkAutoStage()` を通す（32G抜けなどの自動判定が実Gの修正でも効く）。

取り消しは「元に戻す」「直前を取り消す」のどちらでも戻る。
前者は op のスナップショット、後者は `_steps` に積んだ `{multi:[{k:"realG",prev},{k:"lcdG",prev}]}` で、
実Gと液晶Gをまとめて戻す（片方だけ戻る状態を作らない）。

`lcdG` を持たない機種（ハイビリターン30）は3択を出さず、従来どおり即確定する。

### フォロー期待値（rec30以降）

打ち始めに入れた想定期待値とは別に、途中で状況が変わった分を session.follow[] に
{ at, amount, note } で積む。「投資・回収・出力」→「フォローで追加」。
想定の合計は expectedTotal() ＝ expected ＋ follow の合計。
ノートの集計・テキスト出力・kado-note のメモはすべて合計を使う。
payload には expected（最初の値）と follow[]、expectedTotal を分けて残す。

---

## tools/rec 実戦記録ツール：機種定義の約束事

### 前兆の自動終了（rec18以降・全機種共通）

前兆はCZ／AT当選と同時に終わる。当選を記録した時点で前兆バッジを消し、
当選ログに「前兆から」と付記する。

### 当選種別ごとの画面（rec20以降・全機種共通）

機種定義 hits[] の各当選に grid を持たせると、その当選中はそのボタンだけを出す。
grid が無い当選は grids[kind]（cz／at）を使う。
CZ中に通常時の操作（ステージ・運命盤など）が出ないようにするための仕組み。

### AT後の流れ（rec21以降）

機種定義 afterAt:{hit, trig} があると、AT終了シートの確定ボタンが
「終了する」ではなく「<hit>へ（AT後）」になり、その当選に自動で移る。
からくりサーカス2は afterAt:{hit:"幕間チャンス",trig:"AT後"}。

AT終了シートで記録するのは AT終了時に見えるもの（1枚絵・ボイス）だけ。
ワイプ演出・開始ステージは AT後のCZ失敗時に czEnd で記録する。

### SHOW TIME復活（rec20以降）

機種定義 revive:{label} があると、AT終了シートに「<label>した（AT継続）」が出る。
押すと終了画面の記録を残したまま AT中に戻り、session.revive を +1 する。
解析にはないが設定差が噂されるため、回数を数える。

### 当選定義の構造（rec22以降）

機種定義 hits[] の各要素が持てる項目。
- `kind` … "cz" か "at"
- `hidden` … true なら当選ボタンに出さない（流れの中で自動的に入るもの。例：AT、運命の一劇）
- `enter` … 別の当選定義で開始する（例：AT直撃 は enter:"AT"。ログには「AT直撃 当選」と残る）
- `inline` … true なら当選種別を選んだ直後に終了シートを開く（BIG/REGのような短いボーナス用）
- `keepG` … 終了シートのG数欄の既定値を現在値にする（当選中もデータカウンターが
  進むCZ。→「当選時のG数の扱い」）
- `grid` … その当選中に出すボタン。無ければ grids[kind]
- `czEnd` … CZ終了シートの中身。"std" なら機種の czEndStd を使う。
  { groups, successToAt:"<当選ラベル>", failNote } で個別指定もできる
- `atStart` / `atEnd` … AT開始時・終了時に聞く項目。null で聞かない
- `afterAt` … AT終了後に自動で入る当選 { hit, trig }。無ければ「終了する」
- `revive` … { label } があると AT終了シートに復活ボタンが出る。null で出さない
- `phases` … AT内のサブ状態 { key:{ label, grid, afterAt:{ hit, trig, skipEnd } } }。
  skipEnd:true なら終了画面を出さずに afterAt へ

グリッドのボタン指定：
- `phase:<key>` … サブ状態に入る
- `goto:<当選ラベル>:<契機>` … 今の当選を終えて別の当選に入る（確認あり）
- `atx:<n>` … 機種の atExtras[n]

並行状態（parallel[]）の追加キー：
- `resetCountOnHitEnd` … true なら「n回目」の番号をCZ／AT終了時に0に戻す。
  からくりサーカス2の運命盤は、CZ／ATを抜けるとスイカ規定回数の数え直しが始まるため付ける

選択肢の書き方（groupSheet / atEndSheet 共通）：
- 文字列 … ラベルだけ
- `[ラベル, 補足]` … 補足を小さく表示
- `{ l, h, set }` … set に示唆を持たせる。選ぶと applyHints() が session に反映する
  - `set:{melonLeft:n}` … 🍉残りn。`approx:true` で「n以下」
  - `set:{reward:"AT以上"}` … 次回の運命盤報酬
  - `set:{mode:"通常C濃厚"}` … モード示唆

CZ成功からATに入るとき、直前のCZの契機が「AT後」なら AT の契機を「引き戻し」にする。
集計で「通常時からのAT当選」と区別するため。

### 当選時のG数の扱い（rec27以降・全機種共通）

当選時に実G・液晶Gは触らない。終了時に「今の実G／今の液晶G」を入力して合わせる。
sf6-record.html と同じ考え方。当選ボタンを押すタイミングと実際の当選G数がずれても、
終了時に台の表示を見て入れれば合う。

終了シートのG数欄の既定値：
- 通常のCZ・AT・ボーナス … 0（終了後は0から数え直す）
- `keepG:true` の当選（幕間チャンス・劇場ジャッジ）… 現在値（通常時のG数がそのまま進む）
- CZ成功 … G数欄を隠して自動で0（次の当選が始まるため）
- ED→運命の一劇（skipEnd）… 自動で0

keepG は「当選中もデータカウンターが進むCZ」に付ける。新しい機種を定義する時は
CZごとに必ず確認する。

### 液晶に出る残り回数の追跡（rec30以降）

session.melonLeft に「🍉残り回数」を持ち、バッジ行に「🍉残りn」と出し続ける。
- 「🍉残り」ボタン（3択）、またはセリフ・ボイスの示唆（→「示唆のバッジ化」）で設定
- 🍉を登録するごとに1減る。0で「🍉規定到達」
- 運命盤の発動、CZ／ATの終了で null に戻す（消える）
- 取り消しで🍉を戻すと1増える

液晶が示す「残り回数」を見た時点で記録するための仕組み。引く前に当たっても
サンプルとして残る。他の機種で同じ性質の表示（残り○回、○回目でCZ など）があれば
同じ形で持たせる。

### 示唆のバッジ化（rec31以降）

セリフ・ボイスの示唆は、選んだ時点でバッジに反映する（session.melonLeft / rewardHint / modeHint）。
消えるタイミングはそれぞれ違う。
- 🍉残り、モード示唆 … CZ／ATを抜けたとき（規定回数・モードが変わるため）
- 次回報酬 … 運命盤の発動まで残る（CZ／ATをまたいでも報酬は引き継ぐ）

AT終了時の反映は afterHitEnd() のあとに applyHints() を呼ぶ。逆にすると、
ボイスで入れた🍉残りが直後に消える（rec31 で実際に起きた）。

### 規定回数のリセットと打ち切り（rec34以降）

rares[] の追加キー：
- `resetOnHitEnd` … true なら CZ／AT終了で回数を0に戻す
- `cutLog` … true なら、0に戻す前に「🍉 n回で打ち切り（<終了した当選>）」を rareCut としてログに残す

からくりサーカス2の🍉に付けている。運命盤の規定回数はCZ／ATを抜けるとリセットされる
（実戦で「🍉残り1→CZ→🍉で発動せず」を確認）。報酬（rewardHint）は引き継ぐが、回数は引き継がない。

運命盤発動時は「🍉 n回目で発動」をログに残し、イベントに melonAt を持つ。
打ち切り（rareCut.n）と発動（parStart.melonAt）を分けて記録するのは、
規定回数の分布を後から推定するため。打ち切られた区間は「n回までは発動しなかった」というサンプルになる。
設定差があると噂される部分なので、集計は記録が溜まってから考える。

### 前兆の段階（rec32以降）

前兆ボタンは押すたびに進む：前兆 → 前兆ステージ → 失敗。「前兆抜け」は無い。
session.zenchou は false / "pre" / "stage" の3値。
前兆ステージに入ったらステージ表示を zenchou.stageLabel に切り替え、失敗時に戻す。
失敗シートで聞くもの（zenchou.outGroups）：最終幕の色、ワイプ演出、戻ったステージ。
ワイプ演出はステージシートには置かない。出るのは 当選終了時 と 前兆ステージ失敗時 だけ。

### 取り消しの復元（rec32以降）

状態を変える操作は、直前に mark() を呼び、次の push() が snapshot を持つ（ev.snap）。
「直前を取り消す」は snap があれば SNAP_KEYS をそのまま戻す。
当選・終了・前兆・運命盤・ステージ・レア役・セリフ・復活・サブ状態が対象。
お金の履歴（deposits / spends / replays）は snap に含めない。「お金の履歴を直す」で扱う。

### CZ終了の2ボタン（rec32以降）

CZ終了シートは「失敗」「成功」の2ボタンだけを出す。
成功 … 即 hitEnd を記録し、G数を0にして successToAt へ。終了画面は聞かない
失敗 … 終了画面セット・メモ・G数欄のシートに進む
迷う余地を無くすため。成功後の「当選→AT」の二重登録を防ぐ。

### v0.1 仕様への対応（rec37〜rec45）

からくりサーカス2の定義は、ネネちゃんが夢爽へのヒアリングで作った karakuri2-spec-v01.md を正本とする。
対応表と段階分けは karakuri2-v01-mapping.md。§13 の通し操作11本は rec45 で全本通過。

器に追加した仕組み：
- `czEnd.failTo` … CZ失敗で別の当選に自動で入る（劇場ジャッジ失敗→女神、神失敗→AT）。終了画面は聞かない
- `keepOnFail` … CZ失敗でも通常時が続くもの（通常時の幕間チャンス）。運命盤回数・🍉・🍉残りを持ち越す
- `hitWhere` … 当選シートで「告知された場所」を任意で記録
- 前兆は `zenchou.stages` で段階を定義。各段階は任意で、飛ばせる。`stageIndex` の段階だけ失敗時に最終幕などを聞く
- `ticket` … 一劇の権利。`autoBy` で激情ジャッジ成功n回目に自動付与。手動トグルあり。`afterAtWithTicket` で行き先分岐
- `sub:<当選>` … AT中から別のCZに入る。失敗で `returnHit` から元のATに戻る（ステージ・権利・復活回数を保持）
- `parallel[].pending` … ミッション成功で報酬待ち。バッジタップで当選へ（契機：運命盤）。CZ／ATをまたいでも残る
- 🍉は場面別（通常中 n／ミッション中 nMission／報酬待ち中 nPending）。報酬待ち開始と昇格で nPending を0に
- `pending.upgrade` … 報酬変更（昇格）。履歴に変更前の🍉回数を残す
- `modeEra` / `modeHints[]` … モード示唆は {内容, 出た区間, 有効区間数} の履歴。区間は `modeEra.advanceOn` の当選で進み、`skipTriggers` の契機（劇場ジャッジ 失敗）では進めない。バッジは今の区間に有効なものだけ
- `lcdRestart` … 当選を抜けて通常時に戻るときの液晶Gの起点（からくり2は1）。keepG で引き継ぐ場合も起点を下回らない
- 選択肢の `{l, h, set}` に `span` … モード示唆の有効区間数

からくり2固有で定義側に置いたもの（器に固定しない）：
- 🍉の女神失敗リセット（仮ルール）、通常時幕間失敗での持ち越し（U07 は未確認、持ち越し側に倒す）
- 区間を進める当選＝女神／劇場ジャッジ／AT／上位AT
- CZ終了画面の示唆と有効区間（確認済み8種のみ。残り6種は「要確認」で記録のみ）

未確認（U01〜U12）は自動処理の根拠にしない。詳細は karakuri2-spec-v01.md §12 と mapping.md §1-10。

### 機種2：ハイビリターン30（rec46〜rec49）

仕様書は docs/hibireturn-spec-v02.md。からくり2と違い、仕様書→ヒアリング（13問）→実装 の順で作った。
実戦前に通し操作8本が通っている。修正は器のバグ2件のみで、流れの見落としは出ていない。

器に足した汎用の仕組み：
- `lcdG:false` … 液晶Gを使わない機種。カウンター・入力欄・ログ表記から液晶を消す
- `zenchou.enterOnHitEnd` … ボーナス終了で自動で段階（32G中など）に入る
- `zenchou.autoEnterIfG / autoEndAtG` … 打ち始めの実G、消化中の実Gで段階に自動で出入り
- `zenchou.endLabel / onEnd` … 段階終了時のラベルと処理（カウンター増減、タグ消去）
- `onHitInStage` … 段階中の当選で起きること（タグ、カウンター）。exceptTriggers（天国抜け扱い）、noTagTriggers（1G連）
- `set:{tag, until}` … タグのバッジ。until は nextHit／stageEnd／hitEnd
- `hitExtraGroups` … 当選シートで追加で聞くこと（告知・揃い）。group / option の showWhen で種別に応じて出し分け
- `afterAtWithTag` … タグがあれば終了先を変える。viaNormal:true で「終了→通常時→即当選」（1G連）
- `askGOnEnd:false` … 終了時にG数を聞かない（G数が決まっている機種）
- `rare:<key>` … レア役の直押しボタン。`toggle:<key>` … ON/OFFのボタン
- `counter3.reset:"never"` … AT終了でも聞かず変えない

器の一般ルールに昇格したもの：
- 選択肢が1グループで必須なら、選んだ瞬間に記録する（「記録する」を出さない）。
  からくり2の枚数表示・EDランプ・セリフ・運命盤の報酬、汎用の当選もこれに従う
- レア役のボタン名は絵文字＋短い文字（🍒チェリー、🍉スイカ、🔔共通ベル）
- あるか無いかの記録はON/OFFトグル。1回押し＝ON、もう一度＝OFF

教訓：
- 列挙値を増やしたら、その値で分岐している箇所を全部 grep する（counter3.reset に "never" を足したとき、"at" 以外を汎用扱いする分岐に入って落ちた）
- 状態を撮る mark() は startHit / leaveAt の冒頭だけ。呼び出し側では撮らない

### 実戦後の追加（rec50〜rec52・ハイビリターン30）

実機で打ったあとのフィードバック。フェザーランプ（消化中の次回モード示唆）の記録で出た。

器に足した汎用の仕組み：
- 選択肢の `set` に `ifTag:"<タグ>"` … 条件付きの示唆。そのタグが立っているときだけ効く。
  立っていなければ示唆を付けず、ログに「（<タグ>中ではないので示唆なし）」と残す
  （ハイビのフェザーランプ虹は、初当り時は次回天国の対象外。天国中に見たときだけ意味がある）
- `atExtras[].once` … 1つの当選で1回しか出ない項目。2回目以降は行を足さず、
  その当選中の最初の記録を上書きする（ログは「…（修正）」、示唆のタグも付け替える）。
  グリッドのボタンには記録済みの値を並べて出す
- `atExtras[].short` … 値を並べて出すときの短縮名。ボタンが2行に折り返さないようにする

器の一般ルールに昇格したもの：
- 修正シートも、1グループ必須なら即判定にする（「この内容に直す」を出さない）。
  即判定のときは、選択済みの値をもう一度押したら選択解除ではなく確定＝「同じ内容です」で閉じる。
  記録済みの項目を開き直すのは「直すため」で、消すためではない
- 記録済みの値をボタンに出すなら、1行に収まる名前にする。390pxで「フェザーランプ 虹」は
  2行になりグリッドが172px→190pxに伸びた（rec51）。short:"フェザー" で1行に戻した（rec52）

残り：
- `fixAtStart`（からくり2の開始ステージ修正）は before を常に渡すため即判定にならず、
  「この内容に直す」が1タップ残る。揃えるなら openExtra の fixAtStart 分岐にも instant を渡す
  → rec66 で解消

### 実戦フィードバック（からくりサーカス2・rec53〜rec66）

9/13 の稼働（9時間）で出た17点。流れの崩れはゼロ。全部「直しやすさ・見やすさ・実機の細部」。

流れ・定義：
- 前兆の本前兆段階は複数持てる（`stageIndex:[2,3]`）。からくり2は前兆ステージと訪れし者
- 「発展せず」を出す段階を `cutStages` で限定。前兆からは必ず本前兆へ
- 前兆の段階中は先頭バッジを段階名にし、「通常時」は出さない
- 失敗先が決まっていて聞くことが無いCZは、失敗ボタンで即遷移（劇場ジャッジ→女神）
- 契機でG数が固定される当選は `fixedEndG`（AT後の幕間チャンス→実4／液晶0）
- 液晶Gの起点は0。「液晶1」は「1G回した後」の表示で、起点ではない（lcdRestart 廃止）
- 運命盤の報酬は何であれ報酬待ちを経る。懸糸傀儡演舞は「発動→上乗せを入れる」で消費

修正の口（「画面に出ている数字は全部タップで直せる」）：
- レア役のカウンターをタップで回数を直す。リセットではなく任意の値
- ログの行を開いて 時刻／実G／液晶G／文章／記憶が曖昧（？）／補足 を直す。時刻を直すと並び直す
- 運命盤の開始行を開いて報酬を直す。報酬待ちの報酬も同期
- 状態を変えた行（snap あり）の削除には確認。行を消しても状態は戻らない（戻すのは「直前を取り消す」）

記録の見やすさ：
- ログ展開時に上部固定の操作行（閉じる／全部／当選／示唆／レア役／お金）。LOG_KIND で自動分類
- 入金は +1,000／+5,000／+10,000 の3つ＋手入力
- 打ち始めに「再プレイでおろした分」。持ちメダルと分けて表示（計算は同じ）
- 終了シートに「今の持ちメダル（任意）」。差枚を更新し medal イベントで残す（時系列グラフ用）

器の一般ルール：
- 修正シートも即判定。開始ステージ修正も同様
- 「？」付きの記録は集計で区別できる（ev.uncertain）

### 押しやすさのルール（rec68〜71）

- グリッドは下段ほど頻度が高いボタンを置く。定義を作るとき「よく押すボタン」を夢爽に聞く
- +1〜+10 はグリッドの最下段（親指に近い）。バッジ行はカウンターの直下
- シートは開いた瞬間に「最初の選択肢群の中心」を測り、グリッド最下段（レア役の行）の中心に合わせる（positionSheet）。
  長いシートは上端8pxで止まる。基準位置は底から25%
- 示唆の選択肢は意味で色分けする（`tone`：p＝運命盤系・c＝通常C・d＝通常D）
- レア役の回数はバッジ行でも出せる（`rareBadges`）。カウンター枠は `rareCounts:false` で消せる
- 本文がまだ下に続いていれば、シート下端に「↓ 下にも項目があります」を出す（rec89・全シート共通）。
  `.more-hint` を本文の末尾に置き、`sheet()` 表示後と `scroll` で `updateMoreHint()`。
  `scrollHeight − clientHeight − scrollTop > 24` なら表示し、一番下に着いたら消す。
  `position:sticky` ＋ `pointer-events:none` で、下にあるボタン（「破棄する」など）のタップを妨げない。
  あふれていない短いシート（前兆の選択・実Gの3択）では出ない

### 有利区間切れ（rec73以降）

有利区間が切れると内部情報は引き継がれないが、切れたかどうかは外から確実には分からない。
だからユーザーが押す。機種定義 `yuuri:{askOnEnd:[当選ラベル], clear:[消すもの]}`。
askOnEnd の当選の終了シートに「有利区間が切れた」スイッチ、「投資・回収・出力」にもボタン。
消すもの：rewardHint（次回報酬）／pendingReward（報酬待ち）／melonLeft／modeHints／tags／parCount。
からくり2は運命の一劇失敗後と上位AT終了後に聞く。他の場面で切れるなら askOnEnd に足すだけ。

### 実戦フィードバック（からくりサーカス2・rec74〜rec77）

解析の読み直しと実戦から出た分。

定義：
- CZ終了画面14種すべてに示唆と有効区間を設定。「天国」はモードの1つ（通常A/B/C/D/天国の5種）で、
  上位ATのことではない。「100G以内の当選濃厚」なので他のモード示唆と同じ扱い
- `rares[].ask` … レア役を押したあとに追加で1つ聞く。からくり2の🍉に「運命盤の煽り：なし／あり」
  （解析にないが残り回数の示唆と推測。データを溜めてから判断する）
- ステージは「フウ屋敷」「仲町サーカス」を分ける。解析では前者がデフォルト、後者は早い規定G数の示唆かも
- AT開始ステージに「引き継ぎ」。天国などで前回のステージを引き継いだ場合に選び、設定判別の集計から外す

器：
- ノートのカードは操作ボタンを本文の上に置く。本文（数百行になる）は `.note.fold` でたたみ、
  「記録を全部見る」で展開。ボタンまでスクロールさせない
- 機種名を手入力したとき、専用機種と同じ名前なら確認して専用モードに切り替える。
  履歴の候補にも専用機種と同名は出さない（同じ名前のボタンが2つ並ぶのを防ぐ）
- `checker` がある機種は「設定判別チェッカーを開く」も置く（送らずに見るだけのとき）

### シートの中からシートを開くときの規律（rec77で3度目の事故）

シートのハンドラから別のシートを開く処理を呼ぶときは、**必ず先に closeSheet() を書く**。
呼んだ後に closeSheet() があると、新しく開いたシートを閉じてしまう。

過去の同種の事故：
- rec32 … 差し替え範囲が広すぎて関数定義が消えた
- rec38 … mark() を呼び出し側と startHit の両方で呼び、途中の状態を撮った
- rec77 … recordRare() が開いたシートを、直後の closeSheet() が閉じた

共通するのは「状態やUIを変える処理の順序」。呼び出し側では何もせず、呼ばれる関数の中で完結させる。

### 記録ボタンの下固定（rec72以降）

シートの記録ボタン（記録する／失敗を記録／行き先／復活）は #shFoot に置き、本文をスクロールしても常に見える。
上の「閉じる」と対。中身が短いシートは footer が空なので変わらない。

### 設定判別ボタンと下部の2列（rec78〜79）

記録画面の下部は「直前を取り消す」「設定判別｜投資・回収・出力（2列）」。
設定判別は押すと「まとめを見る／チェッカーを開く／チェッカーへ送る」。
checker の無い機種はまとめのみ。「投資・回収・出力」からは設定判別の項目を外した（重複解消）。

1画面に収める都合で下部は2列にした（縦52px）。390×844は余裕70px、390×744は30pxはみ出す。
次に削るなら ログ3行→2行（−40px）か、.mrow の折り返しを止める（−17px）。
条件による差：投資前（.mrow が1行）なら13px、打ち始め直後（ログ0行）なら88pxの余裕。

### ネネ監査への対応（rec80〜rec88）

9/21 のネネ監査（54項目・16件）と再検証（5件）に対応。監査ハーネスの FAIL は 31→2、新規の劣化0。

**取り消しを「操作単位」に（rec82）**

- 1操作＝1つの op。mark() で開き、その操作中の push は同じ op に属する。取り消しは op の行をまとめて戻す
- 1操作が複数行を作る場面（BIG終了→32G中、女神失敗→🍉打ち切り、報酬待ち発動→上乗せ）が1回で戻る
- mark したのに push しない経路を無くす（修正は extraFix として記録）。closeSheet で未使用の snap を捨てる
- お金・フォロー・持ちメダルは tk / prevCredit で行と実データを紐づけ、取り消しで両方戻す
- 自動の32G抜けは _steps に autoOp を積む。後に別の記録があれば「直前を取り消すで順に戻す」と止める

**入力の検証（rec82〜84）**

- int() は小数・文字混じりを NaN で返す。全角数字・カンマは許容。受け取る側は badNum() で拒否
- 教訓：int() の仕様を変えたとき、受け取る側を全部洗わなかったので NaN が5箇所に漏れた（列挙値を足したときと同じ失敗）
- 終了シートは確定前に全入力を検証（validateEndInputs）。行の編集は全検証→一括反映

**データ保護（rec85〜88）**

- 読めない保存データは slot-tools-rec-broken に退避して止める。自動保存しない
- 取り込みは validSession で行ごとに検証。不正は除外、既存は不変
- JSON書き出しに稼働中（cur）を含める。形は {sessions, cur, exportedAt}
- 複数タブ：保存に世代番号。操作したときだけ上げる（起動時は上げない）。古い状態のタブは相手が保存した瞬間に凍り、
  「再読み込み」か「このタブの内容を書き出す」だけ。merge はしない
- 検証は2段階：起動時は構造だけ（validSessionShape、型の乱れは fixSession が補正）、取り込み時は中身の型まで（validSession）。
  既存の実戦データを誤って退避しないため
- 凍結は根元で効かせる（rec87）：document の click キャプチャで警告バーと「閉じる」以外を止め、push / mark も staleShown で return。
  再描画でボタンが戻っても freezeButtons で無効に戻す。凍結時は開いていたシート・テンキーを閉じる
- 世代は保存に成功したときだけ上げる（rec87）。失敗中に他タブが保存したら、同じ世代でも自分が凍る
- 取り込みの重複は同一JSON内と稼働中の台の id も見る（rec87）。取り込み後はノートの件数を更新
- 破損時の案内には直し方の順序（書き出す→空で始める→正常なバックアップを取り込む）と退避の成否を出す（rec87）

**仕様として確定（テストの期待値と設計の選択が違うもの。FAIL のままでよい）**

- D12-B：取消済の行は「（取消済）」を付けて残す
- D13：行の削除は記録を消すだけ。状態・集計は戻さない
- Q2：同じ台の再送は上書き。差分計算はしない
- R02：自動終了の後に記録があれば「元に戻す」は止める（飛び越えない）
- P04：古い書き込みは拒否する
- P06・P02：nExpJson はオブジェクト（配列ではない）

**その他**

- Q1：劇場ジャッジ失敗→女神はセットで初当り1回
- D06：AT中の神が失敗しても元のATに戻る
- D07：選択肢の showWhen で隠れたら選択値も外す
- 監査ハーネスの正本は Downloads/rec87-evidence/rec87-recheck（リポジトリには入れない）。
  置き場所と基準値は `NOTES-tsumugi.md` に書く。rec89 時点で 151件・FAIL 0（OBSERVED 1 は数えない）。
  アプリが新しいブラウザAPIを使い始めるとスタブ切れで全件 HARNESS_ERROR になるので、rec 側の欠陥と読み違えない

**仕様変更でテストの操作列が変わるとき（rec89）**

UIに手順が増えた版では、古い操作列のテストが FAIL に化ける。**実装の欠陥と読み違えない。**
rec89 では実Gの直接修正に3択が挟まったため、「電卓の決定＝即確定」を前提にしていた
`C05-direct-edit-undo` が、3択を押さないまま取り消す別のテストになっていた。

直し方の順序：

1. 実装ではなく操作列が古いことを確かめる（新しい手順を踏めば期待値どおりになるか）
2. テスト側に不足した1手を足す。**期待値は変えない**（変える必要があるなら、それは仕様変更の報告対象）
3. 新しい振る舞いそのものを見るテストを1件足す（`C05-direct-edit-uncommitted`＝3択を押すまで値もログも動かない）
4. **中立性を確認する。** パッチ後のスイートで前の版を流し、差が新機能の範囲だけに閉じていることを見る
   （rec88 を流すと上記2件以外の92件は判定が全件一致した）

ハーネス本体（`harness.cjs`）には手を入れない。手を入れたときは前の版との判定一致を別途確認する

### 機種定義を作る順序（教訓）

項目の一覧より先に「状態の遷移」を描く。
からくりサーカス2では AT終了→幕間チャンス→引き戻し の流れを項目一覧に書いていながら、
遷移として組み込めておらず、実機で気づいた。

実機で1日打ったあとの見直し（rec28）で出た項目：
- 弱レア役をまとめない。レア役は基本的に全部個別に持つ（多すぎる時は相談）
- 前兆ステージ中は通常ステージと被らない。ステージ表示を「前兆ステージ」に切り替える
- ワイプ演出は前兆抜けで見る。前兆抜けシートに置く
- 示唆演出は「よく見るもの」だけ。解析にあっても意味が伝わらない項目は入れない
- 液晶に出る数字（🍉残り回数）は、見た時点で記録できる専用ボタンにする。
  引く前に当たるとサンプルが残らないため
- CZ成功なら終了画面は出ない。成功を選んだ時点で他の項目を隠す

### 遷移図はATの中の枝も描く（教訓・追記）

からくりサーカス2では、AT終了後の幕間チャンス、エンディング→運命の一劇、
上位AT→運命の一劇、激情ジャッジ3回→運命の一劇 の4本を、解析の項目一覧からは
読み取れず、実機を知る夢爽の説明で初めて遷移として組み込めた。

解析は「何がある」は書いてあるが「どの順で来る」は書いていない。
2機種目からは、洗い出しのあとに夢爽が遷移を口頭で説明する工程を挟む。
稀なルート（機械仕掛けの神、ロングフリーズ、引き戻し）は流れを作り込まず、
当選の種別と契機に名前を足して記録だけ残す。挙動が分かったら流れに昇格させる。

## tools/rec 実戦記録ツール：機種定義を作る工程（v0.1 で確定）

1. 解析サイトを読み、状態の一覧と項目を洗い出す（シオン）
2. 実機を知る人（夢爽）へのヒアリングで、遷移を「入口・中で記録するもの・出口・出口ごとに聞くもの」の形に落とす。
   解析には「何がある」は載っていても「どの順で来る」は載っていない。ここは省略できない
3. 確認済み／操作合意／仮ルール／未確認 を分けた仕様書を作る（karakuri2-spec-v01.md の形式）
4. 現行の定義・器との対応表を作り、「同じ／定義／器／保留」に振り分け、段階に切る
5. 段階ごとに実装し、各段の終わりに通し操作で確認する
6. 全段が終わったら、仕様書の通し操作を全本回す
7. 実戦で使い、出てきた差を仕様書に戻す

からくりサーカス2では 1→（2を飛ばして実装）→ 実戦で15回修正 → 2〜3 → 4〜6 の順になった。
2機種目からは 1〜3 を先に済ませてから実装に入る。

## tools/rec 実戦記録ツール：設定判別チェッカーへの送信

### 送信の仕組み（rec22以降）

機種定義に `checker:{ key, url, name }` があると、ノートと「投資・回収・出力」に
「チェッカーへ送る」が出る。`toChecker(session)` が events を走査して
チェッカーの状態（checker-data の DEF と同じ形）に変換し、localStorage の key に書く。
チェッカー側は無改修。起動時に localStorage を読む共通エンジンの仕組みをそのまま使う。

- 既存データがあれば「合算」「上書き」を選ぶ。元データは key+"-rec-bak" に退避
- 同じブラウザで rec とチェッカーを開く前提
- 総回転数は 打ち始めと終了時の「台の総回転数」の差。片方でも無ければ0（チェッカーで手入力）

### からくりサーカス2の集計定義

- CZ当選 ＝ 機械仕掛けの女神＋劇場ジャッジ。AT後の幕間チャンスは含まない
- AT当選 ＝ 通常時からの当選のみ。引き戻しは含まない
- 強チェリー直撃 ＝ 契機が強チェリーで機械仕掛けの女神に当選した回数（rares[].trackHit）
- 激情ジャッジ ＝ ATごとに1回目のキャラだけ
- 1枚絵・ボイス ＝ 復活時に見たものも数える
- ラベル→チェッカーのキーの対応は K2_MAP。checker-data 側の定義と全件突合済み

## tools/rec-kanokari：彼女、お借りします 専用の記録ツール（別箱）

rec89 をコピーして作った別箱。KEY="slot-tools-rec-kanokari"。本線 tools/rec は触らない。
定義が実戦2〜3回で固まったら本線に統合する（期限付きの二重管理）。
仕様書：kanokari-spec-v02.md、器の拡張仕様：rec-kanokari-engine-v01.md、定義：kanokari-def-v12.js（正本はシオン）。
コミット名義は実装した担当に合わせる（2026-09-18 夢爽の裁定）。この箱の最初の7本（a8db5e9〜1da84f7）は本文に「実装：ミコト（codex）」と書きながら Shion 名義で出ているが、シオンがこの原則を知らずに指示書へそう書いたためで、書き換えない。507051e 以降はミコトが実装したぶんを Mikoto 名義で出す。

### 工程（からくり・ハイビとの違い）

動画観察（ネネ、チェックリスト）→ 流れの下書き＋ヒアリング★25 → 仕様書 v0.1 → ミコトの器レビュー（4点）→ v0.2
→ 実戦データ6台で補正（1G目/2G目の役・2択当て・保証1個・救済用の失敗回数）→ 器の拡張（順序1〜6）と定義（v0.1→v0.8）
→ 通し操作10本 → limited 公開 → ネネの流れデバッグ（6件）→ 実機テスト1回目（7点）。
からくりは実装が先で15回直した。かのかりは仕様が先で、実装中の直しはレビューと実戦データからの補正だけ。

### 器に足した仕組み（11点＋後から4点）

- `stocks`（ストック2本：レンCHANCE／1G恋）。獲得は終了時にまとめ、消費は結果入力から。当選時に開始時の値を保存
- `stockPhase`（1GレンCHANCE のまとめ入力）。「n個目で成功／全部失敗」＋ALL変換＋成功セットの1G目/2G目の役
- `counter3.incOnTriggers`（攻略人数を契機で加算）。初当り・引き戻し・DREAM後は増えない
- `stockFail`（失敗の連続回数）。救済抽選（2〜5回）の判定用。引き戻し当選と初当りで0
- `gauge`（ハートメーター）。レア役の ask で個数、MAX で変換高確タグ。前兆中・引き戻し中は `waitIfZen` で待機
- `zenchou.sheet`（引き戻し記録・追記型・複数選択）。段階終了や当選で流す
- `atExtras[].type:"slots"`（REG のキャラ紹介5枠、デフォルト入り）
- `extras[].byState`（場面で選択肢が変わるアイキャッチ）
- `zenchou.remindAtG`（66G で促す。自動終了しない）／`enterOnHitEnd:{unlessStocks,stage}`（入口条件と段階指定）
- `yuuriReset` に c3・stocks・gauge ／ `yuuri.levels`（有利切れ／濃厚）
- `phases[].onEnter:{yuuri,ask}`（ENDING 突入で有利切れ＋LAST枚数）／`hits[].onStart:{stockInc,onlyTriggers}`（保証1個）
- 後から：`stages.order`（バッジタップで次のステージ）、`stages.options[].tone/enterZen`（色と前兆段階への自動遷移）、`zenchou.backTo`（連続演出失敗→前兆ステージへ戻る）、段階の終了が1つだけならシートを出さない

### 定義と器の往復で出た教訓

- CZ成功の契機の語は hitTriggers と揃える（onStart.onlyTriggers がその語で判定する）
- atExtras は当選ごとに持つ（makeGridButton は hitProp で引く）。機種直下のみ見ていた順序1の判断は誤り
- 自動遷移（afterAt）は startHit(keepOp) で同じ op に。DREAM後→BONUS、ENDING→DREAM TIME、ハイビの1G連→BIG（本線にも当てた）
- CZ成功→当選で G数を先に0にしない（当選G数が消える）
- enterZen はステージ表示を段階名で上書きするので、ステージ選択から段階に入るときは選んだステージを入れ直す
- 「変換高確」はボタンで開始せず、メーターMAX で自動。ボタンを1つ減らせた
- 打ち始めは3項目に絞る。引き戻し中・ユメカノ・ストックは打ち始めてからバッジで直す
- 本線との突き合わせ（同じ操作列で画面・ログ・状態・イベント列を比較）は毎回。本線に無いタップ動作が箱側だけ増えた事故を1回捕まえた

### ネネの流れデバッグ（9/25）：修正6件（F01〜F06）は反映済み。仕様として確定したのは次の2件

- Q01：ななかりDREAM の攻略人数は1G恋成功の時点で+1、DREAM後の BONUS では増えない（合計+1）
- Q02：ALL変換は残りストックだけを1G恋に。成功した1個は消費済み

### ネネの流れ再検証（9/25・507051e）：F01〜F06 は再現せず。新規3件の裁定

- F07（修正）：`zenchou.backTo` で段階を戻すと、ステージ表示が「連続演出」のまま残っていた。`enterZen` で段階に入る直前のステージを `c.zenPrevStage` に控え、戻るときに `c.stage` へ入れ直す。既存の `c.prevStage` は「前兆の一連に入る前のステージ」で別物なので名前を分けた（取り消し用に `SNAP_KEYS` にも追加）
- F08（修正）：`extras` の `type:"toggle" menu:true`（ユメカノ）は ON のときしかバッジが無く、OFF からはメニューからしか入れなかった。OFF のときも薄いバッジ（破線・半透明）を出してタップで ON にする。ON 側の挙動とメニューの項目は変えていない
- F09（仕様）：生データは `shop` / `dai`、kado-note 用のエンベロープが `storeName` / `unitNo`。キー名が違うのは設計どおりなので直さない
- Q03（裁定）：小役変換の「超高確だった」は**今回の変換の振り返り**。ログには残すが、変換後に「変換超高確」バッジを付け直さない（定義 v0.10 で `set` を外した）
- 定義 v0.10：かのかりBONUS の背景を3択に（水族館＜クリスマスデート＜ゼロ距離）。その他1／その他2／不明は廃止
- 定義 v0.11（42b49a9）：レア役シートの下部を「回数を直す」から「メーターの状態」（0〜5・MAX）へ。前兆の段階名「前兆煽り」→「前兆」。extras の「小役変換」→「小役変換・示唆」
- 器 v0.11：`rareSheetGauge`（openRare の下部の分岐。選ぶと即記録し、MAX のタグ付けはレア役の ask と同じ `applyHints` 経由。MAX の設定は器に書かずその機種の ask から引く）。この旗を持たない機種は従来どおり「回数を直す」
- 定義 v0.12（270579f）：小役変換・示唆を全項目任意（`anyOne:true`）に。変換先に `enterZen:0` を入れて変換したら段階「前兆」へ。「高確終了（10G抜け）」は削除（変換高確バッジのタップで消せるため導線が重複していた）
- 器 v0.12：`anyOne`（groupSheet の「記録する」。全グループ任意のシートで、どれか1つも選ばれていなければ閉じずに toast）、`set.enterZen`（applyHints。示唆の適用で段階に入る。すでに段階中なら何もしない。`keepOp:true` なので取り消し1回で変換と段階が一緒に戻る）

---

# 1.7 モード推測ツール（mode-estimator.html）

## 実装済み

### リプフラ判定：ゲーム数ベース（従来）

- [x] 起点別の事前分布（設定変更後・飛翔モード終了後・超飛翔モード終了後）にポアソン尤度でベイズ更新する
- [x] 当選区間リスト＋進行中区間の多区間入力。2区間目以降はバタフライ＋ときめき約41Gを自動控除する
- [x] 内部200G超え確認で、チャンス・飛翔準備B・超飛翔準備を事前分布から除外する
- [x] nerai-record の稼働記録から区間を取り込む

### リプフラ判定：リプレイ回数ベース（2026-09-18 追加）

- [x] 入力方式を「ゲーム数で入力」「リプレイ回数で入力」の2択に切り替える。既存のゲーム数ベースは変更しない
- [x] 対象リプレイ回数 n とリプフラ回数 k から、二項尤度でベイズ更新する
      1リプレイあたりの発生率は解析値（1/xxx G）をリプレイ確率 1/7.3 で割って求める（p = 7.3 ÷ 解析値）。
      機種データ側は `replayFlash.replayCycle` に 7.3 を持ち、`rates` はゲーム数ベースと共用する
- [x] 主表示は「通常B以上期待度 ＝ 1 − 事後(通常A)」。モード別の事後確率を補助表示する
- [x] 境界条件：n=0 は事前分布をそのまま表示（エラーにしない）／k>n と負数・小数はバリデーションエラー
- [x] 起点の選択は2方式で共用する。リプレイ回数ベースでは内部200G超えの除外を適用しない（事前分布は起点の初期振り分けのまま）

### 終了時の色とタイミングの記録欄（2026-09-18 追加）

- [x] リプレイ回数ベースの入力欄の下に「終了時のリール色」「出たタイミング」の選択欄を置く
      色は 未入力／赤／緑／青／変化なし／虹。タイミングは 未入力／飛翔抜け直前（0回目）／
      飛翔抜け後1回当てた後／リセット後1回目
- [x] 選んだ内容は結果欄の末尾に `記録：緑（飛翔抜け後1回当てた後）` の形で表示する。
      片方だけなら片方だけ、両方未入力なら記録行を出さない
- [x] **期待度の計算には一切使わない。** 表示のみ。色による補正・重み付けはしない
- [x] localStorage には保存しない（画面上の表示だけ。保存の要否は別途判断）
- [x] 入力欄は `#repflashReplayInputs` の中に置く。これにより、対象外機種と
      ゲーム数ベース選択時に出ないことが構造的に担保される

### 区間リスト形式（2026-09-19 追加・第1段階）

- [x] リプレイ回数モードを、ゲーム数モードと同じ「区間リスト＋現在の区間＋合算」に揃えた
      過去区間は 当選G数／対象リプレイ回数／リプフラ回数／含む／削除。
      現在の区間は 現在G数／対象リプレイ回数／リプフラ回数／含む
- [x] 合算は「含める」にチェックのある行の対象リプレイ回数・リプフラ回数を合計する
- [x] 天井除外は**現在の区間のG数だけ**で判定する。201G以上で `over200Excludes`、
      501G以上でさらに `over500Excludes`（飛翔準備A）。過去区間のG数は記録用
- [x] 合算リプフラ率を表示する（1/○回、0回なら「出現なし」）。
      参考値の 1/52.0回・1/37.0回 はハードコードせず `rates ÷ replayCycle` で算出する
- [x] 除外が効いているときは「現在250G：チャンス・飛翔準備Bを除外」を1行出す

#### 設計上の判断（記録）

- **合算は「全区間で同じモードにいた」と仮定した控えめな値。** この台はモードが下がらないため、
  途中で上がっていた場合は実際にはこれより高い。画面の注意書きにも明記した
- **過去区間のG数を除外に使わない。** 過去に200Gを超えていても、その後モードが上がった
  可能性があるため。除外は現在の区間だけで判定する
- **ボーナス時のモード移行率は数値化しない**（未公開）。「1回目が200G以内だったから2回目はチャンス」
  といった推測は入れず、既存の「状況別モード推測」の文章表現に任せる
- 「3回目の区間から見たい」は含める/含めないのチェックで代替する。専用の仕組みは作らない
- 除外したモードは0.0%の行として残す。既存のゲーム数モード（内部200G超え）と同じ挙動に揃えた

#### CSS の落とし穴（教訓）

区間行に専用のグリッドを足すときは、**現在の区間の行の詳細度も確認する。**
既存の `.interval-row.current .interval-line`（0-3-0）が新しい単一クラス（0-1-0）に勝つため、
現在の区間だけ旧レイアウトのまま残り、入力欄が潰れて要素が2行目へ折り返した。
`.interval-row.current .interval-line.<新クラス>` を別途定義して解決した。
既存セレクタの書き換えはしない（ゲーム数モードが崩れる）。

### 実戦向けUI改善（2026-09-19・コミットA）

見た目は整っていたが、実戦中の操作で結果まで遠かった。スマホで打ちながら使う前提に直す。
UI提案はネネちゃん。コミットAとBに分割し、Aを先に出した。

- [x] **対象リプレイ0回は「未判定」にする。** 0/0 でも事前分布（設定変更後なら70.0%）が
      大きく出て「A否定・やや強」が付き、実測で強くなったように見えていた。
      0回のときは大きな％と判定バッジを出さず、バッジ「未判定」と
      「起点の振り分けのみ：通常A以外 70.0%」の1行にする。見出しも「起点の振り分け」に変える。
      事前分布の値自体は消さない（何もないより情報がある方がよい）
- [x] **結果を入力の直下に置く。** 起点 → 入力方式 → 現在の区間＋カウント → 結果 →
      過去の区間 → 色・タイミング → 数え方 → 判定基準表 の順。
      数え方・色入力・判定基準表は結果より下へ移した
- [x] **カウントボタン3つ**（リプレイ＋1／リプフラ＋1／1つ戻す）。
      リプフラ＋1はリプレイもリプフラも+1する（リプフラはリプレイでもあるため）。
      この設計だとボタン操作では「リプフラ回数＞リプレイ回数」が起きない。
      数字の直接入力は残す（後からまとめて入れる人向け）

#### 設計上の判断（記録）

- **結果の置き場をモードごとに分けた。** `#repflashResultArea` を動かすと
  ゲーム数モードのレイアウトまで変わるため、リプレイ回数モード専用に
  `#repflashRepResultArea` を新設し、既存は位置も役割も据え置いた
- **カウントボタンは `input` イベントを発火させない。** 値を代入して直接再描画する。
  発火させると「手で編集したら履歴を空にする」処理が自分で走り、1つ戻すが効かなくなる
- 戻す履歴はページ内のみ。localStorage に保存しない

#### 結果までの距離（測定値・2026-09-19）

390px幅で、大きな％の下端は文書先頭から **836px**。
画面高844pxなら1画面、812px・667pxでは1スクロール（依頼の条件内なのでコミットAでは対応せず）。

内訳は ページヘッダ 0〜164px ／ セクション先頭〜カウントボタン 164〜623px ／
カウントボタン 623〜671px ／ 結果〜％下端 671〜836px。

- [ ] **結果までの距離を縮める（候補）。** 効くのは上の2つ。
      ページヘッダ164px（TOPリンク3本＋タイトル＋説明文）と、
      起点3択＋入力方式2択で約460px。
      コミットBの折りたたみ（判定基準表・数え方・色）はすべて結果より下なので、この距離は縮まない

### 実戦向けUI改善（2026-09-19・コミットB）

- [x] リプフラ判定と状況別モード推測を**タブで分けた**（既定はリプフラ判定）。
      既存4セクションは中身を変えず `#tabModes` に包んだだけ。起点は別計算なので統合しない。
      切替は `hidden` の付け外しのみ。`scrollTo` / `scrollIntoView` / `focus` /
      `location.hash` を使わない（スクロール位置を保つため）
- [x] 区間行の入力欄・追加/削除ボタン・チェック欄を**44px**に揃えた（両モード一括）。
      40px・42px を 44px に置換しただけで、4列/5列の構成は変えていない
- [x] 主表示を「**通常A否定期待度**」に（両モード）。直下に「＝通常A以外の合計」。
      中身は通常A以外の合計で、チャンスは通常Bより下。セクション名とも揃う。ガイドも置換
- [x] 参考値を「チャンス・通常B・飛翔準備A：1/37.0回」に（算出方法は不変）
- [x] 判定基準の4行表・数え方の詳細・色記録を**折りたたみ**に（いずれも既定は閉）。
      数え方の常時表示は2行のみ
- [x] 機種データ読み込み前に「計算データを読み込み中…」を出す

#### 結果までの距離：足したら伸びた、を測って戻した（教訓）

上の6項目で、結果（大きな％）までの距離が **836px → 944px** に伸びた。
内訳は タブバー +64px ／「＝通常A以外の合計」+22px ／
参考値の文字数増で合算行が3行に +18px ／ 区間行 40→44px +4px。
**UI改善の目的が「結果まで遠い」の解消だったのに、改善項目を足したことで悪化していた。**
折りたたみ（判定基準・数え方・色）はすべて結果より下にあるため、この距離には効かない。

夢爽の裁定で2点を追加し、765px（コミットAの836pxより71px短い）まで戻した。

- [x] **B1**: タブに「リプフラ判定」と出るため重複する `section-label` と
      `section-lead` を削除（−67px）。状況別モード推測側の `section-label` は残す
- [x] **B2**: リプレイ回数モードの結果の並びを
      通常A否定期待度 → ＝通常A以外の合計 → 大きな％ → 合算リプフラ率 → 除外表示 →
      注記 → モード別の事後確率 → 記録行 に変更（−112px）。
      1/37.0に近いかを見るのが主目的なので、**比率を％の直下**に置く。
      未判定時も同じ骨格。ゲーム数モードの結果は触らない

到達状況: 画面高844px・812pxで1画面。667px（SE系）は1スクロール（従来どおり）。

**教訓: UI項目を足すときは、足す前後で「結果までの距離」を実測する。**
上に足したものは全部効き、下に足したものは効かない。

### 未着手（第2段階）

- [ ] リプレイ回数モードでも稼働記録から取り込む
      nerai-record の timeline から、区間ごとに 液晶42G以降かつさざなみ前兆窓（G+1〜G+8）を
      除いた `t_nangoku_replay` と `t_nangoku_replay_flash` を数える。取り込み後は手で修正できる。
      8/10より前はリプレイの記録が疎なため、取り込み時に「記録漏れの可能性」を1行出す（判定はしない）
### ガイド・出典（2026-09-18）

- [x] `guide/mode-estimator.html` に「リプレイ回数から推定する（南国育ちSPECIAL）」の節を追加
      （既存の「使い方」の直後）。入力3つ・数え方・読み方・できないこと
- [x] `replayCycle: 7.3` に出典コメントを付けた。リプレイ確率 1/7.3 は6.5号機の規則上の
      共通値で、解析ページに個別記載が無いため URL は付けない（2026-09-18 確認）
### 設計上の判断（記録）

- チャンス・通常B・飛翔準備Aはリプフラ率が同一（1/269.8G）のため、リプフラだけでは区別できない。
  主表示を個別モードの点推定ではなく「通常A以外の合計」に置いているのはこのため（UI設計原則：解釈を含む点推定を主役にしない）
- 対象リプレイの数え方（液晶42G以降のみ／さざなみ前兆中の8Gは除外）は入力欄の直下に置く。
  数え方を間違えると結果が丸ごと変わるため、判断の場に前提を置く（AGENTS §9-46）

## 検討中・将来

- [ ] 飛翔・超飛翔の終了時の色による事後更新（次フェーズ。今回は入れない）
- [ ] ゲーム数ベースとリプレイ回数ベースの同時併用（今回は切替のみ）
- [ ] リプレイ回数ベースでも稼働記録から取り込めるようにする（nerai-record 側にリプレイ回数の記録が必要）

---

# 2. 店舗データ蓄積・分析ツール（Phase 2）

## データ設計

- [ ] 入力単位は「店の合算」ではなく「台単位」とする
  - 記録項目: 日付・イベント名・機種・総G数・BIG回数・REG回数
  - 理由: 店平均だけでは「全台設定4」と「設定1と6の混在」を区別できない
- [ ] 店平均は台単位データからの集計値として自動算出する（別途入力しない）

## 出力（3階層）

- [ ] 階層1: 店平均（合算のBIG/REG確率）
- [ ] 階層2: 設定配分の推定
  - ベタ置き型（全台中間設定）か、メリハリ型（低設定と高設定を分ける）かの傾向
  - 高設定の投入率の推定 → Phase 3で事前分布の元データになる
- [ ] 階層3: 当日速報比較
  - 今日の途中経過 vs 過去の同イベント平均で「前回より強い / 弱い」を提示
  - 総G数が閾値未満の場合は「参考値」と明示する
  - 将来案: 時刻付き記録にして「過去の同時刻帯の途中経過」と比較する（まずは終日データ比較から）

## 検討事項

- [ ] 当日データの収集手段（手入力か、転記か）と運用負荷の整理
- [ ] 開催回数が少ないイベントの配分推定は信頼できないため、件数に応じて均等分布と混ぜる（縮小推定）を検討

---

# 3. ツール間連携（Phase 3）

- [ ] 店舗分析ツールの推定配分を、判別ツールの事前分布プリセットとして選択可能にする
- [ ] 判別ツール＝計算機（状態を持たない）、店舗分析＝データ蓄積（状態を持つ）として分離を維持し、連携はデータ形式のみで行う

---

# 完了済み

（なし）
