# slot-tools｜機能構想メモ

> 本書は「実装項目の台帳」と「未着手アイデアの置き場」。
> 実行順（優先順位）は持たない。順位は docs/ARCHITECTURE.md §5 が唯一の正本。
> 最終更新日は本書に書かない（git log が正）。
> 記号: 完了 [x] / 進行中 [~] / 未着手 [ ] / 取りやめ [-]

このファイルは、今すぐ着手しない案も含めて、サイトの機能案・改善案・将来構想を忘れないために保管する場所。

## 運用ルール

- 思いついた案は、完成度を気にせず該当カテゴリへ追加する
- 着手する案は「次にやる候補」へ移す
- 実装中は `[~]`、完了は `[x]`、未着手は `[ ]` として扱う
- 実装後も削除せず、完了済みに残す
- AIへ作業依頼するときは、最初にこのファイルを確認させる

## UI設計原則（全ツール共通）

- カウント・入力系の画面は「スクロールなしの固定全画面」とする。ユーザーの操作負担を最小にすることを最優先する（2026-08-09 確立）
- 数値入力はOSキーボードではなくページ内の電卓パッドで行う。開いた時点で入力欄は空にし、初期値0を消す手間を発生させない
- タップカウンターは長押しで数値の直接入力（まとめて入力）に対応する
- 不用意に不安を煽る表示を避ける。解釈を含む数値は常時表示せず、ユーザーが見に行った時に文脈（信頼度・G数）と共に示す
- タップ可能な要素は高さ44px以上を下限、48px以上を推奨とする（WCAG 2.5.5 / Apple HIG 44pt / Google Material Design 48dp準拠）。小型画面では44pxまで圧縮を許容し、スクロールなしを優先する
- 最頻入力項目は片手持ちの親指ホームポジション（下段左）に配置する
- 破壊的操作（クリア等）は「元に戻す」で復元可能にする
- 使う人によって必要性が分かれる記録項目は、既定で表示しつつ利用者側で非表示にできるようにする
- 固定全画面の原則はカウント・入力系の画面に適用する。選択モーダルなど項目数が増える一覧は、高さ上限を設けて内部スクロールを許容する

---

# 現在の主軸

- [x] 設定判別ツール（Phase 1）… エンジン・機種データ・UI・ガイドページまで公開済み。実戦記録ツールへ発展
- [x] 9/7導入の新台6機種（リコリコ／青ブタ／かのかり／見える子ちゃん／タコスロ／モグモグ風林火山）を2026/9/6に正式公開。guide作成・checkers.html導線・sitemap登録まで実施し、公開機種数は21。分母条件が未確認のため6機種とも1/x表示は保留し、回数のみ記録する
- [x] トップページ（index.html）を再構成（2026-09-08）。セクション順を 設定判別／パチンコ計算（遊タイム実戦→遊タイム期待値→回転単価）／実戦ログ／L南国育ち SPECIAL／北斗の拳 転生の章2 に並び替え、サイト説明本文を全セクションの下へ移してリード文だけを上部に残した。更新履歴ブロックは廃止し、代わりに「NEW」欄を新設（1行＝公開日｜ツール名、最大5件・最新が上・6件目の追加で最古を削除、履歴は残さない。運用は AGENTS.md §9-92）。title を「スロット稼働ノート｜パチスロ・パチンコ無料実戦ツール」に変更し、meta description と og:title / og:description の「パチスロ実戦ツール」を「パチスロ・パチンコ」へ広げた
- [x] 公開ページの文言整備（2026-09-08）。公開ガイドに残っていた内部開発用語（公開前QA・フェーズ1/1.5・内部フラグ・B群仕様）を除去し、実装と食い違っていた説明を現行機能へ合わせた（godeater＝ベイズ推定・自動推定を持たないことを確認、monkey5＝5枚役の1/x表示とベイズ推定は実装済みのため実態に合わせて記述）。9/7新台6機種のガイドは「1/x表示は現在準備中です」を「1/x表示は現在行っていません」に統一し、分母条件が未確認である理由文はそのまま残した（§9-70）。運営者情報・提携ページはツール一覧を現状の分類（設定判別チェッカー／パチスロ実戦記録／パチンコ計算／機種専用／実戦ログ・記事）へ更新し、計算ロジックとツール設計は独自実装・機種の仕様や解析値は各ページ明記の出典による旨と、利用者の入力データを当サイトが取得・提供しない旨を明記した。アグネスPE記事の導入前時制も言い換えた
- [x] 実戦記録・設定推測ツール（Phase 1.5）… ホール用のカウント+投資記録+リアルタイム推測。6号機ジャグラー全9機種対応、ガイドページ公開済み
- 対応機種: SマイジャグラーV、SアイムジャグラーEX（6号機）、Sゴーゴージャグラー3（6号機）、Sファンキージャグラー2（6号機）、SハッピージャグラーVⅢ（6号機）、SネオアイムジャグラーEX（6号機）、Sウルトラミラクルジャグラー（6号機）、SジャグラーガールズSS（6号機）、Sミスタージャグラー（6号機）。6号機ジャグラーシリーズ全9機種に対応完了。以降の機種追加はJSON1本+機種リスト1行で完結する構造
- [x] マイログ（Phase 1.75）… 打った台の記録を蓄積し振り返る自分専用の場所。実戦記録ツール側の保存機能と、一覧・集計ページ（mylog.html）を公開済み
- [~] マイホレポ（Phase 2）… 店の台データを蓄積して傾向を比べるツール。入力・保存・比較まで実装済み
- [x] 遊タイム期待値計算ツール（yutime-calc.html）… 記録機能なしの1画面計算ツール。記事・外部サイトへのiframe埋め込み用。期待値エンジンは yutime-v3.html からインラインで複製し、`tests/yutime-calc.test.js` の文字列完全一致テストで二重管理を防ぐ。カウンター基準の回転数はエンジンのプリセットが持つ `spec.counterOffset` で内部低確回転数に換算する（アグネスPE＝11、大海5SP＝0。B97でv3と共通化）。localStorage不使用。公開範囲（トップ導線・sitemap）は未判断
- [x] 期待値パラメータの出典一本化（B98）… yutime-v3.html の `MACHINE_PRESETS` はUI情報（roundTypes・evSupported等）だけを持ち、`spec` / `defaults` は期待値エンジンのプリセットから解決する。天井・当り確率・時短テーブル・残保留・counterOffset・純払い出しをページ側のプリセットに書かないことを `tests/yutime-v3.test.js` のアサートで固定する
- [x] 履歴の実収支・期待値の並列表示と日別サマリー（B96）… yutime-v3.html の履歴カード上部を「実収支」「期待値」の2枠にし、どちらにも必ずラベルを付ける（金額だけを単独で大きく出さない）。日付区切りの直下に「台数／実収支／期待値／実働／時給」の日別サマリーを出す。実働は各セッションの `endTime − startTime` の合計で、時刻欠損セッションは実働から除外（実収支・期待値の合算には算入）、`endTime < startTime` は0扱い。表示のみで計算式・保存データ・schemaは不変
- [x] コーナー基準値を選択中のコーナーの機種で解決（G5）… yutime-v3.html の「このコーナーの基準値」チップが `umi-sp5` 決め打ちだった不具合の修正。選択中のコーナー（島）の左右プリセットと、そこに並ぶ台の機種から遊タイム機種を解決し、機種ごとに1つチップを出す。表示・「変更」フォームの初期値・保存先プリセットIDが同じ解決結果を使う。期待値計算は従来どおり台ごとの `normalizeMachinePresetId(machine)` で機種を決めるため計算式・保存データ・schemaは不変
- [x] 通常時区間モデルの導入とデータ移行（S1）… yutime-v3.html のセッションに `segments[]`（kind: normal / yutime、startSpin・endSpin・startTrackedBalls・endRemainBalls・endTrackedBalls・holdSpins）を持たせ、通常回転数と通常消費玉を通常区間の合算から求める。`investments[]` / `hits[]` は `segmentId` で所属区間に紐づく。旧フラットフィールド（hitSpin / hitRemainBalls / yutimeEnterBalls 等）は削除せず並走させる。schema 30→31、移行前データは `ytv3:backup:latest` へ退避してから変換する。`holdSpins` はS1では常に0（時短抜け区間はS2）。この移行で、遊タイム経由の当選で遊タイム中の回転を通常回転数に数えつつ消費玉は突入時で止めていた不整合が直り、当該セッションの通常回転数が `hitSpin − startSpin` から `yutimeEnterSpin − startSpin` に変わる（表示値が変わるのはこのパターンのみ。合成データ10パターン×2店の全表示値比較で確認）
- [x] 区間はフラットフィールドの写像として保存時に組み直す（S1）… 記録の修正で当選回転数や遊タイム突入回転数を直したとき、保存済みの区間が古いまま残って表示値が追従しない事故を防ぐため、`persist()` で `resyncSessionSegments()` を通す。区間IDは種別ごとに引き継ぎ、参照先が消えた `segmentId` は付け直す。S2で区間を直接編集できるようにする際は、ここで上書きしてよい範囲を見直すこと
- [x] 時短抜け入力と区間の連鎖（S2）… yutime-v3.html の「現在回転数をリセット」モーダルを時短抜けの選択に変え、選ぶと新しい通常時区間を追加する。選択肢はプリセットの `spec.jitanTable`（ST機）または `spec.jitanNormal` / `jitanChain`（確変ループ機）と `spec.stSpins` から `時短回数 + ST回転数` で生成し、値をページ側に書かない（アグネスPE＝25/50/100、大海5SP＝100/200）。任意の値を入れる直接入力も置く。区間には `source`（migrated / user）と `endSource`（hit / yutime / end）を持たせ、当選・遊タイム突入・ヤメでそれぞれ区間の終端を確定する
- [x] 区間の打ち出し回転数から先頭の無料保留を引く（S2/G1）… `打ち出し回転数 = endSpin − startSpin − holdSpins`。時短抜けで作った区間はプリセット既定（アグネスPE＝5）、打ち始め区間と遊タイム区間は0。引いた結果が負になる場合は0で止める（終端が開始より小さい入力ミスは従来どおり負のまま警告に回す）。記録の修正画面から区間ごとに0〜9で変更できる。既存データは全区間 holdSpins:0 なので表示値は変わらない
- [x] resync はユーザーが追加した区間を上書きしない（S2）… user 区間が無いセッション（S1と同じ形）だけを従来どおり丸ごと組み直し、user 区間があるセッションはフラットフィールドと1対1で対応する端点（先頭区間の開始／遊タイム区間の境界／endSource が hit・end の区間の終端）だけを追従させる。これで「時短抜けで追加した区間が記録の修正操作で消える」事故を防ぐ。区間ごとの holdSpins は組み直し経路でも保つ
- [x] 通常区間が複数あるセッションの消費玉と投資集計（S2）… 通常区間が2つ以上あるとセッション単位の集計をそのまま区間の消費玉に使うと二重計上になるため、B95 と同じ式（区間のタップ合計 −（台の持ち玉表示 − ツール追跡値））を区間単位に適用する。投資は打っていた区間の `segmentId` に紐づける。2周目以降の投資が「当選前」フィルタから漏れる問題も、通常区間が複数のときだけ遊タイム以外を全て通常時として集計して直す。単一区間セッションは従来の経路のままで表示値は不変
- [x] 転記用の貯玉引出に再プレイを算入（S2/§5）… `貯玉引出 = 開始持ち玉 + 再プレイ投資の合計`。従来はパーソナル店で開始持ち玉のみ、非パーソナル店で持ち玉投資＋再プレイを見ていて再プレイ分が落ちていた。実収支・差玉の式は変更しない
- [x] 実戦リザルト画面（S3b）… yutime-v3.html の履歴カードをタップすると、1台ぶんの振り返り用モーダルを開く。構成は ①今回の結果（実収支・実測回転率・大当たり・1R平均・実働・時給）②打つ前の判断の記録（開始カウンター・遊タイムまでの残り・想定回転率と出所・開始期待値）③想定と実測のズレ（回転率と期待値を想定／実測／差の表で出す）④区間ごとの内訳 ⑤これまでの積み上げ ⑥転記用 ⑦フッタ（実戦一覧に戻る／記録の修正／記録の削除）。戦果報告からも「リザルトを見る」で遷移できる。計算は既存の算出をそのまま使い、式は一切変えない
- [x] 単発の結果に良し悪しを付けない（S3b の設計原則）… 1回の稼働の「期待値と実収支の差」は事実として中立に表示し、判定バッジを置かない。勝った日は良い判断・負けた日は悪い判断という結果論を作らないため。ズレの表の直下には「1回のブレです。判断の良し悪しは下の通算で見ます。」を必ず出す。回転率の差は実測−想定で符号を取る（大きいほど良いので、想定18.0・実測18.5 は +0.5）。「上振れ」「下振れ」「ほぼ想定どおり」「サンプル不足」「良化」「悪化」等の語がリザルト画面のコードに無いことをテストで固定する
- [x] 積み上げも数字だけを出す（S3b）… ブロック名は「これまでの積み上げ」。同一機種（プリセット、解決できない台は機種名）の確定済みセッションと、同一店×同一機種の2軸で、実働・期待値合計・実収支合計・その差・期待値時給・実収支時給を出す（店軸は平均回転率も）。時給は合計÷実働。実働はB96の日別サマリーと同じ算出（時刻欠損は除外、日またぎは0扱い）。台数による分岐や判定は作らない
- [x] 履歴カードは展開せずリザルトを開く（S3b）… 履歴一覧は「日付見出し／日別サマリー／機種・台番・時刻／実収支・期待値の2枠／確定バッジ」だけのスキャン用にし、従来カード内に展開していた転記用・記録の修正・期待値の内訳・差玉行はリザルト画面へ集約した。同じ情報を2箇所に持たないため
- [x] 2周目以降の回転率が更新されない不具合の修正（S4/A-1）… 稼働中パネルの回転率・通常時合計を区間モデル（`deriveSession`）から求めるようにした。閉じていない通常区間の終端は、当選の有無に関わらず現在回転数で閉じる（従来は当選が1回でもあると `segmentEndSpinForRate` が null を返し、`runningNormalSpinCount` も `hitSpin − startSpin` で止まっていたため、時短抜け後に回した分が回転率に反映されなかった）。遊タイム突入回転数が未記録の旧データのフォールバックと、完了済みセッションの算出は従来のまま。検算＝区間①20回転/200玉＝25.0・時短抜け50・現在70回転/消費375玉 → 区間②10.0・セッション合計15.2
- [x] 投資タップの取り消し（S4/A-2）… 投資ボタンの隣に「取り消し」を置き、直前の1件（残高調整を除く）を削除する。投資が無いときは無効表示。トーストの「元に戻す」と同じ `deleteInvestment` を共有し、削除処理を二重に持たない
- [x] 大当たり履歴の一覧・修正・削除（S4/B-1）… 大当たり登録画面とリザルト画面から開く一覧。区間ごとに見出しを付け、新しい区間・新しい当選が上。R種別・当選カウンター・累計獲得出玉を修正でき、削除は二段確認。累計獲得出玉の修正はB88の差分方式（入力した累計 − それより前の当選の獲得出玉合計）で今回分を出し直す。当選には記録時に `segmentId` を持たせ、持たない旧データは当選カウンターで区間へ寄せる
- [x] 大当たり登録画面の整理（S4/C）… 画面名を「時短抜け」から「大当たり登録」に変更（C-1）。当選カウンターは当選ウィザードの記録値を読み取り専用で表示し、二重入力をやめる（C-2）。ラベルの無い数値を残さない（C-3）。R数は「今回の連チャン（直前の通常時区間に紐づく当たり群）」と「このセッション」を分けて出し、引いていないR種別は表示しない（C-4）。理論値の「R数ベース出玉」を削除し、実測の獲得出玉合計 ÷ 合計R で「1R当たり◯◯玉」を出す（実測が無ければ「—」で、理論値では代用しない）（C-5）。持ち玉は確定ボタンを廃止し入力した時点で反映する（C-6）。他人が当てた回数を含む「累計大当たり」を出さず「今回◯回」だけにする（C-7）
- [x] 時短抜けで投資元を持ち玉へ自動切替（S4/C-8）… 実戦の流れは「再プレイ→当たる→出玉で打つ」。再プレイのままだと気づかずに再プレイを減らすため、時短抜けを選んだ時点で投資元を持ち玉に切り替える。持ち玉が0のときは切り替えない。切り替えたことはトーストの1行で伝え、手動で戻すことは従来どおりできる
- [x] 状態バッジは「通常時」「遊タイム中」の2つだけにする（S4/D-1）… S2で時短抜けを入力できるようになり、当選後に宙ぶらりんになる期間がほぼ無くなったため「当り後（時短消化中）」と補助表示「リセット前」を削除した
- [x] 「総投入の内訳」を「消費玉数の内訳」に改称（S4/D-2）… 「総投入」は一般的な用語ではなく伝わらない。ラベルのみの変更で値の定義は変えない
- [x] 入力の即反映化と未保存警告（S5）… 原則は「入力した値はその場で反映される。保存ボタンを押させない。明示保存が妥当な画面では、未保存のまま閉じようとしたら警告する。黙って捨てない」。台情報は「台情報保存」ボタンを廃止し、各欄の `change`（フォーカスが外れた時）で保存する（釘評価チップと同じ挙動。トーストは400msのデバウンスで連続保存を1回にまとめる）（§1）。朝イチチェックはチェック・チップ選択の時点で保存し、「保存して閉じる」を「閉じる」に変えた（§2）。閉店チェックは連続入力の途中の桁を確定値として保存してしまうため即保存にせず、入力が残ったまま閉じようとしたら［保存して閉じる／破棄して閉じる／キャンセル］を出す（§2代替案）。フロアマップ編集は明示保存を維持し（編集途中が即反映されると台データとの対応が壊れるため）、未保存の変更があるまま閉じようとしたら同じ確認を出す（§3）。戦果報告の理論値「R数ベース出玉」を削除し、S4/C-5と同じ算出（実測の獲得出玉合計 ÷ 合計R、実測が無ければ「—」）の「1R当たり◯◯玉」に置き換えた（§4）。共通の仕組みとして `modalGuard`（false を返すとモーダルを閉じない関門）を追加した
- [x] 獲得出玉の累計を連チャン単位にする（S8）… 実機のデータカウンターが出す獲得出玉は「その連チャンの累計」で、時短が終わると0に戻る。B88はこれをセッション累計として扱っていたため、2回目の初当りで入力値が前回までの合計を下回り、警告が出て今回分が0で保存されていた（実測出玉が過少になり1R平均が小さく出る）。累計の範囲を連チャン＝同じ区間に紐づく当たり群（`hits[].segmentId`、S4/C-4と同じ切り出し）に変え、今回分＝入力値 − 同じ連チャン内のそれ以前の合計とする。下回り警告も同じ連チャン内の比較にする。セッションの実測出玉合計（`sessionActualBallsTotal()`）は各当選の今回分の単純合計に一本化し、B90でヤメ入力に置いた「累計獲得出玉」欄は実機が表示しない数字だったので削除する（`sessionActualBalls` フィールドは旧データのフォールバックとして残す）。大当たり履歴の累計表示と修正フォームの初期値も連チャン単位に揃えた。検算＝連チャン①（4R 1,380／6R 2,800）→時短抜け→連チャン②（4R 1,380／4R 2,600）で今回分 1,380/1,420/1,380/1,220・合計5,400玉・18R → 1R当たり300玉
- [x] 1R実質出玉の可視化・実測平均化（S9）… 期待値エンジンが使う `netBallsPerWin`（1R実質出玉）が判定画面のどこにも出ておらず、回転率にはある「手入力欄／実測平均／参考値」の3点セットが無かった。台詳細の期待値判定パネルに回転率の手入力と並べて「1R実質出玉（手入力）」を追加し（空欄なら自動決定・プレースホルダに自動値と出典を出す）、採用順位を パネルの手入力 → プリセットの手入力 → 実測平均 → ラウンド集計 → 理論値 にする。実測平均はS8の「今回分」を出典にし（当選ごとの記録が無い旧データだけヤメ入力の累計を使う）、実測出玉合計と出典を1つにそろえた。台詳細のメトリクスと稼働中パネルの再判定行に「参考1R出玉（出典・n付き）」を出し、根拠行を `使用回転率 17.0（手入力） / 使用1R実質出玉 990玉（実測平均・n=2）` の形にした。理論値の桁は落とさない（アグネスPE＝587.5玉）。期待値の式・保存データ・schemaは不変
- [x] 1R実質出玉の実測平均を合計R数ベースに統一（S10）… S9の実測平均は当選件数で割っており、当り1回あたりの構成R数（4R/6R/10R）を無視していた。`netBallsPerWinInfo()` の実測平均を `Σ今回分の獲得出玉 ÷ Σ消化R数` に変え、戦果報告の「1R当たり」（`sessionBallsPerRound`、S5で統一済み）と同じ考え方にそろえた。消化R数の出所は `roundCountFromRoundType` 1本（`totalRoundsForPreset` と同じ）。獲得出玉が未入力の当選は分子・分母とも除外する。`count` は合計R数の意味に変わったので表示も `n=◯` から `◯R分` にした。手入力・ラウンド集計・理論値の経路は不変。検算＝6R540玉→90玉／10R1,300玉＋4R420玉→123玉／10R未入力＋4R420玉→105玉／当選1回10R1,300玉→130玉
- [x] 1R実質出玉の単位を期待値エンジンとそろえる（S11）… S10で実測平均だけが「玉/R」になり、理論値・ラウンド集計・手入力は当選あたりのままだったため、同じ `netBallsPerWin` に2つの単位が混ざっていた。方針B（全部を玉/Rへ揃える）を採用し、エンジンのプリセット `spec` に `averageRoundsPerWin`（大海5SP=10R、アグネスPE=587.5÷108≒5.44R）を足して `winBalls = expectedWins × (netBallsPerWin × averageRoundsPerWin) + 電サポ増減` に変えた。平均R数は既存の `standardPayout ÷ standardRounds` の関係から出し、新しい推定は足していない。`defaults.netBallsPerWin` は 587.5→108 / 1400→140、定数も `DEFAULT_NET_BALLS_PER_ROUND = 140` に改名。ラウンド集計も玉/Rへそろえ（R種別の公称出玉 ÷ R数）、`yutime-calc.html` のページ側変換はアグネスPEが素通し・大海5SPが平均R数で割る形になった。schema 32 で 31以前の手入力値（当選あたり）を平均R数で割って移行する。受け入れ検算＝カウンター150・回転率17・1R実質100玉・等価・現金 → +1,569円、カウンター0・105玉 → +310円、記事v5の期待値表90セル全一致、いずれも変換前と同値
- [x] 貯玉引出の店種別判定への修正とリザルト表示の整理（S12）… 転記用の `貯玉引出` が `transferSummaryForSession()` で `store` を見ておらず、非パーソナル店でも常にパーソナル式（開始持ち玉＋再プレ）を返していたバグの修正。B84で確定した条件がS4の修正で落ちた同型の事故の2回目のため、分岐を `usesPersonalBalanceFormula(store)` に、投資玉数を `playerInvestedBalls()` に1本化し、`profitYenForSession()` と `withdrawBalls` が同じ関数を通るようにした。条件式もパーソナル式そのものもファイル全体で1箇所だけであることをテストで固定している。**分岐の主軸は店の種別だけにする**（タップ投資モードを条件に混ぜると、パーソナル店に実在する investments 空の旧記録が非パーソナル式＝0玉へ落ちるため）。検算＝非パーソナル125玉×19回→2,375個／パーソナル2,500＋再プレ500→3,000個（不変）／パーソナル・investments空・開始持ち玉26,637個・11,462個→そのまま26,637個・11,462個。差玉の式は不変。パーソナル店でtapModeでない記録は実収支も `開始持ち玉＋再プレ` を引く形に変わる（従来は投資玉0扱いだった）。あわせて表示を2件、リザルトの「期待値との差」を「開始期待値との差」に改称（数値は不変）、「区間ごとの内訳」を 区間／起点／終点／回転数／消費玉／回転率 の表にした（保留控除の注記は表の下に1行でまとめる。320pxで表幅264px・はみ出しなしを実測）
- [x] 開始持ち玉と稼働中の持ち玉の分離（S15）… `session.startMochidama` が稼働中の持ち玉修正で上書きされ続け、「開始時点の持ち玉」ではなく「最後に修正した時点の持ち玉」になっていたバグの修正。9/7の実戦データ37セッション中17件で判定パネルの入力と保存値が食い違っていた。`startMochidama`（打ち始めの持ち玉。以後不変）と `currentMochidama`（稼働中の持ち玉の起点）を分け、稼働中の残高修正はすべて後者に載せる（書き込み先の分岐は `balanceCurrentKey()` の1箇所）。`deriveBalances()` は `mochidamaBaseValue()` を通すので画面上の現在持ち玉は不変。実収支・貯玉引出・投入玉の参照先は `startMochidama` のままで変えない。**区間の `segments[0].startTrackedBalls` も同じ値で汚染されていた**（`persist()` が毎回 `resyncSessionSegments()` で `startMochidama` から作り直すため）ので復元根拠に使えず、移行は ①引き継ぎ元の `終了合計玉＋残保留` ②`startEv.mochidamaInput` ③現行値維持 の順で復元する。schema 32→33、移行前データは `ytv3:backup:s15` へ1度だけ退避する（`backup:latest` は persist のたびに上書きされるため）。修復前の値は `currentMochidama` として残す
- [x] 遊タイム状態の判定を区間モデル基準にする（S7/A-1・A-2）… `isYutimeInvestmentPhase()` が `yutimeEnterTime || yutimeEnterBalls !== null` というフラグだけを見ていたため、遊タイム経由で当選したあとも「遊タイム中」から戻らず（9/7実戦③）、以後の投資・当選が遊タイム区間に紐づき続けて「今回の連チャン」が前回のまま止まっていた（同①）。判定を「末尾の区間が kind: yutime かつ endSpin が空」に変え、`yutimeEnterTime` / `yutimeEnterBalls` は記録としてだけ残す。遊タイム区間を持たない旧データ（区間が無い／突入回転数が未記録で遊タイム区間を組めない）は従来の判定へフォールバックするので表示は変わらない。あわせて遊タイム突入の玉数入力が `yutimeEnterBalls` にしか入らず稼働中パネルの持ち玉に反映されなかった不具合（同②）を、`updateMochidamaBalanceWithUndo()` を通して直した
- [x] 消費玉を起点・終点の実測差で出す（S7）… `消費玉 = 起点の持ち玉 + 区間内に外から足した玉 − 終点の持ち玉`。起点は打ち始め入力／時短抜け入力／遊タイム突入入力、終点は当選ウィザードの残り持ち玉かヤメ入力の終了玉で、いずれも実測値。外から足した玉は再プレイ投資と現金投資（玉換算）だけで、持ち玉タップは起点に含まれるため加算しない。区間に `startBallsSource`（measured / tracked）を持たせ、起点が実測の区間だけ新式を使う。大当たり登録の時短抜けに「そのときの台の持ち玉」欄を追加し（初期値＝現在の持ち玉＋この連チャンの獲得出玉合計）、入力値を新しい通常区間の `startTrackedBalls` と `currentMochidama` の両方へ入れる。データカウンターの獲得出玉は1R平均専用で回転率には使わない。**適用範囲はセッションの `consumedModel: "endpoints"` で切り、S7以降に打ち始めたセッションだけが新式**。旧セッション（フラグ無し）はB95/B85式のままで表示値は1つも変わらない（HEAD版と新版の同一データ比較で旧セッション1,388件の全表示値・全区間が一致することを実測）。新式の区間ではB89の乖離選択UIを出さない。schema 33→34。検算＝起点0/再プレイ1,000/終点50→950、起点1,620/追加なし/終点120→1,500、起点1,620/現金1,000円(250玉)/終点370→1,500、起点1,620/持ち玉タップ1,500玉/終点120→1,500（タップは加算しない）、起点1,620/終点1,700→警告・null。回転率＝区間①20回転/200玉＝25.0・区間②15回転/375玉＝10.0・合計35回転/575玉＝15.2（S4/A-1と同値）
- [x] 持ち玉を修正したタイミングで回転率を再計算（S7b/A）… S7で回転率の主役が「起点と終点の実測差」になったので、稼働中に持ち玉を実測値へ直す操作も終点と同じ扱いにする。開いている区間に**途中測定点**（`lastMeasuredBalls` / `lastMeasuredSpin`、最新の1つだけ）を持たせ、`その時点の消費玉 = 起点 + 区間内の外部投入（再プレイ・現金換算） − 修正後の持ち玉` で確定する。書き込み経路は `recordSegmentMeasurement()` の1箇所にまとめ、稼働中パネルの「修正」（`openBalanceEditForm`）と `updateMochidamaBalanceWithUndo()` の両方から通す。トーストの「元に戻す」では測定点も戻す（`segmentMeasurementSnapshot` / `restoreSegmentMeasurement`）。当選・ヤメで終点が入ったら終点が優先（`segmentMeasuredEndBalls`）。測定点が無い区間は従来どおりタップ合計を目安に使う。稼働中パネルの回転率の横に出典を1語添える（`（実測）`＝終点または途中測定点で確定／`（目安）`＝タップ合計。B94「判断の場に前提を置く」）。旧セッション（`consumedModel` 無し）には測定点を記録せず表示も出さない。検算＝起点1,620・追加なし・持ち玉を1,120に修正・現在75/起点50/保留5 → 20回転・500玉・10.0（実測）／同条件で修正せずタップ500玉 → 10.0（目安）／その後に当選（終点120）→ 1,500玉で確定
- [x] 残保留当選の自動判定（S7b/B）… 時短抜け直後の残保留（既定5個）で当たると、実態は通常時0回転なのにツールは時短抜けカウンターから当選カウンターまでの数回転を通常時として数えていた（9/7実戦の⑯⑳、時短抜け25→28で当選）。期待値エンジン（B91）が残保留を連チャンの引き戻しに含めている立場に記録側も揃え、**残保留当選は前の連チャンの続き**として扱う。入力は増やさず、`当選カウンター − 区間の起点カウンター ≦ その区間の holdSpins` で自動判定する（境界は「≦」）。判定対象は時短抜けで始まった区間だけで、区間に `startSource: "jitan"` を持たせて識別する（打ち始め区間・遊タイム区間は対象外。旧データはこの印を持たないので判定が走らず表示が変わらない）。該当区間は通常時なし＝打ち出し回転数0・消費玉0で回転率の集計から外し、投資が残っていれば警告を出す。当たりの `segmentId` は打っていた区間のまま保存し、寄せるのは参照するときだけ（`resolveSegmentChainId`）にして、手動で戻したときに元の区間へ戻るようにする。これで「今回の連チャン」（S4/C-4）・獲得出玉の累計（S8）・1R当たりが紐づけ先の連チャンで集計される。大当たり登録画面とリザルトの区間内訳に「残保留当選（通常時なし）」を明示し、大当たり履歴に「残保留当選の扱い」の節を置いて区間ごとに手動で切り替えられるようにする（`holdCarryHit` が null なら自動判定、true / false は手動指定）。エンジン側は変更しない。検算＝時短抜け25→当選28（保留5）は残保留当選／25→31は通常当選／50→54で保留4に編集済みは残保留当選（≦）／打ち始め区間の0→3は通常当選
- [x] 残保留当選の判定を「打ち出し状態」方式へ変更（S7c）… S7bはカウンターの差（起点からの回転数 ≦ 残保留数）で判定していたが、残保留で当たったかどうかは回転数ではなく**打ち出しがあったか**で決まる。遊タイム狙いは時短抜けで打ち出しを止めて保留が回るのを待つ。打ち切りは時短抜け後も打ち続ける（保留5個以上で短縮変動が効く）ので、数回転後に当たっても新しい当たりになる。判定を `kind: normal` かつ `startSource: "jitan"` かつ `endSource: "hit"` かつ **`shooting === "before"`** に差し替え、回転数の条件は判定から外した。セッションに `playStyle`（"yutime" / "continuous"、既定は遊タイム狙い）、区間に `shooting`（"before" / "started"）を持たせる。`playStyle` は時短抜けで作る区間の初期状態を決めるだけで判定には使わない。`"before"` → `"started"` は「打ち出し開始」ボタンか、その区間への投資タップのどちらか早い方で切り替わり、一度 started になったら戻さない（戻すときは大当たり履歴の手動切り替え）。回転数は警告にだけ使う：`before` で `当選カウンター − 起点 > holdSpins + 3` なら「打ち出し開始の押し忘れ？」、`started` で `≦ holdSpins` かつ投資タップ無しなら「打ち出し前の当選では？」（どちらも判定は変えない）。打ち出していない区間は当選の有無に関わらず通常時なし（回転数0・消費玉0）として回転率の集計から外す（`segmentSkipsNormalPlay`）。S7bの部品（`holdCarryHit` の手動指定・前の連チャンへの寄せ・大当たり履歴の切り替え・区間内訳の「残保留当選」表示）はそのまま使う。schema 34→35、既存区間は **S7bの回転数ベースの判定（`legacyHoldCarryHit`）が true だったものだけ `shooting: "before"`** に補完するので、既存データの判定結果・回転率・集計は変わらない（S7b期のデータ384件＋実データ37件＋合成旧データ1,388件をa11a7c6版と並走比較して確認）。UIは打ち始めウィザードに「打ち方」の2択、時短抜け画面に「この後（打ち出しを止める／続ける）」、稼働中パネルに状態バッジ「保留消化中」と保留消化中だけ出る「打ち出し開始」ボタンを追加した。**稼働中パネルには常時表示の操作を足していない**（320pxではパネルの縦が既に余白ゼロ（530/530）で、44pxの常時表示を1つ足すだけで固定全画面がスクロールに変わるため）。方針のセッション中変更は稼働中パネルの「記録の修正・削除」に置いた。検算＝時短抜け25→当選28は、止めていれば残保留当選／打ち出し開始を押していれば通常当選／タップ1回でも通常当選。打ち切りの初期状態は started だが時短抜け画面で「止める」を選べば残保留当選
- [x] 投資phaseの修復（S17/第1部）… 9/7時点のバグ（`isYutimeInvestmentPhase` が当選後も true のまま。S7 rev2/A-1で修正済み）で、遊タイム経由の当選後に打ったタップが `phase: "yutime"` のまま保存されていた。区間の紐づけ（`segmentId`）は正しく通常区間を指しているのに phase が遊タイムなので、通常時の消費玉から漏れて回転率が壊れる（実例：9/7・台325の「時短抜け50→当選170」区間が消費84玉・回転率342.3）。schema 35→36 の移行で、**`segmentId` が指す区間の種別と phase が矛盾していれば区間の種別に合わせる**。`segmentId` が解決できない投資は触らない。移行前データは `ytv3:backup:s17` へ1度だけ退避し、修復件数をコンソールに出す。検算＝実データ37件で修復10件・1,250玉（すべて台325の1セッション、他は0件）。当該区間は 84玉/342.3 → **1,334玉/21.6**、セッション実測回転率は 24.25 → **21.78**。他36セッションの表示値は1つも動かない
- [x] 獲得期待値（Σ区間期待値）（S17/第2部）… 「開始期待値」は最初の判断しか表さないので、1日打ち切りだと仕事量が見えない。**獲得期待値＝Σ（通常区間ごとの、起点カウンターからの期待値）** を追加する。各通常区間は「その起点から当たるまで打つ」という判断なので、足せば1日の仕事量になる。遊タイム区間・残保留当選の区間・打ち出しをしなかった区間（`segmentSkipsNormalPlay`）は判断としての打ち出しが無いので含めない。入力は区間ごとに揃える：起点は区間の `startSpin`（宵越しは打ち始め区間だけ）、回転率は**セッション全体の実測**（区間ごとの実測はタップ数に引きずられて異常値になる）、1R実質出玉は既存の優先順位（`netBallsPerWinInfo`）、持ち玉・再プレ・現金は `startEv` の入力を流用。計算は既存の `calculateMachineExpectation()`（＝`YUTIME_EXPECTATION_ENGINE.calculate`）を呼ぶだけで、**エンジンは1文字も変えない**（yutime-calc との一致テストで担保）。実測が出せないセッションは開始期待値の想定回転率で代用し出典を「想定」と出す。**実測が 1〜50（試算カードの `clampTrialRate` と同じ帯）の外なら実測として採らない**（2回転でヤメた記録＝持ち玉11,462個・0.04回転率をそのまま渡すと −580万円が出るため）。表示はリザルトの区間内訳に「期待値」列（対象外は「—」）＋合計行「獲得期待値 ◯◯円（回転率◯◯・実測／1R◯玉・出典）」、「今回の結果」に1項目、履歴カード・日別サマリー・積み上げの期待値を獲得期待値ベースへ置き換え、積み上げに注記を1行。**開始期待値は「打つ前の判断」「想定と実測のズレ」「転記用」にそのまま残す**（判断の記録と仕事量は役割が違う）。検算＝9/7・台325は13区間・**+20,586円**（回転率21.78・実測／1R108玉）で開始期待値 −22円と対照的。実データ37件では開始期待値合計 53,032円 → 獲得期待値合計 63,019円
- [x] S17の追補3件（S17b）… ①**遊タイム玉減りを区間の端点から出す**。`突入時玉数 ＋ 遊タイム区間内の投資 − 遊タイム区間の endRemainBalls`。セッションのフラット項目（`hitRemainBalls`＝最後の当選時の残り持ち玉）は、当選が複数あると遊タイム区間の終点と別物になるので使わない。区間の終点が無い旧データだけ従来式へフォールバックする。検算＝9/7・台325が `1,535 − 1,500 = 35玉`（S17時点では −777 で算出不可＋警告になっていた）。②**残保留当選の手動切り替えの対象を広げる**。`holdCarrySegments` の条件を「`startSource === "jitan"`」から「`startSource === "jitan"` または 起点がプリセットの時短抜けカウンター（`jitanExitOptions` の `counterSpin`）と一致し、かつ直前の区間が当選で終了」に変えた（当選で終わった通常区間であることは共通の前提）。S7b以前の記録には `startSource` の印が無く一覧に出せなかったため。**自動判定（`detectHoldCarryHit`）は変えない**ので既存の判定結果は不変。台325では0件→10件になり、区間9（25→28）・区間13（50→51）が切り替えられるようになる。③**獲得期待値の回転率の出典を3段にする**。①セッションの実測（通常回転数 `EARNED_EV_MIN_SPINS`＝100以上、かつ 1〜50）→ ②その台の参考回転率（`machineStats`。同じ条件で判定）→ ③開始期待値の想定回転率。出典表示は「実測／参考／想定」。母数の薄い実測（10回転で当たった記録の10.0など）が期待値を大きく歪めていたための追加。実データ37件では 実測29件 → 実測20/参考6/想定4/算出不可7件、獲得期待値合計 63,019円 → 100,578円。8/26・台360（10回転）は「参考20.8」へ、8/5・台290（2回転・0.04）は引き続き算出不可
- [x] アグネスPEの1R実質出玉の既定値を記事の実戦基準へ（S18）… エンジンの `agnes-pe` の `defaults.netBallsPerWin` が公称払い出し（648÷6＝108）のままで、yutime-calc の `payoutDefault: 100` と記事v5（1R100玉が中心）とずれていた。9/7の実測（4R 370〜430／6R 570〜630／10R 1,000〜1,010）もほぼ100玉/Rなので、**既定を100玉/Rに変更**。`averageRoundsPerWin` は当選あたりの平均R数（R構成の重み）であって出玉ではないので `587.5 / 108` のまま動かさない。エンジンは yutime-calc.html にも同じブロックがあるので同時に直し、一致テストで担保する。既定値の出典ラベルは機種ごとに性格が違うので `MACHINE_PRESETS` に `defaultNetBallsLabel` を持たせ、アグネスPEは「**基準値（記事の1R100玉）**」と出す（公称ではなく実戦基準だと分かる表記）。大海5SPの1,400玉/当選＝140玉/Rは夢爽が実戦基準で置いた値なのでラベルも既定値もそのまま。検算＝カウンター150・回転率17・等価・現金・手入力なし・実測なしで **+1,569円**（記事v5の代表点と一致。変更前は 2,002円で、同条件のcalcの1,569円とずれていた）。calcとv3が同条件で同値になることと、calcの `payoutDefault` とエンジンの既定値がそろっていることをテストで固定した。実データ37件では、実測平均やラウンド集計が出ているセッションは不変で、既定値に落ちる2件（9/3・台200 1,678→905円、9/5・台201 2,342→1,181円）だけが動く。umi-sp5の29件は全て不変
- [x] 1R実質出玉の「ラウンド集計」の段を廃止（S19）… `netBallsPerWinInfo()` の採用順位から「ラウンド集計（R種別の公称出玉 ÷ そのR数）」を外し、**パネル手入力 → プリセット手入力 → 実測平均（合計R数ベース・S10）→ 基準値（プリセット既定）** の4段にした。アグネスPEは 1080/10・648/6・432/4 がどれも108玉/R なので、当選のR種別を記録しただけで常に公称108玉が採用され、実測（S18で100玉に直した基準値）より優先されて甘い判定を出していた。獲得出玉を入れていない台が全部108玉で判定されるのが実害。実測が無いときは公称に寄せず基準値へ落とす。umi-sp5も同じ扱い。出典表示は「手入力」「実測平均・◯R分」「基準値」の3種になり、ラウンド集計専用だった `n=◯` の書式も削除した。検算＝アグネスPEで当たりのR種別だけ記録して獲得出玉が無いセッションは 100玉「基準値（記事の1R100玉）」、獲得出玉ありは実測平均のまま不変。実データ37件では108玉→100玉に変わったのは2セッション（9/7・台325 獲得期待値 20,586→15,567円、9/5・台201 2,342→1,181円、合計 −6,180円）で、実測平均が出ているセッションと umi-sp5 は不変。エンジンは変更なし（この関数はエンジン外）
- [x] 1R実質出玉（減算込み）を持ち玉差から算出（S20）… 記事とエンジンの「1R実質出玉」は減算込み（電サポ中の減りを含む純増）の定義なのに、ツールの実測平均（S8〜S10）はデータカウンターの獲得出玉＝払い出し（グロス）で集計していて定義が食い違っていた。S7で時短抜け時の持ち玉を入力するようになったので、**連チャンごとの純増＝次の通常区間の起点持ち玉 − 当選時の持ち玉 − 時短中に外から足した玉**を直接出せる。1R実質出玉＝Σ純増 ÷ ΣR数。連チャンの起点は「当選で閉じた区間」で、残保留当選の区間は前の連チャンの続き（S7bの寄せ先）なので起点にせず、純増の窓もその区間をまたいで次の通常区間まで延ばす。最後の連チャンで次の区間が無いときはヤメ入力の終了玉を次の起点に使う。採用は**連チャン単位**で判定し、①持ち玉差 → ②カウンターの獲得出玉（S8。従来値）→ ③どちらも無ければ集計から除外。出典表記は「実測平均・持ち玉差・◯R分」「実測平均・カウンター・◯R分」、混在時は「実測平均・持ち玉差◯R＋カウンター◯R」。手入力・基準値の経路は不変。時短中の投資は当選で閉じた区間に紐づいて通常時の投資と区別できないため、`addInvestment` が「開いている区間が無いとき（＝当選から時短抜けまで）」に `duringJitan` の印を付ける（印の無い旧データは推測で差し引かない）。リザルトの「今回の結果」に「1R実質出玉 ◯玉（減算込み）／ 払い出し ◯玉 ／ 時短中の減り ◯玉」を1行足し、区間ごとの内訳に「純増／1R」列（8列目）を追加した（純増と1Rは1列にまとめ、320pxでは表を横スクロールで見る）。エンジンは変更なし（この関数はエンジン外。yutime-calcとの一致テストで担保）。検算＝9/7・台325は連チャン12件の純増 +2,323／+1,354／+588／+595／+1,614／+1,539／+417／+389／+2,528／+978／+1,168／+1,072 で合計 **14,565玉 ÷ 150R ＝ 97.1玉**（カウンター未入力なので払い出しは「—」）。実データ37件では9セッションの実測平均が動き（台325は基準値100玉→実測97.1玉で獲得期待値 15,567→13,747円）、獲得期待値の合計は 93,625 → 76,052円。8/26・台360は 純増0玉／10R になる（ヤメ入力の終了玉に大当り出玉が入っていない記録）ため、実測平均が実態より辛くなる。記録側の確認が要る
- [x] 1R実質出玉の実測平均を機種単位（全店）で集計（S22）… 1R実質出玉はアタッカーのこぼれ具合が主因で、釘（店ごと）より台の個体差・機種の性質が効くので、機種単位の集計を**アクティブ店から全店の同機種セッション**へ広げた。台単位（その台の記録がある台）は現行どおりで不変。採用順位は **パネル手入力 → プリセット手入力 → その台の実測平均 → 全店の同機種の実測平均（＋店補正）→ 基準値** の5段。集計条件（持ち玉差→カウンター→除外の連チャン単位フォールバック・分母は合計R数・`status === "completed"` のみ）は S10・S20 のまま。店ごとの差は店設定の「1R実質出玉の補正（玉）」（既定0・マイナス可・保存ボタンなしの即反映）で吸収し、**全店の実測平均を使うときだけ**足し引きする（台単位の実測・手入力・基準値には掛けない）。出典表記に集計範囲を添える（「実測平均・持ち玉差・150R分（この台）」「（全店・n台）」、補正時は「（全店・4台、この店 −2玉）」）。回転率の集計範囲は店単位のまま（釘は店ごと）。エンジンは変更なし（この関数はエンジン外。yutime-calcとの一致テストで担保）。実データ37件では、台単位の実測がある13台・16セッションは値も獲得期待値も不変（表記に「（この台）」が付くだけ）で、実測が無い台だけが動いた＝アグネスPE 35台が基準値100→96.33玉（全店・4台・188R）、大海5SP 105台が理論値140→87.70玉（全店・9台・240R）。セッションは17件で使用値が変わり、いずれも大海5SPの140→87.70で獲得期待値は約 −5,300〜−5,700円ずつ動く。**注意**：大海5SPの全店平均は 8/31・アムズ台103 の −73.75玉/R（40R・ヤメ入力の終了玉に大当り出玉が入っていない記録）に引きずられており、この1台を除くと120.0玉/R。記録側の確認が要る（S20の8/26・台360と同じ性質の記録）
- [x] テスト店の除外と、純増0以下の連チャンの除外（S22b）… ①店設定に「テスト用の店（集計から除外）」（既定OFF・即保存・店一覧に「テスト」バッジ）を追加し、ONの店の記録を**店をまたぐ集計**（全店の1R実質出玉の実測平均・リザルトの「これまでの積み上げ」の機種軸）から外す。その店を選んでいる間の自分の表示（台の参考値・履歴・リザルト）は従来どおり出す＝除外の判定は `sessionCountsForAggregate(session, keepStoreId)` 1本で、keepStoreId（見ている台の店／そのリザルトの店）だけは通す。店×機種軸の積み上げと店内の累計参考回転率はもともと店単位なので変更なし。schema 37 へ上げ、移行時の初期値だけ店名「アムズABCビッグトマト」をONにする（以後は保存値を尊重し、名前で上書きしない）。②`netBallsActualTotals` で**純増が0以下の連チャンを分子・分母から除外**する（当たって玉が減ることはないので入力ミスとみなす）。除外した連チャンはカウンター経路にも落とさない（同じ当選の二重採用を避ける）。カウンター経路は元から `actualBalls > 0` の当選しか採らないので実質は持ち玉差の段の防御。除外したR数は出典に「除外◯R」で添え（例：`129.6玉（実測平均・持ち玉差・150R分（全店・6台、除外10R））`）、リザルトの区間内訳では該当行の純増セルに⚠を付けて表の下に「⚠ ①は純増が0以下のため集計から除外（入力を確認）」を出す。判定は `chainExcludedFromNetBalls` 1本で集計と表示が共有する。エンジンは変更なし。実データ37件（アムズ＝テスト店）では、大海5SPの全店平均が 87.70→**129.56玉/R**（DSG＋KEIZの6台・150R、除外10R。理論値140との差 −10.4玉/R）、アグネスPEは 96.33→**96.85玉/R**（DSG2台・160R。負の連チャンが無いので②の除外は0R）。8/26・DSG台360の純増0/10Rが外れて台360は 111.38→139.225玉（40R）、8/31・アムズ台103の −73.75玉/R は台単位の実測から外れた。テスト店以外では17セッションの獲得期待値が動き（+2,954〜+4,541円／件、合計 **+70,784円**）、大海5SPの機種軸の積み上げは29台 −45,841円 → 26台 +43,625円（DSG選択時）
- [x] 出来事の時刻を自動記録し、通常時の時速と当たり消化時間を出す（S23）… 当選（当選ウィザード完了）・時短抜け・遊タイム突入・ヤメの瞬間を区間の `startAt`/`endAt` にISOで自動保存する。手入力欄は増やさず、事後の修正だけ大当たり履歴（当選時刻）と記録の修正（時短抜けの開始時刻）にHH:MM:SSの欄を1つずつ足した。導出は 区間の所要時間＝`endAt − startAt`、通常時の時速（回転/分）＝打ち出し回転数÷所要時間、セッション平均＝Σ打ち出し回転数÷Σ通常時所要時間、当たり消化時間＝当選`at`→次の通常区間の`startAt`（連チャン全体で1つ。残保留当選の区間はまたぐ）。表示はリザルトの「今回の結果」に通常時の時速、区間内訳に「所要」列と当選行の下の「当たり消化 ◯分◯秒」、日別サマリーと積み上げに「平均時速」、台詳細の参考回転率の枠に「参考時速」。時速は単独では意味が薄い（回転率が高いほど短縮変動で速くなる）ので、表示は必ず回転率とセットにする。期待値の想定時速は 手入力（プリセット設定）→ 台の実測 → 店の実測 → 既定250/h の4段で、根拠行に出典を出す（実測として採るのは 100回転以上・20分以上・1〜12回転/分の範囲のみ）。schema 37→38。移行では区間の時刻をISOへそろえ、当選で閉じた区間の `endAt` を `hits[].at` から埋める（埋められない時刻はnullのまま）。**TZ対策**：HH:MMの欄は記録した端末のローカル時刻、`hits[].at` はISO（絶対時刻）なので、混ぜて差を取るには記録時のUTCオフセットが要る。`startTime` と `createdAt` は打ち始めの同じ瞬間に書かれるので、その差の最頻値（15分刻み）を記録時のオフセットとし、実行中の端末のTZに依存させない（`deriveRecordedUtcOffset`）。同じ理由で、投資の前後判定 `isoTimeToMinutes`（S17から実行中のTZで `hits[].at` を読んでいた）も記録時のオフセットで読むよう直した。エンジンは変更なし。実データ37件では期待値・回転率・獲得期待値・参考回転率はすべて不変で、想定時速が実測に変わった28件の消化時間・時給だけが動く（判定の変化は0件）。TZ=UTC と Asia/Tokyo で全表示値が完全一致することを実データで確認
- [x] yutime-calc に打ち切りモード（時間制約つき期待値シミュレーター）（C2）… 遊タイム狙いモードが「打ち始め→1回目の当たり（＋連チャン）」までの期待値なのに対し、打ち切りモードは時短抜けから打ち続ける前提で、残り時間が尽きるまでの合計期待値・時給を出す。閉店取り残し（打ちかけの投資が回収できない損失）を織り込む（B案）。エンジンは解析式で時間上限を扱えないため、ページ内で固定シードのモンテカルロ（既定20,000試行）を回す。**エンジンブロックは変更しない**（yutime-v3との文字列完全一致テストで担保）。1回転ずつ乱数を引くと数秒かかるので、区間ごとに「何回転目で当たるか」を幾何分布の逆関数で1回引き、時間と玉代をまとめて進める（分布は1回転ずつと同じ。別実装の1回転ずつシミュレータと4万試行で突き合わせて確認）。1試行を360分まで回して各チェックポイント時点の累積収支を記録するので、5分刻み72点の探索が追加コストなしで出る。**やめるルール（C2-2）**：時短抜けの時点で残り時間が「その起点から1サイクル打って期待値が0以上になる最短時間 T*」を下回っていたら、そこで打ち止める（実戦で座り直さない判断）。T* は残り時間に依存しないので、試行は最後まで回してサイクルの区切りを記録し、チェックポイントごとに「どこでやめたか」を後から当てる。出力は合計期待値・時給・想定当たり・遊タイム到達・時短抜け内訳・時間切れの損失見込みと、**起点ごと（現在カウンター／15抜け／40抜け／90抜け）× 残り時間（30/45/60/90/120/180分）の1サイクル期待値・時給・判定の表**。**再スタート加重は振り分けの30/66/4ではなく終端分布の約36/62/2**（長い時短ほど引き戻して連チャンが続き、サイクルの終端になりにくい。終端分布は w_J × (1-p)^(J+残保留) に比例する）。ST確定機のモデルなので大海5SPは未対応としてトグルを無効化する（推測値を出さない）。localStorage不使用・埋め込み可。検算＝カウンター150・17回転・1R100玉・等価・現金の1サイクル期待値がエンジンの +1,590円 と +1.6%、平均連 2.49連と +0.3%、24時間の時給が終端加重のサイクル時給と −1.4%。既存の遊タイム狙いモードは記事v5の90セル一致のまま不変。**C2-3で「座れる残り時間」の定義を期待値±0（T*）から時給基準へ変更**：座れる残り時間＝その起点から残り T 分で1サイクル打ったときの**時給が閾値以上になる最小の T**（5分刻み）。届かなければ「この条件では打てる水準（時給2,400円）に届きません」と明示する。表の判定はB93と同じ（時給≧閾値＝打てる／期待値プラスで閾値未満＝微妙／期待値マイナス＝打てない）。閾値の既定はv3と同じ2,400円で、`?hourly=` で変えられる（打ち切りモード限定。遊タイム狙いモードの判定は2,400円固定のまま）。**やめるルールも時給基準**（時短抜けで「残り時間での1サイクル時給 < 閾値」ならヤメ）にし、合計期待値・時間切れ損失をこれで集計する。この表の時給は「1サイクルの期待値 ÷ 消化時間」で、パネル上段の時給（合計期待値 ÷ 入力の残り時間）とは分母が違う。**やめるルールは時給を上げるが合計期待値は下げうる**（打てる水準に届かない残り時間では、期待値がプラスでも座らないため）。検算＝17回転・時速250回転/h・c50 は定常の時給が約500円なので全起点で「届きません」、22回転・時速350回転/h・c50 は 今から50分〜／15抜け60分〜／40抜け50分〜／90抜け40分〜。**C2-4で入力を実戦の形に寄せた**：①通常時の時速の単位を回転/分 → **回転/時**（チップ 250＝記事の前提／300／350＝導入日の実測）。②残り時間の手入力を廃止し、**現在時刻・ヤメ予定時刻・閉店時刻（既定23:45）から残り時間を出す**（残り時間＝min(ヤメ予定, 閉店) − 現在時刻）。現在時刻は端末の時計から入れて1分ごとに進め、手で直したら以後は上書きしない。根拠を「残り 63分（22:45ヤメ予定・現在21:42）」の形で添え、終了時刻を過ぎていれば計算せず「終了時刻を過ぎています」と出す。URLは `?minutes=` を廃して `?close=23:45&quit=22:30` に。③持ち玉を「持ち玉」と「再プレイ」の2枠にし（v3と同じ）、合計を `availableBalls` としてエンジンへ渡す（どちらも交換単価の玉）。`?saipurei=` を追加。持ち玉2枠は両モード共通。**打ち切りモードの再計算は入力停止から150msのデバウンス**（20,000試行は実ブラウザで1計算あたり約365msかかるので、1打鍵ごとに走らせると入力が固まる）。待っている間は「計算中…」を出し、未対応機種・入力不足のメッセージは計算せずに即時で出す。保留中にモードを戻したり機種を変えたら計算を捨てる。遊タイム狙いモードは解析式なので即時反映のまま（実測0.2ms）
- [x] 記事md→HTMLの変換規約（太字）… `**…**` は **改行（`<br>`）をまたぐものも `<strong>…</strong>` に変換する**。対象は1段落の中で閉じている `**…**` だけで、段落をまたぐ対応は取らない。変換後のHTMLに `**` が1件でも残っていたら変換漏れとして扱い、`grep -c '\*\*'` が0件であることを公開前に確認する。agnespe-yutime.html で複数行の太字6箇所（`**` 12個）が生テキストのまま表示されていたための規約化
- [ ] 判別ツールと店舗分析ツールの連携（Phase 3）

---

# 1. 設定判別ツール（Phase 1）

## コア設計

- [x] ベイズ推定＋二項分布による判別エンジン
  - 各要素（BIG確率、REG確率、小役確率など）ごとに尤度を計算して掛け合わせる
- [x] 解析値はコードに埋め込まず、出典URL・取得日付きのデータファイルで管理する
  - 数値の更新とロジックを分離し、検証・差し替えを容易にする
- [x] 実在の根拠が確認できないフラグ（例: ハナハナのBIG中ハズレ）は入力欄を設けない

## 事前分布（他ツールとの差別化ポイント）

- [x] 事前分布をプラグイン可能なインターフェースとして設計する
- [x] 選択肢1: 均等分布（各設定1/6）
- [x] 選択肢2: 手動プリセット（自分で設定配分を入力）
- [ ] 選択肢3: 過去イベントデータからの推定配分（Phase 3で接続）
- [x] チェック式で「事前分布を参考にする / しない」を切り替え可能にする
- [x] 事前分布ON時は、均等時との比較など「事前分布が結果に与えた影響」を可視化する

## 信頼性・検証

- [x] シミュレーションによる自己検証
  - 既知の設定の確率値で疑似データを生成し、ツールが正しい設定を最上位に出すかを全設定×多数回で確認する
- [x] サンプル数（総G数）に応じた信頼度表示
- [x] Phase 1の時点で、事前分布のデータ形式（設定1〜6の重み＋出典ラベル）を確定させておく
  - Phase 3の連携時に手戻りを発生させないため

---

---

# 1.5 実戦記録・設定推測ツール（juggler-record.html）

ホールで打ちながら使う統合ツール。設定判別ツール（settei-estimator.html）は事後の一括計算用として併存させる。

## 実装済み

- [x] 2タブ構成（カウント / 設定推測）+ 現在地サマリー常時表示（消化G・BIG/REG・投資）
- [x] データカウンタ（打ち始め時点）入力。現在値はデータGのみ入力し、ボーナスは自分のカウントから算出
- [x] タップカウント6ボタン: ぶどう・単独BIG・チェリー重複BIG / チェリー・単独REG・チェリー重複REG
- [x] 不明BIG/REGボタン。不明分がある種別は内訳ではなく合算で計算に切り替え（統計的整合のため）
- [x] チェリーは記録・確率表示のみ（設定2〜5の数値が未公開のため判別には不使用）
- [x] 元に戻す（カウント・投資共通のundoログ）
- [x] 投資記録（+500円/+1000円、貸レート選択、推定持ち枚数から推定差枚・等価換算を表示）
- [x] 区間分割ベイズ推定（打ち始め前データ + 自分の実戦区間）
- [x] セッションの端末内保存（打ち始めスナップショット含む。Phase 2の台単位データ形式の母体）
- [x] カウントタブの固定全画面化（スクロールなし）。数値入力は電卓パッド（初期値0なし・即入力可）
- [x] カウンター長押しで数値の直接入力（誤リセット時の復旧用）
- [x] 入力エラーは実際の値と修正すべき欄を明示する文言に変更
- [x] 「計算に使うカウント」トグル（ぶどう／ボーナス内訳）。オフ時は記録のみ・推測には不使用。部分カウントによる推定の歪みを防ぐ
- [x] カウントボタンの高さ圧縮と投資エリアの引き上げ、最頻項目ぶどうを親指ホームポジション（下段左）へ配置。片手持ちの親指可動域を基準にボタンを並べる
- [x] 投資をカード残高方式へ変更（yutime-v3と同仕様）。「入金」でカード残を追加、「−1000円」でカード残−1000・投資+1000。入金は投資に含めない。カード未使用なら投資のみ記録
- [x] カウントタブに「クリア」ボタン。カウントのみクリア（台データ・投資は保持）と、すべてクリア（新しい台）の2択モーダル
- [x] カウントのみクリアを「元に戻す」で復元可能に（クリア前スナップショットをundoログに保持）
- [x] すべてクリアも「元に戻す」で復元可能に（保存削除ではなく全状態スナップショット方式へ変更）
- [x] 上部サマリーから「最有力」を削除（点推定の常時表示は不安を煽るため。設定推測タブで信頼度と併せて表示）
- [x] 打ち始め入力の強調（「最初に入力」バッジ＋未入力時のパネル注意枠）
- [x] 全タップ要素を48px以上に統一（タブ・不明BIG/REG・元に戻す・クリア・投資系・数値フィールド・電卓決定）。6ボタン領域は23dvhに再配分し固定全画面を維持。720px以下の小型画面は44px下限で圧縮表示
- [x] タブ切替の視認性改善（選択中はランプ赤の下線、非選択側は背景・文字を沈める、アイコン付与 ✚/📊）
- [x] 投資額・カード残高をタップで電卓修正可能に（押し忘れの事後修正用。持ち枚数と同じ操作性）
- [x] カウントボタンのJSON駆動化（機種データの ui.count_buttons から並び・ラベル・色・記録専用フラグを読んで生成。機種追加はJSON1本で完結）
- [x] 機種選択UI（ヘッダの機種名タップで切替モーダル。記録は機種ごとに分離保存）
- [x] 2機種目: SアイムジャグラーEX（6号機）追加。REGが設定5・6同値のためぶどう(設定6のみ1/5.80)が6判別の主役になる構造をJSONで表現
- [x] 打ち始めパネルの折りたたみ（現在G入力後は1行サマリーに縮小。修正は投資エリアの「初期登録」ボタンから）
- [x] 現在のデータGを主役化（中央寄せ・大型数字・アクセント枠）

- [x] 3機種目: Sゴーゴージャグラー3（6号機）追加。REG合計が最大の判別要素（設定1と6で約1.51倍）だが設定5・6が近接。ぶどうは全設定に段階的な設定差があり補助要素として有効。単独ボーナスは前作比率からの推定値のため信頼度区分を derived_estimate として明示

- [x] 4機種目: Sファンキージャグラー2（6号機）追加。REG合計が設定1と6で約1.68倍と全設定単調で判別しやすい。内訳はアプリ算出値をそのまま使うと公表合算値と最大約12%乖離するため、公表値をアンカーに「公表合算 − チェリー重複」で単独ボーナスを算出（derived_estimate）。第三者の独立推定値と概ね一致することを確認済み

- [x] 実戦値パネルのJSON駆動化。BIG・REG・合算の固定3項目に加え、ui.count_buttons の全項目を自動生成して確率表示する。記録専用項目は「（参考）」を付記。機種ごとにカウント項目を増やしても推測タブに自動で反映される

- [x] ベル・ピエロを全機種にカウントボタンとして追加（record_only）。判別要素としては引き続き不採用だが、稼働ノートとしての記録価値を優先（2026-08-23 方針変更）。確率値は出典未確認のため機種JSONに記載せず、実戦値のみ表示する
- [x] 8ボタン3行に対応した高さ再配分（通常30dvh・最大280px、小型画面26dvh・最大200px）。3行時も1ボタン48px以上を確保し固定全画面を維持

- [x] 打ち始めパネルを常時折りたたみに変更。未入力時は「打ち始めデータ未入力／タップして入力」の1行を警告色で表示し、行自体をタップで展開できる。未入力時に投資エリアが下へ押し出される問題を解消
- [x] カウントボタンの表示カスタム機能。記録専用ボタン（チェリー・ベル・ピエロ）を推測タブのトグルで表示/非表示にできる。非表示にしてもカウント値は保持され、判別計算には影響しない。判別に使う項目は常に表示

- [x] カウントボタンのラベルを2行化（BIG/単独、BIG/🍒重複 のように種別と内訳を分離）。文字を拡大し視認性を改善。表示名は機種JSONの label_top / label_sub で定義
- [x] 設定推測タブの並び替え。判別結果を最上部へ移動し、タブを開いてすぐ結果が見えるようにした
- [x] 収支計算の修正。開始時の持ちメダルを差枚から控除するようにし（未控除のバグを修正）、交換率（等価20円/18.5/17.9/16.6）を選択可能にした。差枚には計算式の内訳を併記して検算できるようにした

- [x] 5機種目: SハッピージャグラーVⅢ（6号機）追加。REG合計が設定1と6で約1.55倍と全設定単調で判別しやすく、BR比率（設定6で約1:1）も材料になる。内訳は公表値アンカー方式で算出し、単独REGが第三者の独立推定値とほぼ完全一致（設定差1.49倍）することを確認。中段チェリー（1/6553.6・BIG確定）は実戦で角チェリー重複と区別できないため cherry_big に合成済み。ベル・ピエロは設定ごとの数値が判明しているが単調性がないため記録専用

- [x] カウントタブのヒント行（タップで+1／長押しでまとめて入力・直前の操作表示）を、3列グリッドに余りセルができる場合はその空きに配置するよう変更。ボタンが8個（3行）のときに行が押し出されて見えなくなる問題を解消し、余白も有効活用する

- [x] 6機種目: SネオアイムジャグラーEX（6号機）追加。アイムEXと同一スペックの後継機で、REG設定差1.72倍・単独REG1.75倍とシリーズ最大級。ただしBIGはほぼ横ばい（1.07倍）で設定5・6はREGが同値のため、設定6の判別にはぶどう（設定1〜5共通1/6.02、設定6のみ1/5.78）が事実上必須。内訳は前作比率からの推定値だが、合成値が公表合算値と誤差0.03%未満で一致するためアンカー調整なしでそのまま採用

- [x] 7機種目: Sウルトラミラクルジャグラー（6号機）追加。前作の変則スペックが解消され、BIG（1.24倍）・REG（1.53倍）とも全設定単調に軽くなる素直な構成。チェリー重複BIGの設定差1.45倍はシリーズの中では例外的に大きい。ぶどうは設定1〜4が同値で設定5・6のみ優遇。非重複チェリーも全設定の数値が判明しており設定差があるが、入力経路が未対応のため記録専用

- [x] 機種選択モーダルのリストに高さ上限（画面の60%）と内部スクロールを設定。機種が増えてもモーダル全体が画面からはみ出さないようにした。10機種以上でも収まることを試算で確認

- [x] 8機種目: SジャグラーガールズSS（6号機）追加。BIG（1.21倍）・REG（1.51倍）とも全設定単調だが、REGの設定差幅はシリーズの中では狭い部類で判別には試行が必要。ぶどうは設定1〜4が同値で設定5・6のみ優遇。BIG/REG個別値は juggler7 と けんのスロットシミュレーション の2ソースで一致を確認（juggler7の合算欄は個別値と食い違うため不採用）。チェリー重複は公表合算と単独の差から算出

- [x] 9機種目: Sミスタージャグラー（6号機）追加。これで6号機ジャグラーシリーズ全機種に対応完了。シリーズで唯一チェリーに設定差がなく（全設定1/37.24）、代わりにぶどうが全設定単調（1/6.29〜1/5.96）という特殊な構成。設定6はBIGとREGが同確率（1/237.4）でBR比率1:1が看破材料
- [x] 内訳（単独・チェリー重複）の確率値を持たない機種への対応。ミスタージャグラーは対応する5号機が存在せず推定値も算出できないため、内訳ボタンを記録専用にしたうえで、カウントした値はBIG・REGの合算として判別に反映する仕組みを buildObservation に実装（hasElement による分岐と undefined 対策）

- [x] マイジャグラーVとアイムジャグラーEXの内訳を公表値アンカー方式へ統一。この2機種はファンキー2以降で確立した方式より前に作成しており、アプリ算出の単独ボーナス値をそのまま採用していたため内訳合計が公表合算値と乖離していた（マイジャグのBIGで最大-14.4%、アイムのREGで-5.9%）。「公表合算 − チェリー重複」で単独を再算出し、全機種で誤差0.03%未満の整合を達成。マイジャグの単独REG設定差は1.88倍→2.07倍に向上し、特殊役によるBIG当選分も単独BIGに含まれる実戦の数え方と一致するようになった

- [x] ガイドページ（juggler-guide.html）作成。ツールの目的、基本の流れ、カウントの取り方、投資と収支の記録、設定推測の読み方（事前分布・信頼度の目安）、数値の出典区分、FAQを掲載。特に「数えるなら最後まで、数えないなら0のまま」という最重要の注意点と、確率であって断定ではないという限界を明記。既存の yoshimune-guide.html と同一の体裁

- [x] 投資エリアの整理（実戦フィードバック）。差枚表示が画面端で途切れる問題に対応し、(1)「開始持ちメダル」を打ち始めパネル内へ移設（初期登録の一部として自然な位置に）、(2)「初期登録」チップを廃止し打ち始めパネル側に「閉じる」ボタンを設置（展開はサマリー行タップ）、(3)「入金」ボタンにカード残高を表示して統合（タップで入金、モーダル内の「残高を直接修正」で修正）。投資行2が2チップになり、差枚と内訳が全幅で折り返せるようになった
- [x] 貸メダル枚数と交換枚数を選択式から数値入力（電卓）に変更。3種類・4種類の固定選択肢では対応できない店が多いため、任意の値を入れられるようにした。交換率は「円/枚」ではなく「1,000円分にするのに必要な枚数」で保持し、ホールの表示（〇枚交換）と直接対応する。等価は50枚。旧形式（円/枚）で保存された端末のデータは読み込み時に自動で枚数へ変換する

- [x] 差枚表示の途切れを解消。投資行の .sum に後勝ちのCSS（flex:1 / white-space:nowrap / text-overflow:ellipsis）が残っており、全幅化の指定を上書きしていた。定義を1本化し全幅・折り返し可に変更。360px端末でも差枚と計算式が省略されずに表示される

- [x] ピエロ重複ボーナスの記録対応（ミスタージャグラー）。ピエロボタンの長押しで「ピエロのみ／ピエロ＋BIG／ピエロ＋REG／まとめて入力」を選べる選択シートを追加し、ボタンを増やさずに記録できるようにした。ピエロ重複を記録してもピエロのみの回数には加算しない（チェリー重複と同じ扱い）。設定推測には使わないが、将来解析値が公開された際にそのまま判別へ組み込める形でデータを残す
- [x] ボーナス総数の集計を一般化。従来は「単独＋チェリー重複＋不明」の決め打ちだったが、機種JSONの count_buttons と sub_counts の group 指定から BIG系／REG系を集約する方式に変更。ピエロ重複が自動的にBIG・REGの総数へ算入される。確率値を持たない重複役が記録されている場合は内訳ではなく合算で計算する安全側の分岐も追加。既存9機種の挙動が変わらないことを検証済み

- [x] 結果カード画像の生成。設定推測タブから、その時点の判別結果を1080×1080のPNGとして作成できる。載せるのは機種名・日付・総回転数・BIG/REG・合算・ぶどう・全6設定の事後確率・信頼度・注意書きのみで、収支と店名は含めない（夢爽の判断 2026-08-28）。端末内のCanvasで生成し外部送信はしない。点推定が独り歩きしないよう、G数・信頼度・「断定するものではない」という注記を必ず画像に焼き込む
- [x] myjuggler5-checker.html（共通エンジン版カウンター）を正式にリタイア。判別性能は juggler-record が全面的に上位で、唯一の独自機能だった共有カードも juggler-record に実装したため役目を終えた。導線からは削除済み、ファイルは404回避のため残置（2026-09-08 sitemap から除外・noindex・juggler-record への案内を追加）

- [x] 結果カードを設定判別ツール（checker系）と同じ意匠に統一。背景・ピンクの発光枠・M PLUS 1pフォント・見出し「SETTING CHECK RESULT」・機種名・日付+G数・右上の丸アイコン・4ブロックの実戦データ・フッタ（slot-tools.jp＋ハッシュタグ＋解析出典）まで揃えた。判別結果は6設定すべてを横バーで表示
- [x] カードに画像を入れられるようにした。「画像を選ぶ」から端末の画像を右上の丸枠に配置でき、256pxへ縮小して機種ごとに端末内保存する。「画像を外す」で解除
- [x] カードのテキスト出力。「テキスト」ボタンで機種名・日付・G数・BIG/REG・合算・ぶどう・全6設定の確率・信頼度・ハッシュタグをクリップボードへコピーする（約200字でXの上限内）。収支帳への貼り付けにも使える
- [x] カードの「長押しで保存できます」という常時表示の案内を削除。「保存する」で保存できるため不要で、保存に失敗した場合のみ案内を出す方式に変更
- [x] 内訳の解析値を持たない機種（ミスタージャグラー）で「ボーナス内訳」トグルが実質無効な件に対応。トグル自体は残したまま、切り替えても結果が変わらない理由を注記として表示する

- [x] マイログ（実戦記録ツール側）。設定推測タブに「マイログに保存して次へ」を追加し、店名・台番号・イベント名・台全体の差枚（概算）・メモを入れて1セッションを記録できる。店名とイベント名は過去の入力から選択できる。保存後はカウントをクリアし、直後なら「元に戻す」で復元できる（貸出枚数と交換枚数は次の台へ引き継ぐ）
- [x] マイログの記録内容。日付・店名・台番号・イベント／打ち始め（総G・BIG・REG・開始持ち）／打ち終わりの台全体（総G・BIG・REG・差枚は「約」表記の任意入力）／自分の区間（消化G・カウント全項目）／判別結果（6設定の確率・信頼度）／収支（投資・持ち枚数・差枚・貸出・交換枚数・換算額）／メモ。台全体のBIG・REGは判別結果の有無に関わらずカウントから算出する
- [x] マイログの一覧表示（ツール内モーダル）。「この機種のみ」「この店のみ」で絞り込め、打ちながら過去の記録を参照できる。記録は個別に削除可能
- [x] マイログの書き出し・読み込み（JSON）。localStorageは端末のデータ削除やブラウザの制限で失われうるため、最初から書き出しを備える。読み込みは追記型で、同じidの記録は重複させない
- [x] 名称を「マイログ」に決定（2026-08-28 夢爽の判断）。サイト名「スロット稼働ノート」と混同せず、解析値を見る場所ではなく自分が打ってきた結果が蓄積される自分専用の場所であることを表す。将来AT機などへ広げてもそのまま使える

- [x] 書き出しファイル名の日付をローカル時刻基準に統一。マイログのJSONと結果カードのPNGのファイル名がUTC基準だったため、日本時間の0〜9時に書き出すと前日の日付になっていた（深夜まで打ってそのまま書き出す使い方で1日ずれる）。localDateStamp() を新設して記録内の日付と共通化

- [x] マイログの一覧・集計ページ（mylog.html）。機種・店舗・期間（月単位）で絞り込み、全体集計（記録数・総G数・差枚合計・収支合計・勝率・BIG/REG合算）と、機種別・店舗別・月別の集計表を表示する。記録一覧からの個別削除、書き出し・読み込みにも対応。実戦記録ツールと同じ localStorage（juggler-mylog:v1）を読むため二重管理にならない
- [x] 集計の設計判断。差枚・収支・勝率は持ち枚数を入力した記録だけを母数にする（未入力の記録を0として扱うと収支が実態より悪く見えるため）。BIG/REGは打ち終わりから打ち始めを引いた自分の区間の回数のみを集計する
- [x] mylog.html は noindex とし、sitemap には登録しない。自分専用の記録置き場であり検索から入る性質のページではないため

- [x] マイログの日付を保存時に指定できるようにした。既定は当日だが変更できるため、過去の稼働をあとから記録することもできる
- [x] マイログの記録を編集できるようにした（mylog.html）。日付・店名・台番号・イベント・打ち始めと打ち終わりの台データ・台全体の差枚・投資・持ち枚数・貸出・交換枚数・メモを修正でき、差枚と収支は入力から再計算する。小役のカウントと判別結果は記録時の値のまま残す（再計算の根拠がないため）
- [x] マイログの集計に時給・BIG確率・REG確率を追加。時給は1時間を800回転として収支を回した時間で割る。機械割は投入枚数の数え方（リプレイを含めるか）で約2%変わり、確認しきれないリプレイ確率が必要になるため採用せず、収支と回転数だけで出せる時給を選んだ
- [x] 設定推測タブのセクション見出しを見やすくした。文字を大きくして本文色にし、左に赤いラインを引いて区切りを明確化。各見出しに役割の一文を添え、目的の項目を探しやすくした
- [x] 結果カードの既定アイコンをサイト共通のキャラクター画像（assets/toaru2-default-icon.jpg）に設定。画像を選んでいない状態でも枠が埋まる。「画像を外す」を押すと既定へ戻る

- [x] 事前分布のトグルのタップ領域を48px（小型画面44px）に拡大。UI設計原則の下限44pxを下回る24pxのままだったのを是正。チェックボックス自体も22pxに拡大

- [x] タップ領域の全面点検と是正。設定推測タブの操作ボタン（.use-btn 13個）を42px→48px（小型画面44px）、「新しい台（すべてリセット）」を35px→同様に、マイログ一覧の「編集」「削除」を32px→44pxで横並びに、mylog.htmlのヘッダリンクを30px→44pxに拡大。あわせて .prior-toggle の旧ルール（18px指定）を削除
- [x] 実戦記録ツールのヘッダリンク（TOP・使い方）は17pxのまま据え置く判断。カウント中の誤タップでページ遷移すると記録中のデータから離れてしまうため、押しやすさより誤タップ防止を優先する。固定全画面の縦の余白を確保する意味もある。WCAG 2.5.5 のインライン例外にも該当する

- [x] 打ち始めデータの連続入力。総回転数→BIG→REG→開始持ちメダルの4項目を、電卓の「次へ」で続けて入力できるようにした。項目ごとに電卓を開き直す手間がなくなる。電卓には「打ち始めの入力 1／4」の進捗を表示する
- [x] 打ち始めの総回転数を入力すると「現在のデータG」にも自動で反映するようにした（現在Gが打ち始めより小さい場合のみ）。座った直後に消化0Gの状態で台データによる判別がすぐ見られるようになり、現在Gを別途入力する手間がなくなる

- [x] マイホレポ（myhorepo.html）を新規作成。ホールの台データ（日付・イベント・機種・台番号・G数・BIG・REG・差枚）を記録し、店全体の傾向を比べられる。名称は夢爽の判断（2026-08-30）。マイログ＝自分が打った台の記録／マイホレポ＝自分がチェックしているホールの記録、という対比
- [x] マイホレポの保存先はIndexedDB。localStorageは1件121文字でも3店舗×50台/日で約5ヶ月で上限に達するため、最初からIndexedDBを採用した。マイログ（localStorage）とは別管理
- [x] マイホレポの入力方式（サイトセブン方式）。日付・店名・イベント・機種を一度指定し、以降は台番号・G数・BIG・REG・差枚を連続入力。追加するたび台番号が自動で+1され、その日の登録済み一覧がその場で確認できる。差枚はサイトセブンで取得できないため任意入力
- [x] マイホレポの分析。各台を判別エンジンにかけて事後分布を出し、店全体の「平均設定」と「設定4以上の割合」を算出する。この日／いつも／平日／同じ曜日／前回の同じイベント、を同じ条件で並べて比較する。日別一覧と台番号別の傾向も表示
- [x] 集計の設計判断。データが少ない台は判別が定まらず平均を基準値3.5へ引き寄せるため、(1)1000G未満を除外する、(2)回転数で重み付けする、の2つを既定でオンにし、切り替えられるようにした。基準線（平均設定3.5／設定4以上50%）を常に併記し、絶対値ではなく「いつもと比べてどうか」で読む指標であることを明示
- [x] マイログからの取り込み。自分が打った台は両方に該当するため、マイログの記録（店名・台番号・台データが揃っているもの）をマイホレポへ流し込める。二重入力が発生しない
- [-] 機械割シミュレーターは取りやめ（2026-08-30）。公開されている機械割は「メーカー公表の出玉率から逆算したぶどう確率」を前提にしており、判別に使うアプリ実測値とは別物だった。同じ機種で2種類のぶどう確率を使い分けることになり説明が破綻するうえ、他者の推定値を中核データにすることになるため
- [x] マイホレポに故障・非稼働の記録を追加。個別入力のチェックボックスで指定でき、一括入力ではG数0の行を自動で非稼働として扱う。集計からは常に除外され、一覧には「故障・非稼働」と表示される。サイトセブンで0Gと表示される台をそのまま貼り付けられる
- [x] マイホレポの故障・非稼働チェックは、登録のたびに自動で外れる。チェックしたまま次の台を登録して意図せず非稼働になるのを防ぐ
- [x] マイホレポに一括入力欄を追加。1行に1台分を「G数 BIG REG（差枚）」のスペース区切りで打ち、まとめて登録できる。台番号は先頭の番号から自動で連番になり、飛び番があるときは「各行の先頭に台番号を書く」に切り替えられる。区切りは半角・全角スペース・カンマ・タブに対応し、項目数の過不足や数字以外は行番号付きで指摘する
- [x] マイホレポに入力を楽にするコツを掲載。Androidの分割画面の手順（履歴ボタン→アイコン→分割画面）と機種ごとの補足、iPhoneには画面分割がないため一括入力を使う案内
- [x] 画像から自動で読み取る機能（OCR）は採用しない方針を確認（2026-08-30）。データサイトの規約は機械的な取得を禁じていることが多く、ツール側にその機能を持つこと自体が自動取得を助長する仕組みと見なされうるため。転記は人が行い、ツールは打鍵回数を減らす方向で支援する

## 検討中・将来


- [ ] セッション履歴の保存・一覧（1日複数台の記録） → Phase 2の入力口を兼ねる
- [~] 残数スナップショット方式による収支管理（SF6ツールで確立したモデルの移植）。財布（現金・持ちメダル・再プレイ）と台（クレジット）を分離し、「今いくらあるか」の残数で差枚を算出する。投入記録は不要になり記録し忘れによる事故が構造的に起きない。第一段階として開始持ちメダルの控除と交換率選択を実装済み（2026-08-24）。クレジット・再プレイ・回収の分離は今後
- [ ] セッション収支のカード保存と分析（機種別・店別・日別の勝敗集計）→ Phase 2の店舗データと統合
- [ ] 非重複チェリーを判別要素として使えるようにする。ハッピージャグラーVⅢは角チェリーに明確な設定差（高設定ほど重い逆方向）があるが、現行の入力経路が対応していないため記録専用にとどめている
- [ ] 実戦結果・判別結果のX（SNS）共有機能 — 別途構想中（アイデア★パチスロ開発側の議論が固まってから着手。共有フォーマットの手戻り防止のため保留）
- [ ] 打ち始めパネルの自動折りたたみ。現在は電卓で確定しても畳まれず「初期登録」のタップが必要。3項目を続けて入力する運用では自動で畳まない方が自然という判断だが、実戦での使用感を見て再検討する

---

# 1.6 汎用 実戦記録ツール（tools/rec/）

## tools/rec 実戦記録ツール：お金の記録形式と移行規則

### 現行形式（rec7以降）
- `money.deposits` … 入金（サンドに入れた現金）
- `money.spends` … メダル貸出（＝投資）
- 投資 = sum(spends) ／ 残高 = sum(deposits) − sum(spends)
- 残高が1,000円未満で「−1000」を押した場合、同額を deposits にも自動追加する
  （サンドに入れず1,000円ずつ使う打ち方に対応するため）

### 再プレイの扱い（rec25以降）
再プレイは「貯メダルから途中で入れる投入」として `money.replays[]` に履歴を持つ。
開始時・終了時の再プレイ欄は廃止。開始時は持ちメダルのみ、終了時は
貯メダルに預けた分も含めた手元の総枚数を入れる。

- 差枚 ＝ 終了持ち − 開始持ち − 現金で買った枚数 − 再プレイ枚数
- 収支 ＝（終了持ち − 開始持ち − 再プレイ枚数）× 1000/lend − 現金

上限が低い店で「一気におろして持ち玉扱い」にしても、開始持ちに含めれば計算は同じ。

旧形式（`startReplay` / `endReplay`）は起動時に `fixMoney()` が変換する：
`startReplay` → `replays` に1件、`endReplay` → `endMedal` に加算。計算結果は変わらない。

kado-note に渡す `payout` は「終了持ち − 開始持ち − 再プレイ」の純増枚数。
rec22 までは終了持ちをそのまま渡していたため、開始持ちがある台で収支がずれていた。

### 旧形式（rec4以前）からの移行
旧形式は `money.cash` のみを持ち、入金と使用を区別していない。
移行は `fixMoney()` が起動時に一度だけ行い、結果を保存する。

規則：
1. `Math.abs(sum(cash))` を求める
2. その額を deposits と spends の両方に入れる（残高0）

符号を無視する理由：rec4 は入金を正、「−1000」を負で記録していたため、
符号に意味がない。−1000 のみで打った台は合計が負になるが、
使った額は変わらないため絶対値で拾う。

限界：入金と使用が混在した台は正確な復元ができない。
rec4 の画面表示と同じ値になるよう、単純合算の絶対値を採用している。
（例：+3,000 と −1,000 の台 → 投資2,000円。実態は投資1,000円の可能性あり）

---

## tools/rec 実戦記録ツール：ログ表記と kado-note 連携

### ログのゲーム数表記（rec13以降）

ログ1行の左に出すゲーム数は、実Gと液晶Gの関係で表記を変える。

| 実G | 液晶G | 表記 |
|---|---|---|
| 80 | 80 | `80` |
| 57 | 86 | `57/86` |
| 80 | なし | `80` |
| なし | 86 | `86` |

液晶Gを持たない機種（沖ドキ等）でも実Gが表示される。
`gsText()`（テキスト用）と `gsHtml()`（画面用・液晶G側に色）の2つを用意し、
画面・テキスト書き出し・kado-note用出力のすべてで同じ表記に揃える。

### kado-note 連携（rec13以降）

rec は schemaVer 2 のエンベロープを出力できる。変換は `toEnvelope()`。

| エンベロープ | rec から入れる値 |
|---|---|
| sessionId | rec の セッションid（毎回同じ値。重複検出のため） |
| machineId | 機種名（日本語は toKebab() で落ちる。下記の制約参照） |
| payloadVer | `"rec-2"` |
| invest | 投資額（円）＝ `invested(money)` |
| payout | 純増枚数 ＝ 終了持ち − 開始持ち − 再プレイ（枚。rec25以降。それ以前は終了持ちをそのまま渡していた） |
| lendRate / exchangeRate | レート（等価前提で同じ値を入れる） |
| memo | 機種名 ／ 想定期待値 ／ 差枚 |
| payload.logs | `{t: epoch_ms, type:"memo", text:"57/86G　強🍒 (1回目)"}` |

payload には機種名・開始終了のG数・持ちメダル・想定期待値・レア役回数も残す。

出力は2種類。どちらも kado-note で取り込める（kado-note 側で配列対応済み）。
- 各台の「この台をノート用に保存」… 単一オブジェクト
- 「同・保存」… 全台の配列。1回の取り込みで複数台が入る

### kado-note 側の対応（55feb5d）

rec 連携のために kado-note.html を3点改修した。

1. `normalizeSchemaV2()` / `isLegacySession()` に `machineLabel` を追加し、
   元の機種名文字列を保持する
2. `machineName()` は、機種名にASCII以外の文字が含まれる場合、
   `machineLabel`（なければ `payload.machine`）を表示する。
   日本語機種名が `unknown-machine` と表示される問題はこれで解消
3. `importFiles()` が配列に対応。1ファイルに複数セッションが入っていても読む。
   途中の要素が壊れていても他は取り込まれ、エラーには `ファイル名[n]` で位置が出る

SF6 の描画は改修前後でDOMがバイト一致することを確認済み。

残る制約：
- 旧版で取り込み済みの日本語機種名セッションは、保存時点で機種名が失われて
  いるため名前が戻らない。該当データは取り込み直しが必要
- `toKebab()` はASCII以外を落とすため、機種名が短い語に潰れることがある
  （例：「沖ドキDUOアンコール」→ `duo`）。表示は machineLabel で正しく出るが、
  **machineId は機種の同一性を判定する値としては使えない**。
  機種別の集計を行う場合は machineId の生成方法から見直すこと

## tools/rec 実戦記録ツール：記録の修正

### 修正の入口（rec23以降）
- 進行中：投資・回収・出力 →「打ち始めの情報を直す」「お金の履歴を直す」
- 終了後：ノートの各台 →「打ち始め・終了の情報を直す」「お金の履歴を直す」「記録の行を直す」

`editInfo` / `editMoney` / `editEventIn` は進行中・終了後の両方で同じ関数を使う。
お金の履歴を消しても記録のログ行は残る（ログは事実の記録、履歴は計算用）。

### フォロー期待値（rec30以降）

打ち始めに入れた想定期待値とは別に、途中で状況が変わった分を session.follow[] に
{ at, amount, note } で積む。「投資・回収・出力」→「フォローで追加」。
想定の合計は expectedTotal() ＝ expected ＋ follow の合計。
ノートの集計・テキスト出力・kado-note のメモはすべて合計を使う。
payload には expected（最初の値）と follow[]、expectedTotal を分けて残す。

---

## tools/rec 実戦記録ツール：機種定義の約束事

### 前兆の自動終了（rec18以降・全機種共通）

前兆はCZ／AT当選と同時に終わる。当選を記録した時点で前兆バッジを消し、
当選ログに「前兆から」と付記する。

### 当選種別ごとの画面（rec20以降・全機種共通）

機種定義 hits[] の各当選に grid を持たせると、その当選中はそのボタンだけを出す。
grid が無い当選は grids[kind]（cz／at）を使う。
CZ中に通常時の操作（ステージ・運命盤など）が出ないようにするための仕組み。

### AT後の流れ（rec21以降）

機種定義 afterAt:{hit, trig} があると、AT終了シートの確定ボタンが
「終了する」ではなく「<hit>へ（AT後）」になり、その当選に自動で移る。
からくりサーカス2は afterAt:{hit:"幕間チャンス",trig:"AT後"}。

AT終了シートで記録するのは AT終了時に見えるもの（1枚絵・ボイス）だけ。
ワイプ演出・開始ステージは AT後のCZ失敗時に czEnd で記録する。

### SHOW TIME復活（rec20以降）

機種定義 revive:{label} があると、AT終了シートに「<label>した（AT継続）」が出る。
押すと終了画面の記録を残したまま AT中に戻り、session.revive を +1 する。
解析にはないが設定差が噂されるため、回数を数える。

### 当選定義の構造（rec22以降）

機種定義 hits[] の各要素が持てる項目。
- `kind` … "cz" か "at"
- `hidden` … true なら当選ボタンに出さない（流れの中で自動的に入るもの。例：AT、運命の一劇）
- `enter` … 別の当選定義で開始する（例：AT直撃 は enter:"AT"。ログには「AT直撃 当選」と残る）
- `inline` … true なら当選種別を選んだ直後に終了シートを開く（BIG/REGのような短いボーナス用）
- `keepG` … 終了シートのG数欄の既定値を現在値にする（当選中もデータカウンターが
  進むCZ。→「当選時のG数の扱い」）
- `grid` … その当選中に出すボタン。無ければ grids[kind]
- `czEnd` … CZ終了シートの中身。"std" なら機種の czEndStd を使う。
  { groups, successToAt:"<当選ラベル>", failNote } で個別指定もできる
- `atStart` / `atEnd` … AT開始時・終了時に聞く項目。null で聞かない
- `afterAt` … AT終了後に自動で入る当選 { hit, trig }。無ければ「終了する」
- `revive` … { label } があると AT終了シートに復活ボタンが出る。null で出さない
- `phases` … AT内のサブ状態 { key:{ label, grid, afterAt:{ hit, trig, skipEnd } } }。
  skipEnd:true なら終了画面を出さずに afterAt へ

グリッドのボタン指定：
- `phase:<key>` … サブ状態に入る
- `goto:<当選ラベル>:<契機>` … 今の当選を終えて別の当選に入る（確認あり）
- `atx:<n>` … 機種の atExtras[n]

並行状態（parallel[]）の追加キー：
- `resetCountOnHitEnd` … true なら「n回目」の番号をCZ／AT終了時に0に戻す。
  からくりサーカス2の運命盤は、CZ／ATを抜けるとスイカ規定回数の数え直しが始まるため付ける

選択肢の書き方（groupSheet / atEndSheet 共通）：
- 文字列 … ラベルだけ
- `[ラベル, 補足]` … 補足を小さく表示
- `{ l, h, set }` … set に示唆を持たせる。選ぶと applyHints() が session に反映する
  - `set:{melonLeft:n}` … 🍉残りn。`approx:true` で「n以下」
  - `set:{reward:"AT以上"}` … 次回の運命盤報酬
  - `set:{mode:"通常C濃厚"}` … モード示唆

CZ成功からATに入るとき、直前のCZの契機が「AT後」なら AT の契機を「引き戻し」にする。
集計で「通常時からのAT当選」と区別するため。

### 当選時のG数の扱い（rec27以降・全機種共通）

当選時に実G・液晶Gは触らない。終了時に「今の実G／今の液晶G」を入力して合わせる。
sf6-record.html と同じ考え方。当選ボタンを押すタイミングと実際の当選G数がずれても、
終了時に台の表示を見て入れれば合う。

終了シートのG数欄の既定値：
- 通常のCZ・AT・ボーナス … 0（終了後は0から数え直す）
- `keepG:true` の当選（幕間チャンス・劇場ジャッジ）… 現在値（通常時のG数がそのまま進む）
- CZ成功 … G数欄を隠して自動で0（次の当選が始まるため）
- ED→運命の一劇（skipEnd）… 自動で0

keepG は「当選中もデータカウンターが進むCZ」に付ける。新しい機種を定義する時は
CZごとに必ず確認する。

### 液晶に出る残り回数の追跡（rec30以降）

session.melonLeft に「🍉残り回数」を持ち、バッジ行に「🍉残りn」と出し続ける。
- 「🍉残り」ボタン（3択）、またはセリフ・ボイスの示唆（→「示唆のバッジ化」）で設定
- 🍉を登録するごとに1減る。0で「🍉規定到達」
- 運命盤の発動、CZ／ATの終了で null に戻す（消える）
- 取り消しで🍉を戻すと1増える

液晶が示す「残り回数」を見た時点で記録するための仕組み。引く前に当たっても
サンプルとして残る。他の機種で同じ性質の表示（残り○回、○回目でCZ など）があれば
同じ形で持たせる。

### 示唆のバッジ化（rec31以降）

セリフ・ボイスの示唆は、選んだ時点でバッジに反映する（session.melonLeft / rewardHint / modeHint）。
消えるタイミングはそれぞれ違う。
- 🍉残り、モード示唆 … CZ／ATを抜けたとき（規定回数・モードが変わるため）
- 次回報酬 … 運命盤の発動まで残る（CZ／ATをまたいでも報酬は引き継ぐ）

AT終了時の反映は afterHitEnd() のあとに applyHints() を呼ぶ。逆にすると、
ボイスで入れた🍉残りが直後に消える（rec31 で実際に起きた）。

### 規定回数のリセットと打ち切り（rec34以降）

rares[] の追加キー：
- `resetOnHitEnd` … true なら CZ／AT終了で回数を0に戻す
- `cutLog` … true なら、0に戻す前に「🍉 n回で打ち切り（<終了した当選>）」を rareCut としてログに残す

からくりサーカス2の🍉に付けている。運命盤の規定回数はCZ／ATを抜けるとリセットされる
（実戦で「🍉残り1→CZ→🍉で発動せず」を確認）。報酬（rewardHint）は引き継ぐが、回数は引き継がない。

運命盤発動時は「🍉 n回目で発動」をログに残し、イベントに melonAt を持つ。
打ち切り（rareCut.n）と発動（parStart.melonAt）を分けて記録するのは、
規定回数の分布を後から推定するため。打ち切られた区間は「n回までは発動しなかった」というサンプルになる。
設定差があると噂される部分なので、集計は記録が溜まってから考える。

### 前兆の段階（rec32以降）

前兆ボタンは押すたびに進む：前兆 → 前兆ステージ → 失敗。「前兆抜け」は無い。
session.zenchou は false / "pre" / "stage" の3値。
前兆ステージに入ったらステージ表示を zenchou.stageLabel に切り替え、失敗時に戻す。
失敗シートで聞くもの（zenchou.outGroups）：最終幕の色、ワイプ演出、戻ったステージ。
ワイプ演出はステージシートには置かない。出るのは 当選終了時 と 前兆ステージ失敗時 だけ。

### 取り消しの復元（rec32以降）

状態を変える操作は、直前に mark() を呼び、次の push() が snapshot を持つ（ev.snap）。
「直前を取り消す」は snap があれば SNAP_KEYS をそのまま戻す。
当選・終了・前兆・運命盤・ステージ・レア役・セリフ・復活・サブ状態が対象。
お金の履歴（deposits / spends / replays）は snap に含めない。「お金の履歴を直す」で扱う。

### CZ終了の2ボタン（rec32以降）

CZ終了シートは「失敗」「成功」の2ボタンだけを出す。
成功 … 即 hitEnd を記録し、G数を0にして successToAt へ。終了画面は聞かない
失敗 … 終了画面セット・メモ・G数欄のシートに進む
迷う余地を無くすため。成功後の「当選→AT」の二重登録を防ぐ。

### 機種定義を作る順序（教訓）

項目の一覧より先に「状態の遷移」を描く。
からくりサーカス2では AT終了→幕間チャンス→引き戻し の流れを項目一覧に書いていながら、
遷移として組み込めておらず、実機で気づいた。

実機で1日打ったあとの見直し（rec28）で出た項目：
- 弱レア役をまとめない。レア役は基本的に全部個別に持つ（多すぎる時は相談）
- 前兆ステージ中は通常ステージと被らない。ステージ表示を「前兆ステージ」に切り替える
- ワイプ演出は前兆抜けで見る。前兆抜けシートに置く
- 示唆演出は「よく見るもの」だけ。解析にあっても意味が伝わらない項目は入れない
- 液晶に出る数字（🍉残り回数）は、見た時点で記録できる専用ボタンにする。
  引く前に当たるとサンプルが残らないため
- CZ成功なら終了画面は出ない。成功を選んだ時点で他の項目を隠す

### 遷移図はATの中の枝も描く（教訓・追記）

からくりサーカス2では、AT終了後の幕間チャンス、エンディング→運命の一劇、
上位AT→運命の一劇、激情ジャッジ3回→運命の一劇 の4本を、解析の項目一覧からは
読み取れず、実機を知る夢爽の説明で初めて遷移として組み込めた。

解析は「何がある」は書いてあるが「どの順で来る」は書いていない。
2機種目からは、洗い出しのあとに夢爽が遷移を口頭で説明する工程を挟む。
稀なルート（機械仕掛けの神、ロングフリーズ、引き戻し）は流れを作り込まず、
当選の種別と契機に名前を足して記録だけ残す。挙動が分かったら流れに昇格させる。

## tools/rec 実戦記録ツール：設定判別チェッカーへの送信

### 送信の仕組み（rec22以降）

機種定義に `checker:{ key, url, name }` があると、ノートと「投資・回収・出力」に
「チェッカーへ送る」が出る。`toChecker(session)` が events を走査して
チェッカーの状態（checker-data の DEF と同じ形）に変換し、localStorage の key に書く。
チェッカー側は無改修。起動時に localStorage を読む共通エンジンの仕組みをそのまま使う。

- 既存データがあれば「合算」「上書き」を選ぶ。元データは key+"-rec-bak" に退避
- 同じブラウザで rec とチェッカーを開く前提
- 総回転数は 打ち始めと終了時の「台の総回転数」の差。片方でも無ければ0（チェッカーで手入力）

### からくりサーカス2の集計定義

- CZ当選 ＝ 機械仕掛けの女神＋劇場ジャッジ。AT後の幕間チャンスは含まない
- AT当選 ＝ 通常時からの当選のみ。引き戻しは含まない
- 強チェリー直撃 ＝ 契機が強チェリーで機械仕掛けの女神に当選した回数（rares[].trackHit）
- 激情ジャッジ ＝ ATごとに1回目のキャラだけ
- 1枚絵・ボイス ＝ 復活時に見たものも数える
- ラベル→チェッカーのキーの対応は K2_MAP。checker-data 側の定義と全件突合済み

---

# 2. 店舗データ蓄積・分析ツール（Phase 2）

## データ設計

- [ ] 入力単位は「店の合算」ではなく「台単位」とする
  - 記録項目: 日付・イベント名・機種・総G数・BIG回数・REG回数
  - 理由: 店平均だけでは「全台設定4」と「設定1と6の混在」を区別できない
- [ ] 店平均は台単位データからの集計値として自動算出する（別途入力しない）

## 出力（3階層）

- [ ] 階層1: 店平均（合算のBIG/REG確率）
- [ ] 階層2: 設定配分の推定
  - ベタ置き型（全台中間設定）か、メリハリ型（低設定と高設定を分ける）かの傾向
  - 高設定の投入率の推定 → Phase 3で事前分布の元データになる
- [ ] 階層3: 当日速報比較
  - 今日の途中経過 vs 過去の同イベント平均で「前回より強い / 弱い」を提示
  - 総G数が閾値未満の場合は「参考値」と明示する
  - 将来案: 時刻付き記録にして「過去の同時刻帯の途中経過」と比較する（まずは終日データ比較から）

## 検討事項

- [ ] 当日データの収集手段（手入力か、転記か）と運用負荷の整理
- [ ] 開催回数が少ないイベントの配分推定は信頼できないため、件数に応じて均等分布と混ぜる（縮小推定）を検討

---

# 3. ツール間連携（Phase 3）

- [ ] 店舗分析ツールの推定配分を、判別ツールの事前分布プリセットとして選択可能にする
- [ ] 判別ツール＝計算機（状態を持たない）、店舗分析＝データ蓄積（状態を持つ）として分離を維持し、連携はデータ形式のみで行う

---

# 完了済み

（なし）
