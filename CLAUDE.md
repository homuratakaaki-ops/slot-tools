# slot-tools 開発規約

このリポジトリで作業するAIエージェントは、作業前に必ずこのファイルと `IDEAS.md` を読むこと。

## セッション開始時の確認事項（既存運用）

作業を開始する前に、以下のコマンドでGitHub Projects「slot-tools 優先度ボード」の現状を確認すること。

    & "C:\Program Files\GitHub CLI\gh.exe" project item-list 1 --owner homuratakaaki-ops --format json

確認する内容：
- 「進行可能」状態のタスクのうち、優先度が最も高いもの
- 「待ち」状態のタスクとその理由
- 依頼された作業がこのボードの項目と対応しているか

このコマンドが失敗する場合（gh未認証・権限不足・キーリング破損等）は、作業自体は通常通り進めてよいが、その旨を報告に一言添えること。

---

## 役割分担

- **設計・レビュー担当（Claude Code / シオン役）**: 仕様の確定、実装方針の決定、コードレビュー、検証、最終確認
- **実装担当（codexツール / ミコト）**: 指示に基づく実装、ファイル操作、コマンド実行
- 実装をcodexへ委任する場合は、作業ディレクトリと変更対象ファイルを明示すること
- codexの成果物は必ずレビューし、指示範囲外の変更が混入していないか差分で確認すること

## 絶対に守る原則

1. **IDEAS.md に記載された未依頼の機能を勝手に実装しない。** 実装前に必ず依頼内容を確認する
2. **計算ロジックを無断で変更しない。** `js/estimator.js` と各HTMLの `buildObservation()` は判別精度の中核。変更が必要な場合は理由を明示して提案し、承認を得てから着手する
3. **解析値を推測で埋めない。** 機種データの確率値は必ず出典を確認し、`sources` に URL・取得日・信頼区分（official / app_estimate）を記録する。数値が見つからない要素は `record_only` として記録専用にし、判別計算には使わない
4. **受け取ったファイルは原文のまま適用する。** 整形・リファクタ・コメント削除を勝手に行わない。改善提案がある場合は適用せず別途報告する

## UI設計原則（全ツール共通）

- カウント・入力系の画面は「スクロールなしの固定全画面」とする。ユーザーの操作負担を最小にすることを最優先する
- 数値入力はOSキーボードではなくページ内の電卓パッドで行う。開いた時点で入力欄は空にし、初期値0を消す手間を発生させない
- タップカウンターは長押しで数値の直接入力（まとめて入力）に対応する
- タップ可能な要素は高さ44px以上を下限、48px以上を推奨とする（WCAG 2.5.5 / Apple HIG 44pt / Google Material Design 48dp準拠）。小型画面では44pxまで圧縮を許容し、スクロールなしを優先する
- 最頻入力項目は片手持ちの親指ホームポジション（下段左）に配置する
- 不用意に不安を煽る表示を避ける。解釈を含む数値（最有力設定などの点推定）は常時表示せず、ユーザーが見に行った時に信頼度・G数などの文脈と併せて示す
- 破壊的操作（クリア等）は「元に戻す」で復元可能にする
- 固定全画面の原則はカウント・入力系の画面に適用する。選択モーダルなど項目数が増える一覧は、高さ上限を設けて内部スクロールを許容する
- 使う人によって必要性が分かれる記録項目は、既定で表示しつつ利用者側で非表示にできるようにする

## 機種の追加手順

`ui.count_buttons` によるJSON駆動化が完了しているため、機種追加はデータ追加で完結する。

1. 解析値を出典付きで調査する（メーカー公表値と実戦推定値を区別すること）
2. `data/machines/<machine_id>.json` を作成する。既存の `my_juggler_v.json` / `im_juggler_ex_6.json` と同じスキーマ（schema_version 1.1）に従う
3. `juggler-record.html` の `MACHINES` に1行追加する
4. `node test/verify.mjs` と、新機種の全設定サニティチェックを実行する
5. `IDEAS.md` の対応機種一覧を更新する

## 検証手順（変更をpushする前に必ず実行）

1. `node test/verify.mjs` → 「サニティチェック: 全設定OK / 区間分割: 全設定OK」を確認
2. HTML内の module script を抽出して `node --check` で構文確認
3. 機種JSONを変更した場合は JSON構文チェックと、判別用 `denominators` が意図通りか確認
4. `git diff --check` で行末・空白の問題がないこと
5. レイアウト変更を含む場合はブラウザで実表示を確認する（構文チェックだけでは表示崩れを検出できない）
6. push後、raw GitHub と Pages の両方で200と反映内容を確認する

## push・公開のルール

- push は自動化しない。実行前に変更内容を報告し、承認を得ること
- 差し替え作業では、指示範囲外のファイルを変更しない
- 他の作業で進んでいたコミットがある場合は、丸ごと差し替えによる巻き戻しが起きていないか確認する

## 記録

- 実装した機能・確立した設計原則は `IDEAS.md` に追記する（完了は `[x]`、進行中は `[~]`、未着手は `[ ]`）
- 実装後も項目を削除せず、完了済みとして残す
