# AGENTS.md

## §9-46: 記録系ツールは全画面式（battle mode overlay）を標準形とする

### 原則

slot-tools.jp の稼働記録系ツール（nerai-record、sf6-record、および今後追加する機種ツール）は、**全画面オーバーレイ型の実戦入力UI を標準形式**とする。縦スクロールのフォーム羅列型は新規採用しない。

基準実装は nerai-record.html の `battleModeOverlay`（`bm-*` 構造）とする。新規ツールはこの構造を写像して作る。

### 標準構成（bm-* 構造の要素）

全画面オーバーレイは以下を備える：

1. **固定ヘッダ**：機種／狙い方タイトル、閉じるボタン
2. **状態バッジ群**：現在の内部状態を常時表示、タップで状態操作
3. **二軸カウンタ**：データG（実G）／液晶G を常時大表示
4. **収支表示**：差枚／投資／クレジット等（機種に応じて）
5. **入力グリッド**：状態に応じて出し分ける主要アクションボタン
6. **直近ログ**：新しい順に1〜3件を常時表示
7. **Undo**：直近操作の取り消し（snapshot復元方式）

### 状態による画面出し分け

通常時とバトル/AT/ST相当の区間で、入力グリッドを**同一オーバーレイ内で丸ごと切り替える**（カバネリの通常時↔ST方式）。区間の開始・終了は**明示的な状態（state）で管理**し、イベントの副次情報からの推測に依存しない。

### 三点ルール（入力の必須要件）

全画面式に追加する各入力ボタンは、以下3点を満たすこと。テストで固定する。

1. **見える**：記録が直近ログに反映される
2. **取り消せる**：Undoの対象になりラベルで識別できる
3. **巻き添えで消えない**：他操作のsnapshot復元で不用意に消えない

### 保存・配色

- 入力は**タップ即保存**（snapshot登録・Undo登録・toast・draft即時保存をまとめて実行）
- 配色は §9-28 に従う。示唆チップは中立背景＋文字色を基本とし、塗り背景による強調は1〜2箇所に限定
- 機種固有色は「3色系統＋アクセント1」の範囲で定義

### 検証

- 全画面式ツールは、状態遷移（通常↔バトル区間）・三点ルール・二軸カウンタの各挙動をE2E/回帰テストで固定すること
- レンダリング確認は end-to-end（実際に描画された状態）を唯一の合格基準とする

### 補足：既存ツールの扱い

- nerai-record.html は既に本標準の基準実装。変更不要
- sf6-record.html は v2.0 で本標準へ移行する（現行の縦スクロール式を全画面式に置換）
- 以後の新規機種ツールは、最初から本標準で設計する

## §9-50: 継続状態は「状態バッジ」で管理する（全機種共通）

### 原則

実戦記録ツールにおいて、**一定期間継続する状態**（高確・前兆・ステージ・液晶色変化・CZ・バトル区間など）は、一発のイベントログではなく**状態バッジ**として扱うことを標準とする。基準実装はカバネリ（nerai-record.html の states 機構）。

イベント（レア役成立・加算・当選など瞬間の出来事）はログ、状態（〜中・〜継続）はバッジ、と区別する。

### 状態バッジの標準挙動

1. **開始**：該当の記録操作（シート選択・自動トリガ等）で状態が開始し、画面上部の**バッジ行に点灯**する。開始イベントをログにも記録する
2. **点灯中のみ表示**：バッジは点灯中の状態のみ表示し、非アクティブ状態のプレースホルダは置かない。複数状態の同時点灯は許容する
3. **終了型と遷移型**：状態には2種類ある。終了型（前兆・液晶色など「終わる」状態）はバッジタップで即終了記録。遷移型（ステージなど常にいずれかに滞在する状態）はバッジタップで選択シートを開き、次の状態へ移る。遷移型に終了操作を設けない
4. **状態全体像の洗い出し**：機種仕様策定時には、通常ステージ群・前兆・CZ・AT等の状態と、相互の遷移・排他関係を必ず洗い出す。ここを漏らすと後からバッジ挙動を修正することになる
5. **連動終了・排他クリア**：状態間に依存関係がある場合（例：前兆抜け＝液晶色のデフォルト復帰）、終了操作1回で関連する状態・イベントをまとめて記録する。排他関係にある状態は、一方の開始で他方を自動消灯する。自動クリアでは追加ログを記録せず、Undoで復元する
6. **確定的な状態変化の自動記録**：機種仕様で確定している状態変化（例：規定G到達での色変化）は gameStateTriggers 方式で自動記録・自動点灯する候補として設計時に洗い出す。自動記録には auto フラグを付け、手動記録済みの境界では重複記録しない。加算等で境界を一気に跨ぐ場合の反映タイミングも確認する
7. **永続化**：現在の状態はセッションstateとして localStorage に保持し、リロードで復元する
8. **三点ルール適用**：バッジの点灯・消灯も「見える・取り消せる・巻き添えで消えない」を満たし、Undoで状態ごと巻き戻る

### 配色

- バッジは常時視認が目的のため、§9-28 の範囲内で**文字色＋枠線**で状態を区別する（例：超高確系=紫、高確系=オレンジ、注意状態=赤系）。塗り背景は battle 等の最重要状態1つまで

### 適用

- 新規機種ツールは、継続状態を洗い出し、最初から本標準で設計する
- 既存ツールは改修のタイミングで順次本標準へ寄せる（sf6-record は v2.8 でステージ、v3.0 で前兆・液晶色に適用済み）

### 背景（記録）

本標準はカバネリ実戦モードの状態管理（🔥無名高確 等のバッジ→タップで終了）を一般化したもの。SF6開発（2026-08）にて「前兆突入・液晶色変化を上部バッジで可視化し、バッジタップで抜け＋色復帰を連動記録する」要望から、機種横断の標準パターンとして採録した。今後の全機種で応用する。

## 機種プリセット作業ルール

`nerai-record.html` の機種プリセット追加・変更を行う場合は、作業前に必ず `docs/machine-preset-guide.md` を読むこと。

`nerai-record` は実戦記録に徹する。狙い目判断・検証・分析の機能は持たせず、記録データを別ツールで活用する前提とする。

検証は同ガイドの3系統を最低ラインとする。
- 新規環境
- 既存データ再現
- 対象外機種

作業中に新しい落とし穴、汎用機構、検証観点を発見した場合は、`docs/machine-preset-guide.md` への追記を提案し、必要なら別コミットで反映すること。

機種プリセット以外の作業では、このルールを無理に適用しない。

## nerai-record データ保護ルール

`nerai-record.html` の起動時処理、特に `loadData()` と正規化処理は、後方定義の `const` / `let` / 関数初期化順に依存させないこと。保存済み旧形式データを読み込む前に、参照される定数・補正テーブル・正規化関数がすべて初期化済みである状態を保つ。

旧形式データのロード経路を変更した場合は、`tests/nerai-record-regression.test.js` を実行し、少なくとも以下を確認すること。
- 旧 `stores` 文字列配列が現行の店舗オブジェクト形式へ正規化される
- 旧東京喰種データのロードで例外・空フォールバックが発生しない
- 旧データに合成ログを加えたケースで、空き保存ガードが保存拒否として働く

## バックログ

機能追加・修正の依頼を受けたら、まず `WISHLIST.md` を確認すること。実装済み・破棄済みの項目が記載されている。

`WISHLIST.md`（台帳）は構想・判断材料であり、実装許可ではない。実装は必ずシオン発行の個別指示書に基づくこと。

`WISHLIST.md` に記載されている未依頼の機能を、指示なく実装してはならない。
## 作業規約（全セッション共通・2026-08-06制定）

どのチャット/セッション経由の依頼でも本規約が正本。依頼内容と本規約が矛盾する場合は停止して確認する。

### コミット・検証
1. 1課題1コミット。**依頼にない変更を混ぜない**（未依頼実装の禁止。アイデア・改善案は実装せず WISHLIST.md へ起票提案）
2. 送信前に `git diff --check`／`node --test`（環境に node が無い場合は REPL 代替で全スイート完走し、その旨を報告に明記）
3. push 後に raw GitHub と Pages の 200・変更マーカーの反映を確認
4. **コミットごとに画面のUIバージョン文字列を更新**（例：`UI 7u`）
5. 改行は LF 維持

### 受け入れ・検収
6. UI変更の受け入れは**実レンダリング**（実ボタンの onclick から辿った実HTML・実挙動）で固定。関数直呼び・静的grepによる受け入れは無効（WISHLIST §9-41）
7. 実戦モードに入力を追加したら「**見える・取り消せる・巻き添えで消えない**」をテスト固定（§9-38）
8. 割合表示は分子/分母を明文化し、**分母定義は1関数に集約**して全表示面の一致をテスト固定（§9-39）
9. 選択UIは**選択対象自身のハイライト**で状態を示す（§9-43）。操作ボタンの文言はドメイン用語（確定・以上・天井等）と衝突しない語を選ぶ（§9-44）

### 報告
10. 報告様式：コミットSHA／原因または実装の1行説明／受け入れ条件の結果／追加テスト一覧
11. 完了報告には WISHLIST.md の更新案（どの§に何を載せるか）を添える（§9-45）
12. 再現しない・情報が足りないときは、当てずっぽうで修正せず**停止して報告**

### 参照（毎回は読まなくてよいが、該当作業では必読）
- 機種プリセットの追加・変更：`docs/machine-preset-guide.md`（クリア契機の系統別定義 §9-42・表/その他の頻度基準 §9-40・シード⇔実UI網羅一致テストを含む）
- 入力フロー/UIの設計思想：`WISHLIST.md` §9（正本・経緯付き。新しいフローを設計するときはここを先に読む）

## §9-47: 詳細カードの%表示

入力画面で%が付く項目は、詳細カードでも同じ意味の%を併記すること。対象は、分母付きの分数系と、グループ内比率を表示する項目群とする。表示形式は原則として `項目名 ×n (xx%)`、分数系は既存の `n/d xx%` 形式を維持する。単独カウント項目（枚数表示・トロフィー・確定示唆など）は件数のみとし、分母0の場合は%を省略する。

## §9-48: ゾーン・周期系の入力標準

規定到達ごとに当選機会がある構造は、ゾーン型・周期型を問わず「到達」と「当選」の2カウントを必須とする。入力UIは `当選` / `ハズレ` の2ボタンを標準とし、`当選` は分母+1・分子+1、`ハズレ` は分母+1のみを加算する。行タップは誤爆防止のため無効化し、ボタンは横書き固定・高さ44px以上を確保する。

## §9-49: 確定契機のサンプル除外

当選が確定するルートは、設定差サンプルから除外すること。例として、ゴッドイーターの逆鱗ハンニバル敗北後100Gなど、当選が仕様上確定しているものは分母にも分子にも入れない。除外する場合は、ユーザーが実戦中に迷わないよう、除外理由をセクションヒントに明記する。

## §9-80: ベイズ推定の搭載基準

ベイズ推定の搭載可否は、夢爽が機種ごとに明示指定した場合のみ実装する。搭載判断の基準は、通常時から高頻度にカウントできる設定差小役・設定差契機が存在すること。ユーザーが入力した確率値や有料情報由来の数値は、カード画像・共有テキスト・テンプレ出力へ表示してはならない。

## §9-51: 説明文の配置

グループ共通の説明は、各行のサブラベルへ繰り返し記載しない。セクション見出し直下、またはセクション下部のヒントに1回だけ集約する。行内サブラベルは、その行固有の示唆・注意・数値に限定し、同じ説明の反復で行高や視認性を崩さないこと。

## §9-52: なな様テンプレの扱い

鈴白なな様テンプレは原型を尊重する。入力構造・表記順・出力形式を変更する場合は、なぜ変えるのかを整理し、必要に応じて本人への報告材料に含める。横展開や表記統一は、本人の希望を優先し、こちら側の都合だけでテンプレ構造を上書きしない。

## §9-53: 機種完成時の振り返り

各機種の正式公開判定後、シオンが「ルール化候補の棚卸し」を提示し、夢爽が採否を裁可する。採用したものはAGENTS.mdへ追記し、以後の作業ルールとして扱う。1例のみの判断は原則として保留し、2例目が出た時点で恒常ルールへの昇格を再検討する。

## §9-54: 1L最強示唆の選定対象

カードサマリー1Lの「最強濃厚示唆」は、設定n以上濃厚のように保証設定が単調に上がる示唆だけを対象とする。設定否定系、2・4・6濃厚のような飛び値系、偶奇示唆、モード示唆、次回挙動示唆は1Lの選定対象外とし、必要に応じて別枠で件数表示する。

## §9-55: 本人提供テンプレと解析サイトの優先関係

鈴白なな様など本人提供テンプレの情報が解析サイトより詳しい、または解析サイトに未掲載の呼称・分類を含む場合は、本人提供テンプレを正として扱う。解析サイトの数値・注意事項と矛盾する可能性がある場合は、実装前に差分を明示し、必要に応じて本人確認または夢爽判断を挟む。

## §9-56: cherry-pick載せ直し時の再適用確認

古いブランチや旧コミットからcherry-pickで現行mainへ載せ直す場合、移植元以降にmainへ入ったレイアウト修正・共通エンジン変更・AGENTS.mdの新ルールを再適用すること。特にカードサマリーの開始Y座標、詳細カードの%表示、ゾーン/周期の入力標準、1L選定対象は、載せ直し後に明示的に確認する。

## §9-57: 寸法・可動域基準（全機種共通）

実戦入力UIは、360px幅・530px級ビューポートでの実測値報告を受け入れ条件とする。見た目の推測ではなく、描画後の `getBoundingClientRect` と computed style で確認する。

- ボタン高さは52〜64px、チップは min-height 48px、タップ領域は48×48px以上を確保する（表示専用要素は対象外）
- 文字は16px以上、補助テキストは12〜13px可、G数数値は20〜28pxに収める
- ボタン間隔は6〜10pxを基準とする
- G数表示と主要操作ボタンは530px級の同一ビューポートに収め、スクロールは原則0回とする
- ボトムシート・テンキーの選択肢先頭行は画面下端から40〜60%帯に置き、シート下端には30px以上の余白を残す
- プルダウンは、最長の選択肢ラベルが16pxで切れずに表示できる幅を最低幅とする

## §9-58: 入力方式標準（全機種共通）

- 数値入力はアプリ内テンキーに統一し、OSキーボードを出さない。`input` は readonly とし、テンキー起動の入口として扱う
- 複合入力シート（複数項目を入れて確定するシート）には必ず明示的な確定ボタンを置き、記録されたことがログ表示で目視確認できるようにする
- 単一選択シートは選択即記録・自動クローズ、複合入力シートは記録後も開いたまま維持する。ただし機種仕様上1回で完結する複合操作は、仕様書で明示したうえで閉じてよい
- 状態区間中にロックしたカウンタ（例：FB中の実G/液晶G）は、区間終了時に実機値の入力で同期し、ズレを構造的に残さない
- 音で照合するラベル（ボイス等）は、字面ではなくかな表記を基本とする。聞こえた音とそのまま突き合わせるため

## §9-59: スキーマ版数運用（全機種共通）

- エクスポートJSONは `ver` を持ち、スキーマ変更を伴う版では必ず版数を上げる
- 正規化層（`*-schema.js`）を各ツールに置き、旧版データを現行形式へ写像して読む
- 旧フィールドは削除せず保持する。必要なら `legacy_` リネームで意味を分離する
- 未知のtype・未知フィールドは捨てずに通す。前方互換を壊さない
- UIのみの変更では版数を上げない

## §9-60: 示唆リファレンスの線引き（実記録原則の解釈）

- 確定済み解析事実の参照表示は可とする。示唆一覧・出現率などの静的リファレンスは、表示専用でデータ非干渉なら判断ロジックに当たらない
- 期待値計算・狙い目判定・推奨は不可とする。この線引きを「実記録に徹する」の正式な解釈とする
- 示唆マスタは1箇所のデータオブジェクトに集約し、解析更新は文言差し替えで対応する
- 出典表記は項目ごとに書かず、ツール内の1箇所に集約する。例：示唆パネル共通フッタまたはページ末尾に「参考：媒体名（年月時点）／一部は実戦調査」と記す。文言自体は転載せず、自前要約を維持する

## §9-61: 示唆入力は発生契機のフローに組み込む

示唆・演出の入力は、それが実機で発生するタイミングの確定フローに組み込むことを標準とする。

- 機種の仕様策定時に、各示唆・演出がどのタイミングで発生するかの対応一覧を最初に作成し、入力UIはその発生契機のシートまたは確定操作に配置する。例：アイキャッチは敗北確定、ボーナス終了画面は勝利記録、暗転はDC結果とWT抜け
- 発生契機から切り離した独立入力ボタンは原則設けない。入力場所を探す迷い、入力漏れ、後追い修正の原因になる
- 同じ操作枠に発生契機の異なる演出を同居させない。例：DCとストリートジャッジの分離は、SF6 v3.4での教訓として扱う
- 発生契機が複数ある示唆は、各契機のフローすべてに入力を置く。暗転をDC結果とWT抜けの両方に置くような形を標準とする

## §9-62: 示唆の強さはデータ側に数値で持たせる

濃厚示唆の強弱を判定する際、表示ラベルの文字列から推測してはならない。各項目の定義に強さを数値（4=設定4以上濃厚、6=設定6濃厚 等）で持たせ、判定はその数値のみを参照する。

理由: 「456OVER」に文字「6」が含まれるため設定6濃厚と誤判定した事故がある。文字列マッチは機種追加のたびに同種の事故を起こす。

## §9-63: 同一示唆のキャラは分類単位でまとめる

示唆内容が同じキャラクターが多数存在する場合、キャラ名を個別の入力項目にせず、「色×強弱」等の分類単位でまとめる。個別のキャラ名は参照タブに一覧を置く。

理由: 入力項目が30を超えると実戦中に探せず、押し間違いが増える。記録すべき単位は示唆の中身であり、キャラ名はその判定材料に過ぎない。

## §9-64: ボタン表記は実機の画面表示に合わせる

入力ボタンの表記は、解析サイトの記載ではなく、実機で遊技者が目にする文言・数値に合わせる。内部キーは変更しない。

理由: 東京喰種の精神世界は解析上「保証13/23/33G」だが、実機画面は「残り10/20/30」と表示される。解析表記のままでは初見の利用者が迷う。

## §9-65: 有料解析の数値はユーザー入力型とし、出力物に含めない

有料媒体の解析数値をベイズ推定等の証拠として用いる場合、数値をツール側に内蔵せず、利用者が自分の手元の数値を入力する方式とする。入力値そのものに加えて、入力値に依存する計算結果（推定確率など）も共有カード・テンプレート等の出力物に含めない。入力値は端末内にのみ保存する。公開されている数値（無料解析サイトに掲載済みのもの）は内蔵してよい。

理由: 出力物に推定結果が載ると、複数枚を突き合わせることで元の数値を逆算できる余地が生まれる。1枚では困難でも枚数に比例して精度が上がるため、経路そのものを断つ。

## §9-66: 判別項目ごとに有効区間が異なる場合、分母を分離する

複数の判別項目が同一の回転数を分母に使う設計は、項目ごとに有効区間が異なる場合に誤った確率を出す。区間が異なる項目には専用の開始G数入力を設け、分母を分離する。開始G数が未入力の項目は、確率表示を「−」とし推定に用いない。

理由: ミリオンゴッドの押し順ナビあり15枚役で、他項目と同じ分母を共有した結果、利用者が意図したカウント区間と、表示・推定に使う区間がズレる事故があった。

## §9-67: 下部タブは5つまでとする

チェッカーの下部タブは最大5つ。これを超える場合は、参照タブ内のジャンプボタンのように、タブ内での分割で対応する。

理由: 390px幅の端末で6つ以上になるとタップ領域が狭くなり、実戦中の誤タップが増える。喰霊-零-Reの5タブ構成が実機での上限であることを確認済み。

## §9-68: 状態を巻き戻すときは判定ロジックのスコープも同時に巻き戻す

カウンタや状態変数を上書き・巻き戻す機能（Undo、実機値での上書き、リセット等）を持つ場合、その状態を参照する判定ロジックの参照範囲も同時に巻き戻す。状態変数だけを直しても、判定側が過去のログや履歴を参照していれば挙動は直らない。

確認観点:
- 状態変数の再基準化だけでなく、重複判定・発火済み判定が参照するログの範囲を区切っているか。例: リセット時刻以降のログだけを対象にする
- Undo用スナップショットに、表示に影響する導出用の内部state（境界トラッカー、リセット時刻、フラグ類）も漏れなく含める
- スナップショット復元後に、復元値を上書きする再計算処理を走らせていないか。二重管理は禁止する
- 同一Undo内で複数処理が走る操作（区間終了時の一括記録など）では、処理順序によって再基準化が打ち消されていないか

理由: SF6 v3.3で「FB敗北後に自動色変化が発火しない」バグを修正した際、状態変数（境界トラッカー）の再基準化のみを行い、重複判定が全ログを参照したままだったため再発した。v3.5.2で判定スコープを時刻で区切って根治したため、自動記録・状態バッジ・カウンタを持つツールの標準確認観点とする。

## §9-69: シート内の入力順は実機の進行順に一致させる

複合入力シートの項目順は、実機でその情報が判明する順序に並べる。入力中に上下へ戻る動きが発生する並びにしない。

- 機種の仕様策定時に、各シートについて「実機で何がどの順に見えるか」を確認して並び順を決める
- 例: SF6の勝利シートは「ボーナス種別 → 枚数 → 終了画面 → クレジット」。消化中に種別・枚数、終了画面が出て、最後にクレジットを読む順に合わせる
- §9-61（示唆入力は発生契機のフローに組み込む）の延長として、どのシートに置くかだけでなく、シート内のどの位置に置くかまで実機の進行に合わせる

## §9-70: 確率表示は、分母の性質が理論値と一致する場合のみ行う

実測の1/x表示やベイズ推定に用いる分母は、比較対象となる解析値と同じ条件で取得できる場合のみ採用する。台のメニュー画面等で総ゲーム数が確認できても、AT中の消化分を含むなど解析値と条件が異なる場合、確率表示は行わず回数と総回転数の記録に留める。

理由: とある2・からくり2・炎炎2で、AT込みの総回転数を分母にした初当り確率を表示していた。解析値より重く出るため、比較材料として誤解を招く状態だった。

§9-47のA群/B群分類は入力UI構成の分類として維持するが、確率表示の可否判定は本条を優先する。

## §9-71【共通ファイルの変更手順】

checker-engine.js など複数機種が共有するファイルの変更は、必ずfeatureブランチ経由とし、mainへのマージ前にactions未定義の既存機種1つで回帰確認(通常カウント・カード生成・詳細カード生成)を行う。mainへの直接コミットは変更の大小を問わず禁止。

## §9-72【カスタムアクションの仕様書要件】

機種固有のカスタムアクション(タップで複数の値を同時に変更する操作)を新設する仕様書には、減算モード時の挙動(無効化するか、対応するか)を必ず明記する。無効化する場合はヒント文に代替の訂正手段を記載する。

## §9-73【表示固定の適用範囲】

position:sticky 等の表示固定を使う仕様書には、「固定が効く範囲(どこまでスクロールしても追従するか)」を必ず明記する。stickyは親要素の範囲でしか効かないため、範囲の指定がないと意図しない位置で固定が外れる。

## §9-74【実装の着手条件】

ミコトは、夢爽・シオンからの明示的な実装指示文がある場合のみ実装に着手する。解析更新の告知文などの情報共有だけでは着手しない。判断に迷う場合は着手前に確認する。

## §9-75: 自動記録は手動記録に劣後する

自動記録（G数到達トリガ、保留発火、自動クリア等）は入力の手間を減らす補助であり、人が実機を見て確定した手動記録を後から上書き・打ち消してはならない。

- 保留中の自動発火がある状態で同じ対象への手動記録が行われたら、その保留を解除する
- 保留は状態遷移、値の上書き、リロード、Undoの対象に含め、snapshotにも保存する
- 迷った場合は「実機を見た人間の入力が正」とし、自動処理はその後追いに留める

理由: SF6で、DC加算による100G跨ぎの保留青が、手動で赤を記録した後に発火してバッジを青へ戻す不具合があった。v3.5.4で手動色記録時に保留を解除して修正したため、全機種共通の標準とする。

## §9-76: 開いたままのシートは選択操作で再描画しない

選択後も開いたまま維持する複合入力シートでは、チップ選択・トグル操作でシート全体のDOMを再描画しない。選択状態は該当チップのクラス・aria属性の付け替えで表現する。

- 再描画は入力値の消失、スクロール位置のリセット、フォーカス喪失を引き起こす
- 値の退避復元やscrollTop復元は対症療法であり、タイミング依存で失敗しやすいため標準手段にしない
- 表示の切替が必要な場合も、該当ブロックの表示/非表示切替に留め、シート全体を作り直さない
- 受け入れ確認は、実描画で「下部までスクロール → 選択 → 位置が動かない」ことを確認する

理由: SF6で、チップ選択時のシート再描画により入力値消失（v2.7）とスクロール先頭戻り（v3.5.4）が発生した。scrollTop復元では実機で直りきらず、v3.5.5で非再描画構造に変更して根治したため、複合入力シートの標準とする。

## §9-77【共通JSのバージョンクエリ運用】

checker-engine.js等の共通JSを変更するコミットでは、同一ブランチ内で全HTMLの参照クエリ(?v=)も更新する。値は日付(YYYYMMDD)とし、同日2回目以降は -2、-3 の連番を付ける。

## §9-78【sitemap登録の基準】

sitemap.xmlには一般利用者向けに検索流入させたいページのみを登録する。kado-note.html と recovery.html は運営用途のため非登録が正であり、導線があることと sitemap 登録は独立に扱う。

## §9-79【外部作成素材の受け渡し】

別環境・別チャットで作成したファイルをミコトへ渡して反映させる場合、作成側は納品直前に最新の main を取得し、差分を当て直してから渡す。依頼文には「作業前に最新の main を取得してください」を毎回明記する。ミコトは受領素材が main より古い内容を含む場合、上書きせず差分を報告する。

## §9-81【canvasで使うWebフォントは明示ロードする】

canvasの font 指定はWebフォントの取得を起動しないため、document.fonts.ready を待つだけでは適用されない。DOM側で同じフォントを使っていない場合は特に表面化しないまま別フォントで描画されるため、描画前に document.fonts.load() で必要なウェイトを明示的に読み込むこと。

## §9-82【描画処理の検証では未捕捉のPromise例外を監視する】

非同期ハンドラ内で発生した例外は画面にエラーが出ないまま以降の描画を中断させるため、一部だけが欠けた成果物が正常に見えてしまう。描画系を変更した回の検証では window.addEventListener("unhandledrejection", ...) を検証ドライバに常設し、あわせて描画結果を画素で裏取りすること。
