# WISHLIST.md — slot-tools 統合バックログ

最終更新：2026-07-28 / 管理：シオン（Claude）
対象：`homuratakaaki-ops/slot-tools`
現在の HEAD：`3f4fb2b`

> **このファイルの目的**
> 構想・要望・宿題をチャットに置かず、ここに集約する。
> 2026-07-26 に、過去チャットから未実装項目を12件発掘する作業が発生した。同じことを二度やらないための台帳。
>
> **使い方**：迷ったら §1 だけ見る。新しい構想が出たら §5 に追記する。完了したら §8 へ移す。

---

## 1. 次にやること（迷ったらここだけ見る）

| 順 | やる人 | 内容 | 参照 |
|---|---|---|---|
| 1 | 夢爽 | **アーカイブ実行**（07-18〜07-22の5日分を推奨） | §3 |
| 2 | シオン＋夢爽 | **案1＋案7の設計**（当選記録フロー刷新）← 本日のメイン | §5-1 / §5-7 |
| 3 | シオン | 案3・案2 の指示書作成 | §5-3 / §5-2 |

**ブロッカーはゼロ。** 容量問題は 2026-07-26〜27 に恒久対策まで完了した（§8）。

---

## 2. 現在の状態

| 指標 | 値 |
|---|---|
| localStorage 使用量 | **4.0MB / 79%**（7/26未明は174%） |
| 内訳 | 本体1.8MB ＋ `last_good`1.8MB ＋ `checkpoint`0.36MB |
| データ | ログ11件（7日分）/ 他台カード9件 / 機種4件 / 店舗5件 |
| 回帰テスト | **PASS**（束Eで復旧） |

**79%が現設計の下限。** 本体とその複製1本は構造上減らせない。下げるには本体を小さくする＝アーカイブの実行が必要（§3）。危険域は80%。

---

## 3. 夢爽の宿題

**アーカイブ実行（最優先・5分）**

- [ ] 「全データをJSONでバックアップ」で全体の控えを取る
- [ ] ログタブ →「古いログをアーカイブ」→ **07-18〜07-22（5日分・6件・約0.5MB）** を選択
- [ ] 「選択したログを書き出して削除」→ ダウンロード確認 → OK
- [ ] **確認点：ログを過半数削除しても保存が拒否されないこと**（`{confirmDanger:true}` の動作確認）
- [ ] 期待値：4.0MB → 約3.0MB（60%）。`last_good` は次の保存時に縮むため、直後は半分しか反映されない場合がある

**書き出したJSONは必ず保管すること。** 読み戻す手段は現状「復元」のみで、現在のデータを上書きする。実質一方通行。

**実機確認（未消化）**

- [ ] **B-2-1**：SDC突入タグ → タップ式ピッカーが出るか。「スキップ」でエントリが残るか
- [ ] **B-2-2**：実戦モードを開いて放置 → 画面が消灯しないか。復帰後も維持されるか
- [ ] **#10**：mode-estimator の「稼働記録から取り込む」で、区間数・G数・リプフラ回数が実戦モードの表示と一致するか

**データ整理**

- [ ] 確認用カード3枚の削除：台706（7/26）/ 台不明（7/26 23:59）/ 台不明（7/21 00:00）

**その他**

- [ ] #11 からくり2の実戦通し検証（打ち始め→収支登録→整理ビュー→PNG出力）

---

## 4. 実装キュー（ミコト向け・優先順）

**保存形式に触る枠（必ず単独コミット）**

- [ ] **案1＋案7 統合** 当選記録フロー刷新 → §5-1 / §5-7 ← **本日のメイン**

**保存形式に触らない枠**

- [ ] **案3** リプレイカウンターの液晶200/201分割 → §5-3
- [ ] **案2** リプフラ区間の複数選択 → §5-2
- [ ] **案8** アーカイブJSONの閲覧専用ビュー → §5-8

**保留（実装時期未定）**

- [ ] **案9** 稼働前思考整理ツール（AI pre-check）→ §5-9

---

## 5. 構想・仕様

### 5-1. 案1：バタフライ成功/失敗 → ときめきゾーン当選G記録

**保存形式：触る** / **案7と統合して設計すること**

成功/失敗の2値をやめ、ときめきゾーンでの当選ゲーム数を記録する。成功/失敗は値の有無から導出できるため、情報量が増えて失うものがない。SDC判定と連チャン把握も容易になる。

| フィールド | 内容 |
|---|---|
| `tokimekiGame` | 液晶基準のゾーン内相対G（1〜32）。`null` = 32抜け |
| 成功/失敗 | 保持しない。`null` 判定から導出 |
| 絶対位置 | 追加不要。既存の `liquidGame` / `game` が保持済み |

**確定事項**

- ときめきゾーンは**液晶32G**。当初メモの「42」は既存の42Gチェーン自動検出との混同
- **記録は液晶基準で統一する**
- 「液晶32 = データカウンター41」は**オフセット+9の状態で観測した値であり、定数ではない**。`liquidGame` / `game` / `liquidOffsetDelta` を持ち、超飛翔後ベルでオフセットが蓄積するため、データカウンター側は状況により変動する

**移行方針**：旧バタフライ成功/失敗の既存データは遡及不能。旧フィールドを残して新フィールドを併設し、集計側で「旧形式は成功/失敗のみ扱い」とする。過去データは捨てない。

### 5-2. 案2：リプフラ区間の複数選択

**保存形式：触らない**

リプレイフラッシュの集計区間を、**任意の複数区間**から選択できるようにする。集計スコープの拡張のみで完結する見込み。緑サイクル仮説の検証サンプルが増える。

### 5-3. 案3：リプレイカウンターの液晶200/201分割

**保存形式：触らない**

液晶200以内 / 201以降 の2分割。境界は**液晶基準**（オフセット蓄積があるためデータカウンター基準は不可）。

`subCounters` は `normalizeSubCounters` が任意キーを受け入れる**スキーマフリーの `{key: count}` マップ**。既存キーの分割ではなく**キーを2本に増やすだけ**で済み、保存形式の定義に手を入れる必要がない。既存データは該当キーを持たないため、集計側で「旧形式」として除外する。

**受け皿は既に存在する**：`mode-estimator.html` に `repflashOver200` チェックボックスが実装済み（L425）。200G境界の概念は向こう側に用意されている。

### 5-4. 案4：SDC突入の契機をタップ式へ ✅ 実装済み

→ §8（commit `0e1a117`）

**段階2として残っている作業**：案1の実装後、選択肢1「ときめき32G目」は自動導出可能になるため4択→3択へ縮小する。**案1と同時に対応すること。**

### 5-5. 案5：古いログのアーカイブ ✅ 実装済み

→ §8（commit `9831670`）。実行は §3 の宿題。

### 5-6. 束D：一般ユーザー向け容量UX ✅ 実装済み

→ §8（commit `af5d407` / `7cba0fd`）

### 5-7. 案7：獲得枚数 → ボーナス終了時クレジット枚数 ＋ 投資ワンタッチ

**保存形式：触る** / **案1と統合して設計すること**

**変更内容**

1. 当選記録フロー（当選契機 → ボーナス種別 → 獲得枚数）の3番目を、**ボーナス終了時のクレジット枚数**に変更する
2. 実戦モードに**「1000円追加投資」のワンタッチボタン**を追加する

**なぜクレジット枚数が優れているか**

獲得枚数は**計算した値**、クレジット枚数は**見たままの値**。獲得枚数は「終了時−開始時」の暗算または払い出し表示の記憶を要し、実戦中はミスを生む。

記録としても上位互換：

```
差枚 = 現在のクレジット − 総投入枚数
```

獲得枚数の積み上げは誤差が蓄積するが、クレジットは**毎回が絶対値なので誤差がリセットされる**。有利区間差枚推定に向く。

**成立条件（確認済み）**

コンプリート機能の警告が15,000枚、上限2万枚未満。**クレジット表示は上限なく増える**ため、物理メダル機の50枚頭打ち問題は起きない。案7は成立する。

**設計上の要点**

- 投資は**「円」で記録する**。1000円あたりの枚数は等価50枚・46枚貸しと店舗で変わるため、枚数で持つと後から直せない。円で貯めてレートは稼働ログ側に持たせる
- 誤タップ用の Undo が必要
- 既存の `payout`（獲得枚数）フィールドを持つデータがあるため、旧フィールドを残して新フィールドを併設する

**案1との統合が必須の理由**

案1と案7は**同じ当選記録フロー（当選契機 → ボーナス種別 → 獲得枚数）を触る**。別々に実装すると同じ箇所を2回壊すことになるため、「当選記録フロー刷新」として一度に設計する。

### 5-8. 案8：アーカイブJSONの閲覧専用ビュー

**保存形式：触らない**

現状、アーカイブしたログを見るには「復元」で丸ごと戻すしかなく、現在のデータを上書きしてしまう。実質一方通行。

アーカイブJSONを読み込んで**閲覧専用で一覧表示**する導線を作る。取り込み・マージはしない（IDの衝突を避けるため）。

急がない。アーカイブを本格運用し始めてから必要性を再判断する。

### 5-9. 案9：稼働前思考整理ツール（AI pre-check）

**状態：保留（実装時期未定・優先度は将来判断）**

**概要**

稼働開始前に頭の中を自由記述させ、AIが**事実 / 推測 / 感情 / 願望**を分離して整理する。**判断はさせない。客観視の支援に徹する。**

**出力**：`summary` / `facts` / `assumptions` / `emotions` / `risks` / `question` のJSON

**方針決定済み**

- 感情は断定せず「可能性」表現にする（ガード必須）
- APIキーはサーバー側で管理する。**Workers 等の中継層が必要**
- 公開時は**回数制限＋Turnstile＋入力上限＋月額キルスイッチ**が必須
- localStorage には**整理結果の要約のみ**保存する（原文は破棄・件数上限あり）

**収益位置づけ**：広告では薄利。**note メンバーシップ特典としての限定公開が本命候補**。ツール自体は差別化・流入装置と位置づける。

**参考**：詳細仕様はネネちゃん作成の構想ドキュメントを参照。

#### シオンからの補足（設計上の論点）

- **これは slot-tools 初のサーバー依存機能である。** 現在サイトは GitHub Pages の静的配信で完結しており、Workers 等の中継層を入れると運用対象が1つ増える。障害点・コスト・監視が発生する。既存ツール群とは**別系統のリスク**として扱うこと
- localStorage に保存する場合、**容量予算を先に決めること**。本体は既に1.8MBあり79%を使っている。要約のみ・件数上限という方針は正しい。実装時は §9-4 に従い単独タスクとする
- 「判断はさせない」は設計の核心。**AIが結論を出した瞬間に、立ち回りの責任所在が曖昧になる。**分離と提示に徹する設計を崩さないこと
- 感情の断定回避も同様に必須。誤ったラベリングは客観視ではなく誘導になる

---

## 6. 積み残し

- [ ] **#3** PWA化（Service Worker / manifest.json）— 参照ゼロ。**稼働中の投入は厳禁**（キャッシュで挙動が読めなくなる）
- [ ] **#6** ダイアログの管理UI改修 ※案4で一部消化
- [ ] **#7** タップ領域44px未満 — 基底 `.btn-small`（実高さ約28px）と `.shop-note-counter-btn`（32px）
- [ ] **#8** shopNote使い勝手（アクティブカードの識別、メモ入力欄が狭い）
- [ ] **#12** 東京喰種の積み残し（ピエロ追加セリフ／AT終了ボイス／スイカ持ち越し／実G照合）※判定保留

**#7の注記**：`.btn-small` の5定義は重複ではなくスコープ付き（`.inline-field .btn-small` 等）。競合はしていない。基底の `min-height` を上げると適用箇所が広範なため、疑似要素でタップ領域のみ拡張する方式を検討する。

### 破棄・完了した項目

- ~~**#9** 実戦モードの入力順変更~~ → **破棄**。案1（§5-1）が上位互換。案1では成功/失敗の分岐そのものが消えるため、`selectNangokuButterfly('success')` が4ステップをスキップする問題も同時に解消する。**旧仕様前提の修正であり、実装してはならない**
- ~~**#5** Wake Lock~~ → 実装済み（§8）
- ~~**#10** mode-estimator フェーズ2~~ → 実装済み（§8）

---

## 7. 判断保留・環境整備

- [ ] ミコト環境で `node` が PATH にない（同梱ランタイムで代替実行中。実務上は回避できている）
- [ ] `git status` 時の `C:\Users\homur/.config/git/ignore` permission warning。**グローバルgitignoreが存在しないだけでエラーではない。**空ファイルを1つ作れば消える
- [ ] A-2 の `--tab-bar-h:46.2px`（小数値）を整数へ丸めるか。端末のフォントレンダリング差で1px前後ずれる。実害が出てから対応でよい
- [ ] アーカイブUIで0件選択時に「削除後の見込み: 約4.0MB」と現在値が出る。実害なしだが「選択してください」等が適切。急がない

---

## 8. 完了ログ

### 2026-07-28 — 区間カードの余白バグ修正

`3f4fb2b`

- [x] `.timeline-text` の `white-space:pre-wrap` が、テンプレートリテラル内の改行・インデントを可視化していた
- [x] HTML要素を包んでいた2箇所を新class（`timeline-variant-list` / `timeline-counter-history-list`）へ分離
- [x] `.timeline-text` はテキスト表示用として温存（他14箇所は正しい用法）

**原則**：`pre-wrap` を持つclassでHTML要素を包まないこと。テキスト用とコンテナ用を混同しない。

### 2026-07-27 — #10 mode-estimator フェーズ2

`a84320d`

- [x] リプフラ区間を `nerai-record` の localStorage から**読み取り専用**で取り込む
- [x] 起点＝リセット or 飛翔/超飛翔突入の直後。区間はボーナス区切り（`segments` をそのまま利用）
- [x] 液晶G基準。draftLog優先、なければ最新の南国ログ
- [x] `nerai-record.html` は**バイト単位で無変更**（稼働中だったため）

**飛翔判定の採否**：`t_nangoku_super_hisho_start` タグと `terminalLabel`/`text` の文字列一致を採用。`machine.chainRule.enterLabel` は**非採用**（nerai側の検出ロジック定義であり、保存済みログの実体ではないため）。

### 2026-07-27 — 案5：古いログのアーカイブ

`9831670` / `ca17dd9`

- [x] 日付グループ選択UI（初期は全て未選択）、件数とサイズ表示
- [x] 削減見込みの表示（本体と `last_good` の両方が減るため**選択サイズの2倍**）
- [x] 書き出し → 確認 → 削除の一連フロー。`persist({confirmDanger:true})` で保護ガードを通過
- [x] アーカイブJSONは既存フォーマットと同形（`restoreJson()` でそのまま読める）。`machines`/`stores` を全件同梱し単体で解釈可能に
- [x] ガイドの案内をアーカイブ機能向けに差し替え

### 2026-07-27 — 束E：回帰テストの復旧

`3944b5a` / `4aa59c3`

- [x] E-1 容量バナーのアサーションを `innerHTML` 対象へ（`fakeElement()` は `innerHTML` と `textContent` が独立なため、文言修正だけでは通らなかった）
- [x] E-2 書き戻しに伴う4箇所の期待値修正。空書き込みガードのテストは「persist直前の値との比較」へ
- [x] **E-3 正規化の収束保証**（新規発見）

**E-3の発見**：`normalizeData()` はデータ形状によっては**1回では収束しない**（レガシーfixtureで 646 → 82,691 → 92,492 → 収束）。束Cで永久ループは止まったが premigrate の書き込みが1回で終わっていなかった。`loadData()` で収束するまで反復（上限4回、超過時は `console.warn`）するよう修正。

**教訓**：テストが不具合を仕様として固定していた。落ちるのが当たり前の状態を放置すると、本物の退行を見逃す。

### 2026-07-26 — 束D：一般ユーザー向け容量UX

`af5d407` / `7cba0fd`

- [x] 容量警告バナーに「空き容量を確保」ボタン（JSON保存 → 確認 → 退避削除を1タップで）
- [x] 警告文から内部キー名を完全除去。診断パネル側は診断用途のため表示を維持
- [x] ガイドの「データの管理」に容量の段落を追加

### 2026-07-26 深夜 — 束C：容量問題の恒久対策

`1a3fd40` / `d60ec26` / `7d2d52f`

使用量 174% → **79%**。原因はNode上で実データを再現させて特定した（推測ではない）。

- [x] **原因1【主犯】premigrate の永久ループ**
  `loadData()` は正規化差分を検出すると premigrate（1.8MB）へ退避するが、**正規化結果を書き戻していなかった**。生データが「正規化前」のまま残り、次回ロードでも同じ差分が出て永久に書かれ続けていた。
  実測した差分：`draftLog` があるとき `state4Registered:true→false` / `finalized:true→false` / `excludeFromStats:false→true` が必ず書き換わる。`draftLog` が `null` のときは冪等。
  → 差分検出時に正規化済みデータを `setItem` で書き戻す（`persist()` は経由しない＝`last_good` を更新しない）
- [x] **原因2 日次バックアップ3世代**
  日付が変わるたびに1.8MBを追加し最大3世代=5.4MB。0時を回った瞬間に再生成されていた。
  → 1世代へ削減、旧 `backup_2`/`backup_3` は起動時に掃除
- [x] **原因3【構造】容量予算チェックの不在**
  本体1.8MBに対しフルコピー最大7本＝約12.9MB。上限5MBの2.5倍を超える設計で、どの書き込み経路にもチェックがなかった。
  → `backupRawStorage()` に容量予算ガードを前置。削除優先順は `backup_1` → `premigrate` → `prerestore`。本体・`last_good`・`checkpoint` は自動削除しない

### 2026-07-26 — 束B-2：SDC突入タップ化 / Wake Lock

`0e1a117` / `f353589`

- [x] **案4** SDC突入の契機入力を `prompt()` からタップ式へ。既存 `suggestPickerOverlay` を流用。後付け方式（エントリ確定＋保存 → 最後にピッカー起動）のため、スキップしても記録が残る
- [x] **#5** Wake Lock：実戦モード開始で取得、終了で解放、`visibilitychange` で再取得、非対応環境では無害に no-op

**判断根拠**：さざなみ（タップ式）46件 vs SDC突入（prompt式）**2件**。同じ人が同じ実戦中に使って23倍の差。データ価値ではなく入力UIの問題であることが数字で確定した。

### 2026-07-26 — 束B-1：容量診断・クリーンアップ

`6acd872`

- [x] localStorage 全キーの容量診断パネル（読み取り専用）
- [x] `checkpoint` を削除可能キーへ追加
- [x] 警告文から「アーカイブ」を削除（当時その機能は存在しなかった）

この診断パネルが原因2・3の特定につながった。

### 2026-07-26 — 束A：表示層のみ

`9126436`

- [x] **A-1** 旧レガシー他台メモUIの物理削除
- [x] **A-2** `--tab-bar-h`（実測46.2px）で sticky の top 衝突を解消
- [x] **A-3** `.bm-overlay` の safe-area 対応（実機確認済 ✅）

**インシデント**：同コミットに未指示の localStorage 圧縮レイヤー（18ハンク／+64行）が混入。指示書で「触るな」と明記した層だった。後に revert 済み。§9-4 のルールはこの一件から。

### 本セッション以前 — 他台メモの日付トグル

`9977155` — 日付2択トグル・ヘッダー同期。実機確認①②③すべて完了 ✅

---

## 9. 運用ルール

### 台帳の運用

1. **新しい構想が出たら §5 に追記**。チャットに置いたままにしない
2. **完了したら §8 へ移す**。破棄した場合は §6 に理由付きで残す（再発掘を防ぐため）
3. **夢爽の宿題も §3 に書く**。ミコトのタスクだけ管理してもタスクは行方不明になる

### ミコトへの依頼

4. **保存形式に触る変更は必ず単独タスク**。他の変更と同一コミットにしない
5. **指示書には制約文を明記**し、完了報告では**行数差分を origin/main 基準で**確認する。指示外の差分が出た場合は内容を問わず差し戻す
6. 完了条件：コミットハッシュ ＋ push確認 ＋ raw反映確認 ＋ 本番URL 200確認
7. **付随ファイルが必要になった場合は、実施前に理由を報告させる**（`.gitattributes` の一件から）
8. **稼働中は `nerai-record.html` に触らせない。** 入力フローが壊れるとその場で記録が止まる。別ファイル（`mode-estimator.html` 等）の作業に回す

### 対症療法を避けるための3つの問い（夢爽 → シオン）

9. **「消す前に、それを作ってるのは何？」**
   削除・回避・リセットを提案されたときの定型質問。2026-07-26 にこれを聞かれていれば、premigrate の再生成に即座に気づけた
10. **「これは対症療法？ 根本治療？」**
    シオンは必ず答えられる。答えが対症療法なら、急ぐか根本を待つかを夢爽が選べる
11. **「2回目だよね？」**
    同じ症状が2度出たら、そこで修正を止めて原因調査へ切り替える

### シオンの既定動作

12. 削除・回避を提案する前に、**生成元を必ず確認する**
13. 修正案を出すとき、**対症療法か根本治療かを明記する**
14. 同じ症状の2回目は、**修正案より先に原因調査を出す**

### 設計原則

15. **観測値を記録し、導出は後回しにする。** 計算値を保存すると誤差が蓄積し、後から検証できない（案7の設計根拠）
16. **`pre-wrap` を持つclassでHTML要素を包まない。** テキスト用とコンテナ用のclassを混同しない（2026-07-28の余白バグ）
17. **テストが落ちたまま放置しない。** 落ちるのが当たり前の状態は、本物の退行を見逃す入口になる（束Eの教訓）

### その他

18. メメントモリ側の構想は `mementomori-miresto/IDEAS.md` で別管理。混ぜない
