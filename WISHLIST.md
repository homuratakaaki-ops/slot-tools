# WISHLIST.md — slot-tools 統合バックログ

最終更新：2026-07-28 / 管理：シオン（Claude）
対象：`homuratakaaki-ops/slot-tools`
現在の HEAD：`23d252b`

> **このファイルの目的**
> 構想・要望・宿題をチャットに置かず、ここに集約する。
> 2026-07-26 に、過去チャットから未実装項目を12件発掘する作業が発生した。同じことを二度やらないための台帳。
>
> **使い方**：迷ったら §1 だけ見る。新しい構想が出たら §5 に追記する。完了したら §8 へ移す。

---

## 0. 開発方針（2026-07-28 確定）

**L南国育ちSPECIAL を基準機として完成させ、検証済みの仕組みのみ他機種へ展開する。**

理由：案1〜案7の設計はすべて南国の実データ（当選310件・リプレイ1,012件）で検証してから実装した。この検証密度は実際に打ち込んでいる機種でしか出せない。データの薄い機種へ先に広げると、検証されていない仕組みが増えるだけである。

機種非依存の仕組み（クレジット支援・レート店舗連動・Wake Lock・タップ化の方式）は南国運用で設計を固め、他機種展開時は固有部分（ゾーン判定・メーター類・示唆マスタ）のみを作る。

これに伴い、**からくり2（#11）と東京喰種（#12）は南国フェーズ完了後まで明示的に保留**とする。

---

## 1. 次にやること（迷ったらここだけ見る）

**開発タスクはゼロ。実装キューは空である。** 残りはすべて夢爽の手作業（§3）。

**現在は「使う期間」。** 案1・案2・案3・案7は実戦で使って初めて評価できる。使用感から要望が出たら §5 に追記する。

**PWA②（オフライン化）の発動条件**：`nerai-record.html` のコードに**1〜2週間変更がない状態**が続いたら「完全な完成品」とみなし、着手を検討する。それまで Service Worker は厳禁（§6 #3）。

---

## 2. 現在の状態

| 指標 | 値 |
|---|---|
| localStorage 使用量 | 4.0MB / 79%（アーカイブ実行で60%前後へ） |
| 回帰テスト | **PASS**（常時緑） |
| 実装キュー | **空** |
| PWA | 第1段階完了（ホーム画面アイコン化）。第2段階は凍結条件待ち |

---

## 3. 夢爽の宿題

**データ整理（合計10分・いつでも）**

- [ ] **アーカイブ実行**：「全データをJSONでバックアップ」→「古いログをアーカイブ」で 07-18〜07-22（5日分）を選択 → 書き出し確認 → OK。**確認点：ログを過半数削除しても保存が拒否されないこと**。書き出したJSONは必ず保管（読み戻しは上書き復元のみ＝実質一方通行）
- [ ] 投資合計の修正：140 → **138**（46.6レートで換算された2枚のズレ）
- [ ] 貸出レートに **46** を設定（KEIZ高岡の区間。次回以降は店舗連動で自動）
- [ ] 確認用カード3枚の削除：台706（7/26）/ 台不明（7/26 23:59）/ 台不明（7/21 00:00）

**実機確認（次回稼働で自然に消化されるチェックリスト）**

- [ ] **案1**：当選時にバタフライピッカーが出ない。`バタフライ` / `ときめき○G` がイベント表示に付く
- [ ] **案7**：2回目以降の当選でクレジット未登録の注意書きが出る。登録済みなら参考値＋〔採用〕が出る。46枚店で3千円→**138枚**
- [ ] **案3**：実戦モードに `リプレイ 〜200: n ／ 201〜: n` が表示される
- [ ] **案2**：リプフラメーターをタップ→過去区間を選択→〔+n区間〕付きで合算表示
- [ ] **B-2-1**：SDC突入がタップ式ピッカー。スキップでもエントリが残る
- [ ] **B-2-2**：実戦モード中に画面が消灯しない。復帰後も維持
- [ ] **#10**：mode-estimator「稼働記録から取り込む」の区間数・G数・リプフラ回数が実戦モードの表示と一致
- [ ] **F-1**：実戦モードの「＋1千円」ワンタップで投資が加算され、トーストに円と枚数が出る。Undoで戻る
- [ ] **F-2**：店舗に貯玉限度枚数を設定 → その店のその日の最初の稼働で自動反映＋トースト。同日2台目では発動しない
- [ ] **PWA①**：Chrome「ホーム画面に追加」→ 手帳＋7アイコン → URLバーなしの独立ウィンドウで起動

---

## 4. 実装キュー（ミコト向け）

**空。** 南国基準機フェーズの開発タスクはすべて完了（§8）。

新規タスクは §5 の構想から、夢爽の指示があったものだけを起票する。

---

## 5. 構想・保留

### 5-A. PWA②：オフライン化（Service Worker）

**発動条件：`nerai-record.html` に1〜2週間コード変更がない状態が続くこと。**

- 電波のないホールでもツールが開けるようになる
- **リスク：キャッシュが更新を遅らせる。** 「push→リロード→即反映」の開発フローが壊れるため、修正頻度が高い時期との相性が最悪
- 導入時は「新版検知→タップで更新」の通知機構を**必ずセット**にする
- PWA①（manifest・アイコン）は実装済み。②は Service Worker の追加のみ

### 5-B. 案8：アーカイブJSONの閲覧専用ビュー

**保存形式：触らない** / アーカイブ運用が定着してから必要性を再判断

現状、アーカイブしたログを見るには「復元」で丸ごと戻すしかなく、現在のデータを上書きする。閲覧専用の読み込み（取り込み・マージなし）を作る構想。

### 5-C. 案9：稼働前思考整理ツール（AI pre-check）

**状態：保留（実装時期未定・優先度は将来判断）**

稼働開始前に頭の中を自由記述させ、AIが**事実 / 推測 / 感情 / 願望**を分離して整理する。**判断はさせない。客観視の支援に徹する。**

- 出力：`summary` / `facts` / `assumptions` / `emotions` / `risks` / `question` のJSON
- 感情は断定せず「可能性」表現（ガード必須）
- APIキーはサーバー側管理。**Workers 等の中継層が必要**
- 公開時は回数制限＋Turnstile＋入力上限＋月額キルスイッチ必須
- localStorage には整理結果の要約のみ保存（原文破棄・件数上限）
- 収益位置づけ：note メンバーシップ特典としての限定公開が本命候補
- 詳細仕様はネネちゃん作成の構想ドキュメント参照

**シオン補足**：slot-tools 初のサーバー依存機能。GitHub Pages 静的配信の外に運用対象が増える（障害点・コスト・監視）。既存ツール群とは別系統のリスクとして扱う。「判断はさせない」が設計の核心——AIが結論を出した瞬間に立ち回りの責任所在が曖昧になる。

### 5-D. 他機種展開（南国フェーズ完了後）

- [ ] **#11** からくり2の実戦通し検証（打ち始め→収支登録→整理ビュー→PNG出力）
- [ ] **#12** 東京喰種の積み残し（ピエロ追加セリフ／AT終了ボイス／スイカ持ち越し／実G照合）

展開時の指針：機種非依存部分（クレジット支援・レート連動等）は流用し、固有部分（ゾーン判定に相当する機種固有ロジック・メーター類）のみ新規設計する。**その機種を実際に打ってデータを貯めてから着手する**（§0）。

---

## 6. 低優先の積み残し

- [ ] **#3** PWA化 → 第1段階完了（§8）。**第2段階は §5-A の発動条件待ち。稼働中・開発中の Service Worker 投入は厳禁**
- [ ] **#6** ダイアログの管理UI改修 ※主要部（SDC prompt）は案4で消化済み。残りは実害の報告があったものから
- [ ] **#7** タップ領域44px未満 — 疑似要素でタップ領域のみ拡張する方式を検討。実害の報告なし
- [ ] **#8** shopNote使い勝手（アクティブカード識別・メモ欄）— 実害の報告なし

### 破棄・完了した項目（再発掘防止のため残す）

- ~~**#9** 実戦モードの入力順変更~~ → **破棄**。案1が上位互換。旧仕様前提の修正であり実装してはならない
- ~~**#5** Wake Lock~~ → 実装済み（§8）
- ~~**#10** mode-estimator フェーズ2~~ → 実装済み（§8）
- ~~案4 SDC突入タップ化~~ → 実装済み。段階2（4択→3択）は案1完了により自動導出可能になったが、実運用で不便が出てから対応
- ~~案5 アーカイブ~~ → 実装済み。実行は §3
- ~~束D 容量UX~~ → 実装済み
- ~~案1・案2・案3・案7~~ → 実装済み（§8）

---

## 7. 判断保留・環境整備

- [ ] ミコト環境で `node` が PATH にない（同梱ランタイムで代替実行中。実務上は回避できている）
- [ ] `git status` の global gitignore warning（実害なし。空ファイル作成で消える）
- [ ] `--tab-bar-h:46.2px` の整数丸め（実害が出てから）
- [ ] アーカイブUIの0件選択時表示（実害なし・急がない）

---

## 8. 完了ログ

### 2026-07-28（追記）— 束F：投資ワンタップ / 貯玉再プレイ自動反映

**F-1** `4b4b34b` — 実戦モード差枚バー行に「＋1千円」。既存 `investYen` 経路を1回呼ぶだけ（シート非表示・トースト・300msデバウンス・Undo対応）
**F-2** `ea80845` — 店舗マスタに `dailyReplayMedals` 追加（保存形式に触るため単独コミット）。初回稼働の実戦モード起動時に `investMedals` として自動反映。**二重計上防止：同日・同店舗の保存済みログがあれば発動しない**（`editingLogId` は除外）。取消はUndoバーで1タップ

**範囲外として保留**：台移動時の持ちメダル引き継ぎ（前の台の残りクレジット→次の台）。必要になったら §5 へ起票

### 2026-07-28 — 南国基準機フェーズ完了日

**PWA①：ホーム画面アイコン化** `52fe56c`
- [x] `manifest.webmanifest`（standalone / start_url=/nerai-record.html / any maskable）
- [x] アイコン4点（512/192/180/32、ネネちゃん制作の手帳＋7デザイン、セーフゾーン0.00%逸脱を実測確認）
- [x] head内4タグのみ追加。body以下はバイト単位で無変更を確認
- [x] Service Worker なし（第2段階は §5-A）

**案2：リプフラ集計への過去区間合算** `8422d55`
- [x] メーターをタップ→区間選択シート。現在区間は固定、選択は実行時 `Set` のみ（保存しない）
- [x] 区間確定・機種切替・リセット系でクリア＋**存在しない区間IDの自動掃除**（ミコトの自発的補強）
- [x] 合算表示 `リプフラ 5／320G（1/64）〔+2区間〕`。案3の200/201分割は対象外（意味が異なる指標のため）

**案3：リプレイ回数の液晶200/201分割** `d76e019`
- [x] タイムラインからの純粋な導出（`liquidGame` 優先・`game` fallback）。新キーなし
- [x] 実データ検証：リプレイ1,012件 → 200以内584 / 201以降428 / G数なし0
- [x] **§5-3 の旧記載「subCountersにキー2本追加」は誤りと判明**。リプレイはサブカウンター（スイカ用）ではなくタイムラインエントリだった

**案7：クレジット連携による獲得枚数支援** `3f9d1c4`
- [x] 7-1 クレジット未登録の注意書き（前回当選後の `mode==='credit'` 履歴で判定。初当選では出さない）
- [x] 7-2 参考値＋〔採用〕（`history` の before/after スナップショットから算出。採用しても「決定」まで未保存）
- [x] 7-3 当選記録後のクレジット登録促しトースト（1回のみ）
- [x] 7-4 **`DEFAULT_LOAN_RATE` を 46.6→50 に変更**（46.6は実在しない貸出レート）。店舗マスタの `lendRate` を未使用区間の初期値に採用。手動設定は `intervalLoanRateManuallyAdjusted` フラグで追跡。L10961の使用判定は実質使用痕跡方式へ
- [x] 発端：KEIZ高岡（46枚）で3千円が140枚と換算されるズレを夢爽が発見

**案1：バタフライ／ときめきゾーンの自動判定** `612dad7`
- [x] バタフライ成功/失敗ピッカーを廃止。`liquidGame` から自動分岐（0=バタフライ / 1〜32=ときめき / 33+=通常、null は安全側の fail 経路）
- [x] `nangokuZoneLabel()` に判定を集約、4箇所の表示へ反映
- [x] **実データ310件で内訳完全一致を確認**（バタフライ168 / ときめき58 / 通常84）。液晶0の168件は全件データG=9＝バタフライゾーンの9Gが液晶に加算されない仕様と一致
- [x] 新フィールドなし。`normalizeHitEvent` はハッシュ一致

**機種知見（案1で確定）**：バタフライゾーン（最大9G）では液晶Gが加算されない。オフセット+9の正体はバタフライ1回通過分であり、通過のたびに蓄積する。

**区間カードの余白バグ** `3f4fb2b`
- [x] `.timeline-text` の `pre-wrap` がテンプレートリテラルの改行を可視化していた。HTML要素用の新class 2つへ分離

### 2026-07-27 — 容量問題の完結・テスト復旧・連携

**#10 mode-estimator フェーズ2** `a84320d` — リプフラ区間を読み取り専用で取り込み（draftLog優先・液晶G基準・飛翔/超飛翔起点）。稼働中のため nerai-record は無変更で実施
**案5 アーカイブ** `9831670`/`ca17dd9` — 書き出し→確認→削除。`persist({confirmDanger:true})` で保護ガード通過。JSONは既存フォーマット同形で `restoreJson()` 互換
**束E テスト復旧** `3944b5a`/`4aa59c3` — 期待値5箇所更新＋**正規化の収束保証**（1回で収束しないデータ形状を発見、上限4回の反復へ）
**束D 容量UX** `af5d407`/`7cba0fd` — 「空き容量を確保」1タップ化、内部キー名の除去、ガイド追記

### 2026-07-26 — 初日：発掘・監査・容量対策

**束C 容量恒久対策** `1a3fd40`/`d60ec26`/`7d2d52f` — 174%→79%。原因1: premigrate永久ループ（正規化結果の書き戻し欠落）/ 原因2: 日次バックアップ3世代 / 原因3: 容量予算チェック不在。Node上で実データ再現により特定
**束B-2** `0e1a117`/`f353589` — SDC突入タップ化（さざなみ46件 vs SDC 2件の実測が根拠）・Wake Lock
**束B-1** `6acd872` — 容量診断パネル（原因2・3の特定に寄与）
**束A** `9126436` — レガシーUI削除・sticky衝突解消・safe-area対応。**インシデント：未指示の圧縮レイヤー混入→revert。§9-4の起源**

### 本セッション以前
**他台メモの日付トグル** `9977155` — 実機確認完了

---

## 9. 運用ルール

### 台帳の運用
1. 新しい構想が出たら §5 に追記。チャットに置いたままにしない
2. 完了したら §8 へ移す。破棄は §6 に理由付きで残す
3. 夢爽の宿題も §3 に書く

### ミコトへの依頼
4. 保存形式に触る変更は必ず単独タスク
5. 指示書には制約文を明記。行数差分は **origin/main 基準**。指示外の差分は内容を問わず差し戻す
6. 完了条件：コミットハッシュ＋push確認＋raw反映確認＋本番URL 200確認
7. 付随ファイルが必要になったら実施前に報告させる
8. **稼働中は `nerai-record.html` に触らせない**。別ファイルの作業に回す

### 対症療法を避けるための3つの問い（夢爽 → シオン）
9. 「消す前に、それを作ってるのは何？」
10. 「これは対症療法？ 根本治療？」
11. 「2回目だよね？」

### シオンの既定動作
12. 削除・回避を提案する前に生成元を確認する
13. 修正案には対症療法か根本治療かを明記する
14. 同じ症状の2回目は、修正案より先に原因調査を出す

### 設計原則
15. **観測値を記録し、導出は後回しにする。** 案1・案3・案7がすべて「保存形式に触らず導出だけ」で実現できたのは、この原則が最初から守られていたため
16. `pre-wrap` を持つclassでHTML要素を包まない
17. テストが落ちたまま放置しない
18. **基準機で検証してから横展開する**（§0）。データの薄い機種へ未検証の仕組みを広げない

### その他
19. メメントモリ側の構想は `mementomori-miresto/IDEAS.md` で別管理。混ぜない
