# WISHLIST.md — slot-tools 統合バックログ

最終更新：2026-07-27 / 管理：シオン（Claude）
対象：`homuratakaaki-ops/slot-tools`（主に `nerai-record.html`）
現在の HEAD：`7d2d52f`（束C完了時点）

> **このファイルの目的**
> 構想・要望・宿題をチャットに置かず、ここに集約する。
> 2026-07-26 に、過去チャットから未実装項目を12件発掘する作業が発生した。同じことを二度やらないための台帳。
>
> **使い方**：迷ったら §1 だけ見る。新しい構想が出たら §5 に追記する。完了したら §8 へ移す。

---

## 1. 次にやること（迷ったらここだけ見る）

| 順 | やる人 | 内容 | 参照 |
|---|---|---|---|
| 1 | 夢爽 | このファイルをリポジトリ直下へ配置 | — |
| 2 | シオン | **束D** の指示書作成（一般ユーザー向け容量UX） | §5-6 |
| 3 | 夢爽 | 実機確認2点（SDCタップ式 / Wake Lock） | §3 |
| 4 | シオン | **案5** の指示書作成（ログのアーカイブ） | §5-5 |

**ブロッカーはゼロ。** 容量問題は 2026-07-26 深夜に恒久対策まで完了した（§8）。

---

## 2. 現在の状態

| 指標 | 値 |
|---|---|
| localStorage 使用量 | **4.0MB / 79%**（前日は174%） |
| 内訳 | 本体1.8MB ＋ `last_good`1.8MB ＋ `checkpoint`0.36MB |
| データ | ログ10件 / 他台カード9件 / 他台メモ19件 / 機種4件 |
| タイムライン総数 | 約1,700件 |

**79%が現設計の下限。** 本体とその複製1本は構造上減らせない。これ以上下げるには本体を小さくする必要があり、それが案5（§5-5）。危険域は80%なので、当面の運用に支障はない。

---

## 3. 夢爽の宿題（実機・手作業）

ミコトの環境では実施不可。

**実機確認（未消化）**

- [ ] **B-2-1**：SDC突入タグをタップ → `prompt()` ではなくタップ式ピッカーが出るか。「スキップ」を押してもエントリが残るか
- [ ] **B-2-2**：実戦モードを開いて放置 → 画面が消灯しないか。ホームへ戻って復帰後も維持されるか

**データ整理**

- [ ] 確認用カード3枚の削除：台706（7/26）/ 台不明（7/26 23:59）/ 台不明（7/21 00:00）

**その他**

- [ ] #11 からくり2の実戦通し検証（打ち始め→収支登録→整理ビュー→PNG出力）

**容量に関する注意**

「前回の続きから入力する」を押すと `premigrate` が**1回だけ**作られることがある。下書きが確定済みログから引き継がれる際に正規化差分が生じるため。束Cの修正で直後に書き戻されるので、そこで止まる。**2回目以降も増え続ける場合は報告すること。**

---

## 4. 実装キュー（ミコト向け・優先順）

**保存形式に触らない枠**

- [ ] **束D** 一般ユーザー向け容量UX → §5-6
- [ ] **案3** リプレイカウンターの液晶200/201分割 → §5-3
- [ ] **案2** リプフラ区間の複数選択 → §5-2

**保存形式に触る枠（必ず単独コミット）**

- [ ] **案5** 古いログのアーカイブ → §5-5
- [ ] **案1＋案7 統合** 当選記録フロー刷新 → §5-1 / §5-7

**推奨順序**：束D → 案5 → 案1＋案7 → 案3 → 案2
容量系（束D・案5）を締め切ってから記録精度（案1・案7）へ移る。土台を安定させてから分析の本丸に触る。

---

## 5. 構想・仕様

### 5-1. 案1：バタフライ成功/失敗 → ときめきゾーン当選G記録

**保存形式：触る** / **案7と統合して設計すること**

成功/失敗の2値をやめ、ときめきゾーンでの当選ゲーム数を記録する。成功/失敗は値の有無から導出できるため、情報量が増えて失うものがない。SDC判定と連チャン把握も容易になる。

| フィールド | 内容 |
|---|---|
| `tokimekiGame` | 液晶基準のゾーン内相対G（1〜32）。`null` = 32抜け |
| 成功/失敗 | 保持しない。`null` 判定から導出 |
| 絶対位置 | 追加不要。既存の `liquidGame` / `game` が保持済み |

**確定事項**

- ときめきゾーンは**液晶32G**。当初メモの「42」は既存の42Gチェーン自動検出との混同
- **記録は液晶基準で統一する**
- 「液晶32 = データカウンター41」は**オフセット+9の状態で観測した値であり、定数ではない**。本ツールは `liquidGame` / `game` / `liquidOffsetDelta` を持ち、超飛翔後ベルでオフセットが蓄積するため、データカウンター側の値は状況により変動する

**移行方針**：旧バタフライ成功/失敗の既存データは遡及不能。旧フィールドを残して新フィールドを併設し、集計側で「旧形式は成功/失敗のみ扱い」とする。過去データは捨てない。

### 5-2. 案2：リプフラ区間の複数選択

**保存形式：触らない**

リプレイフラッシュの集計区間を、**任意の複数区間**から選択できるようにする。集計スコープの拡張のみで完結する見込み。緑サイクル仮説の検証サンプルが増える。

### 5-3. 案3：リプレイカウンターの液晶200/201分割

**保存形式：触らない**

液晶200以内 / 201以降 の2分割。境界は**液晶基準**（オフセット蓄積があるためデータカウンター基準は不可）。

`subCounters` は `normalizeSubCounters` が任意キーを受け入れる**スキーマフリーの `{key: count}` マップ**。したがって既存キーの分割ではなく**キーを2本に増やすだけ**で済み、保存形式の定義に手を入れる必要がない。既存データは該当キーを持たないため、集計側で「旧形式」として除外する。

### 5-4. 案4：SDC突入の契機をタップ式へ ✅ 実装済み

→ §8（commit `0e1a117`）

**段階2として残っている作業**：案1の実装後、選択肢1「ときめき32G目」は自動導出可能になるため4択→3択へ縮小する。**案1と同時に対応すること。**

### 5-5. 案5：古いログのアーカイブ

**保存形式：触る**

本体1.8MBを縮小する。使用量79%を下げる最後の手段。

- 対象は7/18〜7/22あたりの古いログ（全体の約30%）
- JSONへ書き出したうえで、アプリ内から削除する
- 削除前に書き出しが完了していることを必ず確認させる（`downloadJsonText` の既存パターンを踏襲）
- 書き出したJSONを後から読み戻せる導線も必要（閲覧専用でよい）

**注意**：単なる削除機能にしないこと。「書き出し → 確認 → 削除」を1つの流れにする。

### 5-6. 束D：一般ユーザー向け容量UX

**保存形式：触らない** / 束Cが前提

**問題**

現在の警告文は内部キー名を露出している。

> 退避データ（**last_good / prerestore / checkpoint**）の削除を検討してください。

一般ユーザーには意味が通らず、行動に移せない。この文言はシオンがB-1-3で指定したもので、夢爽向けの前提で書いた設計ミス。

**さらに深刻な点**：これは夢爽固有の問題ではない。束C以前は実効容量が 5MB ÷ 7本 ≒ 700KB しかなく、一般ユーザーは**10〜15回の稼働で上限**に達する計算だった。束Cの予算ガードで大幅に改善したが、UI側は未対応のまま。

**実装内容**

1. **「空き容量を確保」ボタン1つ**で、JSON保存 → 不要な退避削除 → 使用量再計算までを自動実行。ユーザーに判断させない
2. **文言を平易にする**。内部キー名を出さない
3. `nerai-guide.html` に容量の記述を追加（現状ほぼ記載なし）
   - データはこの端末のブラウザにのみ保存されること
   - 定期的なJSONバックアップの推奨

3番は保存形式にもコードにも触らないため、**夢爽さん単独でも進められる**。

### 5-7. 案7：獲得枚数 → ボーナス終了時クレジット枚数 ＋ 投資ワンタッチ

**保存形式：触る** / **案1と統合して設計すること**

**変更内容**

1. 当選記録フロー（当選契機 → ボーナス種別 → 獲得枚数）の3番目を、**ボーナス終了時のクレジット枚数**に変更する
2. 実戦モードに**「1000円追加投資」のワンタッチボタン**を追加する

**なぜクレジット枚数が優れているか**

獲得枚数は**計算した値**、クレジット枚数は**見たままの値**。獲得枚数は「終了時−開始時」の暗算または払い出し表示の記憶を要し、実戦中はミスを生む。

記録としても上位互換：

```
差枚 = 現在のクレジット − 総投入枚数
```

獲得枚数の積み上げは誤差が蓄積するが、クレジットは**毎回が絶対値なので誤差がリセットされる**。有利区間差枚推定に向く。

**成立条件（確認済み）**

コンプリート機能の警告が15,000枚、上限2万枚未満。**クレジット表示は上限なく増える**ため、物理メダル機の50枚頭打ち問題は起きない。案7は成立する。

**設計上の要点**

- 投資は**「円」で記録する**。1000円あたりの枚数は等価50枚・46枚貸しと店舗で変わるため、枚数で持つと後から直せない。円で貯めてレートは稼働ログ側に持たせる
- 誤タップ用の Undo が必要
- 既存の `payout`（獲得枚数）フィールドを持つデータがあるため、旧フィールドを残して新フィールドを併設する

**案1との統合が必須の理由**

案1と案7は**同じ当選記録フロー（当選契機 → ボーナス種別 → 獲得枚数）を触る**。別々に実装すると同じ箇所を2回壊すことになるため、「当選記録フロー刷新」として一度に設計する。

---

## 6. 積み残し

- [ ] **#3** PWA化（Service Worker / manifest.json）— 参照ゼロ
- [ ] **#6** ダイアログの管理UI改修 ※案4で一部消化
- [ ] **#7** タップ領域44px未満 — 基底 `.btn-small`（実高さ約28px）と `.shop-note-counter-btn`（32px）
- [ ] **#8** shopNote使い勝手（アクティブカードの識別、メモ入力欄が狭い）
- [ ] **#10** mode-estimator フェーズ2（repflash-bayes 連携）— 参照ゼロ
- [ ] **#12** 東京喰種の積み残し（ピエロ追加セリフ／AT終了ボイス／スイカ持ち越し／実G照合）※判定保留

**#7の注記**：`.btn-small` の5定義は重複ではなくスコープ付き（`.inline-field .btn-small` 等）。競合はしていない。基底の `min-height` を上げると適用箇所が広範なため、疑似要素でタップ領域のみ拡張する方式を検討する。

### 破棄した項目

- ~~**#9** 実戦モードの入力順変更（当選契機をバタフライ結果より前へ）~~
  **破棄理由**：案1（§5-1）が上位互換。案1では成功/失敗の分岐そのものが消えるため、`selectNangokuButterfly('success')` が4ステップをスキップする問題も同時に解消する。旧仕様前提の修正であり、実装してはならない。

- ~~**#5** Wake Lock~~ → 実装済み（§8）

---

## 7. 判断保留・環境整備

- [ ] **回帰テストの期待値更新** ← 束Cで判明。`tests/nerai-record-regression.test.js` が「正規化差分があっても本体rawを書き戻さない」旧仕様を期待しており、**テストが不具合を仕様として固定していた**。放置すると以後ずっと落ちる
- [ ] テストが日付をハードコードしている件（別の実バグ）
- [ ] **ミコトのローカルが origin/main とずれている疑い**。束Cの行数報告が自身のB-2報告と47行食い違った（差分+43は一致）。行数チェックは無断混入の検知手段なので、基準がずれると機能しない。**origin/main から取得した状態で計測させること**
- [ ] ミコト環境で `node` が PATH にない（回帰テストが実行できない）
- [ ] A-2 の `--tab-bar-h:46.2px`（小数値）を整数へ丸めるか。端末のフォントレンダリング差で1px前後ずれる。実害が出てから対応でよい

---

## 8. 完了ログ

### 2026-07-26 深夜 — 束C：容量問題の恒久対策

`1a3fd40` / `d60ec26` / `7d2d52f`

使用量 174% → **79%**。原因はNode上で実データを再現させて特定した（推測ではない）。

- [x] **原因1【主犯】premigrate の永久ループ**
  `loadData()` は正規化差分を検出すると premigrate（1.8MB）へ退避するが、**正規化結果を書き戻していなかった**。生データが「正規化前」のまま残り、次回ロードでも同じ差分が出て永久に書かれ続けていた。
  実測した差分：`draftLog` があるとき `state4Registered:true→false` / `finalized:true→false` / `excludeFromStats:false→true` が必ず書き換わる。`draftLog` が `null` のときは完全に冪等（946,215 → 946,215、2回適用しても不変）。
  → 差分検出時に正規化済みデータを `setItem` で書き戻す（`persist()` は経由しない＝`last_good` を更新しない）
- [x] **原因2 日次バックアップ3世代**
  日付が変わるたびに1.8MBを追加し最大3世代=5.4MB。0時を回って7/27になった瞬間に再生成されていた。
  → `STORAGE_BACKUP_KEYS` を1世代へ削減、旧 `backup_2`/`backup_3` は起動時に掃除
- [x] **原因3【構造】容量予算チェックの不在**
  本体1.8MBに対しフルコピー最大7本＝約12.9MB。上限5MBの2.5倍を超える設計で、どの書き込み経路にもチェックがなかった。
  → `backupRawStorage()` に `makeRoomForBackupStorage()` を前置。全退避書き込みがこの関数を通るため1箇所で全経路を守れる。削除優先順は `backup_1` → `premigrate` → `prerestore`。本体・`last_good`・`checkpoint` は自動削除しない

**実機確認済**：premigrate を削除 → 無編集リロード → 再生成なし ✅

### 2026-07-26 — 束B-2：SDC突入タップ化 / Wake Lock

`0e1a117` / `f353589`

- [x] **案4** SDC突入の契機入力を `prompt()` からタップ式へ。既存 `suggestPickerOverlay` を流用
      後付け方式（エントリ確定＋保存 → 最後にピッカー起動）のため、スキップしても記録が残る
- [x] **#5** Wake Lock：実戦モード開始で取得、終了で解放、`visibilitychange` で再取得、非対応環境では無害に no-op

**判断根拠**：バックアップの実測で、さざなみ（タップ式）46件 vs SDC突入（prompt式）**2件**。同じ人が同じ実戦中に使って23倍の差。データ価値ではなく入力UIの問題であることが数字で確定した。

### 2026-07-26 — 束B-1：容量診断・クリーンアップ

`6acd872`

- [x] localStorage 全キーの容量診断パネル（読み取り専用）
- [x] `checkpoint` を削除可能キーへ追加（`STORAGE_DELETABLE_KEYS` 新設、`RECOVERY_KEYS` は非破壊）
- [x] 警告文から「アーカイブ」を削除（当時その機能は存在しなかった）

この診断パネルが原因2・3の特定につながった。

### 2026-07-26 — 束A：表示層のみ

`9126436`

- [x] **A-1** `shopNoteOverlay` 内の旧レガシー入力UIを物理削除 ＋ `hideLegacyShopNoteControls()` 削除
- [x] **A-2** `--tab-bar-h`（実測46.2px）を導入し sticky の top 衝突を解消
- [x] **A-3** `.bm-overlay` の padding を safe-area 対応式へ（実機確認済 ✅）

**インシデント**：同コミットに未指示の localStorage 圧縮レイヤー（18ハンク／+64行）が混入。指示書で「触るな」と明記した層だった。後に revert 済み。§9-4 のルールはこの一件から。

### 本セッション以前 — 他台メモの日付トグル

`9977155`

- [x] 日付2択トグル（既定 `todayValue()`）
- [x] 作成後のヘッダー同期
- [x] 実機確認①②③すべて完了 ✅

---

## 9. 運用ルール

### 台帳の運用

1. **新しい構想が出たら §5 に追記**。チャットに置いたままにしない
2. **完了したら §8 へ移す**。破棄した場合は §6 の「破棄した項目」に理由付きで残す（再発掘を防ぐため）
3. **夢爽の宿題も §3 に書く**。ミコトのタスクだけ管理してもタスクは行方不明になる

### ミコトへの依頼

4. **保存形式に触る変更は必ず単独タスク**。他の変更と同一コミットにしない
5. **指示書には制約文を明記**し、完了報告では**行数差分を origin/main 基準で**確認する。指示外の差分が出た場合は内容を問わず差し戻す
6. 完了条件は従来どおり：コミットハッシュ ＋ push確認 ＋ raw反映確認 ＋ 本番URL 200確認

### 対症療法を避けるための3つの問い（夢爽 → シオン）

7. **「消す前に、それを作ってるのは何？」**
   削除・回避・リセットを提案されたときの定型質問。2026-07-26 にこれを聞かれていれば、premigrate の再生成に即座に気づけた
8. **「これは対症療法？ 根本治療？」**
   シオンは必ず答えられる。答えが対症療法なら、急ぐか根本を待つかを夢爽が選べる
9. **「2回目だよね？」**
   同じ症状が2度出たら、そこで修正を止めて原因調査へ切り替える

### シオンの既定動作

10. 削除・回避を提案する前に、**生成元を必ず確認する**
11. 修正案を出すとき、**対症療法か根本治療かを明記する**
12. 同じ症状の2回目は、**修正案より先に原因調査を出す**

### その他

13. メメントモリ側の構想は `mementomori-miresto/IDEAS.md` で別管理。混ぜない
14. 設計原則：**観測値を記録し、導出は後回しにする**。計算値を保存すると誤差が蓄積し、後から検証できない（案7の設計根拠）
