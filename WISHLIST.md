# WISHLIST.md — slot-tools 統合バックログ

最終更新：2026-08-01（デバッグフェーズ） / 管理：シオン（Claude）
対象：`homuratakaaki-ops/slot-tools`
現在の HEAD：`7238e5d`（2026-08-02 実戦改善フェーズ完了時点）
データ schema：**v4**（束AL で保存スリム化。読み込みはフル形補完・保存時のみ省略）

> **このファイルの目的**
> 構想・要望・宿題をチャットに置かず、ここに集約する。同じ発掘作業を二度やらないための台帳。
> **使い方**：迷ったら §1 だけ見る。新しい構想は §5 へ。完了は §8 へ。

---

## 0. 開発方針

**L南国育ちSPECIAL を基準機とし、検証済みの仕組みのみ他機種へ展開する。**
（東京喰種：v1持ち込み方式で実戦1日でv2完成。カバネリ：2026-07-30 に実装済み（束AS改）・初稼働完了）

**フェーズ**：実装完走（07-31）→ **実機フィードバック反映フェーズ（6a〜6h・バグ4件修正・UI再設計3巡）**。次＝実機最終確認→記事公開準備→データ蓄積。

**入力＝実戦モード、閲覧・修正＝STEP2、STEP1＝宣言。**
束AKにより1稼働の全操作が実戦モードで完結：着席→自動反映（貯玉/持ちメダル/打ち始めG）→タップ記録→当選（クレ入力→獲得自動）→**ヤメる1タップ→STEP4→終了時メダル自動転記**→収支登録。

**quickPanel 設計思想**：枠組みは全機種共通、高頻度枠の中身は機種マスタ定義。南国＝毎Gカウント型／東京喰種＝低頻度イベント確実拾い型／カバネリ＝チャ目4枠＋状態文脈導出型（実装済み・運用調整中）。

**入力の思想**：
- **観測値を聞き、導出値は計算する**（§9-17。獲得枚数＝クレ差分、状態＝タイムラインから導出）
- **同じ観測値を2回入力させない**（§9-21）
- **修正は絶対値上書きでなく履歴に差分を積む**（§9-15）
- **整合性チェックは登録を止めない**（§9-23）
- **カウンターのリセット＝当選登録の境界**（§9-27）

**宣言的プリセット機構**（束ATで確立。機種別ロジックの重複実装を避けるための再利用表）：

| フィールド | 意味 | 導入 |
|---|---|---|
| `variantRequired` | 当選トリガーの契機（variant）選択を必須化 | 2a |
| `hitCauseFirst` | 当選入力を契機→種別の順にする | 2a-fix |
| `displayBase` | カウンターの表示起点オフセット（保存は実測値） | 2a |
| `hitFlowAutoThrough` | トリガー別に through を自動確定しステップ省略 | 2b |
| `hitThroughNoFlow` | through=no 時の保全・自動タグ・示唆シート直行 | 2c |
| `chanceFollowUp` | 単独チャ目タップ後の4択（発光/高確の開始タグ合成） | 2d |
| `startViaFollowUpOnly` | 状態ピッカーから開始選択肢を除去（ONは4択経由） | 2d |
| `stateAutoEnd` | タグ登録時に終了タグを同一エントリ自動合成 | 2e |
| `chanceFollowUpMulti` | 複合チャ目の二段階フォロー（members 定義） | 2f |
| `preserveOnArchive` | カウンターをアーカイブのリセットから除外 | 3a |
| `numberPicker`（endOption） | 状態終了時の数値ピッカー＋attachments 保存 | 3c |
| `triggers`（flow step） | hit フローステップのトリガー条件付け | 3d |
| `choiceTextColor` / `labelParts` | 選択ボタンの中立配色・部分色 | 4a |
| `badgeStyle` | 状態バッジの特殊装飾 | 4a |
| `flattenItems` | 示唆プレースの1階層目直展開 | 4b |

---

## 1. 次にやること

| 順 | やる人 | 内容 |
|---|---|---|
| 1 | 夢爽 | 実機最終確認：テンキー値チップ／リロード耐性／比較ビュー／系譜ビューのスクショ |
| 2 | 夢爽 | 技術介入ボイス全種リスト提出（S-F用） |
| 3 | 夢爽 | 恒常運用：ヤメ時の設定ラベル付与を継続し、カバネリ実測データを蓄積 |
| 4 | シオン | 示唆まとめ記事の構成案（記事公開準備・データ蓄積戦略） |
| 5 | ミコト | 待機（新ストックは凍結。次の実装は個別指示書待ち） |

**PWA②凍結カウント起点：2026-07-30 夜**（束AS改＋指示外PWA revert後。PWAは§5-Aの条件を満たすまで着手禁止）。

---

## 2. 現在の状態

| 指標 | 値 |
|---|---|
| localStorage | 容量実測完了：カバネリ全部入り1稼働=150,299chars→5MBで約33稼働。プールサマリー=136chars/件（キー込み約298B）→事実上無制限。運用方針：フルログは随時削除可（プールはS/7mで保全） |
| 回帰テスト | PASS。テスト資産：復元系5本相当＋AL往復・legacy互換＋AK/AM/AS改 |
| 実装キュー | **空**。束AT第6波〜7oまで完了。新ストック S-A〜S-G は凍結中（個別指示書待ち） |
| schema | v4。**実データ検証済み：1,074,884→813,804 chars（24.3%減）・保存→再読込→再保存でバイト完全一致（収束保証）** |
| バックアップ | 07-29 JSON＋アーカイブ2件（夢爽の手元。恒久保管場所への退避は§3） |

---

## 3. 夢爽の宿題

- [ ] 実機チェック残：①テンキー値チップ（6h反映後） ②リロード耐性（再読み込みで記録残存） ③比較ビュー表示（設定ラベル付与後） ④系譜ビューのスクショ（記事素材・最後の1枚）
- [ ] 提出物：技術介入ボイス全種リスト（S-F用）
- [ ] 恒常運用：毎稼働のヤメ時＝設定ラベル付与。これが #25/7m プールの生命線
- [x] シオンのタスク：容量予算の再計測 → 完了（§7容量予算欄へ転記済み）
- [x] 済：メモ復元（経路A）／チャ目4択の見た目／AKヤメ導線／束AG・AI・ABの実地確認／ST区間フロー／STEP4推定設定シート化

---

## 4. 実装キュー（ミコト向け）

**空。** 束AT第6波〜7oまで完了。新ストック S-A〜S-G は§5へ起票済みだが、台帳記載は実装許可ではない。次フェーズは実機最終確認→記事公開準備→データ蓄積。実装は個別指示書の発行を待つこと。

---

## 5. 構想・保留

### 5-A. PWA②：オフライン化
凍結1〜2週間で発動。「新版検知→タップで更新」必須。台帳記載は実装許可ではない。

### 5-B. 案8：アーカイブ閲覧専用ビュー ／ 5-C. 案9：AI pre-check
従来どおり保留。

### 5-D. からくり2（材料確保済み・保留）
解析：https://chonborista.com/slot/sankyo-slot/256699/（AT終了画面6種・ボイス8種調査中・CZ終了画面12種※天国系は予想＋反例あり・ピエロセリフ4種・天井 液晶1200/実890/CZ4スルー5回目/AT間実2500・リセットCZ天井500）。登録時は確定/示唆/予想の3段階（§9-22）。

### 5-E. 東京喰種（v2運用中）
残：スイカ持ち越し／実G照合。v2実機評価の残り＝上段3ボタン・招待状直行。

### 5-F. 案10：quickPanel編集UI
v2固定配置の運用実績待ち。原則：頻度が高いほど大きく・右（§9-24）。

### 5-G. 案11：入金残高の照合
AD-2（今日の現金合計）の運用実績を見てから。保存形式に触るため慎重に。

### 5-I. カバネリ（v1実装済み・初稼働完了）
- **実装状況**：束AM〜束AS改でv1実装済み。2026-07-30に初稼働完了。束AT 実装対象も完走。残＝#2/#3 の実機確認のみ。
- 解析：https://chonborista.com/slot/sammy-slot/248689/（サミー・AT機・純増6.0・天井996G or 6周期・チャ目合算1/15・評価1.3の荒れ台）
- **検証テーマ**：「どの示唆が出てから対応チャンス目を何回入れたらCZが来るか」の分布実測。規定pt非公開＝空白地帯。示唆の信頼度定量化はユーザー需要も大（note化有望）
- **現行UI骨子**：quickTags＝無名/生駒/カバネ/複合、events＝状態/pt示唆/おみくじ/ステージ、その他＝当選・ヤメ・上位STなど、カウンター＝周期/下段ベル、チャ目バー＝AS改の状況ビュー付き
- **状態トグル**：開始/終了のタグ対で記録し、現在状態はタイムラインから導出。状態バッジ・法則配置・中立背景＋キーワード色方式まで実装済み
- **CZの扱い**：§9-27適用。CZ3種＝カウンター非リセット→通常時イベント（突入/成功/失敗タグ）。駿城/エピボ＝リセット→hitフロー
- 初稼働フィードバック25件は§5-Jへ隔離。台帳記載は実装許可ではない

### 5-I-2. 新ストック（08-02 起票・実装凍結）

> **重要**：以下は構想・判断材料の記録であり、**実装許可ではない**。実装は個別指示書の発行を待つこと。

- **S-A note発信者連携**：招待制インポートコード方式。コード=Σ分子/Σ分母+メタの圧縮文字列、生成器非公開、出所必須表示、公式連携リスト、規約に転用時の連携解除と経緯公表を事前明記。初号提供者=夢爽自身（南国SP note）。**実装条件：#25プールの実測蓄積後**。
- **S-B トロフィー履歴ビュー**：台の累計回転数×トロフィー出た/出なかった一覧。データは既存（開始G+hitEvents+トロフィー「なし」記録）で揃っており表示のみ。店長カスタムのG数区間・環境変化検知に直結（記事2-1連動）。
- **S-C 生駒CZライフ減算記録**：CZ中のハズレ/減算/回復の構造化。前作のライフ減算部分の設定差が根拠（記事4-2「予定」節と連動）。
- **S-D 区間遷移時の状態自動セット（汎用化）**：区間遷移（ST終了・AT終了・リセット等）で確定する状態を宣言的に自動反映する機構。6eのST終了→カバネ高確が先行事例。他機種（南国の天国示唆等）へ展開可能な形で設計する。
- **S-E 5000G閾値トラッカー**：台総回転数=開始G＋データG累計＋ボーナス内部補正（駿城中約20-21G等、データカウンタ停止分）。ただし近似値のため、台メニュー／マイスロログイン画面に表示される「総ゲーム数」を最優先の真値とし、任意入力でトラッカーを上書き補正（キャリブレーション）できること。入力があった時点以降の推定はその値から積み上げる。総回転数〜4000Gは何もしない／4000G超で「あと◯Gで5000G」案内＋ST突入時にデータカウンタ総G登録欄を条件表示／5000G接近で警告（メニュー確認の促しを兼ねる）／**5000Gを跨いだボーナス以降は示唆のG帯属性を切替**（美馬・水着系の5001G〜出現率変化に対応）。閾値は宣言的（5001/7001）に持ち汎用化。
- **S-F 技術介入ボイス全種対応**：夢爽さんのリスト受領後、その他ピッカーを網羅に更新（器は現行7種）。
- **S-G（軽微ストック）サンド関連表記の統一**：STEP4「サンド残額（返却額）」・履歴文の「サンド残」→「残高」系へ。過去ログ互換に配慮し別タスク化・7L報告時のミコト提案による。

**済の記載**
- 共通ベルカウンター撤去（6d・存在しない小役）
- 「確定級」表記全廃（6c/6f）

### 5-J. 束ATストック（束AT26） — カバネリ初稼働フィードバック（26件）

> **重要**：以下は構想・判断材料の記録であり、**実装許可ではない**。実装は個別指示書の発行を待つこと。

**完走宣言**：束ATストック（07-30 起票26件）は #2/#3 の実機確認を残し全件決着。以後の追加要望は新ストックとして起票する。

**残存**：#2/#3（実機の舵取りセリフ網羅確認でクローズ予定）

**完了（§8参照）**
- #18 実戦モードメモ入力消失修正
- #1 チャ目4択フォロー＋状態ピッカー痩身
- #9 当選契機の必須化＋周期1起点。07-30 実機フィードバックにより入力順を契機→種別へ是正（`hitCauseFirst`）
- #11 駿城失敗フロー自動遷移
- #14 エピボST直行
- #23 イベント連動の状態自動消灯
- #7 無名CZ撃破数記録
- #8 駿城ボーナスpt記録
- #10 下段ベル台単位累積
- #22 リールフラッシュ記録
- #26 複合チャ目の二段階フォロー（07-30 実機フィードバック→束AT-2f で即日実装）。運用注記：オールスターの部分集合（例：3キャラ中2キャラのみ発光）はピッカー非対応。「なし」等で登録後 STEP2 から開始タグを手動追加で補正
- #4 ステージピッカー§9-28化
- #6 pt示唆2タップ化
- #12 アイキャッチのデフォルト選択肢
- #21 複合チャ目シート配色
- #24 超高確バッジ視覚仕様
- #17 チャ目状況の5分類表（凍結仕様 v4）
- #5 状態バッジのワンタッチ終了
- #19 G数到達の高確自動対応
- #13 キャラ別チャ目→CZ系譜ビュー（凍結仕様 v2）
- #15 ST区間フロー統合
- #16 ST中共通ベル＋総G転記欄
- #20 トロフィー入力
- #25 ラベル別プール集計＋比較ビュー（凍結仕様 v2）

**束AT-4（表示・配色）**
- **#13 キャラ別チャ目→CZ系譜ビュー【完了・凍結仕様 v2】**：1行=1系譜（無名・生駒=CZ間、カバネ=周期単位）。チャ目チップ（#17 と同一5分類記号・無地）＋示唆/イベントチップ（枠付き・G数付き）。輪廻くじ採録：無名・小吉/中吉→無名、生駒系→生駒、周期系（◯廻/輪廻の果報/六根清浄）→カバネ周期。**圧縮は `無→無` のみ「×n」可。発光系・🔥・⚡は個別チップ必須**（凍結理由：発光はCZ起点であり、約20G後のCZとの対応を個別に追えることが本前兆中チャ目数の把握→次CZ必要数→設定判別の精度・速度に直結する。一般則：圧縮してよいのは情報を失わないものだけ）。`無→💡` にG数付記。完了3本＋もっと見る、進行中は金左線。リールフラッシュは採録先の根拠なしとして初版見送り（実測で対応が見えたら追加）。
- **#17 チャ目状況の表形式化【完了・凍結仕様 v4】**：列＝`無→無`／`無→💡`／`💡`／`💡🔥`／`🔥`／`⚡`／計（4択と同一語彙）。行＝無名/└複合/生駒/└複合/カバネ/└複合/合計。分類は成立時点の滞在状態（§9-31）で、優先順位は ⚡＞💡🔥＞💡＞🔥＞無→💡＞無→無。**🔥判定は該当キャラの高確のみ。チャ目高確は列判定に用いない**（減算優遇の主体はキャラ対応カバネリ高確。判定は純関数1箇所に閉じ込め済み・将来変更可能）。持ち越し分離：無名・生駒＝CZ開始から液晶G遡り20G以内／カバネ＝最終防衛ステージ滞在中。核心指標＝無→💡率（単独・無名+生駒・通算固定）。分母0は「—」。高確中の発光有無はデータ保持のみ。「高確中なのに発光なし」の実測が出たら列分割を検討。
- **#19 G数到達の高確自動対応【完了】**：チャ目高確（50G飛び）＝自動ON＋自動フラグ。カバネ高確系（0/100/250→高確、450/650→超高確）＝1タップ確認プロンプト。液晶G基準・G数テーブルはプリセット定義。実装はG加算イベント内の前後値比較に限定するオプトイン方式で、手動補正・リロードでの誤発火が構造的に起きない。

**束AT-5（データ基盤）**
- **#25 ラベル別プール集計＋比較ビュー【完了・凍結仕様 v2】**：指標は3つに厳選（下段ベル／無→💡率／単チャ目3000pt率）。共通ベル・CZ成功率は指標から除外（記録は継続、比較対象としては採用せず）。設定ラベルは2軸：`n推定`（exact/estimated）・`n以上確定`（atLeast/confirmed・トロフィー等の根拠がある場合のみ）。偶数/奇数グループは構造のみ将来対応。外部参照データセット（他サイト実測の転記）は初版から除外・器（出所フラグ）は温存。無→💡率は公表解析値が存在しない独自指標＝band なし「実測蓄積中」表示。**蓄積データ自体が note の独占素材**。

**束AT-2/3 完了**
- **#15 ST区間フロー統合【完了・選別確定】**：ST中タリーはマイスロ委譲（§9-33）。ツール側は「取り込めない確定系（1段目常時）」「マイスロ代替の選択式（折りたたみ・マイスロを出す時間を省く用途）」「その他シートの冗長導線」の三層。夢爽発案の開閉式構造。
- **#16 ST中共通ベル＋総G転記欄【完了】**：共通ベル `commonBell` は `preserveOnArchive` で台単位累積。マイスロ転記欄は総G・CZ回数・ST回数を任意入力として受ける。
- **#20 トロフィー入力【完了】**：ST終了時と駿城失敗後の両方に任意配置。なし/銅/銀/金/キリン/虹を示唆登録し、確定系示唆として期待度系と分離。

**その他**
- **#2 青セリフに舵取り系追加**（青文字）。会話演出プレースに舵取り・青文字／舵取り・返答あり（強）が定義済みであることを確認（07-30）。次回稼働で実機の舵取りセリフがこの2項目で拾い切れるか確認。OK ならクローズ。
- **#3 カバネ対応示唆の欠け**：#2 で解決見込み。他演出で発見あれば追加。会話演出プレースに舵取り・青文字／舵取り・返答あり（強）が定義済みであることを確認（07-30）。次回稼働で実機の舵取りセリフがこの2項目で拾い切れるか確認。OK ならクローズ。
- **#5 バッジのワンタッチ終了（完了）**：原案の「再タップで開始」は廃止。理由：バッジはactive状態のみ表示でOFF後にタップ対象が消える／2dのON経路一本化と矛盾するため。ON＝4択・自動、OFF＝バッジ・自動・シート、で状態ライフサイクル完結。
- **示唆マスタ拡張：実測タリーモード**：旧 suggestNotes 構想（07-28・実測/転記2モード）を吸収。転記＝既存の示唆マスタ登録が担う。前提＝からくり2 示唆マスタ本体の実装。南国SP はマイスロ非対応の可能性があるため実測タリーの価値が残る。

---

## 6. 課題・低優先

### 6-A. 容量問題 → **第2弾まで解決済み**（経緯は§8）
運用ルール：月次アーカイブ＋診断パネルの定期確認。v4安定後（1週間目安）にpremigrateを「空き容量を確保」で掃除可。

### 6-B. 低優先
- [ ] #8 shopNote使い勝手 ／ #6 ダイアログ改修の残り ／ #7 残余28px
- [ ] 旧 `quickTagIdSet()` 死にコード — 近傍を触るときに掃除

### 破棄・完了（再発掘防止）
- ~~#9／#5／#10／案1-5,7／束A-H／PWA①／#7絞込／#12誤帰属／§6-A容量~~ → §8 と §5-D

---

## 7. 判断保留・環境整備
- [ ] ミコト環境の node PATH ／ global gitignore warning ／ `--tab-bar-h` 丸め ／ アーカイブUI 0件表示
- [x] 容量予算の再計測：カバネリ全部入り1稼働=150,299chars→5MBで約33稼働。プールサマリー=136chars/件（キー込み約298B）→事実上無制限。運用方針：フルログは随時削除可（プールはS/7mで保全）。
- [ ] AGENTS.md 注記候補（ミコト起票）：ミコト環境は PATH に node が無く `node --test` 直接実行不可。REPL/createRequire ラッパーで全スイート完走を確認し、シオンが正規 `node --test` で二重検証する運用（確立済み）。※本依頼では AGENTS.md 変更禁止のため台帳起票のみ。

---

## 8. 完了ログ

### 2026-08-01〜02 — 束AT 第6波／実戦改善フェーズ総括

- **6a**（`1c3d373`/`14efa0d`/`38997ba`＋微修正`cead7ab`）— 実機バグ2件修正＋表記統一。pt示唆の保存経路（4b退行）／エピボの空ステップ即完了→`kabaneriStStart`新設／「確定級」全廃・「設定N」統一。
- **6b**（`e416b2b`）— ST中パネルのレイアウト重なり修正→**切り替え式UI**。ST中は通常時入力を隠す。`renderBattleModeGrid`入口分岐。
- **6c**（`4bc6a8d`）— **undo巻き添え消失バグ修正**。シオンが動的デバッグで発見：示唆タップが非commit経路のため、他操作のsnapshot復元で示唆が消失。示唆登録4関数をcommit経路化・直近フィードをtimeline+suggestLogマージに。
- **6d**（`28fd61b`）— ST中パネル再構成：表=高頻度（ボイス個別5種・紹介2・下段ベル）／その他=確定系。**共通ベル完全撤去**（本機に存在しない小役・4章レビューで判明）。下段ベルST中分は比較ビュー分子から表示時導出で除外。
- **6e**（`f8eeb1d`）— ラベル明確化・集計行常設・テンキー化・転記欄縮小・**ST終了→0Gカバネ高確プロンプト**（実機確認済み・二重防止）。
- **6f**（`2edc8da`）— 5分類表の合計行削除・無名/生駒⚡列「—」・確定級レガシー表示のマスタ解決・**設定示唆まとめのグループ内比率化＋グルーピング**・確定系は「出現」列挙のみ。
- **6g**（`bcacdd9`）— **分母定義の1関数集約**（`settingGroupTotal`/`settingGroupPercentText`）＝パネル・直近・まとめ3面一致。景之含む全体比。「うち3000pt獲得」改名・最終ptテンキー化。
- **6h**（`216aa52`）— 数値入力の値チップ統一（`battleModeValueChipHtml`・生input廃止・誤タップ源除去）。
- **6i〜7o（08-01〜02 実戦第2〜3日・25コミット）**：6i(`bea1b6f`)カバネ高確単独開始／6j(`8edec76`)おみくじ・キリ番表記／6k(`8bee542`)バッジ折返し／6l(`8ebaddb`)ステージ文字色／6m(`2765a3e`)成立時状態の自動プリセット／6n(`35be5e0`)ST突入で通常時状態を自動クローズ／6o(`3bb27ac`)縦レイアウト自然高化／6p(`7755724`)下段ベル分子バグ修正＋ST除外撤廃（実機確認：出現率は全区間同一）／6q(`7f8732d`)景之をその他へ（§9-40定量化）／6r（既存回帰で担保）駿城 shunjoPt→through 順／6s＝6y同梱／6t(`bebac0a`)**チャ目フォローを遷移限定「ここから何が起きた？」へ再設計**（宣言的遷移表・1択は即記録）／6u(`6e79fe1`)ステージ色の経路統一・死にコード削除／6v(`040fd00`)行グリッド全廃・flexカラム化／6w(`45d2cbc`)**「前回CZから」をキャラ別起点に**（§9-42）／6x(`7ab608c`)無名/生駒高確単独開始・チャ目状況をその他へ／6y(`45cdb10`)**実レンダリング検証で残存バグ3件確定修正**（景之集計行・CSS詳細度・flex-shrink）＋バージョン表示導入／6z(`a172734`)⚡超高確のチャ目成立自動終了／7a(`1fc69fe`)確定級の最終ガード・駿城ptカチカチ式／7b(`6bde197`+`7238e5d`)他ボイス/美馬ショートカット（景之込み6択）／7c(`929fd91`)**減算大の可視化**（無→💡＋💡🔥＋🔥中→💡開始。判別ルール：減算大6以内のCZ到達=高設定の可能性）／7d(`6915256`)輪廻くじ復元＋**シード⇔実UI網羅一致テスト**／7e(`e0f475e`)状態/CZ統合・カバネ発光単独開始／7f(`0dd758d`)+7k(`933e033`)+7n(`65fddbb`)**STEP4推定設定の三段修正**（動的パネル被覆→シート式へ置換）・「以上確定」統合／7g(`68be0f1`)高確中チャ目の発光自動記録（仕様確定）／7h(`e3eaa60`)**サンド残高トラッカー**（入金→自動減算→STEP4自動反映）／7i(`224786c`)クレ更新テンキー直行／7j・合算行削除・減算大改名（`7238e5d`）／7L(`f430aac`)**南国SP投資減少のデータ破損修正**（a417fd0の機種ガード漏れ・全機種×当選登録の投資不変を恒久テスト化）／7m(`2692600`)**プールサマリー永続化**（`nerai_pool_v1`・約200B/稼働・ログ削除耐性）／7o(`f68b117`)選択ハイライト・「登録/キャンセル」文言。
- **容量実測（シオン・08-02完了）**：カバネリ全部入り1稼働=150,299chars→5MBで約33稼働。プールサマリー=136chars/件（キー込み約298B）→事実上無制限。**運用方針：フルログは随時削除可（プールはS/7mで保全）**。
- **検証記録**：シオンによる全域動的スモーク2回（28項目＋10項目・全パス）。当選ルート14本完走・ST収支通し・4fトリガー・保存復元・3面分母一致を実データで確認。**実機フィードバック→即日修正のループを8巡、全件一発検収**。

### 2026-07-30 — 容量第2弾根治（束AL）／ヤメ導線完成（束AK）／カバネリv1初稼働

- **束AM〜AS改（07-29〜30）** — カバネリ実装（3機種目・quickPanel＝チャ目3種＋状態トグル型ほか）。6巡検収済み。HEAD `08548d2`。07-30 初稼働完了（実地検収パス・フィードバック25件は§5束ATストックへ）
- **依頼外コミット事件（07-30）** — `7433a02`（PWA install prompt / index.html）を `08548d2` で revert。検収パス・クローズ。再発防止として §9-29 および AGENTS.md:31 を明文化
- **束AT 実装第1波（07-30 夜）** — **全13件・全件一発検収パス**。カバネリ初稼働フィードバックのうち、実戦導線と観測欠落に直結する即応分を実装完了
  - **束AT-1** `111b318` — #18 実戦モードメモ入力消失修正：draft2層（メモリ＋`nrBattleMemoDraft`）・visibilitychange/pagehide フラッシュ・sessionId ガード。**実機検証済み（経路A：他アプリ切替→復帰で復元 PASS）**
  - **束AT-2a** `cea66f9` — #9 当選契機の必須化＋周期1起点：契機は hitTriggers の variants＋`variantRequired`、周期は `displayBase:1`（保存0起点・表示+1・migrate不要）
  - **束AT-2b** `d8a0f76` — #14 エピボST直行：`hitFlowAutoThrough={episode_bonus:'yes'}`・`applyAtThroughBranch` へ副作用一本化
  - **束AT-2c** `1998b4c` — #11 駿城失敗フロー：`hitThroughNoFlow`（周期・液晶G保全／データG0／雪月風花 `auto:true`／キリ番シート直行）。スナップショット永続化なし（リセット除外方式）
  - **束AT-2d** `e51dbed` — #1 チャ目4択フォロー＋状態ピッカー痩身：`chanceFollowUp`・1エントリ合成・二重開始防止（`battleModeStateActiveForStartTag`）・`startViaFollowUpOnly`
  - **束AT-2e** `c73cef5` — #23 イベント連動の状態自動消灯：`stateAutoEnd`（CZ突入→該当発光終了を同一エントリ合成・自動印は表示時導出）
  - **束AT-2a-fix** `7a3e46e` — 当選フロー順序是正：`hitCauseFirst` で契機→種別へ。天井/黒煙は種別自動確定→2bと連鎖しST確認も省略
  - **束AT-2f** `5aca541` — #26 複合チャ目の二段階フォロー：`chanceFollowUpMulti`（発光→高確の二段階・部分集合はSTEP2補正運用）
  - **束AT-3a** `25ea50e` — #10 下段ベル台単位累積：`preserveOnArchive`（§9-35の実装）
  - **束AT-3b** `88be0ae` — #22 リールフラッシュ記録：`t_kabaneri_reel_flash`（プレーンタグ・その他シート自動表示）
  - **束AT-3c** `4adc09f` — #7 無名CZ撃破数：endOptions の `numberPicker`（1〜10＋不明・色帯文字色）・attachments 構造化保存（`defeatCount`）
  - **束AT-3d** `742c198` — #8 駿城pt記録：ステップ `triggers` フィルタ（`hitBranchStepAppliesToEvent`）・pt＋単チャ目3000ptトグル・attachments（`shunjoPt`/`tanCha3000`）
  - **検証**：シオンが全コミットを独立環境で検収（クローン＋回帰スイート再実行＋コード読み）。統合スモーク21項目 21/21 PASS（シード9種→チャ目4択→自動消灯→失敗フロー→下段ベル保全→メモdraft の連結シナリオ）
- **束AT 第2波（07-30 深夜）** — **3件とも一発検収パス**。カバネリ初稼働後の表示・配色・負の示唆受け皿を整備し、07-30 通算は実装16本・台帳更新4回・全依頼一発検収パス
  - **束AT-4a** `598bd1d` — #4・#21・#24 ピッカー配色の§9-28統一：`choiceTextColor` / `labelParts` / `badgeStyle:'super'`。ステージ＝中立背景＋坑道赤字/作戦会議緑字/最終防衛金字/雪月風花水色字。複合＝キャラ名部分色・オールスターのみ金字。超高確＝カバネ青＋「超」金字（#19 実装時に同スタイル流用）
  - **束AT-4b** `8ee9212` — #6 pt示唆の2タップ化：`flattenItems`。**初稼働実測（suggestLog n=16、pt示唆10件中ミニキャラ9件）を根拠**にミニキャラ8項目＋会話演出6項目を1階層目へ直展開。低頻度プレースは「▼」で従来経路維持。項目並びは実測でなくキャラ×強さの規則配置（n=9への過剰適合回避）
  - **束AT-4c** `5805bce` — #12 アイキャッチのデフォルト選択肢：`sgi_kabaneri_point_eyecatch_default` を先頭追加。カウンター型集約は見送り（初稼働のアイキャッチ実測0件のため。負荷が実測で問題化したら `flattenItems` 昇格またはカウンター化を検討——将来拡張として本行に残す）
  - **検証**：シオンが独立環境で検収（クローン＋回帰スイート再実行＋コード読み）
- **束AT 第3波（07-30 深夜〜07-31 朝）** — **2件とも一発検収パス**。チャ目状況ビューを検証用の5分類表へ固定し、状態バッジからOFFできる最短導線で状態ライフサイクルを完結。07-30〜31 通算：実装18本・台帳更新5回・全依頼一発検収パス
  - **束AT-4d** `ff7c6bb` — #17 チャ目状況の5分類表：純関数 `kabaneriChanceEyeTableSummary`（timeline→{sinceCz,total,core}）。旧・重複計上サマリを置換。実装中、指示書の推定タグID `t_kabaneri_stage_saishu` を実ID `t_kabaneri_stage_final_defense` へ読み替え（既存ID不変の制約を優先・報告明記の模範対応）
  - **束AT-4e** `cfa7b7c` — #5 状態バッジのワンタッチ終了：バッジを button 化し既存終了経路へ接続（通常状態＝即時終了、CZ系＝終了ピッカー→#7 撃破数まで連鎖）。連打二重登録防止。原案の「再タップで開始」は廃止（バッジはactive状態のみ表示でOFF後にタップ対象が消える／2dのON経路一本化と矛盾するため）
  - **検証**：シオンが独立環境で検収（クローン＋回帰スイート再実行＋コード読み）
- **束AT 第4波（07-31 昼・実質完走）** — **3件とも一発検収パス**。G数トリガー、系譜ビュー、ST区間フロー統合まで完了し、束ATストック26件中24件決着（実装22・実質充足2）。07-30〜31 通算：実装22本・台帳更新7回・全依頼一発検収パス・停止1回（テスト衝突・正しい停止判断）
  - **束AT-4f** `17ab2fa` — #19 G数到達の高確自動対応：`gameStateTriggers`（every:50=チャ目高確 auto／at:[0,100,250]=カバネ高確 prompt／at:[450,650]=超高確 prompt）。**実装側の改善**：指示書の `lastProcessedLiquidG` 変数案に対し、ミコトは発火をG加算イベント（`{triggerGameState:true}`）内の前後値比較に限定するオプトイン方式を採用。手動補正・リロードでの誤発火が「防がれる」のではなく「構造的に起き得ない」上位解
  - **束AT-5a** `d38957a` — #13 系譜ビュー：純関数 `kabaneriGenealogyChains({timeline,suggestLog,hitEvents},machine)`→mumei/ikoma/kabane 各 `{completed,ongoing}`。4d の分類を `kabaneriChanceEyeMetaRows()` 経由で再利用（複製なし）。キャラ割当は label キーワード方式
  - **束AT-5b** `ceb2736` — #15・#16・#20 ST区間フロー統合：payout スキップ（`shouldSkipHitPayoutForEvent`＝カバネリ×through=yes）・ST終了フロー（終了画面→トロフィー→最終クレ→マイスロ転記欄）・ST中パネル（1段目=確定系：アタック上乗せ44/55/66/77・456/666枚・共通ベル `commonBell`（preserveOnArchive）／2段目=折りたたみ選択式タリー：ボイス/キャラ紹介女/男/アイテムくじ）・その他シート冗長導線・駿城失敗後の任意トロフィー・ST中は 4f トリガー抑止。**収支整合の解**：ST区間全体を1セグメントとしアーカイブをST終了（`st_end`）まで遅延、クレ差分は区間入口→最終クレで成立（新保存形式なし）
  - **検証**：シオンが独立環境で検収（クローン＋回帰スイート再実行＋コード読み）
- **束AT 第5波（07-31・実装完走）** — #25 比較ビュー初版まで完了し、束ATの実装対象は完走。残るのは #2/#3 の実機確認のみ。
  - **束AT-5c** `5cd1657` — #25 比較ビュー初版：指標3つ（下段ベル／無→💡率／単チャ目3000pt率）・`compareMetrics` 宣言的定義＋解決関数 `compareMetricPairForLog` 一本化・`settingLabel:{value,atLeast,confirmed}`（**logs への新フィールド。束AT で唯一の保存形式変更**・正規化/compact/復元の3点確認済み）・プール合算は表示時のみ（同一ラベル／n以上合算／全ラベル）・minDen 未満は寄りバッジ非表示・駿城ptシートに単独チャ目回数/3000pt回数の2欄追加（旧トグルは回数1として読み替え・migrate なし）
  - **下段ベル分母の定義**：各通常区間の初回 hitEvent データGまでを合算・初回区間は `startCounterGame` 控除・ST区間G除外
  - **検証**：シオンが独立環境で検収（クローン＋回帰スイート再実行＋コード読み）
  - **束AT 最終集計（07-30 朝〜07-31）**：実装23本・台帳更新8回・全依頼一発検収パス・停止1回（テスト衝突・正しい停止判断）・データ事故ゼロ
- **束AL** `4625a6f`（AL-1 suggestLogスリム化）／`67f031b`（AL-2 timelineスリム化）— **schema v4**。導出できるものを保存しない（ラベルはidから表示時解決・既定値キー省略・分精度時刻）。移行は premigrate 正規化書き戻しに内包（読み込みフル補完・保存時のみスリム）。**シオンが実データで独立検証：1,074,884→813,804 chars（24.3%減）・収束バイト完全一致**。実機：**86%→39%（1.9MB）**、数日で30%前後へ
  - 調査（07-29夜・実データ解剖）：suggestLog 708件×549chars＝**ラベル二重保存＋null既定10個**が37%／timeline 1,970件×216chars＝**空配列・フルISO**が41%／main+last_good×UTF-16で全肥大が4倍増幅
- **束AK** `8bde64a` — 実戦モード最下部「ヤメる（打ち終わりを記録）」→3択シート（記録して収支登録へ＝STEP4遷移＋終了時間自動／記録だけする／キャンセル）。既存 `setEndLogFromCurrentPosition()` のみ使用・ヤメ記録もUndo対象化（ミコトの自発的補強）・Y-1連携確認済み
- 台帳配置 `4fd0b5a`（07-29版・検証3語一致）

### 2026-07-29 — 東京喰種v1実戦投入日／20束（T〜AJ）／障害2件完全復旧
方式の実証：v1持ち込み→実戦フィードバックで即日20回改修。障害（容量満杯・登録ブロック）も同日中に根治し**データ損失ゼロ**。
- 東京喰種：束T `5174b1b`/`23d252b`（v1）・束U `161ea78`/`ac224a3`（v2：4+4・招待状直行・上段3ボタン）・束V `eecbbf8`・束W（followMode always）
- 投資・クレ系：束X `61247e1`（＋1000/クレ更新/状態色/初期0省略）・束Y `266cf31`（クレ⇔収支双方向）・束Z `a8060a0`（現金合計）・束AA `51bcde7`（右端104px化・§9-24起源）・束AD `f44a11a`（内訳・今日の現金合計）・束AE `dff1318`（色強調）・**束AG** `a417fd0`（獲得枚数→ボーナス後クレジット入力へ置換。発案：夢爽「二度手間ではないし、忘れない」）・束AI `cd8daaf`/`fb33085`（投資リセット＝resetInvest履歴・全選択＋クリア）
- 不具合5件：束AB `f83d8f0`（台移動の投資引き継ぎ→clearLogInputリセット漏れ＋持ちメダルのみ引き継ぎ）・束AC `ed6d9ff`（リプフラ分母＝観測区間基準へ）・束AF `ce07498`（復元の区間文脈喪失＋テスト2本）・束AH `eba87e9`/`6afc3d2`（**schema v3** timelineCounterState＋液晶0丸め。停止条件3回目の成功例）・**束AJ** `54bb377`（登録ブロック撤廃→confirm化・算出不可表示・prompt廃止。§9-23起源）
- 障害記録（13:36〜13:59）：容量満杯→保護機構が復元を正しく中止→JSONバックアップ成功→アーカイブ→CP復元→誤値42のブロックを手動修正133で突破し登録完了。**損失ゼロ。防護層すべて設計どおり機能**

### 2026-07-28 — 束A〜H・案1〜7・PWA①・#7絞込・台帳確立
束H `5fa8189`/`2c55c2c`・束G改 `ac93a22`/`9357238`・束F `4b4b34b`/`ea80845`・#7 `dd74d9e`・案1 `612dad7`・案7 `3f9d1c4`・案3 `d76e019`・案2 `8422d55`・PWA① `52fe56c`・余白 `3f4fb2b`
機種知見：バタフライゾーン（最大9G）は液晶G非加算＝オフセット+9の正体。

### 2026-07-27 以前
#10 `a84320d`／案5 `9831670` `ca17dd9`／束E `3944b5a` `4aa59c3`／束D `af5d407` `7cba0fd`／束C `1a3fd40` `d60ec26` `7d2d52f`（174%→79%）／束B `0e1a117` `f353589` `6acd872`／束A `9126436`（§9-4起源）

---

## 9. 運用ルール

### 台帳
1. 構想は§5へ／2. 完了は§8へ・破棄は理由付き／3. 夢爽の宿題は§3へ

### ミコトへの依頼
4. 保存形式に触る変更は単独タスク・単独コミット
5. 制約明記・行数差分は origin/main 基準・指示外差分は差し戻し
6. 完了条件：ハッシュ＋push＋raw＋本番200
7. 付随ファイルは実施前報告／8. 稼働中は触らない——例外はオーナー明示承認時のみ（push最優先報告・区間の切れ目でリロード）
9. 停止条件を指示書に書く。停止→調査結果を織り込んだ改訂版で再依頼（改訂版は別ファイル名）
10. 台帳の配置依頼には検証キーワードを必ず添える

### 対症療法を避ける3つの問い
11. 「消す前に、それを作ってるのは何？」／12. 「対症療法？根本治療？」／13. 「2回目だよね？」

### シオンの既定動作
14. 削除・回避の前に生成元を確認／15. 修正案に対症/根本を明記／16. 2回目は原因調査を先に

### 設計原則
17. **観測値を記録し、導出は保存しない**（束ALで保存側にも適用拡大：ラベル・既定値は導出/補完）
18. `pre-wrap` でHTML要素を包まない／19. テスト放置禁止
20. **基準機で検証してから横展開**（v1を持って打ちに行く方式を含む）
21. **同じ観測値を2回入力させない**
22. 示唆マスタは確定/示唆/予想の3段階
23. **整合性チェックは警告に留め、登録・保存を止めない**（止めてよいのは保存形式の破壊を防ぐ場合のみ）
24. **頻度が高い操作ほど大きく・右（親指側）に**
25. **prompt() を実戦導線に使わない**
26. メメントモリ側は `mementomori-miresto/IDEAS.md` で別管理
27. **カウンターのリセット＝当選登録の境界**：データカウンターがリセットされる当選は hit フローで記録し、リセットされない事象（CZ等）は通常時のイベントとして記録する（発案：夢爽 2026-07-30。カバネリ設計で確立、以後の全機種に適用）
28. **UI配色原則**：塗り背景ボタンは「強調」であり、同時に目立たせるのは1〜2個まで。多数が塗りになると色が相殺して走査性が落ちる。多数の項目リストは中立背景＋キーワードのみ文字色（分類用）。使用色数は3系統＋強調1程度に抑える。
29. **台帳≠実装許可**：`WISHLIST.md`（台帳）は構想・判断材料であり、実装許可ではない。実装は必ずシオン発行の個別指示書に基づく。（AGENTS.md:31 と同旨・相互参照）
30. **当選契機必須**：当選には必ず契機がある。契機なしの hit を保存しない。契機マスタは機種プリセット定義。「その他＋メモ」を逃がし弁として必ず残す。
31. **時間順序**：チャ目の状態分類は「成立時点の滞在状態」で判定。チャ目が引き起こした状態遷移は次Gから適用。
32. **頻度3層配置**：毎G級＝最前面・最短タップ／数十G毎＝シート1階層／参照系・低頻度＝「その他」配下。
33. **マイスロ分担**：出現回数の集計はマイスロ等の外部に任せ、ツールは「タイミングと文脈」（何G・どの状態中・何の直後）を取る。外部集計値は転記欄で受けて突合に使う。マイスロはログイン忘れ・非対応店で欠測するため、確定系の受け皿は保険として残す。
34. **検証ビュー設計（§9-28拡張）**：装飾を足さず、等幅数字（tabular-nums）・密度・意味色で「データ感」を作る。核心指標1つだけ強調。
35. **カウンターリセット例外**：設定判別役のカウンター（下段ベル等）は§9-27（当選登録境界でリセット）の例外とし、台単位でヤメまで累積する。例外は機種プリセットで宣言する。
36. **§9-36 ST区間の意味論（§9-27 の例外）**：カバネリの ST 区間（through=yes 当選〜ST終了）は区切りを「ST終了」とし、区間全体を1セグメントとする。ST中の各当選は hitEvents に記録されるが個別アーカイブしない。獲得区間の収支は入口クレ→最終クレの差分で測る（5b で確立）。
37. **圧縮の一般則**：表示の圧縮・省略をしてよいのは情報を失わない場合のみ。個別の対応関係（例：発光→CZ）が検証価値を持つ要素は、頻度が高くても圧縮しない（#13 凍結時の判断を一般化）。
38. **§9-38 実戦モード入力の三点ルール**：実戦モードに入力を追加するときは「**見える**（直近フィード・取消ラベル）・**取り消せる**（undoスタック）・**巻き添えで消えない**（他操作のsnapshot復元で消失しない）」の3点を必ずテストで固定する（6cのデータ消失バグの再発防止）。
39. **§9-39 割合の定義原則**：割合を表示するときは分子と分母の定義を必ず明文化する。**分母は常に「同グループの総記録数」**（異種混合の全件母数は禁止）。分母定義は1箇所の関数に集約し、全表示面が同一値を返すことをテストで固定する（6f/6gで確立）。
40. **§9-40 表とその他の役割分担（定量基準）**：実戦モードの表（常設パネル）に置くのは「**1区間（1ボーナス/1ST）あたり期待1回以上**」の高頻度入力のみ。それ未満の低頻度と確定系は「その他」へ。同じ登録は常に両経路から可能にする（6dで確立・6qで定量化。例：ボイス男女=毎ボーナス級→表、景之3種=低頻度→その他）。
41. **§9-41 UI検収はエンドツーエンド**：UI変更の受け入れテストと検収は、対象関数の直呼びではなく**実際のボタンの onclick から辿った経路**で固定する。色・表示・保存のいずれも「実UIが通る経路」で検証する（6lのステージ色が別関数に付き実UIに出なかった事故が根拠。テストが緑でもユーザーに届かない変更を防ぐ）。
42. **§9-42 クリア契機の系統別定義**：カウント・ポイント系の表示は「何を契機にクリアされるか」を系統ごとに定義してから実装する（例：カバネリの前回CZから＝無名/生駒/周期で起点が独立）。周期制・ポイント制の全機種で発生する論点のため、新機種プリセット導入時の必須確認項目とし machine-preset-guide.md にも記載（6wで確立）。
43. **§9-43 選択フィードバックの原則**：タップで何かを選ぶUIは、**選ばれた対象自身の見た目変化（ハイライト・金枠）で選択状態を示す**。離れた場所の小さなテキスト表示だけでは不可。ツール全般の基本方針（7oで確立・ST終了フロー6dの選択ハイライトと同系）。
44. **§9-44 ドメイン用語との衝突回避**：UI操作ボタンの文言は、パチスロのドメイン用語（確定・以上・天井等）と意味が衝突する場合、別の分かりやすい語を選ぶ（例：シートの「確定」→「登録」。7oで確立）。
